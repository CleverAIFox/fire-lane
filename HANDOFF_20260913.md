# HANDOFF — 2026-09-13

다음 세션은 **이 문서만 읽고 시작한다.** 설명을 다시 하지 않는다.
`HANDOFF_20260910.md` 를 대체한다.

---

## 0. 한 줄

**B3(사본 제거)를 닫았다.** 그리고 저장소 밖 세 층 — 레이크 · GitHub · 기계 —
이 통째로 흔들렸고 그것을 복구하며 **감사 분모가 232 하나가 아니라는 것**이
드러났다. 다음은 **B5 나머지**이고 첫 작업은 **DMS 소급**이다.

★ **DMS 가 CDC 앞이다.**

    DMS(소급)   이미 쓰인 것을 지금 규약으로 전수 검사해 바로잡는다
    CDC(증분)   그 뒤의 변경만 검사가 잡는다

  오늘 만든 강제자는 **전부 CDC 다.** 만들 때 이미 통과하는 상태로
  맞춰놨으므로 **이미 어긴 것은 안 센다.** 증거 — `§123~143` 스물한 절이
  없는 검사를 강제자로 들었는데 **어떤 검사도 안 잡았다.** 사람이 눈으로 봤다.
  소급이 끝나야 기준선이 서고, 기준선이 서야 증분이 의미를 갖는다.

---

## 1. 지금 위치

```
커밋  4b9399f  feat(web)  B3 색 사본
      5a8543a  docs       PLAN 슬롯 폐지 · 21건 흡수 · 재배번 도구
      8aa5ee4  feat(env)  .env · V-World 키 분리 · ruleset REPO · Secrets 강제자
      60d4cc7  data       파이프라인 전량 재생성

pytest      534 passed · 44 skipped
widen       68건 (W2 임계값 정본 0 — 되박으면 1건, 프로브 생존 확인)
lakecheck   0건 · prep --check 정상 28 · 손상 1(eais raw 결손)
ruleset     룰셋 3종 · 저장소 설정 · admin · bypass · Secret 전부 OK
verify.sh   golden 만 빨갛다 — 코드 지문이 lock 시점보다 최근이다.
            **재잠금은 B4 에서 한 번만 쓴다. 여기서 소진하지 마라.**
```

### 환경 — 전부 바뀌었다

```
저장소   ~/fire-lane → ~/projects/fire-lane
레이크   /mnt/f/data/fire-lane → /mnt/f/projects/fire-lane/data
설정     ~/.fire-lane.local(죽은 파일) → 저장소 루트 .env
GitHub   woongtopia 조직 → CleverAIFox 개인 (미러 이관)
Pages    cleveraifox.github.io/fire-lane/
OS       Ubuntu 26.04 · node 22 · .venv 재생성됨
```

★ `.venv` 를 옮기면 셔뱅이 죽는다. `ls` 로는 파일이 보이는데 실행하면
  `No such file or directory` 가 난다 — 파일이 아니라 **셔뱅의 인터프리터**가
  없다는 뜻이다. `rm -rf .venv && uv sync --dev`.
  `~/.local/bin/hygiene.sh` H1 이 이것을 본다.

---

## 2. 감사 분모가 하나가 아니다

232건은 **저장소 203파일 44,899줄** 이었다(`BASELINE.txt` · `ROUNDS.md`).
그 감사는 정합했다. 오늘 흔들린 것은 전부 **그 분모 밖**이다.

| 층 | 분모 | 닫는 법 | 상태 |
|---|---|---|---|
| L1 저장소 | 203파일 44,899줄 · 232건 | `golden` 지문 | B1·B2·B3 닫힘 |
| L2 레이크 | 118개 4.58GB | `lakecheck` · `prep --check` | 무결 아님. **대조 가능**이 목표 |
| L3 GitHub | 룰셋 · Secrets · Pages | `ruleset_check` | 오늘 복구. 코드로 못 닫는다 |
| L4 기계 | WSL · `.env` · `.venv` | `hygiene.sh` · `env_check` | 재현만 보장 |

★ **"완전 정합 무결" 은 L1 에만 성립한다.** 입력이 고정이고 출력이
  결정적이라 지문으로 못박을 수 있다. L2~L4 는 외부 사건이 바꾸므로
  목표가 다르다 — 무결이 아니라 **어긋났을 때 검사가 먼저 우는 것**이다.

---

## 3. 배치 계획 — 다시 짰다

```
B1  강제자 축        닫힘  eb83356
B2  데이터 축        닫힘  f39b47f + b2_interview
B3  사본 제거 81     ★ 닫힘 (오늘)
      params 3 · const 7 · rules 4 · palette
      union-find 5벌 · 등거리 근사 3벌 → B4 로 이관 (판정이 바뀐다)

B5  환경·배포·비밀   진행중  ← **다음 세션**
B4  판정 + 새 자료   미착수  ★ 재잠금 1회
B6  증분 감사 + 새 축 미착수
```

★ **B5 를 B4 앞에 둔다.** 재잠금은 한 번뿐인데, 입력이 정리 안 된 상태에서
  1,281구간을 잠그면 되돌릴 수 없다. `fire-lane-raw` 2.3GB 의 정본이
  아직 안 정해졌고 `its` 258MB 가 두 벌이다.

### B5 — 남은 것

```
⓪  DMS 소급 — **강제자 칸 미기재 417절**     ★ 첫 작업
      MASTER 133 · DECISIONS 201 · PLAN 65 · README 18   (2026-09-13 실측)
      각 절이 무엇으로 강제되는가를 한 줄로 적는다. **없으면 "없다".**
      ★ "없다" 를 적는 순간 세어진다. `grep -c "강제자  없다"` 가 분모다.
        있는 것을 적는 것은 자랑이고, 없는 것을 적어야 일이 보인다.
      ★ 회차로 나눠 **사람이 읽는다.** 기계가 못 판단한다.
        232건 감사의 `ROUNDS.md` 와 같은 방식 — **북마크가 필요하다.**

      ★ 프로브를 먼저 정밀화한다. 1차 소급에서 192건이 나왔는데
        **174가 오탐**이었다. 제외해야 할 것 —
          `(삭제됨)` 표기 · 코드펜스 안 · 예시 이름(xxx·foo·name) ·
          파일명 참조(`test_guards` 는 파일이지 함수가 아니다) ·
          과거 서술(DECISIONS 의 일회성 패처 이름은 사실이다)

      2026-09-13 에 닫은 것 — 죽은 참조 27건
        b2_sweep.py → sweep.py          실행 안내가 없는 파일을 가리켰다
        bottleneck.py → bridge_audit.py 같음
        강제자 이름 2곳                  test_ruleset_policy_… → test_rules_table_…
        PLAN 네이버 절차 블록             삭제된 도구를 절차로 적었다
        §123~143 강제자 21곳             흡수 작업이 만든 것

①  os.environ 12파일 25곳         ★ 두 번째. 최대 일감
      datalog · guards · pipeline · seg/params · doctor · lakecheck ·
      matchcheck · pr_body_check · pull_data · scan_data · sweep
      ★ 이것들은 **.env 를 못 읽는다.** 오늘 lakecheck 가 그것으로 실패했다.
        `env_check --readers` 가 세는 숫자가 곧 .env 가 안 먹는 곳의 수다.
      ★ 두 일을 가른다 — ⒜ `from firelane import paths` 한 줄(.env 를 읽게)
        ⒝ os.environ → paths 접근자(단일 독자). ⒜가 먼저이고 훨씬 싸다.
        stage_pages.py 가 ⒜의 선례다.

②  (B) 파일명 날짜 규약 강제자
      `naming.vintage` 가 *"데이터 기준일. 다운로드일이 아니다"* 를 적었는데
      `_plausible_date` 는 **형식만 본다.** 그래서 `its_nodelink` 가
      20260810 · 20260812 두 이름으로 258MB 두 벌이다.
      대장 `updated: 2026-08-12` 가 정본이므로 20260810 을 버린다.
      같은 부류가 더 있는지 전수로 보는 `vintage_check` 를 신설한다.

③  fire-lane-raw 2.3GB ↔ 대장 대조
      `/mnt/f/projects/fire-lane/raw-legacy`. **어느 쪽도 상위집합이 아니다.**
      `ngii_1k` 1599개는 그쪽에만, `eais`·`mois`·`nfa` 는 이쪽에만 있다.
      `safety` 만 그쪽이 크다. `intake --plan` 에 경로를 물려 대조한다.
      ★ **대조 전에 합치지 마라.**

④  raw_20260813_norm.tar.gz 1.7GB — 아직 아무도 안 열었다

⑤  배포·비밀 12건 (원 목록)

⑥  훅 문서 넷
      README:87 · MASTER:1846 · .devcontainer/setup.sh:20 · PR템플릿:54 가
      `git config core.hooksPath .githooks` 를 시킨다. **치는 순간 로컬이
      전역을 이기고 자격증명 검사가 죽는다.** setup.sh 는 자동이라 더 위험하다.
      → "전역이 저장소 훅에 위임한다. 로컬로 박지 마라" 로 고친다.
      `tools/doctor.py` 에 실측 검사 — 가짜 `.env` 를 스테이지해 훅이
      0 이 아닌 코드를 내는지 본다. 어느 경로로 돌든 참이면 통과다.
```

### B4 — 재잠금 1회. **동시에** 해야 하는 것

```
새 자료 배선   TL_SPOT_CNTC 15,518 접속선 → 출동 종점 보정
               TL_SPBD_ENTRC 17,069 출입구 → poi 좌표 부재 해소
판정 규칙      F-024 nfa_designated(총합→연속 100m) · F-009 wmin/wmax 표본
               F-027 노드 접합 두 구현 · F-005 VERDICT_RULE ↔ verdict()
계약 해제      F-162 — test_contract 가 width_verified=false 를 못박는다
문서 숫자      1,101 → 새 값. docnum_check 가 산출물을 정본으로 본다
B3 이관분      인코딩 후보 4벌 통합 · TEXT_EXT 3벌 통합 ·
               union-find 5벌 · 등거리 근사 3벌   ← 근거는 DECISIONS §144

★ 절차
  1  tools/baseline.py freeze --tag pre-B4
  2  변경 적용
  3  tools/baseline.py diff pre-B4        전이 건별 기록
  4  사람이 전이표를 건별 승인
  5  tools/golden.py lock                 재서명 — 한 번만
```

### B6 — 북마크 · CDC 증분

★ **DMS(B5 ⓪)가 끝나야 시작한다.** 기준선이 틀려 있으면 증분도 틀린 데서
  출발한다. 북마크는 어디까지 읽었는지를 남기는 장치이고, 417절을 회차로
  나눌 때 필수다 — 232건 감사가 `ROUNDS.md` 로 한 것과 같다.

```
기존   오늘 이후 발견 전부

★ 신축 — 강제자 ↔ 규약 정합 (DECISIONS §149)
   오늘 다섯을 봤다. 규약은 있는데 강제자가 없거나, **강제자가 옛 규약을
   강제한다.** `deadcheck` 는 검사가 도는지 보고 `widen` 은 범위를 보는데,
   *검사가 옳은 것을 검사하는가* 는 어느 쪽도 안 센다. 자동화가 안 된다.

   첫 작업 — `강제자` 칸을 3축 공통 규약으로 올린다.
     지금은 DECISIONS 에만 있다. MASTER 절마다 · PLAN 항목마다 한 줄.
     **없으면 "없다" 라고 적는다. 적는 순간 세어진다.**
     `grep -c "강제자  없다"` 가 이 축의 분모다.
     `test_tools_are_wired` 도 README 하나만 보는데 3축으로 넓힌다.
   분모 — verify.sh 38단계 + pytest 578 + 훅
   방법 — 232건 감사와 같다. 회차로 나눠 **사람이 읽는다.**
```

---

## 4. 코드로 못 닫는 것 — 갱신

```
V-World 키        브라우저가 쓴다. 숨길 수 없다. 도메인 잠금이 유일한 방어이고
                  만료 2027-02-04. 이력 노출은 **감수한다**(무료·도메인 고정).
                  판단이며 누락이 아니다 — DECISIONS §146

hydrant_point     원본 기준일이 2024-02-07 이다. 2026-08-30 재수집에서도
                  같았다(5002건 동일). 배선 누락이 아니라 **자료의 한계**다.
                  판정에 쓰려면 그 사실이 화면에 나가야 한다.
                  메타 → raw/safety/_meta/

bypass            개인 저장소라 승인 1을 혼자 만족할 수 없다. 영구 예외이고
                  회수는 **조건**이다 — 협업자가 생기면. ruleset_check 이 건다.

D-30 축거·회전반경  pending.vehicle_spec_measured. fallback 을 적었다.
상공 장애물        sources.yaml 이 근거 없이 단정한다. juso_etc_geom 으로 확인 가능
```

---

## 5. 원칙 — 오늘 값을 치른 것

```
① 규약이 있어도 강제자가 없으면 안 지켜진다
   vintage · ruleset_check · contract.yml · PLAN §0-2 · 안내문.
   다섯 다 규약은 멀쩡했다.

② 잘못된 것을 정확히 지키게 만드는 검사가 제일 위험하다
   검사가 있다는 사실이 규약을 검증한 것처럼 보이게 한다.
   PLAN 슬롯 규약이 그랬고 그것을 강제하는 검사를 만들 뻔했다.

③ 경고는 읽히지 않는다. 멈추는 것만 읽힌다
   plan_renumber 가 "지워진 번호를 가리킨다" 를 경고만 하고 진행했다.
   재배번 뒤 그 참조가 **다른 항목**을 가리켰다 — 죽은 참조보다 나쁘다.

④ 실패 메시지가 엉뚱하면 침묵보다 나쁘다
   ruleset_check 이 404 에 *"로그인·권한을 확인하라"* 를 붙였다.
   원인을 딴 데서 찾게 만든다.

⑤ 옮길 때 글자를 안 바꾼다
   PLAN → DECISIONS 21건. 옮기면서 고치면 "옮긴 것" 과 "고친 것" 이
   섞여 나중에 대조가 안 된다.

⑥ 감사 건수는 하한이다
   규칙 조립이 3벌이 아니라 4벌이었고, 색 사본 5벌 중 둘은 사본이
   아니었다. 분모는 세었지만 각 항목의 내용은 다시 봐야 한다.

⑦ 파괴적 명령 앞에 스테이지 상태를 본다
   `git checkout -- docs/PLAN.md` 로 미커밋 작업분을 두 번 날렸다.
```

---

## 6. 신설한 것

```
재현적 (tools/)
  env_check.py       .env.example ↔ 코드. **양방향** + 단일 독자. --selftest
  plan_renumber.py   PLAN §1 번호를 1..N. 죽은 참조가 있으면 **멈춘다**

일회성 (tools/batches/)
  b3_palette · b5_env · b5_vworld · b5_repo · b5_secrets ·
  b5_docs · b5_plan · b5_plan2 · b5_dec

계정 단위 (저장소 밖)
  ~/.githooks/credential-check   자격증명 정본. 저장소 훅에 위임
  ~/.gitignore_global
  ~/.local/bin/hygiene.sh        H1 셔뱅 · H2 루트 잔재 · H3 홈 · H4 .env · H5 마운트

배선
  verify.sh        env_check 추가 · uv sync --dev
  secret-scan.yml  gitleaks · fetch-depth 0
  ruleset_check    REPO 자동 · BYPASS_DECLARED · _secret_gaps
  stage_pages      web/key.js 생성
```

---

## 7. 다음 세션 첫 메시지

> fire-lane B5 시작. 기준선 60d4cc7 (feat/audit-b1b2 → main 머지분).
> pytest 534 · widen 68 · lakecheck 0 · ruleset OK.
> golden 만 빨갛고 **재잠금은 B4 에서 한 번만 쓴다.**
> 첫 작업은 `os.environ` 12파일 25곳 — `uv run python tools/env_check.py --readers`.
> ⒜ `from firelane import paths` 한 줄로 .env 를 읽게 하는 것과
> ⒝ 단일 독자로 만드는 것을 가른다. ⒜가 먼저다. stage_pages.py 가 선례다.
> 배치 계획은 이 문서 §3, 원칙은 §5.
