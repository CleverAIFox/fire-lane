#!/usr/bin/env python3
"""
docfix3_20260818.py — 문서를 2026-08-18 확정 산출(1101)에 맞춘다.

    uv run python tools/docfix3_20260818.py --check
    uv run python tools/docfix3_20260818.py

★ 어제(08-17) 쓴 서술 두 개를 뒤집는다. 그때 근거로 삼은 실행이 잘못이었다.

  §3-1  "폭 소스가 실질 3개에서 2개로 줄었다" → 사실이 아니다.
        ngii 131 → 1 은 맞지만 silpok 이 29 → 203 으로 는 것은
        V-WORLD 동구 SHP 판이 스코프 북부를 안 덮어서였다.
        NGI 보완분을 넣자 silpok 84 로 돌아왔다 — 베이스라인과 같은 값이다.
        폭 소스는 줄지 않았다. ngii1k 가 제 몫을 못 하던 것이다.

  §4    절대편차 합 9.23m → 8.99m. 9.23 은 북부 미커버 상태의 값이었다.

이 두 번의 정정 자체가 기록으로 남을 값이 있다. 소스를 갈아엎은 뒤
숫자가 나왔다고 바로 문서에 박으면, 중간 상태를 확정값으로 적게 된다.
"""
from __future__ import annotations

import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
M = ROOT / "docs/MASTER.md"
R = ROOT / "README.md"

M_PAIRS: list[tuple[str, str]] = [
    # ── §2 판정 ────────────────────────────────────────────
    ("""## 2. 현재 판정 (2026-08-17)

| 판정 | 구간 | 뜻 |
|---|---:|---|
| `clear` 통행 가능 | 383 | 양쪽 주차가 있어도 통과. 영상판정 불필요 |
| `needs_cv` 판정 보류 | 216 | 상습주차 여부로 갈린다. 영상판정 대상 |
| `blocked` 통행 불가 | 65 | 차가 없어도 통과 불가 |
| `unknown` 영상판정 불가 | 429 | CCTV 25m 밖. 영상판정 자체가 불가능 |

```
segments 1093
```

> 08-14 대비 `1102 → 1093`. 수치지형도를 통째로 교체하고 평면교차점 실형상을
> 도입한 결과다. 판정이 바뀐 구간은 59 / 1093 (5.4%)이고 총연장 48,580m 는
> 변하지 않았다. 구간 경계는 재배치됐으나 도로망 총량은 같다는 뜻이다.""",
     """## 2. 현재 판정 (2026-08-18)

| 판정 | 구간 | 뜻 |
|---|---:|---|
| `clear` 통행 가능 | 400 | 양쪽 주차가 있어도 통과. 영상판정 불필요 |
| `needs_cv` 판정 보류 | 209 | 상습주차 여부로 갈린다. 영상판정 대상 |
| `blocked` 통행 불가 | 63 | 차가 없어도 통과 불가 |
| `unknown` 영상판정 불가 | 429 | CCTV 25m 밖. 영상판정 자체가 불가능 |

```
segments 1101
```

> 08-14 대비 `1102 → 1101`. 수치지형도를 통째로 교체하고 평면교차점 실형상을
> 도입한 결과다. 판정이 바뀐 구간은 27 / 1101 (2.5%)이고 총연장 48,580m 는
> 변하지 않았다. `seg_uid` 유지율 99.1%.
>
> ### 08-17 · 08-18 오전 숫자는 무효다
>
> 이 값에 이르기까지 두 번 잘못된 숫자를 냈다. 둘 다 문서에 박힐 뻔했다.
>
> ```
> 1093  08-17  ngii1k 이 FAIL 인데 08-13 구 gpkg 가 남아 그것으로 판정했다
>              _manifest 에 FAIL 이 적혔지만 아무도 읽지 않았다
> 1091  08-18  진짜 V-WORLD 인데 동구 SHP 판이 스코프 북부를 안 덮었다
>              1,091구간 중 755개(69%)가 도로경계 폴리곤 밖이었다
> 1101  08-18  NGI 보완분 143도엽 등재. 미커버 47(4%). 확정
> ```
>
> 1093 과 1091 이 기계별로 갈려 "재현성 붕괴"로 오인해 반나절을 썼다.
> 재현성은 무죄였다 — 같은 기계 두 실행은 언제나 동일했고, 갈린 것은
> **낡은 산출물을 이번 실행 것으로 착각한 것**이었다.
> 방어는 §18-9 에 적었다.""") ,

    # ── §3-1 폭 소스: 어제 서술을 뒤집는다 ────────────────
    ("""채택 소스   ngii1k 885 · silpok 203 · ngii 1 · null 4      ← 2026-08-17
            ngii1k 925 · ngii 131 · silpok 29 · null 2     ← 2026-08-13

★ `ngii` 가 사실상 이탈했다(131 → 1). 버그가 아니라 제품 교체의 결과다.
  B030 국가기본공간정보 → B020 연속수치지도로 바뀌면서 도로경계면 밀도가
  줄었다(스코프 내 216건). 레이어명도 NF_A_A01000 → N3A_A0010000 이다.
  그 자리를 `silpok` 폴백이 메웠다(29 → 203).

  **폭 소스가 실질 3개에서 2개로 줄었다.** 교차검증 축이 하나 빠진 것이라
  판정 신뢰도 해석에 직접 영향을 준다. 다만 `rvwd`(도로폭 속성)는 소실이
  아니라 도로중심선 N3L_A0020000 으로 옮겨갔다 —
  `datasets.ngii_road_center` 로 등재했고 폭 교차검증에 쓸 수 있다.""",
     """채택 소스   ngii1k 1014 · silpok 84 · ngii 1 · null 2     ← 2026-08-18 확정
            ngii1k  925 · ngii 131 · silpok 29 · null 2    ← 2026-08-13

★ `silpok` 폴백이 베이스라인과 같은 84 다. 구 원본(NGI 20도엽)이 덮던
  범위를 V-WORLD 74 + NGI 143도엽이 온전히 대체했다는 뜻이다.

★ `ngii` 가 사실상 이탈했다(131 → 1). 버그가 아니라 제품 교체의 결과다.
  B030 국가기본공간정보 → B020 연속수치지도로 바뀌면서 도로경계면 밀도가
  줄었다(스코프 내 216건). 레이어명도 NF_A_A01000 → N3A_A0010000 이다.
  `ngii1k` 가 그 자리까지 덮어 폭 산출 자체에는 영향이 없다.
  `rvwd`(도로폭 속성)는 소실이 아니라 도로중심선 N3L_A0020000 으로
  옮겨갔다 — `datasets.ngii_road_center` 로 등재했고 교차검증에 쓸 수 있다.

> ### 08-17 서술 정정
>
> 어제 이 자리에 "폭 소스가 실질 3개에서 2개로 줄었다(silpok 29 → 203)"고
> 적었다. **사실이 아니다.** 그 실행은 V-WORLD 동구 SHP 판만 들어간 상태였고,
> 그 상품이 스코프 북부(1:5,000 부모 3561609 대)를 통째로 흘려서
> `ngii1k` 가 제 몫을 못 하니 `silpok` 이 그 자리를 메운 것이었다.
> NGI 보완분을 넣자 84 로 돌아왔다.
>
> 소스를 갈아엎은 직후의 숫자를 확정값으로 적으면 이렇게 된다.
> **중간 상태와 확정 상태를 구분하고, 베이스라인 대조로 확인한 뒤 적는다.**"""),

    # ── §4 외부 검증 ───────────────────────────────────────
    ("""### 대조 결과 (2026-08-17, 도로명 매칭 7구간)

| 도로명 | 소방서 | 우리(중앙) | 편차 | 세그 |
|---|---:|---:|---:|---:|
| 필문대로205번길 | 8m | 7.95m | **-0.05** | 40 |
| 동명로20번길 | 5m | 5.00m | **+0.00** | 36 |
| 밤실로4번길 | 5m | 5.13m | +0.13 | 3 |
| 중앙로272번길 | 5m | 5.55m | +0.55 | 4 |
| 제봉로184번길 | 5m | 7.02m | +2.02 | 19 |
| 동계로9번길 | 6m | 8.97m | +2.97 | 18 |
| 필문대로289번길 | 8m | 4.49m | **-3.51** | 30 |

**절대편차 합 9.23m.** 8/14 기준 7.24m 였다.""",
     """### 대조 결과 (2026-08-18, 도로명 매칭 7구간)

| 도로명 | 소방서 | 우리(중앙) | 편차 | 세그 |
|---|---:|---:|---:|---:|
| 동명로20번길 | 5m | 4.99m | **-0.01** | 35 |
| 필문대로205번길 | 8m | 7.94m | **-0.06** | 41 |
| 밤실로4번길 | 5m | 4.90m | **-0.10** | 3 |
| 중앙로272번길 | 5m | 3.68m | -1.32 | 4 |
| 동계로9번길 | 6m | 7.97m | +1.97 | 17 |
| 제봉로184번길 | 5m | 7.02m | +2.02 | 19 |
| 필문대로289번길 | 8m | 4.49m | **-3.51** | 30 |

**절대편차 합 8.99m.** 8/14 기준 7.24m 였다.

> 앞의 셋이 ±0.1 안이고(동명로20번길 · 필문대로205번길 · 밤실로4번길), 뒤의 넷이 8.99 중 8.82 를, 필문대로289번길 하나가 3.51 을 차지한다.
> 이 대조는 이제 매 실행 `data/processed/nfa_compare.json` 으로 남는다
> (08-18). 종전에는 `print` 뿐이라 터미널에만 존재했다."""),
]

R_PAIRS: list[tuple[str, str]] = [
    ("골목 1,093구간의 실제 통행 가능 폭을 산출해",
     "골목 1,101구간의 실제 통행 가능 폭을 산출해"),
    ("  segments.py             노딩 → 폭 → 판정 (1,093)",
     "  segments.py             노딩 → 폭 → 판정 (1,101)"),
    ("""세그먼트     1,093   (동명동 414 + 119안전센터 접근 회랑)
판정        통행 가능 383 · 판정 보류 216 · 통행 불가 65 · 영상판정 불가 429""",
     """세그먼트     1,101   (동명동 416 + 119안전센터 접근 회랑)
판정        통행 가능 400 · 판정 보류 209 · 통행 불가 63 · 영상판정 불가 429"""),
    ("""1,102 교차부 실제 폴리곤 반영 (8/14)
1,093 수치지형도 교체 + 평면교차점 실형상 (8/17)   ← 현재 판정 단위""",
     """1,102 교차부 실제 폴리곤 반영 (8/14)
1,101 수치지형도 교체 + 북부 12도엽 보완 (8/18)   ← 현재 판정 단위"""),
    ("`seg_uid` 유지율 93.0% · 판정이 바뀐 구간 59 / 1,093 · 총연장 48,580m 동일.",
     "`seg_uid` 유지율 99.1% · 판정이 바뀐 구간 27 / 1,101 · 총연장 48,580m 동일.\n\n"
     "취득 주의 — **행정구역 단위 다운로드는 스코프를 보장하지 않는다.**\n"
     "V-WORLD 동구 SHP 판(74도엽)이 1:50,000 경계에 걸친 도엽 12장을 흘려\n"
     "스코프의 69%가 도로경계 폴리곤 밖이었다. 북·남·서구 상품도 그 띠를\n"
     "비껴간다. 같은 상품의 NGI 포맷판(143도엽)으로 메웠다.\n"
     "취득 후 반드시 스코프 bbox 와 교차 검증할 것."),
]


def run(path: Path, pairs, check: bool) -> int:
    t = path.read_text(encoding="utf-8")
    bad = 0
    for i, (old, _) in enumerate(pairs, 1):
        n = t.count(old)
        if n != 1:
            print(f"! {path.name} #{i} 앵커 {n}회 — {old.splitlines()[0][:52]}")
            bad += 1
    if bad or check:
        return bad
    for old, new in pairs:
        t = t.replace(old, new, 1)
    path.write_text(t, encoding="utf-8")
    print(f"  {path.name} 적용")
    return 0


def main() -> int:
    check = "--check" in sys.argv
    if "segments 1101" in M.read_text(encoding="utf-8"):
        print("  이미 적용됨 — 건너뜀")
        return 0
    bad = run(M, M_PAIRS, check) + run(R, R_PAIRS, check)
    if bad:
        print(f"\n★ 앵커 {bad}건 실패. 아무것도 쓰지 않았다.")
        return 1
    print("\n검사 통과." if check else "\n다음: uv run python tools/docnum_check.py")
    return 0


if __name__ == "__main__":
    sys.exit(main())
