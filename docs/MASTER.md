# Fire-Lane · 마스터 문서

동명동 소방차 진입 판정 시스템 — 2026-08-11 기준

> **현재 상태를 말하는 단일 문서.** 지금 무엇이 어떤 값인지는 전부 여기 있다.
>
> | 문서 | 성격 |
> |---|---|
> | `docs/기획서_Fire-Lane.docx` | 대외 제출용 기획서 |
> | **`docs/MASTER.md`** | **현재 상태** · 데이터 · 운영 · 패치 적용 (이 문서) |
> | `docs/PLAN.md` | 남은 일 · 설계 결정 · 담당 공백 |
>
> **문서는 이 셋뿐이다.** 데이터 정본만 `sources.yaml`(기계가 읽는다).
> 2026-08-11 에 문서가 6개로 흩어져 판정 기준 변경이 UI 에 전달되지 않은 사고가 있었다.
> 완료된 일은 PLAN 에서 지우고 여기에 결과를 쓴다.
>
> | 찾는 것 | 절 |
> |---|---|
> | 지금 상태·판정 결과 | 1~4 |
> | 용어가 무슨 뜻인지 | 10 |
> | UI 작업 시작하기 | 11 |
> | 브랜치·푸시 규칙 | 12 |
> | 데이터 이력 | 13 |
> | 패치 적용 | 14 |
> | 기획서에 반영할 것 | 15 |

---

## 1. 무엇을 만들었나

전남광주통합특별시 동구 동명동의 골목 **641개 구간**에 대해
소방차가 지나갈 수 있는지를 판정한다. 판정 근거와 **판정할 수 없는 이유**를
함께 남기는 것이 이 시스템의 핵심이다.

```
https://woongtopia.github.io/fire-lane/
```

---

## 2. 현재 판정 (2026-08-11)

| 판정 | 구간 | 연장 | 뜻 |
|---|---:|---:|---|
| `clear` 도면상 통과 | 311 | 11,731m | 주차 1대가 있어도 통과 |
| `needs_cv` 판정 보류 | 72 | 3,009m | 영상판정 대상 |
| `blocked` 도면상 불가 | 27 | 1,374m | 차가 없어도 통과 불가 |
| `unknown` 판정 불가 | 231 | 8,060m | 아래 참조 |
| — `no_cctv` | 195 | 6,745m | **CCTV 25m 밖. 영상판정 자체가 불가능** |
| — `width` | 36 | 1,315m | 폭 산출 불가 |

### 판정 기준의 출처

임의 임계값이 아니다. **소방청 「2025 화재현장 골든타임 확보 종합대책」**을 따른다.

```
진입불가   폭 2m 이하, 구간 100m 이상
진입곤란   폭 3m 이상 + 장애물·상습주차, 구간 100m 이상
기준 차량  중형펌프차량 폭 2.5m
```

`TRUCK = 2.5`, 주차 1대 점유 2.0m → `clear` 기준 4.5m.
소방청 지정 기준(연속 100m 이상)을 충족하는 구간은 **173개**다.

---

## 3. 이 프로젝트가 발견한 것

### 3-1. "222구간"은 근거가 없었다

기존 문서의 세그먼트 수를 검증한 결과 출처를 찾지 못했다.
정식 행정경계로 재산출하여 **641**로 확정했다.

```
도로명주소 도로구간   206
실폭도로              219
노딩 후 세그먼트      641   ← 동명동만
스코프 확장 후       1266   ← 접근 회랑 포함. 현재 판정 단위
```

### 3-2. 도로명주소는 위상이 보장되지 않는다

원본은 "도로명 하나 = 선 하나"라 교차로에서 분할되지 않는다.
노딩 후에도 밀리미터 단위 좌표 오차로 구간이 분리된다(**T자 접합**).

| 처리 | 연결요소 | 최대요소 연장비 |
|---|---:|---:|
| 원본 | 40 | 93.29% |
| `set_precision(0.5)` | 22 | 97.74% |
| **끝점 투영 스냅 0.5m** | **6** | **99.76%** |

잔여 5구간(77m)은 알고리즘 한계가 아니라 **도로명 미부여 구간**이다.
그중 3곳이 관리폭 1.0~1.5m로 소방차 진입이 원천 불가하다.
**데이터의 공백 자체가 위험구간 지표다.**

### 3-3. `ROAD_BT`는 쓰면 안 된다

도로대장의 명목 폭은 **171구간(84%)에서 실측 트랜섹트보다 넓다.**
중앙 차이 +1.00m. 못 지나가는 길을 지나간다고 판정하는 방향이다.

### 3-4. 폭은 하나로 못 정한다

| 이름 | 데이터 | 성격 |
|---|---|---|
| 노면폭 하한 | 실폭도로 폴리곤 | 확실히 비어 있는 폭 |
| 담~담 상한 | 건물 폴리곤 | 물리적 최대 통로폭 |

담~담이 전 구간에서 넓고 중앙 차이 **+2.08m**. 그 사이에
전신주·화분·실외기·계단·주차 차량이 들어 있다.
**밴드를 좁히는 것이 영상판정과 실측의 역할이다.**

### 3-5. ★ 골목의 64%는 영상판정이 불가능하다

CCTV 유효범위를 25m로 잡으면(25m 초과 시 호모그래피 오차 급증):

```
CCTV 유효범위 안         229 / 641  (36%)
CCTV 거리 중앙값          38.6m
최대                      129m
영상판정 불가 구간         195개 · 6,745m (전체 연장의 28%)
```

**영상 기반 진입 판정 시스템을 만들어도 골목의 64%에는 적용할 수 없다.
알고리즘의 한계가 아니라 인프라의 한계다.**

완화 요소: 영상판정 불가 구간이 담당하는 소방 통행량은 **11%**다.
주요 동선에는 대체로 CCTV가 있다.

### 3-6. 출동은 소방서가 아니라 119안전센터에서 나간다

대인(서 1.0km) · 지산(동 1.2km). 둘 다 동명동 밖이다.

```
동 경계 진입점                60곳
실사용 진입점                 23곳
출동 통행의 80%를 담당         5곳
출동 통행의 50%를 담당         2곳
```

**진입점 5곳에 대책을 집중하면 출동의 80%가 개선된다.**

### 3-7. 소화전은 동명동에 1개뿐이다

```
동부소방서 관할   지상식 431 + 일반지하식 157 = 588
공개된 것         31개 (5%)
그중 동명동       1개
```

588 의 출처는 `hydrant_summary`(소방용수시설 현황, 관할서별 5행)다.
**공개 데이터에는 분모가 없다.** 이 표가 유일한 근거이므로 발표에 쓸 때 함께 인용할 것.

데이터 공백 자체가 결과다.

### 3-8. 런타임 프루닝은 폭이 아니라 경로로 한다

정적 폭 기준으로는 61%가 영상판정 대상으로 남는다(동명동이 원래 좁다).
반면 실제 최단경로에 등장하는 엣지는:

```
1회 이상 사용        665 / 4,227  (15.7%)
통행량 80% 담당      177개  (4.2%)
통행량 50% 담당       71개  (1.7%)
```

**71개 엣지가 출동 통행의 절반을 처리한다.** 여기부터 영상판정을 걸면 된다.

---

## 4. 외부 검증 — 소방서 지정 구간과의 대조

동부소방서 「소방통로확보대상 지역 현황」(2025-07-31, 20구간 7,120m 3,452세대)의
기록 폭과 우리 산출값을 비교했다.

| 도로명 | 소방서 | 우리(중앙) | 편차 |
|---|---:|---:|---:|
| 중앙로272번길 | 5m | 5.33m | **+0.33** |
| 동명로20번길 | 5m | 4.70m | **-0.30** |
| 동계로9번길 | 6m | 7.34m | +1.34 |
| 필문대로289번길 | 8m | 2.00m | -6.00 |

**동명동 2건이 각각 -0.30m, +1.34m.** 실측 없이 얻은 첫 외부 검증이다.

`필문대로289번길`의 -6.00m는 산출 오류가 아니라 매칭 한계다.
소방서 데이터에 좌표가 없어 도로명 전체(대로 구간 포함)를 잡았다.
정밀 대조에는 구간 좌표가 필요하며 D-30 인터뷰 요청 항목이다.

> 8/9에 "미연결 파편"으로 분류했던 **동명로20번길이 실제 소방서 지정 구간**이었다.
> 데이터 결함으로 본 곳이 위험구간이다.

---

## 5. 시스템 구성

```
data/raw/          2.5GB · git 제외 · sources.yaml 의 url 로 재취득
  ↓ src/etl/ingest.py            선언형. sources.yaml 만 고치면 된다
data/processed/    18종 · EPSG:5186(계산) / 4326(표출)
  ↓ src/etl/segments.py          노딩 → 폭 → 판정 → 경로
  ↓ src/etl/publish_web.py       표출용 경량 사본
web/data/          1.2MB · UI 입력 · git 포함
  ↓ web/index.html               MapLibre 5 + deck.gl 9 (interleaved) + V-World
GitHub Pages       gis 브랜치 푸시 시 자동 배포
```

### 계약

`tests/test_contract.py` 8종이 CI에서 검증한다.

```
얼린다:  좌표계 EPSG:4326 · 필드명/타입 · verdict 어휘 4종 · seg_id 불변성
바뀐다:  width_min_m · width_max_m · 각 구간의 verdict · route_usage
```

**값은 실측 후 바뀌고 구조는 안 바뀐다.** UI 코드는 손대지 않는다.

---

## 6. 데이터 현황

### 도로 소스를 전자지도로 통일 (2026-08-12)

`road_link` / `road_rw` 가 보던 `juso_road_geom`(291MB, 6월) 을
`juso_elctrnmap`(7MB, 7월) 으로 바꿨다. 전자지도 안에
`TL_SPRD_MANAGE`(도로구간) 와 `TL_SPRD_RW`(실폭도로) 가 이미 들어 있다.

```
도로구간   1812 → 1508
실폭도로   2371 → 1957
세그먼트   1266 → 1266   변화 없음
판정       clear 227 · needs_cv 139 · blocked 67 · unknown 833   변화 없음
```

구간 수는 줄었지만 **동명동 범위 안에서는 같은 데이터**다.
291MB 를 받지 않아도 되고 기준월도 7월로 맞는다.

### 확보 (19종)

도로구간 · 실폭도로 · 수치지도 도로경계면 · 표준노드링크 · 회전제한 ·
행정경계 · 건물 · 건물출입구 · CCTV · 소화전 · 소방서 · 상가정보 ·
단속이력 · 소방통로확보대상 · 주차장 · 공개DEM · 항공정사영상

### 쓸 수 없는 것 — 확인 완료

| 데이터 | 이유 |
|---|---|
| `ROAD_BT` | 84% 구간에서 과대. 미탐 방향 |
| 화재 연간통계 | 읍면동 컬럼 없음. 시군구까지 |
| 단속이력 **시간** | 14시가 39.1%. 입력 기본값 오염. **시간대 분석 불가** |
| 주차장 | 노외 64 / 노상 1. 유효폭과 무관 |
| 공개DEM | 90m 격자. 동명동이 12×12 픽셀 |
| 한전 전주 | 좌표 없음. 전산화번호만 |
| 가로등·보안등 | 공개 데이터 없음 |
| 지적도 | 소유권 경계 ≠ 통행 노면 |

### 미확보

| 데이터 | 용도 | 경로 |
|---|---|---|
| **5m DEM** | A* 경사 비용 + 3D 지형 | 국토정보플랫폼 신청 |
| **소화전 588개 좌표** | 소방용수 접근성 | D-30 인터뷰 |
| **소방통로 구간 좌표** | 정밀 대조 | D-30 인터뷰 |
| **폭 실측 10~20지점** | 소스 판정 | D-25 레이저 거리계 |

---

## 6-2. 데이터 보관 전략

### 현재 — 로컬 + 환경변수

```
data/raw        2.2GB · 저장소 밖 · FIRE_LANE_RAW 로 위치 지정
data/processed  로컬 생성물 · *.gpkg / *.tif 는 git 제외
web/data        13MB · git 포함 (UI 담당이 raw 없이 작업해야 한다)
```

**심링크는 쓰지 않는다.** `data/raw` 를 심링크로 걸었더니 git 이 추적했고,
exFAT 에서 `git reset --hard` 시 경로 문자열 파일로 체크아웃되면서
원본 2.5GB 가 소실됐다(2026-08-11). `src/etl/paths.py` 의 환경변수 방식으로 대체했다.

```bash
export FIRE_LANE_RAW="/mnt/f/.../FIRE_LANE/data/raw"     # 리눅스
setx FIRE_LANE_RAW "D:\...\FIRE_LANE\data\raw"        # 윈도우
```

미설정 시 `<repo>/data/raw` 를 쓴다. 단일 머신이면 그걸로 충분하다.

`web/data` 는 CI 가 **60MB 상한**을 감시한다. 타일이 늘어 넘으면 머지가 막힌다.

### 향후 — AWS S3

raw 를 S3 로 옮기고 GitHub Actions 와 엮으면 데이터 파이프라인이 완성된다.

```
S3  fire-lane-gis-raw/        원본
    fire-lane-gis-processed/  중간 산출물
Actions  raw 갱신 감지 → ingest → segments → publish → Pages 배포
```

`paths.py` 만 `s3://` 를 받도록 고치면 되고, 나머지 스크립트는 그대로다.
그래서 지금 환경변수로 분리해둔 것이다.

**다만 지금은 이르다.** 2.2GB 를 위해 S3 비용과 IAM·CI 설정을 감수할 단계가 아니고,
D-25 실측과 A* 가 먼저다. 4달 일정 중반쯤 파이프라인이 안정되면 옮긴다.

### 3-9. 단속 이력의 쓰임 — 시간대는 못 쓰고 빈도는 쓴다

```
85,380건 · 2022-01 ~ 2023-10 · 위반장소명(도로명주소) 보유
고유 장소 2,123개
14시 33,412건 = 39.1%   ← 입력 기본값. 시간대 분석 불가
```

**구간별 발생 빈도는 유효하다.** 도로명 상위: 충장로 5,068 · 구성로 3,797 ·
금남로 3,739 · 백서로 2,462 · 동계천로 2,432.

이것이 상습주차 추정 모델의 **레이블**이 된다.
CCTV 가 없어 영상판정이 불가능한 223구간의 주차 압력을
도면 특징(도로 폭·상가 밀도·주차장 거리 등)으로 추정하고 이 빈도로 학습한다.

**출동 시점의 상태가 아니라 사전 위험도다.** 그 구분을 흐리면 안 된다.
시점 정보는 CCTV 유효범위 안에서만 얻을 수 있고, 그 밖은 확률로 대비하는 수밖에 없다.

## 7. 남은 일

```
1. node_link + turn_restriction 투입     A* 필수. 이미 보유
2. A* 비용함수 설계                      거리 + 폭 + 통행제한 ∞ + 경사
3. 영상판정(호모그래피) 모듈 연결         needs_cv 72구간
4. D-25 레이저 실측                       width_verified: false → true
5. D-30 인터뷰                            소화전 좌표 + 소방통로 구간 좌표
6. 5m DEM 신청
```

**1번이 먼저다.** 새 데이터 없이 되고, 이것 없이 A*를 만들면 역주행 경로가 나온다.

### 알려진 근사

- `length_m` 은 **수평거리**다. 경사 보정 전이며 실주행거리는 0.05~1.2% 더 길다.
  보정에는 5m DEM 이 필요하다.
- 지도의 지형은 공개DEM 90m 를 보간한 **표현용**이다. `exaggeration` 기본값은
  1.0(실제 비율)이며, 올리면 표고가 왜곡되므로 발표에 쓸 때는 명시해야 한다.
- **CCTV 유효범위 25m 는 미확인 값이다.** 이 하나로 1,266구간의 판정이 갈린다.
  호모그래피 담당(백진욱)의 확인이 필요하다. 값이 바뀌면 전체가 재산출된다.

### 아직 검증되지 않은 것

- **폭 실측 0건.** 8/6 답사는 GPS 미기록으로 무효. 실폭도로 1.76m vs
  수치지도 3.86m 중 무엇이 맞는지 미판정. 그때까지 좁은 쪽으로 보수적으로 간다.
- **CCTV 25m**는 잠정값이다. D-25 실측 검증 대상.
  `segments.py`의 `CCTV_RANGE` 한 줄이며 바꾸면 전체가 재계산된다.
- `midpoint_fallback` 98개, `inherited` 67개는 정상 산출이 아니다.
  결과 해석 시 구분할 것.

---

## 8. 역할

| | 담당 | 영역 |
|---|---|---|
| 오창준 `@AIMasterFox` | GIS | `src/etl/` · `sources.yaml` · `data/` |
| 우지혜 `@marscoolcat` | UI · 3D | `web/index.html` |
| 경계 | CI | `tests/test_contract.py` |

브랜치 `gis` 정본. 작업은 `gis/*` · `ui/*` 에서 하고 PR로 합친다.
매일 푸시를 권장한다. 완성해야 푸시하는 게 아니라 동작하는 지점마다 푸시한다.

---

## 9. 이 프로젝트의 성격

숫자를 만들어내는 것이 아니라 **어디까지 알 수 있고 어디부터 모르는지를 긋는 것**이다.

- 골목의 42%는 도면만으로 판정이 안 된다 → 주황으로 표시한다
- 그중 다시 64%는 CCTV가 없어 영상판정도 안 된다 → 회색으로 표시한다
- 폭 값은 실측 전이다 → 화면 상단에 상시 경고한다

**모르는 것을 모른다고 표시하는 것**이 이 지도의 설계 원칙이다.
전부 초록으로 칠하면 첫 질문에 무너진다.


---

# 10. 용어

지도에 나오는 말들. 팀 누구나 이 문서만 보면 화면을 읽을 수 있어야 한다.

---

### 지금 어디까지 왔나

```
1단계  도면 프루닝     완료      ← 지금 지도에 보이는 색
2단계  영상판정        미착수    ← 호모그래피. 주황색이 여기서 갈린다
3단계  현장 실측       D-25      ← 레이저 거리계. 폭 값이 확정된다
```

**지금 색상은 최종 판정이 아니다.** 도면(공공 GIS 데이터)만으로 낼 수 있는
1차 분류다. 그래서 라벨에 "도면상"이 붙어 있다.

---

### 판정 4종

| 색 | 라벨 | 기준 | 뜻 |
|---|---|---|---|
| 초록 | 도면상 통과 | 최소 폭 ≥ 4.5m | 주차 1대가 있어도 통과. 영상판정 불필요 |
| 주황 | **판정 보류** | 그 사이 | **상습주차 여부로 갈린다. 영상판정 대상** |
| 빨강 | 도면상 불가 | 최대 폭 < 2.5m | 중형펌프차 폭 미달. 장애물이 없어도 못 지나간다 |
| 회색 | 산출 불가 | — | 실폭도로·건물 폴리곤이 없어 폭을 잴 수 없다 |

#### 기준의 출처

임의로 정한 값이 아니다. **소방청 「2025 화재현장 골든타임 확보 종합대책」**을 따른다.

```
진입불가지역   폭 2m 이하, 또는 이동불가 장애물로 진입 불가한 구간이 100m 이상
진입곤란지역   폭 3m 이상 도로 중 장애물·상습주차로 상시 장애, 구간 100m 이상
기준 차량      중형펌프차량 폭 2.5m
```

그래서 `TRUCK = 2.5`, 주차 1대 점유 2.0m를 더해 `clear` 기준이 4.5m다.
"왜 하필 그 숫자냐"는 질문에 국가 기준이라고 답할 수 있다.

#### 연속 구간 (`run_length_m`)

소방청 지정 기준의 **"구간 100m 이상"**은 세그먼트 하나가 아니라
같은 판정이 이어지는 **연속 구간 전체**의 길이다. 5m짜리 조각 하나로는 지정되지 않는다.

```
소방청 지정 기준 충족   173세그먼트
  진입불가급              4구간 · 448m
  진입곤란급            169구간 · 5,987m
연속 구간 최장             918m
```

`nfa_designated`가 `true`면 툴팁에 표시된다.

**주황 268개가 이 프로젝트의 미완성 부분이다.** 전체의 42%다.
영상판정이 붙으면 이 268개만 초록·빨강으로 갈리고 나머지는 그대로다.

---

### 폭이 왜 두 개인가

같은 골목을 두 가지로 잰다. 하나로 못 정하는 게 아니라 **둘 사이가 답이기 때문**이다.

| 화면 표기 | 필드명 | 데이터 | 뜻 |
|---|---|---|---|
| **최소 폭** | `width_min_m` | 실폭도로 폴리곤 | 장애물이 없을 때 확실히 비어 있는 폭 |
| **최대 폭** | `width_max_m` | 건물 폴리곤 | 양쪽 건물 벽 사이 거리. 물리적 최대 |

> 이전에 쓰던 "노면폭 하한 / 담~담 상한"은 **최소 폭 / 최대 폭**으로 통일했다.
> 문서·UI·발표 자료 전부 이 표기를 쓴다.

최대 폭이 최소 폭보다 **전 구간에서** 넓고 중앙값 차이가 2.08m다.
그 사이에 전신주·화분·에어컨 실외기·계단·주차 차량이 들어 있다.

3.0m 기준으로 보면 **최소 폭 기준 125구간 vs 최대 폭 기준 35구간**이 걸린다.
즉 90구간(47%)은 어느 쪽을 믿느냐에 따라 판정이 뒤집힌다.
**이 밴드를 좁히는 것이 영상판정과 현장 실측의 역할이다.**

> `ROAD_BT`(도로대장의 명목 폭)는 쓰지 않는다. 실측 대비 84% 구간에서
> 1m씩 넓게 나온다. 못 들어가는 길을 들어간다고 판정하는 방향이라 가장 위험하다.

---

### 소방 동선 (`route_usage`)

**이 골목을 소방차가 얼마나 자주 지나는가.**

119안전센터 2곳(대인·지산)에서 동명동 건물출입구 358곳까지
최단경로를 **전부** 계산한 뒤, 각 골목이 몇 개의 경로에 등장하는지 센 값이다.

**선 굵기는 이 값이 아니다.** 선 굵기는 최소 폭이다(아래 참조).

> 소방 동선은 현재 지도 화면에서 빼두었다. `route_usage` 필드는 데이터에 남아 있으므로
> 필요할 때 다시 꺼내 쓸 수 있다. 프루닝 근거로는 계속 유효하다.

```
골목 641개 중 실제로 쓰이는 것        495개
출동 통행의 50%를 담당하는 골목        71개
출동 통행의 80%를 담당하는 골목       177개
```

**왜 중요한가.** 영상판정을 641개 전부에 돌리면 출동할 때마다 계산이 쌓인다.
실제로 밟히는 골목만 보면 대상이 확 준다. 이게 프루닝이다.

### 선 굵기 = 최소 폭

화면의 선 두께가 곧 그 골목의 폭이다. 미터 단위로 그려지므로
확대·축소해도 실제 비율이 유지된다. 굵은 선이 넓은 길이다.

폭을 못 잰 구간(회색)은 최소 굵기로 가늘게 그린다.

### 길이 (`length_m`)

그 구간의 **수평거리**(m). 평면 좌표계(EPSG:5186)에서 잰 값이다.

**경사 보정 전이다.** 실제 주행거리는 `수평거리 / cos(경사)` 이므로 더 길다.

```
동명동 평균 경사 1.8°  →  실주행거리 +0.05%   (100m 당 5cm)
최대 경사      9.0°  →              +1.2%    (100m 당 1.2m)
```

무시할 만한 수준이지만 **"같다"가 아니라 "차이가 이만큼이고 반영하지 않았다"** 가 정확하다.
보정하려면 구간별 경사가 필요하고, 그러려면 5m DEM 이 있어야 한다.
공개DEM 90m 로는 한 픽셀이 골목 20개를 덮어 산출이 불가능하다.

**폭은 경사 영향을 받지 않는다.** 폭은 진행 방향과 수직으로 재므로
도로가 기울어도 좌우 거리는 변하지 않는다. 다만 횡단경사(편경사)가 있으면
유효폭이 줄어드는데, 이 역시 90m 격자로는 잡을 수 없다.

---

### 세그먼트 / 노딩

**세그먼트** = 교차로에서 잘라낸 도로 조각. 판정의 기본 단위다.

원본 데이터(도로명주소 도로구간)는 "도로명 하나 = 선 하나"라
제봉로가 중앙로를 가로질러도 통짜 한 줄이다. 그대로는 경로 탐색이 안 된다.
교차점마다 잘라서 그래프로 만드는 것이 **노딩**이다.

```
원본 도로구간   206개
노딩 후         641개 세그먼트   ← 판정 단위
```

> 기존 문서의 "222구간"은 근거를 찾지 못했다. 641로 교체됐다.

---

### 스코프

지도가 보여주는 범위. **동명동 + 접근 회랑 + 안전센터 반경 300m**, 약 2.8 × 1.4km.

동명동만 보면 안 되는 이유는 **119안전센터가 동 밖에 있기 때문**이다
(대인 서쪽 1.0km · 지산 동쪽 1.2km). 출동 경로가 화면에서 잘린다.

**접근 회랑** = 안전센터에서 동명동까지 오는 길. 최단경로 중 동 밖 구간이다.
광주 전체가 아니라 얇은 리본이다.

화면 밝기가 3단계다.

```
동명동      밝게        판정 대상
접근 회랑   살짝 어둡게  오는 길
그 밖       덮음        스코프 밖
```

이 범위 밖으로는 이동도 축소도 안 된다. 작업 범위가 구조적으로 고정된다.

---

### 시설 마커

마커는 전부 **3D 입체**다. 2D 점은 기울인 화면에서 지면에 붙어 안 보인다.
원기둥 3~4개를 쌓아 실제 시설의 실루엣을 근사한다.

| 마커 | 색 | 형상 | 건수 |
|---|---|---|---|
| **119안전센터** 출동 시작점 | 빨강 | 기둥 + 헤드 + 첨두 | 2 |
| 동부소방서(본서) | 주황 | 본체 + 안테나 | 1 |
| 소화전 | 파랑 | 받침 + 몸통 + 플랜지 + 캡 | 11 (동명동 **1**) |
| CCTV | 보라 | 지주 + 하우징 + 렌즈 | 104지점 312대 |

**비율은 실물을 따르되 크기는 약 5배 과장했다.** 소화전 실물은 0.9m라
실제 치수로 그리면 1km 시야에서 점 하나가 되어 보이지 않는다.

소화전이 안 보인다면 버그가 아니라 **동명동 안에 1개뿐**이기 때문이다.

**CCTV 커버리지**는 반경 30m 원으로 표시된다. 촬영방면이 전부 360도라
방향 콘이 아니라 원이다. **커버리지가 닿지 않는 골목**을 보이게 하는 것이 목적이다.

> CCTV 원본은 설치연도별로 행이 쪼개져 있다. 동명동 53행이 실제로는 29지점 90대다.
> 좌표로 묶어 지점 단위로 집계했고, `설치회차`에 증설 횟수가 남는다.

**소화전이 동명동에 1개인 것은 버그가 아니다.**
동부소방서 관할 588개 중 공개된 것이 31개(5%)뿐이고 그중 동명동이 1개다.
이 희소성 자체가 발표 논거다. 패널의 `1 / 11`이 그 표기다.

가로등·보안등은 공개 데이터가 아예 없다. 야간 시인성 논거는 CCTV 분포로 대체한다.

---

### 자주 나오는 질문

**Q. 왜 다 초록으로 안 칠하나**
칠할 수 있는데 안 한다. 42%는 도면만으로 판정이 안 되고,
그걸 초록으로 칠하면 못 들어가는 골목을 들어간다고 말하는 것이 된다.
모르는 것을 모른다고 표시하는 게 이 지도의 핵심이다.

**Q. 회색(산출 불가)은 데이터가 부족한 건가**
37개다. 대부분 교차로에서 잘린 3m 미만 조각이라 폭을 잴 대상이 아니다.
데이터 부족이 아니라 측정 단위가 안 되는 것이다.

**Q. 폭 값은 언제 확정되나**
D-25 레이저 거리계 실측 후다. 실폭도로는 1.76m, 수치지도는 3.86m로
같은 구간을 다르게 잰다. 어느 쪽이 맞는지는 현장에서만 판정된다.
그때까지 좁은 쪽을 기준으로 보수적으로 간다.

**Q. 값이 바뀌면 UI를 다시 만들어야 하나**
아니다. `verdict` 문자열만 보고 그렸다면 색만 바뀐다.
그래서 임계값(3.0 / 5.0)을 UI 코드에 복사하면 안 된다.

---

# 11. UI 인수인계

`@marscoolcat` 앞. 2026-08-11 갱신.

---

### 30초 안에 띄우기

```bash
git checkout gis && git pull
cd web && python -m http.server 8000
```

`http://localhost:8000`

**`index.html` 을 더블클릭하면 빈 화면이 뜬다.** `file://` 에서는 `fetch()` 가 CORS 로 막힌다.
설치할 것은 없다. MapLibre 와 deck.gl 은 CDN 에서 온다.

---

### 먼저 — 어제 충돌은 제 잘못입니다

`index.html` 한 파일에 HTML·CSS·JS 를 다 넣은 채로 드려서 같은 줄을 동시에 고치게 됐습니다.
**계층 분리를 안 한 상태로 넘긴 게 원인**이고, 그래서 아래처럼 나눴습니다.

지혜님이 만드신 **출동 모드, FAB 버튼, 미니맵, 소화전 물결, 판정 기준표,
"경로 빈도는 계산값" 주석** 전부 살려서 옮겼습니다. 23개 항목 검증 완료.

특히 **"경로 빈도는 실제 출동 기록이 아니다"** 주석은 제가 놓친 부분이었습니다.
그대로 뒀으면 심사에서 "출동 데이터가 있느냐"는 질문에 무너졌을 겁니다.

---

### 파일 구조 — 이제 충돌이 안 납니다

| 파일 | 주인 | 내용 |
|---|---|---|
| `web/style.css` | **지혜님 단독** | 색·간격·타이포·애니메이션·레이아웃 |
| `web/config.js` | **공동** | 판정 색상·마커 형상·카메라·출동모드·미니맵 설정 |
| `web/index.html` | 공동 | 뼈대 마크업. 거의 안 바뀜 |
| `web/app.js` | 오창준 단독 | 데이터 로딩·레이어·판정 렌더링 |
| `web/data/` | 생성물 | 손으로 고치지 말 것 |

**UI 작업은 `style.css` 와 `config.js` 두 파일이면 됩니다.**
`app.js` 를 건드릴 일이 생기면 그건 로직 문제이므로 알려주세요.

CODEOWNERS 도 파일별로 나눠뒀습니다.

---

### `config.js` — 지혜님이 만지실 값

```js
/* 판정 색상. style.css 의 --blocked 등과 같은 값을 유지할 것 */
verdict: {
  blocked : { color:[255, 77, 61], label:"도면상 불가", desc:"…" },
  needs_cv: { color:[255,171, 46], label:"판정 보류",   desc:"…" },
  clear   : { color:[ 74,209,143], label:"도면상 통과", desc:"…" },
  unknown : { color:[ 90, 98,114], label:"판정 불가",   desc:"…" },
},

noCctvColor: [168,132,78],   // CCTV 사각으로 죽은 구간. 주황+회색 혼합

/* 출동 모드 — 지혜님 기능 */
dispatch: {
  clearWidthScale: 1.6,   // 통과 구간 선 굵기 배수
  dimAlpha       : 102,   // 나머지 투명도 (0~255). 102 = 40%
  pulseMs        : 1200,  // 소화전 물결 주기(ms). 작을수록 빠르다
},

minimap: { showFromZoom: 16 },   // 이 줌 이상에서 미니맵이 뜬다

markers: [ … ],                  // 마커 3D 형상. 원기둥을 쌓아 실루엣을 만든다

terrain: { enabled:true, exaggeration:1.0 },
```

**임계값(3.0 / 7.0 / 25.0)의 정본은 `src/etl/segments.py` 입니다.**
`config.js` 의 같은 숫자는 화면 설명용 사본이라 바꿔도 판정은 안 바뀝니다.

---

### 지혜님 기능이 어디로 갔나

| 기능 | 위치 |
|---|---|
| 출동 모드 토글 마크업 | `index.html` (`.row.dispatch`) |
| FAB 버튼 마크업 | `index.html` (`#dispatch-fab`) |
| FAB 디자인 · sos 점멸 | `style.css` |
| 미니맵 컨테이너 | `index.html` (`#minimap`) |
| 미니맵 스타일 · `.show` 전환 | `style.css` |
| 미니맵 로직 (뷰박스·현위치·루트) | `app.js` |
| 소화전 물결 | `app.js` |
| 출동 시 강조·흐림 | `app.js` + `config.js` |
| 조정값 (1.6 / 102 / 1200 / 16) | **`config.js`** |

값은 그대로 유지하고 위치만 `config.js` 로 옮겼습니다.

소화전 물결은 마커가 3D 기둥으로 바뀌면서 **지면에 링이 퍼지고 그 위에 기둥이 서는**
구조가 됐습니다. 원본 의도는 그대로입니다.

---

### 지금 지도에 있는 것

**판정 4색** — 골목 1,266개

| 색 | verdict | 뜻 |
|---|---|---|
| 초록 | `clear` | 도로 폭 7m 이상. 양쪽 주차해도 통과 |
| 주황 | `needs_cv` | 도면만으로 결론 안 남. **영상판정 대상** |
| 빨강 | `blocked` | 벽 사이 폭 3m 미만. 차가 없어도 불가 |
| 회색 | `unknown` | 판정 불가 |
| 탁한 황갈 | `unknown(no_cctv)` | **CCTV 25m 밖이라 영상판정 자체가 불가능** |

**선 굵기가 실제 도로 폭입니다.** 굵은 선이 넓은 길입니다.

**3D 마커** — 119안전센터 · 소방서 · 소화전 · CCTV. 원기둥을 쌓아 실루엣을 만듭니다.
비율은 실물, 크기는 약 5배 과장했습니다. 소화전 실물이 0.9m 라 실제 치수로는 안 보입니다.

**지형** — 공개DEM 90m 를 보간한 Terrain-RGB 타일. 지면이 실제로 휩니다.
동명동은 2.8km 에 기복 42m 라 완만합니다. `exaggeration` 기본은 **1.0(실제 비율)** 입니다.
올리면 보기는 좋아지지만 표고가 왜곡되므로, 발표에 쓸 때는 그 사실을 명시해야 합니다.

---

### 값은 바뀝니다. 구조는 안 바뀝니다.

폭 값은 아직 **미검증**입니다(`width_verified: false`). 레이저 실측 후 바뀌고,
일부 구간의 `verdict` 도 바뀝니다.

그때 UI 를 다시 만들지 않으려면 세 가지만 지키면 됩니다.

**1. 숫자가 아니라 `verdict` 문자열로 분기**

```js
if (p.verdict === "blocked") …     // 이렇게
if (p.width_max_m < 3.0)  …        // 이러면 안 됨
```

**2. 색 매핑은 `config.js` 한 곳에만**

**3. `verdict` 는 이 4개가 전부**

`tests/test_contract.py` 가 CI 에서 검증합니다. 깨지면 머지가 막힙니다.

| 항목 | 보장 |
|---|---|
| 좌표계 | `EPSG:4326` 고정 |
| 필드명·타입 | 고정 |
| `verdict` 어휘 | 4종 고정 |
| `seg_id` | 불변 키 |

---

### 데이터 필드

`web/data/segments.geojson` — 1,266개

| 필드 | 설명 |
|---|---|
| `seg_id` | 불변 키 |
| `verdict` | 판정 4종 |
| `width_min_m` | **도로 폭.** 포장 노면만. 선 굵기가 이 값 |
| `width_max_m` | **벽 사이 폭.** 건물 벽에서 벽까지. 대로는 `null` |
| `length_m` | 수평거리. 경사 보정 전 |
| `run_length_m` | 같은 판정이 이어지는 총 길이 |
| `nfa_designated` | 소방청 지정 기준(연속 100m) 충족 |
| `cctv_dist_m` | 가장 가까운 CCTV 까지 거리 |
| `cv_feasible` | CCTV 25m 이내인가 |
| `unknown_reason` | `no_cctv` \| `width` \| `null` |
| `route_usage` | 소방 동선 빈도(계산값) |
| `z` | 표고(m). 표현용 |

**`width_max_m` 이 `null` 인 것은 오류가 아닙니다.** 큰 도로는 건물이 40m 밖이라
벽 사이를 잴 수 없고, 그런 구간은 도로 폭만으로 이미 판정이 끝납니다.

---

### 다른 파일

- `buildings.geojson` — 2,085동. `h` = 층수 × 3.3
- `stations.geojson` — `kind` 로 `center`(119안전센터) / `station`(소방서) 구분.
  둘은 **주소가 같습니다**(제봉로 210). 겹치므로 형상을 다르게 그립니다.
- `hydrants.geojson` — **동명동 안은 1개뿐입니다.** 버그가 아닙니다.
  관할 588개 중 31개(5%)만 공개돼 있고 그중 하나입니다. 패널의 `1 / 11` 이 그 표기입니다.
- `cctv.geojson` — 104지점 312대
- `terrain/{z}/{x}/{y}.png` — Terrain-RGB 22장

---

### 작업 흐름

```
gis ─────────────── 정본
 └ ui/*  ────────── 지혜님 작업 브랜치 → gis 로 PR
```

```bash
git checkout gis && git pull
git checkout -b ui/작업이름
## 작업
git add -A && git commit -m "ui: …" && git push -u origin ui/작업이름
```

**하루 안에 머지하고 브랜치를 지웁니다.** 어제 충돌이 커진 건 브랜치가 3시간 떠 있는 동안
제가 같은 파일을 두 번 갈아엎었기 때문입니다.

`web/data/` 에서 충돌이 나면 손으로 풀지 마세요. `git checkout --theirs web/data/` 로
gis 쪽을 취하면 됩니다. 생성물이라 재생성됩니다.

---

### 막히면

| 증상 | 원인 |
|---|---|
| 빈 화면, 콘솔 CORS | `file://` 로 열었다. 서버로 띄울 것 |
| 판정 색이 안 보임 | 콘솔 확인. `map.getLayer("seg-l")` 존재 여부 |
| 미니맵이 안 뜸 | **줌 16 이상**에서만 뜬다. `map.getZoom()` 확인 |
| 지형이 안 보임 | `map.getTerrain()` 이 `null` 이면 미적용 |
| 건물이 납작함 | 줌 14.4 미만이다 |
| 소화전이 1개뿐 | 정상. 위 참조 |

콘솔 오류는 그대로 보내주세요.

---

### 지금 손대면 좋을 것

값이 안 바뀌는 영역이라 안전합니다.

- `style.css` 전반 — 패널 레이아웃, 타이포, 여백
- `config.js` 의 `dispatch` — 강조 정도, 물결 속도
- `config.js` 의 `markers` — 마커 형상·색
- 라이트 모드(`[data-theme="light"]`) 색상 다듬기
- 판정 기준표(`.crit`) 디자인
- 모바일 레이아웃 (820px 브레이크포인트)

**아직 손대지 마세요:** `unknown` 구간 표현 방식.
1,266개 중 833개라 비중이 큰데, 이걸 어떻게 보여줄지는 방법론 결정이라
제가 먼저 정해야 합니다.

---

# 12. 협업

브랜치는 `gis` 하나다. 각자 작업 브랜치를 파서 PR 로 합친다.

```
gis ─────────────────────── 정본. 여기가 항상 동작해야 한다
 ├ gis/*   오창준 (@AIMasterFox)   데이터·판정·경로
 └ ui/*    우지혜 (@marscoolcat)   화면·3D
```

### 하루 시작

```bash
git checkout gis
git pull
git checkout -b ui/오늘작업이름     # 또는 gis/오늘작업이름
```

### 하루 끝 — 푸시를 권장한다 (강제 아님)

```bash
git add -A
git commit -m "ui: 판정 색상 대비 조정"
git push -u origin ui/오늘작업이름
```

GitHub 에서 `gis` 로 PR 을 연다. 미완성이면 **Draft PR** 로 열어두면 된다.

**왜 매일 푸시하나**

- 로컬에만 있는 작업은 없는 것과 같다. 노트북이 죽으면 그대로 날아간다.
- 상대가 뭘 하고 있는지 보인다. 4달짜리 프로젝트에서 이게 제일 크다.
- CI 가 매번 돌아 계약이 깨졌는지 즉시 안다. 일주일 뒤에 알면 못 고친다.

완성해야 푸시하는 게 아니다. **동작하는 지점마다 푸시한다.**

### 자동으로 도는 것

| 워크플로 | 시점 | 하는 일 |
|---|---|---|
| `contract` | `gis` 로 push·PR | 계약 8종 검증. 깨지면 머지 차단 |
| `지도 배포` | `gis` 의 `web/**` 변경 | GitHub Pages 갱신 |

CI 가 데이터를 다시 만들지는 **않는다.** `data/raw` 2.5GB 가 저장소에 없기 때문이다.
파이프라인은 오창준 로컬에서만 돈다.

```bash
uv run python src/etl/ingest.py        # raw → processed (15종)
uv run python src/etl/segments.py      # 노딩 → 폭 → 판정
uv run python src/etl/terrain.py       # 공개DEM → Terrain-RGB 타일
uv run python src/etl/ortho.py         # 항공정사영상 → 배경 타일 (25cm)
uv run python src/etl/publish_web.py   # → web/data
uv run pytest tests/test_contract.py   # 계약 확인
```

이 4줄을 돌린 뒤 `web/data` 를 커밋하면 지혜님이 `git pull` 만으로 새 판정을 받는다.

### 지도 URL

`Settings > Pages > Source: GitHub Actions` 를 한 번 켜면
`gis` 에 푸시할 때마다 아래가 자동 갱신된다.

```
https://woongtopia.github.io/fire-lane/
```

로컬 서버를 띄우지 않아도 되고, `file://` 로 열어 빈 화면을 보는 사고도 없다.

### 충돌이 나면

거의 안 난다. 파일이 갈려 있다.

| 경로 | 주인 |
|---|---|
| `src/etl/`, `sources.yaml`, `data/` | 오창준 |
| `web/index.html` | 우지혜 |
| `web/data/` | 생성물. 아무도 손으로 안 고친다 |

`web/data/` 에서 충돌이 나면 손으로 풀지 말 것.

```bash
git checkout --theirs web/data/     # gis 쪽을 취한다
uv run python src/etl/publish_web.py  # 또는 재생성
```

### 커밋 메시지

```
gis:  데이터·파이프라인·판정
ui:   화면·스타일·인터랙션
docs: 문서
fix:  버그
```

한 줄이면 충분하다. 무엇을 왜 바꿨는지만 남기면 된다.

### 1. 브랜치 생성 · 초기 푸시 (GIS 담당, 1회)

```bash
git checkout -b gis
git add -A
git commit -m "gis: 전자지도 승인분 반영 + 세그먼트 판정 + 표출 지도

- sources.yaml: boundary_emd / building / building_entrance / hydrant_point 추가
- src/etl/segments.py: 노딩(끝점투영 0.5m) → 폭 밴드 → 진입판정
- src/etl/publish_web.py: web/data 경량 사본
- web/: MapLibre + deck.gl(interleaved) + V-World
- tests/test_contract.py: GIS↔UI 계약 검증 8종
- 세그먼트 641개 확정 (기존 문서의 '222'는 근거 없음)"
git push -u origin gis
```

### 2. 보호 규칙 (GitHub 웹)

`Settings > Branches > Add branch protection rule`

- Branch name pattern: `gis`
- ☑ Require a pull request before merging
- ☑ Require review from Code Owners  ← **이걸 켜야 CODEOWNERS가 동작한다**
- ☑ Require status checks to pass → `contract`
- ☐ Allow force pushes (끄기)

이걸 안 켜면 `.github/CODEOWNERS`는 그냥 텍스트 파일이다.

### 3. UI 담당 (지혜님)

```bash
git fetch origin
git checkout -b ui/base origin/gis
cd web && python -m http.server 8000
```

이후는 `docs/HANDOFF_UI.md` 참조.

---

### 왜 락이 3중인가

| 층 | 막는 것 | 한계 |
|---|---|---|
| `.gitignore` | `data/raw` 커밋 | 이미 있음 |
| `CODEOWNERS` | GIS 코어 무단 수정 | 리뷰 강제일 뿐, 승인하면 통과 |
| **계약 테스트** | **구조 파괴** | **실제로 막는 건 이것** |

CODEOWNERS는 "누가 봐야 하는가"를 정하고, 계약 테스트는 "무엇이 깨지면 안 되는가"를
정한다. 후자가 본체다. 좌표계가 5186으로 나가거나 `verdict`에 새 값이 생기면
CI가 즉시 잡는다.

### 락을 거는 대상은 값이 아니라 스키마다

폭 값은 **미검증 상태(`width_verified: false`)**다. D-25 레이저 실측 후 바뀐다.
값을 얼리면 거짓말이 되고, 스키마를 얼리면 UI가 안 깨진다.

```
얼린다:  좌표계 · 필드명/타입 · verdict 어휘 5종 · seg_id 불변성
바뀐다:  width_min_m · width_max_m · 각 구간의 verdict · route_usage
```

실측 후 갱신은 이 순서다. **UI 코드는 손대지 않는다.**

```bash
python src/etl/segments.py       # 폭 재산출
python src/etl/publish_web.py    # web/data 갱신
pytest tests/test_contract.py    # 계약 유지 확인
```

### 데이터를 저장소에 넣는 기준

| 경로 | git | 이유 |
|---|---|---|
| `data/raw/` | ✗ | 2.5GB. `.gitignore` |
| `data/processed/*.gpkg` | ✗ | 재생성 가능 |
| `data/processed/segments.geojson` | ✓ | UI 입력. 재생성에 raw가 필요하다 |
| `data/processed/_manifest.json` | ✓ | 재현성 기록 |
| `web/data/` | ✓ | UI 담당이 raw 없이 작업할 수 있어야 한다 |

**핵심은 지혜님이 2.5GB raw 없이도 지도를 띄울 수 있어야 한다는 것.**
그래서 `web/data/` 1.2MB는 커밋한다.


---

### 한글 파일명 주의

`docs/기획서_Fire-Lane.docx` 가 윈도우/리눅스를 오가면서 삭제 + 미추적으로 갈린 적이 있다.
git 이 한글 파일명을 escape 로 저장하는데, 두 OS 의 정규화 방식이 달라 생기는 문제다.

**전역 설정 한 번이면 해결된다.**

```bash
git config --global core.quotepath false     # 한글 파일명을 그대로 표시
git config --global core.precomposeunicode true
```

`git status` 에 `#Uae30#Ud68d...` 같은 게 뜨면 이 설정이 안 된 것이다.

**앞으로 새로 만드는 파일은 영문 파일명을 쓴다.** 문서 제목은 파일 안에 쓰면 된다.
`docs/PROPOSAL_Fire-Lane.docx` 처럼.

---

# 13. 데이터 이력

---

### 0. 2026-08-11 갱신

#### 해소된 pending

도로명주소 전자지도 11종 심사 승인. `juso/juso_elctrnmap_jngj_20260811.zip`

| pending | 상태 | dataset 키 | 동명동 |
|---|---|---|---:|
| boundary | **해소** | `boundary_emd` | EMD_CD 12210108 · 0.4289 km² |
| building | **해소** | `building` | 2,085동 |
| — (신규) | 추가 | `building_entrance` | 1,313개 |
| hydrant_point | **부분해소** | `hydrant_point` | 공개 31개 중 동명동 1개 |

`hydrant_point` 원본(`safety_hydrant_point_jngj_20260809.csv`)은 8/9부터 raw에
있었으나 sources.yaml에 미등록 상태였다. 이번에 등록했다.
공개율 5%는 그대로이므로 D-30 인터뷰는 유효하다.

#### ★ "222구간" 확정 — 근거 없음

정식 경계로 재산출했다.

| | 값 |
|---|---:|
| 도로구간 (TL_SPRD_MANAGE) | 206 |
| 실폭도로 (TL_SPRD_RW) | 219 |
| **노딩 후 세그먼트** | **641** |

206도 219도 222가 아니다. **전 문서의 "222"를 641로 교체할 것.**
bbox 기준 4,336도 폐기.

#### ★ 노딩 실패의 진짜 원인 = T자 접합

`unary_union`만으로는 부족하다. 지선 끝점이 본선 세그먼트의 *중간*에 접하는데
밀리미터 단위로 벌어져 있어 위상이 생기지 않는다. 파편 21개 중 18개가
본선까지 거리 0.0~0.3m였다.

| 처리 | 연결요소 | 최대요소 연장비 |
|---|---:|---:|
| 원본 | 40 | 93.29% |
| `set_precision(0.5)` | 22 | 97.74% |
| **끝점 투영 스냅 0.5m** | **6** | **99.76%** |
| 두 방법 병용 | 33 | 95.18% ← 악화 |

병용하면 격자 스냅이 투영해둔 점을 다시 흩뜨린다. **하나만 쓸 것.**

남은 5개(77m)는 알고리즘 한계가 아니라 **도로명 미부여 구간**이다.
그중 3곳이 관리폭 1.0~1.5m로, 소방차 진입이 원천 불가한 지점이다.
데이터의 공백 자체가 위험구간 지표다. 답사 대상.

```
35.15167,126.92707  동명로        1.2m 이격
35.15091,126.92715  동명로26번길   0.0m
35.14904,126.92947  지호로        10.6m  ← 실폭도로로도 미연결. 최우선 확인
35.15344,126.92392  동계로15번길  17.3m
35.15332,126.92409  동계로15번길  14.8m
```

#### ★ 도로폭은 하한/상한 밴드로 쓴다

`ROAD_BT` 배제 근거가 실측으로 확정됐다. **171구간(84%)에서 실폭도로
트랜섹트보다 넓고, 중앙 차이 +1.00m다.** 미탐 방향으로 편향된다.

| 소스 | 역할 |
|---|---|
| 실폭도로 트랜섹트 | `width_min_m` — 무장애 확실 폭(하한) |
| 건물 폴리곤 트랜섹트 | `width_max_m` — 담~담 물리 상한 |
| `ROAD_BT` | 값 아님. 교차검증 플래그로만 |
| 지적도 | 파이프라인 제외 (소유권 경계 ≠ 통행 노면) |
| 정사영상 | 파이프라인 제외. 3D 배경용 |

담~담이 노면폭보다 **전 구간에서** 넓고 중앙 차이 +2.08m다.
3.0m 임계로 보면 노면 기준 125구간 vs 담~담 기준 35구간 — **90구간(47%)의
판정이 뒤집힌다.** 이 밴드를 좁히는 것이 영상판정과 실측의 역할이다.

#### 런타임 프루닝 — 폭이 아니라 경로로 한다

정적 폭 기준으로는 61.3%가 CV 대상으로 남는다. 동명동이 원래 좁아서 안 걸러진다.

반면 안전센터 2곳 → 건물출입구 358개 최단경로를 전부 뽑으면
**4,227개 엣지 중 665개(15.7%)만 실제로 밟힌다.**

```
1회 이상 사용        665개  15.7%
20회 이상            267개   6.3%
통행량 80% 담당      177개   4.2%
통행량 50% 담당       71개   1.7%
```

71개 엣지가 출동 통행의 절반을 처리한다. **CV는 여기부터 걸면 된다.**
1·2단계는 전부 사전계산이라 출동 시 비용이 0이다.

#### 현재 판정 분포 (641 세그먼트)

| verdict | 구간 | 통행량 |
|---|---:|---:|
| clear (통과 확실) | 162 | 53.0% |
| needs_cv (영상판정) | 268 | 21.8% |
| likely_clear | 105 | 16.3% |
| unknown | 44 | 6.8% |
| blocked (진입 불가) | 62 | 2.1% |

`midpoint_fallback` 98개, `inherited` 67개는 정상 산출이 아니다. 해석 시 구분할 것.

#### 신규 산출물

```
data/processed/segments.geojson · segments_5186.gpkg · segments.schema.json
web/index.html + web/data/          MapLibre + deck.gl + V-World
src/etl/segments.py                 노딩 → 폭 → 판정
src/etl/publish_web.py              표출용 경량 사본
```

#### ⚠ ingest.py 알려진 문제

`--only`로 실행해도 `_manifest.json`을 **전체 덮어쓴다.** 지정한 키만 남고
나머지 기록이 사라진다. 부분 실행 후에는 전체를 다시 돌리거나 병합 로직을 넣을 것.

#### 여전히 미해결

- **폭 실측 0건.** 8/6 답사는 GPS 미기록으로 무효. 원본 사진에도 EXIF 없음(재확인 완료).
  실폭도로 1.76 vs 수치지도 3.86 중 무엇이 맞는지 아직 판정 불가.
  → D-25 레이저 거리계 10~20지점. **네이버 지도 거리재기로 대체 불가**
    (영상을 영상으로 검증하는 순환 논증이고, 노면폭 중앙값 2.01m는 항공영상 판독 한계 이하).
- 화재 이력: `연간화재통계` 파일데이터에 **읍면동 컬럼이 없다.** 시군구까지다.
  2024년 광주 동구 68건(주거 23 = 단독 12 + 공동 11 / 부주의 28 · 전기 20).
  배경 통계로만 사용 가능하고 구간 단위 검증은 불가. 구간 검증이 필요하면 D-30 인터뷰로.

---

## 데이터 인벤토리 — 2026-08-09

> 이 문서는 `sources.yaml`의 사람용 요약본이다. 기계가 읽는 정본은 `sources.yaml`.
> `data/processed/_manifest.json`에 매 실행의 SHA-256과 산출 건수가 기록된다.

---

### 1. 확보 완료

| 키 | 데이터 | 좌표계 | 동명동 건수 | 용량 |
|---|---|---|---:|---:|
| `road_link` | 도로명주소 도로구간 (RN, ROAD_BT) | 5179 | 1,812 | 285MB |
| `road_rw` | 도로명주소 실폭도로 (면) | 5179 | 2,371 | ↑동일 zip |
| `ngii_road` | 수치지도 도로경계면 `NF_A_A01000` (rvwd) | 5179 | 3,740 | 15MB |
| `node_link` | 표준노드링크 LINK | 5186 | 1,366 | 258MB |
| `node_point` | 표준노드링크 NODE | 5186 | 968 | ↑동일 zip |
| `turn_restriction` | 회전제한 TURNINFO | — | 87 | ↑동일 zip |
| `cctv` | CCTV (촬영방면·설치연도) | 4326 | 27지점/86대 | 756KB |
| `fire_station` | 소방서·119안전센터 좌표 | 4326 | 3 | 168KB |
| `poi_store` | 상가(상권)정보 | 4326 | 1,070 | 337MB |
| `enforcement` | 불법주정차 단속 (좌표 없음) | — | 85,380 | 4.8MB |
| `hydrant_summary` | 소방용수 집계표 (좌표 없음) | — | 5행 | 4KB |
| — | 항공정사영상 4도엽 (파이프라인 미투입) | 5186* | — | 1.3GB |

**합계 약 2.5GB.** `data/raw/`는 git에 안 올린다. S3 `fire-lane-gis-raw` 버킷 생성이 아직 안 됐다.

### 2. 미확보

| 데이터 | 경로 | 상태 |
|---|---|---|
| **행정구역경계 (읍면동)** | juso.go.kr 전자지도 | **심사 대기** ★ 최우선 |
| 건물 폴리곤 | 동일 (11종에 포함) | 심사 대기 |
| 소화전 좌표 | D-30 인터뷰 | 미착수 |
| 가로등·보안등 | — | **공개 데이터 없음 확정.** 야간 답사로 대체 |
| 화재 출동 이력 | data.go.kr 소방청 화재현황 API | 활용신청 미착수 |

---

### 3. 오늘 확인된 것 — 기존 문서와 어긋나는 부분

#### ★ 3-1. 도로구간이 교차로에서 안 끊겨 있다 (D-05)

```
도로명주소 도로구간   교차 노드    19개 / 미분할 접점 2,338개   ← 그래프 성립 불가
표준노드링크         교차 노드   452개 / 미분할 접점     7개   ← 정상
```

도로명주소는 "도로명 하나 = 선 하나"라 제봉로가 중앙로를 가로질러도 통짜 한 줄이다.
**노딩 없이 networkx에 넣으면 다익스트라가 "경로 없음"을 뱉는다. 코드 문제가 아니다.**

노딩 결과(bbox 기준): `1,812 → 4,336 세그먼트 / 노드 3,702 / 연결요소 338개(최대 80%)`

> **"222구간"의 근거를 찾지 못했다.** 정식 행정경계 확보 후 재산출하여 전 문서를 교체할 것.

#### ★ 3-2. 도로폭 소스 3종이 서로 다른 값을 준다

동일 구간(`ROAD_BT`=2.0)을 세 방법으로 측정:

| 소스 | 폭 |
|---|---:|
| `ROAD_BT` (명목값) | 2.00 |
| 실폭도로 `TL_SPRD_RW` 트랜섹트 | 1.76 |
| 수치지도 `NF_A_A01000` 트랜섹트 | 3.86 |

**`ROAD_BT` 사용 금지 확정.** 정수 비율 90%, 값의 30%가 정확히 2.0에 몰려 있다.
실측값이 아니라 도로명 부여 기준의 명목 관리 폭이다.
게다가 실제보다 **크게** 나오는 방향이라 D-03의 미탐(막혔는데 통과 판정) 쪽으로 편향된다.
가장 위험한 방향이다.

수치지도가 실폭도로보다 넓게 나오는 이유는 대상이 다르기 때문이다
(수치지도=포장면 전체, 실폭도로=도로명주소 관리 실폭).
**어느 쪽이 맞는지는 현장 실측만이 판정할 수 있다.**
8/6 실측은 GPS 미기록으로 무효 처리된 상태이므로, D-25 레이저 거리계 확보 후 재실측이
우선순위 1번인 이유가 이것이다.

#### 3-3. 수치지도는 최협소 골목을 그리지 않는다 (문서 §4 확인됨)

표본 100개 중 **70개만** 도로경계면 폴리곤이 존재. 나머지는 `road_rw` 폴백 필요.
문서의 "수치지도 207 + 실폭도로 폴백 14 = 221/222" 구조가 실측으로 재확인됐다.

#### 3-4. `rvwd` 컬럼 — 문서에 없던 것

수치지도 도로경계면에 도로폭 속성이 있다. `1.5 ~ 46.0m`, 정수 비율 76%
(`ROAD_BT`는 90%). `ROAD_BT`보다 세분화돼 있다.
**지오메트리 계산과 병행해 교차검증용으로 쓸 것.** 둘이 크게 어긋나는 구간이 계산 오류 구간이다.

#### 3-5. 교차로 blob 문제 재현

```
천변좌로434번길  ROAD_BT 2.0 → 수치지도 트랜섹트 60.00m
필문대로         ROAD_BT 4.0 → 수치지도 트랜섹트 56.01m
```

중심선이 교차로 폴리곤 위를 지나며 폭이 폭발한다(문서 §4 "최대 39m 관측"과 동일 현상).
**해법은 최솟값을 쓰는 게 아니라 교차로를 빼는 것이다.**
노드 반경 5m 안의 샘플을 버린 뒤 최솟값을 잡아야 한다. 현재 `width_min_m` 정의에 이 조건이 없다.

#### 3-6. 전남광주통합특별시 개편 (2026-07-01)

시군구 코드가 전면 변경됐다.

| | 구 | 신 |
|---|---|---|
| 광주 동구 | 29110 | **12210** |
| 시도 | 29 | **12** |

기존 파이프라인이 `29110`으로 필터링하면 **오늘 받은 데이터에서 아무것도 안 걸린다.**
단 상가정보의 `건물관리번호`는 구 코드(`2911...`)를 유지하므로 조인키로는 유효하다.

#### 3-7. 숫자 정정

| 항목 | 기존 문서 | 실제 |
|---|---|---|
| CCTV | 53개소 | **고유 27지점 / 카메라 86대** (설치연도별 행 분리 탓) |
| 단속 이력 | 동명동 3,228건 | 원본 **85,380건**, 동명동권 도로명 매칭 2,379건 |
| 소화전 | 1개 (이상함) | 동부소방서 관할 **588개 중 공개 31개(5%)**, 동명동 1개 |
| 세그먼트 | 222구간 | 근거 불명. 노딩 시 4,336 (bbox 기준) |

**소화전 건은 발표 논거로 쓸 것.** "588개 중 31개만 공개돼 있다"가
"1개밖에 없다"보다 훨씬 강하고, D-30 협조요청 명분이 된다.

#### 3-8. 소방서가 아니라 119안전센터가 출발점

```
동부소방서 / 대인119안전센터   제봉로 210      35.154579, 126.914765
지산119안전센터               밤실로30번길 16  35.149963, 126.938531
```

**출동은 안전센터에서 나간다.** 동명동은 서쪽(대인 1.0km)·동쪽(지산 1.2km) 양방향
진입이 가능한 구조라 D-28 우회 비용 비교의 자연스러운 무대다.
문서 §5의 소방서 하드코딩 좌표(126.922737, 35.136010)와 다르므로 확인 필요.

#### 3-9. 단속 이력 시간 필드 오염

```
14시 → 33,412건 (전체의 39%)
```

입력 기본값이다. **시간대 분석에 쓸 수 없다.** 문서 §10의
"14시가 전체의 38%는 단속반 순찰 시각"이라는 해석보다 더 나쁜 상황이다.
D-21 시간대 근거는 답사 촬영으로만 확보할 것.

---

### 4. 지도 스택 결정

**MapLibre GL JS + V-World 타일.** Mapbox도 Naver도 아니다.

- MapLibre는 Mapbox GL v1 포크라 기존 `dongmyeong_map.html` 코드가 거의 그대로 붙는다
- V-World가 한국 배경(건물·지명·지적·항공영상)을 국토부 원본으로 제공한다
- Naver의 "한국 로컬 데이터 풍부" 이점이 사라지고, GeoJSON 데이터 기반 스타일링은
  MapLibre가 압도적이다
- 상가 POI는 `poi_store`로 직접 얹으므로 Naver가 필요 없다

`dongmyeong_map_naver.html`은 갱신 중단. 두 벌 유지가 구버전 폭을 물고 있던 원인이다.

---

### 5. 다음 순서

```
1. juso.go.kr 전자지도 승인 대기        ← 행정구역경계 + 건물
2. 승인 후 동명동 정식 경계로 재클립
3. 노딩 → 세그먼트 수 확정 → 전 문서의 "222" 교체
4. 폭 산출 (수치지도 주 / 실폭도로 폴백 / 교차로 5m 제외)
5. D-25 레이저 거리계 → 10~20지점 GPS 기록 실측 → 소스 판정
```

**1번이 없으면 3~4번을 시작하면 안 된다.** bbox 기준으로 산출한 숫자를 문서에 박으면
승인 후 전부 다시 고쳐야 한다.

---

# 14. 패치 적용

## 순서

```bash
cd ~/fire-lane
rm -rf _backup_* ~/_patch
unzip -o <패치zip> -d ~/_patch
uv run python ~/_patch/apply.py ~/fire-lane
```

`apply.py` 가 하는 일

```
1. FIRE_LANE 루트 확인
2. 해시 비교 → 달라지는 파일만 백업
3. 병합 + 잔재 정리
4. pipeline.py 실행
```

## 파이프라인은 한 명령이다

```bash
python src/etl/pipeline.py              # 전체
python src/etl/pipeline.py --check      # 실행 없이 상태만
python src/etl/pipeline.py --from segments   # 그 단계부터 끝까지
python src/etl/pipeline.py --only terrain ortho
```

```
ingest → segments → terrain → ortho → publish → 계약 테스트 → 기대값 대조
```

**단계를 하나씩 손으로 치면 반드시 빠뜨린다.** terrain 을 건너뛰면 지형이 안 뜨고,
publish 를 건너뛰면 지도가 옛 데이터를 본다. 순서도 중요하다 —
publish 는 terrain/ortho 가 기록한 타일 범위를 읽어 보존한다.

실패하면 그 단계에서 멈추고 재개 명령을 알려준다.

끝나면 기대값과 대조한다.

```
세그먼트 1266  OK
  clear      227  OK
  needs_cv   139  OK
  blocked     67  OK
  unknown    833  OK
지형 타일 22장 · 항공영상 388장 · web/data 12MB
```

이 숫자가 다르면 뭔가 잘못된 것이다. `src/etl/pipeline.py` 의 `EXPECT` 에 있다.

몇 번을 돌려도 결과가 같다. 두 번째부터는 "동일" 로 표시되고 백업도 안 생긴다.
`--dry-run` 으로 무엇이 바뀌는지만 볼 수 있다.

## raw 위치

```bash
export FIRE_LANE_RAW="/mnt/f/.../FIRE_LANE/data/raw"
```

**심링크를 쓰지 않는다.** 심링크를 git 이 추적했다가 exFAT 에서
`git reset --hard` 시 경로 문자열 파일로 체크아웃되면서 원본 2.5GB 가 소실됐다(2026-08-11).

원본을 새로 받으면

```bash
uv run python src/etl/normalize_raw.py /mnt/c/Users/Fox/Downloads
```

## 푸시

```bash
uv run python ~/_patch/push.py ~/fire-lane
```

`data/raw` 가 스테이징되면 즉시 중단한다.

## 확인

```bash
cd web && uv run python -m http.server 8000
```

```js
map.setZoom(16.5)   // 미니맵
map.setZoom(17.5)   // 상가 상호
map.getTerrain()    // null 아니면 지형 적용
```

| 확인 | 기대 |
|---|---|
| 판정 색상 | 지형 on/off 무관 |
| 시설 마커 | 건물처럼 지면에 붙어 있다 |
| 미니맵 | 줌 16↑ 우하단 |
| 항공영상 | 토글 + 줌 15↑ |
| 출동 모드 | 초록 강조 + 소화전 물결 |
| 하단 폭 밴드 | `–` 가 아니라 숫자 |

**콘솔에 `DOM 없음:` 경고가 뜨면 그것이 원인이다.**
index.html 에서 지운 요소를 app.js 가 참조하면 거기서 실행이 멈춘다.
계약 테스트가 이것을 잡는다(`test_web_dom_refs_exist`).


---

# 15. 기획서 반영분

`docs/기획서_Fire-Lane.docx` 에 반영할 것. 아래는 **오늘 확정된 사실로 바뀐 부분만** 뽑았다.
나머지(588개·공개율 5%·펌프차 기준 등)는 이미 정확하므로 그대로 둔다.

---

### 1. 판정 기준 — 소방청 기준 + 현장 답사

#### 바꿀 것

> ※ 판정 기준 차종 전제 … 정확한 표준 제원은 소방청 소방차량 제작규격 및 소방서 문의로
> 확보 예정이며, **그 이전까지는 잠정값을 사용한다.**

#### 이렇게

> ※ 판정 기준: 소방청 「2025 화재현장 골든타임 확보 종합대책」의 진입불가·진입곤란
> 지정 기준을 따른다. 기준 차량은 **중형펌프차량 전폭 2.5m**, 지정 요건은 **연속 구간 100m 이상**이다.
>
> 다만 2.5m 는 차량 전폭이므로 실제 통과에는 사이드미러와 조향 여유가 필요하다.
> **2026-08-06 현장 답사 결과 3.0m 를 통과 하한으로 확정**했다. 국가 기준보다
> 보수적인 방향이므로 규정과 충돌하지 않는다.
>
> | 판정 | 기준 |
> |---|---|
> | 도면상 통과 | 도로 폭 ≥ 7.0m (통과 하한 3.0 + 주차 2.0 × 양쪽) |
> | 판정 보류 | 그 사이. 상습주차 여부가 결정 |
> | 도면상 불가 | 벽 사이 폭 < 3.0m |

---

### 2. 대상 구간 수 — "222" 폐기

#### 바꿀 것

문서 전반의 **"222구간"** 표기.

#### 이렇게

> **1,266 세그먼트**
>
> 기존 "222구간" 은 출처를 확인하지 못해 폐기했다. 정식 행정구역경계
> (EMD_CD 12210108, 0.4289km²)로 재산출한 결과는 다음과 같다.
>
> ```
> 도로명주소 도로구간      206
> 실폭도로                219
> 노딩 후 (동명동)        641
> 스코프 확장 후         1,266   ← 판정 단위. 119안전센터 접근 회랑 포함
> ```
>
> 접근 회랑을 포함한 이유는 **119안전센터가 동명동 밖에 있기 때문**이다
> (대인 서 1.0km · 지산 동 1.2km). 출동 경로가 대상 밖에서 시작한다.

---

### 3. 방법론 — 도로망 위상 복원

#### 새로 넣을 것 (Part II 방법론)

> **도로명주소 도로구간은 위상(topology)이 보장되지 않는다.**
> 원본은 도로명 단위 통짜 선형이라 교차로에서 분할되지 않으며(미분할 접점 2,338개),
> 노딩 후에도 밀리미터 단위 좌표 오차로 구간이 분리된다(T자 접합).
>
> | 처리 | 연결요소 | 최대요소 연장비 |
> |---|---:|---:|
> | 원본 | 40 | 93.29% |
> | `set_precision(0.5)` | 22 | 97.74% |
> | **끝점 투영 스냅 0.5m** | **6** | **99.76%** |
>
> 잔여 5개 구간(77m)은 알고리즘 한계가 아니라 **도로명 미부여 구간**이며,
> 그중 3곳이 관리폭 1.0~1.5m 로 소방차 진입이 원천 불가한 지점이다.
> **데이터의 공백 자체가 위험구간 지표**이므로 제거하지 않고 답사 대상으로 남긴다.

---

### 4. 폭 산출 — 단일 값이 아니라 밴드

#### 새로 넣을 것

> 폭은 하나로 정할 수 없다. 두 소스가 같은 구간을 다르게 재기 때문이다.
>
> | 표기 | 데이터 | 성격 |
> |---|---|---|
> | 도로 폭 | 실폭도로 폴리곤 트랜섹트 | 장애물이 없을 때 확실히 비어 있는 폭 |
> | 벽 사이 폭 | 건물 폴리곤 트랜섹트 | 물리적으로 가능한 최대 통로폭 |
>
> 벽 사이 폭이 전 구간에서 넓고 중앙 차이가 **+2.08m** 다. 그 사이에
> 전신주·화분·실외기·계단·주차 차량이 들어 있다. 3.0m 임계로 보면
> 도로 폭 기준 125구간 vs 벽 사이 폭 기준 35구간이 걸리며,
> **90구간(47%)의 판정이 어느 쪽을 믿느냐에 따라 뒤집힌다.**
> 이 밴드를 좁히는 것이 영상판정과 현장 실측의 역할이다.
>
> **도로대장의 명목 폭(`ROAD_BT`)은 사용하지 않는다.** 171구간(84%)에서
> 실측 트랜섹트보다 넓고 중앙 차이 +1.00m 로, 못 지나가는 길을 지나간다고
> 판정하는 방향으로 편향된다.

---

### 5. ★ 핵심 발견 — 골목의 64%는 영상판정이 불가능하다

#### 새로 넣을 것 (추진배경 또는 결과)

> CCTV 유효범위를 25m 로 잡으면(초과 시 호모그래피 오차 급증) 다음과 같다.
>
> ```
> CCTV 유효범위 안        229 / 641  (36%)
> CCTV 거리 중앙값        38.6m
> 최대                    129m
> 영상판정 불가 구간       223개 · 6,745m (전체 연장의 28%)
> ```
>
> **영상 기반 진입 판정 시스템을 만들어도 골목의 64%에는 적용할 수 없다.
> 알고리즘의 한계가 아니라 인프라의 한계다.**
>
> 다만 영상판정 불가 구간이 담당하는 소방 통행량은 **11%** 다.
> 주요 동선에는 대체로 CCTV 가 있다.
>
> ※ 25m 는 잠정값이며 현장 실측으로 검증한다. 이 값 하나가 1,266구간의 판정을 가른다.

---

### 6. ★ 외부 검증 — 소방서 지정 구간과의 대조

#### 새로 넣을 것 (실현 가능성 또는 결과)

> 동부소방서 「소방통로확보대상 지역 현황」(2025-07-31, 20구간 7,120m 3,452세대)의
> 기록 폭과 본 시스템 산출값을 대조했다.
>
> | 도로명 | 소방서 | 산출(중앙) | 편차 |
> |---|---:|---:|---:|
> | 중앙로272번길 | 5m | 5.55m | **+0.55** |
> | 동명로20번길 | 5m | 4.70m | **-0.30** |
> | 동계로9번길 | 6m | 7.34m | +1.34 |
> | 제봉로184번길 | 5m | 8.90m | +3.90 |
> | 필문대로289번길 | 8m | 2.00m | -6.00 |
>
> **동명동 2건이 -0.30m, +1.34m 로 일치**한다. 현장 실측 이전에 확보한 외부 검증이다.
>
> 편차가 큰 2건은 산출 오류가 아니라 매칭 한계다. 소방서 데이터에 좌표가 없어
> 도로명 전체(대로 구간 포함)를 잡았다. 정밀 대조에는 지정 구간의 시·종점 좌표가
> 필요하며 소방서 인터뷰 요청 항목이다.
>
> **소방서는 관할 전체에서 20구간을 지정해 관리하며, 지정 요건은 연속 100m 이상이다.
> 동명동에는 2구간이 해당한다.** 본 시스템은 동명동 1,266구간을 전수 판정해
> 도면상 진입 불가 67구간, 판정 보류 139구간을 확인했다. 대부분 연속 100m 미만이라
> 현행 기준으로는 관리 대상에 포함되지 않는 구간이다.

---

### 7. 검토했으나 배제한 것

#### 새로 넣을 것 (방법론 부록)

배제 근거를 남기면 "왜 안 썼냐"는 질문이 사라진다.

> | 데이터 | 배제 사유 |
> |---|---|
> | `ROAD_BT` (도로대장 폭) | 84% 구간에서 과대. 미탐 방향 편향 |
> | 화재 연간통계 | 읍면동 컬럼 없음. 시군구까지만 |
> | 단속 이력의 **시간** | 14시가 39.1%. 입력 기본값 오염. 시간대 분석 불가 |
> | 주차장 정보 | 노외 64 / 노상 1. 골목 노면과 무관 |
> | 공개DEM | 90m 격자. 동명동이 12×12 픽셀. 구간별 경사 산출 불가 |
> | 한전 전주 | 좌표 없음. 전산화번호만 공개 |
> | 가로등·보안등 | 공개 데이터 자체가 없음 |
> | 지적도 | 소유권 경계 ≠ 통행 노면 |
> | 항공정사영상 | 판정에는 미사용. 배경 텍스처 전용 |
>
> **경사는 검토 후 배제했다.** 공개DEM 90m 기준 동명동 표고 33.3~75.3m,
> 기복 42m, 평균 경사 1.8°, 최대 9.0° 로 소방차 등판 한계(11~17°)를 넘지 않는다.
> 경사는 진입 제약 요인이 아님을 확인했다.

---

### 8. 역할 분담 — 실제와 맞추기

#### 현재 문서

> | 오창준 | GIS 전담 — 데이터 파이프라인 · 도로폭 산출 · 그래프 구축 · 경로탐색 구현 |
> | 우지혜 | 경로 알고리즘 설계(A*·휴리스틱) / 소방서 인터뷰 · 차량 제원 확보 / 장애물 조사 |

#### 실제

우지혜가 **지도 UI·3D 시각화**를 구현하고 있다(출동 모드·미니맵·소화전 물결 등).
경로 알고리즘은 오창준이 GIS 파이프라인과 함께 진행 중이다.

#### 이렇게

> | 오창준 | GIS 전담 — 데이터 파이프라인 · 도로폭 산출 · 그래프 구축 · 경로탐색(A*) |
> | 우지혜 | 관제 화면 설계·구현 — 3D 지도 UI · 출동 모드 · 시각화 / 소방서 인터뷰 |

**바꿀지는 팀 합의로 정할 것.** 다만 발표에서 역할과 산출물이 어긋나면 질문이 나온다.

---

### 9. 수치 표기 원칙 갱신

#### 바꿀 것

> ※ 수치 표기 원칙: 본 문서의 모든 수치는 **2026-08-09** 재확보한 원본 데이터에서
> 코드로 재산출한 값이다. 행정구역경계 확보 이전 bbox 기준으로 산출된 잠정치는
> '(잠정)'으로 별도 표기하며, 확정 시 교체한다.

#### 이렇게

> ※ 수치 표기 원칙: 본 문서의 모든 수치는 **2026-08-12** 기준으로 원본 데이터에서
> 코드로 재산출한 값이다(`src/etl/pipeline.py` 한 명령으로 재현된다).
> 행정구역경계(EMD_CD 12210108)를 확보하여 bbox 기준 잠정치는 전부 교체했다.
>
> **폭 값은 아직 미검증이다**(`width_verified: false`). 레이저 거리계 실측 후
> 값이 바뀌며, 그때 판정 일부가 변경된다. 산출 구조와 필드는 바뀌지 않는다.
