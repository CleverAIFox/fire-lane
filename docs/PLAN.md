# Fire-Lane — 실행 계획

> **앞으로 할 일만 담는 문서다.** 끝난 것은 결과를 `MASTER.md` 로, 왜
> 그렇게 됐는지는 `DECISIONS.md` 로 옮기고 **여기서는 행을 지운다**(§0-2).
>
> | 문서 | 시제 | 담는 것 |
> |---|---|---|
> | **`docs/PLAN.md`** | **미래** | **남은 일 · 미결정 · 담당 공백 · 결함 대장 (이 문서)** |
> | `docs/MASTER.md` | 현재 | 판정 · 데이터 · 용어 · UI 계약 · 운영 |
> | `docs/DECISIONS.md` | 과거 | 왜 그렇게 됐나 (append-only) |
> | `docs/proposal.docx` | — | 대외 제출용. 시제 규칙 밖 |
>
> 문서 넷은 병렬 축이 아니라 한 항목의 **생애주기**다.
>
> ```
> PLAN(미래)  →  도래  →  MASTER(현재)  →  회고  →  DECISIONS(과거)
> ```
>
> **한 항목은 한 문서에만 산다.** 남은 일의 정본은 §1 하나다.

## 0. 서술 규약 · 상태 표기

강제자 없음 — 사유: 절 머리말이다. 서술 규약은 `§0-1`, 상태 표기는 `§0-2` 가 들고 둘 다 제 강제자를 적는다

### 0-1. 서술 규약

`MASTER §0` 을 따른다. 이 문서에만 적용되는 것은 셋이다.

| 항목 | 규칙 |
|---|---|
| 시제 | **미래·미완**. 끝난 일은 즉시 뺀다. 완료 목록을 여기 쌓지 않는다 |
| 절 구성 | 절은 **주제**로 나누고 날짜는 본문에 적는다 |
| 항목 형식 | 남은 일 한 줄에 `상태` · `전건` · `막고 있는 것` 이 함께 있어야 한다 |

★ 날짜를 절 제목으로 쓰지 않는다. 그날 나온 것을 그날 절로 만들면 번호가
날짜만큼 늘어나고, 같은 번호가 두 곳을 가리키면 인용이 성립하지 않는다.
강제자 — `tests/test_reproducibility.py::test_plan_headings_are_numbered_and_unique`

### 0-2. 상태 표기

| 표기 | 뜻 |
|---|---|
| 📄 | 결정은 됐으나 코드·실측으로 확인 안 됨 |
| 🟡 | 미결정. 논의 필요 |
| 🔴 | 담당자 없음. 공백 |
| ⏳ | 대기 중 — 외부 응답·타 파트 산출을 기다린다. **여기서 못 푼다** |
| ⚠ | 전제가 틀리기 쉬운 항목 |

★ **닫힘 표식은 없다.** 위 다섯이 전부이고 `✅` 도 `⬛` 도 어휘 밖이다.
표식을 만들면 사람은 행을 지우는 대신 표식을 단다 — 2026-09-06 에
`✅` 셋이 그렇게 생겨 아흐레를 남았고, 검사는 폐지된 `⬛` 를 찾고
있어서 울지 않았다(DECISIONS §159).
강제자 — `tests/test_doc_fsck.py::test_plan_status_vocabulary_is_closed`
        카나리아 — `::test_plan_status_probe_is_alive`

**완료되면 결과를 `MASTER.md` 에 쓰고 여기서는 행을 지운다.**
번호는 **안 당긴다** — 지운 번호는 비워 둔다(아래 ★).

★ PLAN 은 **빚 목록**이다. 갚은 빚은 목록에 없다. 슬롯으로 남기면
그것이 완료 목록이 되고, 이 문서는 완료 목록을 쌓지 않는다(§0-1).

★ **`⏳` 는 담당이 없다는 뜻이지 필요 없다는 뜻이 아니다.**
설계와 막힌 이유가 행 안에 그대로 있으므로 이어받는 사람이 처음부터
판단하지 않아도 된다. **지우지 않는 이유가 그것이다**(DECISIONS §102).

★ **행 번호는 영구 식별자다. 결번이 정상이고, 지운 번호는 다시 안 쓴다.**
2026-09-20 에 뒤집었다(DECISIONS §205). 종전 규약은 「닫힌 행을 지우고 뒤
번호를 당긴다」였고 `plan_renumber.py --apply` 가 그 일을 했다. 그 도구는
`docs/PLAN.md` **하나만** 여는데, §1 행을 가리키는 인용은 **밖에 83곳** 있다 —
`DECISIONS` 70 · `MASTER` 7 · `sources.yaml` 6. 당길 때마다 그 83곳이 **조용히
다른 행을 가리켰다**(`plan_renumber.py` 주석의 `§1 #16` 사고가 그 자국이다).
DECISIONS 70곳은 append-only 역사라 고칠 수가 없다. 그러면 답은 하나다 —
**당기지 않는다.** 그리고 이것이 **이 절이 줄어들 수 있게 만든다** —
닫힌 행이 목록에 그대로 앉아 있던 이유가 「지우면 뒤가 당겨진다」였다.
이제 그냥 지운다.
강제자 — `tests/test_declaration_sync.py::test_plan_row_numbers_are_unique_and_sorted`
        `::test_plan_renumber_cannot_shift_numbers_any_more`
검사 — `uv run python tools/plan_renumber.py` (중복·역순·밖의 죽은 인용)

★ 2026-09-14 정정. 여기에 "행 번호는 자산이다 · `⬛` 슬롯으로 남긴다" 가
남아 있었다. **2026-09-13 에 강제자는 이미 반대로 바뀌었는데 문서만
안 따라왔다.** 그래서 그날 이 문단을 읽은 사람이 "지우면 검사가 운다" 고
단정했고 틀렸다. 규약이 두 판으로 갈려 있으면 **먼저 읽은 쪽이 이긴다** —
그것이 이 저장소가 232번 당한 형태다.

---

★ **닫힘 표기는 없다.** 완료·기각한 항목은 결과를 `MASTER` 로, 왜 그렇게
됐는지를 `DECISIONS` 로 옮기고 **이 문서에서 행을 지운다.**

PLAN 은 빚 목록이다 — **갚은 빚은 목록에 안 남는다.** 슬롯을 남기면
영원히 안 비고, 그러면 이 문서는 "앞으로 할 일" 이 아니라 "했던 일 목록" 이
된다. 그것은 `DECISIONS` 가 하는 일이고, 한 항목이 두 문서에 사는 것이다.

★ **개발이 끝나면 이 문서는 비어야 한다.** 남은 일이 없다는 뜻이다.
남은 일이 없는데 문서가 두꺼우면 무언가 잘못된 것이다.

강제자 — `tests/test_doc_fsck.py::test_plan_status_vocabulary_is_closed`

## 1. 남은 일 — 113행

★ **2026-09-20 신설**(DECISIONS §205). 제목이 수를 든다. §13 이 같은 것을
하루 먼저 세웠고 **세우자마자 갈린 것이 걸렸다**(문단 30 · 제목 28 · 표 28).
이 절은 62행을 굴리면서 **수를 어디에도 안 적었다** — 그래서 「얼마나 남았나」를
물으면 매번 손으로 세야 했고, 세는 일을 사람에게 맡기면 갈린다.
강제자 — `tests/test_declaration_sync.py::test_plan_section1_count_agrees`. 하위 넷이 이 칸을 물려받는다

★ **2026-09-23 신설 — 결번 대장.** 행 번호는 **영구 식별자**다(DECISIONS §205). 닫힌 행은 지우고
번호는 비워 두며 **절대 다시 쓰지 않는다** — 밖에서 이 절을 가리키는 인용이 83곳이고
그중 70곳이 append-only 인 DECISIONS 다. 번호를 재사용하면 그 인용들이 **조용히 다른
행을 가리킨다 — 죽은 참조보다 나쁘다.** 아래 줄이 지금까지 비운 번호의 정본이고,
`tools/plan_renumber.py` 가 실물 결번과 대조한다. 재사용하면 결번이 하나 사라지므로 운다.

결번 — #12 · #23 · #24 · #25 · #27 · #35 · #36 · #37 · #42 · #43 · #44 · #45 · #46 · #48 · #49 · #50 · #61

**판정을 움직이는** 남은 일의 정본은 이 절 하나다. `MASTER.md` 는 "현재 무엇이
어떤 값인가"만 적는다.

★ **판정을 안 움직이는 남은 일은 §13 이 정본이다.** 배선·정본화·문서 결함이
그것이고, 수용 조건이 「판정 산출물 바이트 동일」이라 이 절과 섞으면 성격이
다른 두 일이 한 표에 앉는다. 가르는 것은 중요도가 아니라 **수용 조건**이다.

| # | 항목 | 상태 | 전건 · 막고 있는 것 |
|---|---|---|---|
| 1 | `node_link` + `turn_restriction` 그래프 투입 — **그래프 방향성** 미결정 | ⏳ | A* 필수. 데이터는 이미 보유(엣지 3,427 · 노드 2,837 · 회전제한 87행). ★ 막힌 것은 데이터가 아니라 `D-07`(일방통행 역주행 허용 여부)이고 그것은 `D-30` 소방서 인터뷰다. ★ 선택지는 둘 — **무향 + 긴급차량 예외**(소방차는 역주행 허용) 대 **유향 + 역주행 페널티**. **안 정하면 코드를 두 번 갈아엎는다** — 그래프 자료구조가 통째로 달라진다. ★ **2026-09-02. GIS 담당이 이 항목을 놓는다**(DECISIONS §102). 안 하기로 한 것이 아니라 **하는 사람이 없어진 것**이다. 설계와 막힌 이유를 그대로 남긴다 |
| 2 | A* 비용함수 설계 | ⏳ | 거리 + 폭 + 통행제한 ∞ + 경사. §4-1 · §4-5 선행. 곱셈형으로 확정돼 있다(§4-1). ★ **2026-09-02. GIS 담당이 이 항목을 놓는다**(DECISIONS §102). 안 하기로 한 것이 아니라 **하는 사람이 없어진 것**이다. 설계와 막힌 이유를 그대로 남긴다 ★ 2026-09-21 — 내비 안전 경로에 **확인 필요 구간 회피 배수 2.0** 이 들어갔다(`web/navi/src/domain/vehicle.ts::TUNING.avoidUncertain` · DECISIONS §211). 그 전에는 안전·빠른 경로가 동명동 목적지 211곳 **전부** 같았다(지산센터 출발 실측) — 와이어프레임 02 의 두 경로 비교가 빈 화면이었다. 배수는 **근거 없는 값**이다. 이 행이 그 근거(단속이력 상습도 #31 · CCTV 없는 구간 추정 #60)를 채울 자리다 |
| 3 | 영상판정(호모그래피) 모듈 연결 | ⏳ | `needs_cv` 226구간. 영상 담당. ★ 2026-09-15. ROI(관심영역 점유율)로 갈아타자는 안이 나왔으나 **판정 축을 버리게 된다** — 출력이 `width_max_m` 이고 임계가 3.0/7.0(MASTER §2-2)인데 점유율은 미터가 아니다. 환산하려면 정적 도로폭이 필요하고 그것은 지적도에서 오는데, 지적도 오차를 메우려고 CCTV 를 쓰는 것이므로 순환이다. 면적 기준은 **세로로 긴 장애물과 가로로 긴 것을 구분 못 한다** — 통행 가능 여부는 면적이 아니라 최소 폭이 정한다. ★ 다만 ROI 는 **변화 감지 트리거**로는 싸고 정확하다. 앵글이 고정이므로 "무언가 들어왔나" 를 GPU 없이 판단한다. 둘을 대립으로 두지 않는다 — ① ROI 로 변화를 잡고 ② 그 위치에서 호모그래피로 통과 폭을 잰다 |
| 4 | D-25 레이저 실측 | 🔴 | `width_verified` 전건이 걸려 있다 |
| 5 | D-30 소방서 인터뷰 | ⏳ | 소화전 좌표 · 차량 제원 · 소방통로 구간. **확답 없음** |
| 6 | 도로대장 정보공개 재청구 | 📄 | 1차(17337943)·보도현황(17337939) 모두 **부존재 종결**(2026-08-24). 사유는 「취합·가공해야 하는 정보」다. 가공 요청을 빼고 재청구했다. ★ **결과에 일정을 걸지 않는다** — `ngii1k_center`(n=909 · 절대편차 중앙 0.32m)가 이미 독립 검증축이고 정본은 D-25 실측(#4)이다. 부존재가 또 오면 발표 논거로 전환한다(§1-26) |
| 7 | 건물 형상 고도화 | 🟡 | 수치지형도 1:1,000 건물 레이어 보유 여부 확인부터. ★ 2026-09-17 레이크 밖 스냅숏에서 나왔다 — 1:1,000 2022 SHP zip 16도엽 안 `N1A_B0010000`(DECISIONS §173-5). 레이크에 편입되기 전이라 **보유가 아니다.** 「대장 · SSD 디렉토리 구조와 해석기 하나」 가 retired 로 보존한 뒤 `ngii1k` 판과 도엽 · 판차를 대조한다. R 교차 대조 재료이기도 하다(#51) ★ **2026-09-20 전수 분류 정정**(DECISIONS §205) — **`#51` 과 반대로 적는다.** 여기는 「레이크에 편입되기 전이라 **보유가 아니다**」인데, `#51` 의 ★2026-09-17 R1 은 「건물 레이어는 raw V-WORLD 묶음에 **이미 있다(반입 불필요)**」라고 적는다. 둘 중 하나가 낡았고, 낡은 쪽이 이 행이면 전건(`#55`)이 이미 풀렸다는 뜻이다. **실물을 먼저 확인하고 한쪽을 지우는 것이 맞다** — 두 행이 반대를 말하는 상태가 제일 나쁘다. |
| 8 | D-29 평가지표 산출 | 🟡 | 설계 §7-2 완료. E-1 · E-2 산출 미착수 |
| 9 | `width_max_m` 결손 — 막을 근거가 하나뿐인 92건 | 🟡 | 노면 3m 미만인데 대장폭이 3m 이상이거나 없다. 건물 담~담이 없어 통행 불가로 못 간다. 실측 또는 건물형상(#7) 대기. ★ 결손 675 전체가 아니다 — 대부분은 동 경계 80m 밖이라 벽 사이를 **재지 않는** 것이다(MASTER §3-4) |
| 10 | 정사영상 미터 단위 정합 미검증 | 🟡 | TIF 에 경계좌표 없음. 도엽 bbox + 사방 균등 pad **가정** |
| 11 | 도로경계 폴리곤 밖 37구간 | 🟡 | `uncovered_units.json`. 게이트는 통과하나 개별 형상 확인 필요 |
| 13 | `turn_restriction` 건수 게이트 | 🔴 | 좌표 없는 DBF(전국 44,125행)라 `node_point` 로만 걸러진다. 그 소스가 실패하면 필터가 사라지는데 상태는 정상으로 뜬다 |
| 14 | clearance 기각 재판정 | 🔴 | 기각 근거가 `n=7` 이고 게이트가 오염돼 있었다. D-25 에 3~7m 대역 20~30곳을 재서 정한다 |
| 15 | 영상판정 유효거리 실측 | 🔴 | `CCTV_RANGE` 가 단일 상수로 못 잡힌다(MASTER §16-1). §1-23 |
| 16 | 촬영 높이 결정 | 🟡 | 유효거리가 높이에 비례한다. #17 결과가 나오면 장비를 정한다. §1-24 ★ **2026-09-20 전수 분류 정정**(DECISIONS §205) — **전건 번호가 틀렸다.** 이 행은 「유효거리가 높이에 비례한다」를 전제로 드는데 `#17`(min → 통과폭 재판정)은 그 얘기가 아니다. 가리키려던 것은 **`#15`(영상판정 유효거리 실측)** 다. 번호를 당기던 시절의 자국이다 — 2026-09-20 부터 번호는 안 당긴다(§0-2). |
| 17 | `min` → 통과폭 재판정 | 🟡 | `min` 은 표본 하나에 뒤집힌다. `width_fn.py` 로 대조 중. **판정 반영은 실측 후** |
| 18 | `node_link` 속성 투입 | 🟡 | `LANES` · `REST_W` 는 **계보가 다른 폭 하한**이다 |
| 19 | 경로 비용을 차량 기준으로 | 🟡 | `access_corridor()` 가 거리만 쓴다. 판정 반영은 golden 재잠금이 필요하다 |
| 20 | 차량 제원 확정 | 🔴 | 축거·최소회전반경이 추정값이다. ★ **2026-09-03 정정. 최소회전반경은 D-30 대기 항목이 아니다** — #30 가 이미 값의 소재를 적는다(`profiles.json` 7,300~11,889mm · 강원 인제소방서 자료 · 나라장터 공고). **재취득 가능하고 대장 밖일 뿐이다.** #30 를 태우면 닫힌다. 관내 보유 차종은 `gjfire_fleet_dongbu`(2026-05-14) 가 이미 든다. 종전 서술이 닫을 수 있는 항목을 인터뷰 병목으로 걸어두었다. 축거(`wheelbase_m`)만 D-30 대상이다. ★ 2026-08-24 팀 일지가 `profiles.json` 근거로 중형펌프차 7.3m · 대형물탱크차 11.2m 를 적었으나 **출처가 확인되지 않았다.** KFS-1-0073 · KFS-1-0030 · MAS 제작규격 셋에는 회전반경이 없다(MASTER §3-13). 출처가 규격 원문이 아니면 `turn_radius_verified` 를 켜지 않는다 — 법정 상한을 성능값으로 쓴 DECISIONS §81 과 같은 형태다. ★ 2026-09-17 관리카드 4개 반입(대인5호 이름분은 지산2호 사본)(`gjfire_vehicle_card` · DECISIONS §172) — 카드 1쪽에 축거·회전반경이 **없다.** 이 행은 카드로 안 닫힌다. 남은 31대는 받을 때마다 대장 `parts` 에 적고 `pull_data` 를 돌린다 ★ 2026-09-17 섀시 제원표(축거)는 공개 경로로 못 구했다 — D-30 인터뷰로 받는다(DECISIONS §177-4). |
| 21 | `LANES` 매칭률 24% | 🟡 | 266/1,101 만 매칭(2026-09 · 1,101구간 기준 측정 <!--stale-ok-->). 소스 문제인지 매칭 로직 문제인지 아직 안 갈렸다 |
| 22 | 미불용지 82구간 | 🟡 | 우리 노선이 지목 `대`(사유지)를 밟는 구간. 폭이 충분해도 권리 문제다 |
| 26 | 1차 사용자 정의 재검토 | 🟡 | 회의록 축 `P-57`. 관제 주체가 소방서가 아니라 도청·시청으로 추정되고 경로 지시 권한이 없다는 진술이 나왔다(MASTER §9-1). 정의가 바뀌면 화면 범위와 발표 포지셔닝이 함께 움직인다. **D-30 정식 인터뷰 전에는 닫지 않는다** |
| 28 | 산출물 형태 — 독립 앱 대 출동 패드 부가기능 | 🟡 | 회의록 축 `P-60`. 현장은 독립 앱보다 기존 출동 패드의 부가기능을 선호한다고 답했다. 지금 산출은 정적 파일이므로(MASTER §20-5) 어느 쪽이든 인터페이스는 같다. **결정 시점은 화면 설계서 착수 전이다** |
| 29 | 소화전 매칭 임계 확정 | 🟡 | #25 에서 갈린 원인 ③ 의 후속. 8m 를 몇 m 로 올릴지는 커버리지 반경(NFTC 401 수평거리 140m · NFSC 109 옥외소화전 40m)과 함께 정한다. §7-5 ★ **2026-09-20 전수 분류 정정**(DECISIONS §205) — **`#25`(소화전 좌표가 도로망에서 멀다)를 여기로 합쳤다.** 그 행은 2026-09-01 재측정으로 원인이 「매칭 반경 8m」 하나로 좁혀졌고, 남은 일이 이 행과 **같은 일**이었다 — 한 항목이 두 행에 살고 있었다. `#25` 번호는 비워 둔다(다시 안 쓴다). |
| 30 | 회전반경 출처를 `raw` 로 | 🟡 | `profiles.json` 의 7,300~11,889mm 는 강원 인제소방서 자료·나라장터 공고에서 왔다. **재취득 가능한데 대장 밖이다.** `landing → acquire → raw` 를 태우고 `datasets` 에 등재한다. 그래야 `absent` 선언(DECISIONS §87-2)이 출처를 가리킬 수 있다 ★ 2026-09-17 정정 — 원문을 반입해도 `turn_radius_verified` 는 **안 켜진다.** 값은 공고의 현행 예시이지 규격이 아니다(MASTER §3-13). 켜는 근거는 D-30 실측이다. 이 행은 출처를 대장 안으로 들이는 일일 뿐이다(DECISIONS §178-3). |
| 31 | 교통량 축을 무엇으로 채우나 | 🟡 | 차량 탑재 내비는 API 없이 TPEG 를 방송 수신한다. **내비의 기본 소양이라 자리를 비워두지 않는다**(DECISIONS §88-6). 우리 소스는 단속이력 상습도다 — `gjcity_parking_enforce` 2024·2025 두 판. 시간대별 주정차 빈도를 간선 가중치로. ITS 소통정보는 간선만 덮어 판정 구간을 하나도 안 덮으므로 안 쓴다(2026-09 · 1,101구간 기준 · 1,281 재대조는 ITS 대조표 배치) ★ 2026-09-17 두 판을 retired 에서 raw 로 복귀했다 — 대장 `parking_enforce`(csv_table_multi — 2024-01-08 판에만 있는 `비고` 를 optional_cols 로 선언하고 이어붙인다 · DECISIONS §179-4). ★ **2026-09-20 전수 분류 정정**(DECISIONS §205) — **제목의 질문을 본문이 이미 답했다.** 소스는 단속이력 상습도로 정해졌고 2026-09-17 에 두 판을 raw 로 복귀시켜 대장 `parking_enforce` 등재까지 끝났다. **남은 것은 「무엇으로 채우나」가 아니라 「간선 가중치로 투입」**이고 그것은 `#2`(A* 비용함수)·`#19`(경로 비용 차량 기준)와 같은 자리에서 닫힌다. |  <!--stale-ok-->
| 32 | D-30 광주 인터뷰 일정 | 🔴 | **대기가 아니라 안 한 것이다.** 절차는 2026-08-05 에 확인됐다(일정팀 전화). 일정을 우리가 못 정한다 — `sources.yaml` `pending`(vehicle_spec_measured)로 옮겼고 fallback 을 적었다. `D-07` · `D-25` · #21 · `P-57`~`P-60` 이 전부 여기 매달려 있다. §9-1 에 날짜를 박는다 — 안 박히면 이탈 후 아무도 안 한다 |
| 33 | 운전요원 여정지도 복원 | ⏳ | 원본이 소실됐고 페르소나(문재현)는 남아 있어 복원 가능하다. ★ **지금 그리지 않는다** — `P-60`(독립 앱 대 패드 부가기능)이 열려 있어 접점이 흔들린 상태다. `D-30` 이후 그려야 근거가 선다. 관제사 여정지도는 최저점이 「이동 중 지원」(-4)이며 `MASTER §9-2` 가 든다. ★ 2026-09-16 `📄` → `⏳`. 행 스스로 `D-30` 이후라고 적으므로 결정이 아니라 대기다(DECISIONS §162) |
| 34 | README 가 루트인데 GIS 전용이다 | 🟡 | clone 한 사람이 처음 보는 파일인데 파이프라인만 설명한다. `src/firelane/README.md` 가 이미 파트 문서의 선례다. 루트는 **무엇을 만드나 · 5분 시작 · 파트별 갈림길 · 문서 어디에** 넷만 두고 나머지를 내린다. `src/cv/README.md` · `web/README.md` 는 해당 파트가 쓴다. ★ 2026-09-16 **코드 배치로 옮긴다** — `part/**` 는 유지로 정해졌으므로 「파트별 갈림길」 은 4계층 그대로 적는다(DECISIONS §163). 남은 것은 README 에 걸린 강제자 다섯의 검사 대상 경로를 함께 옮기는 일이다. 문서만의 일이 아니다(DECISIONS §162). ★ 2026-09-17 **README 전수 대조를 같이 한다** — 루트 · `src/firelane/README.md` · `web/README.md` 셋이 실물과 어긋났을 수 있다(예: `web/README.md` 는 MapLibre 5 라고 적는데 내비는 6 이다 · `src/cv/README.md` 는 없다 · `web/navi` 는 README 가 없다). 배포 주소는 N1.1 에서 `cleveraifox.github.io` 로 먼저 고쳤다(DECISIONS §181-6). `data/baseline/*/README.md` 셋은 당시 기록이라 고치지 않는다 |
| 38 | `vehicle_profiles` 에 섀시 세대 축이 없다 | 🔴 | 같은 중형펌프차가 섀시 세대로 갈린다 — 메가트럭(~2019) / 파비스(2019~). 그런데 `vehicle_profiles` 는 차종당 축거 하나라 **축거를 확보해도 넣을 자리가 없다. 값보다 스키마가 먼저다.** 판정 단위는 **센터 × 차종**이고 서로 독립이다(`DECISIONS §113`) — 그 조합의 보유 대수만큼 차종을 전부 알 때만 최대가 확정된다. ★ 갈 곳은 `CONFIG.fleet` 각 항목에 `chassis` · `wheelbase_m` · `wheelbase_verified` 를 달고 `offtracking()` 이 선택된 항목의 플래그를 보는 것이다. `can_turn()` 이 `turn_radius_verified` 를 보는 구조가 이미 있으므로 **이식이지 발명이 아니다.** ★ 지금 확정된 조합은 둘뿐이다 — 대인 펌프차(중형) 2대 파비스 · 대인 굴절 27m 1대 구쎈(사양 미확정). 기본 판정 차량인 지산 펌프차 2대는 0대 확인이다 |
| 39 | 119 · 구조 데이터로 경로 비용 외부 검증축 | 🟡 | `route_vehicle.csv` 의 `cost` 는 **한 번도 실물과 대조된 적이 없다.** 폭에는 `ngii1k_center` 라는 독립 검증축이 있는데(n=909 · 절대편차 중앙 0.32m) 경로 비용에는 그런 축이 없었다. `nfa_dispatch_119` 의 `DSPT_DRTV_DT` → `GRNDS_ARVL_DT` 가 실제 도달 소요이고 `DCLR_PSTN_LOT/LAT` 이 그 지점이다. `nfa_rescue` 도 좌표를 갖는다(동명동 212행). `nfa_fire_incident` 는 좌표가 없는 대신 `FRSTN_GRNDS_DSTNC` · `CNTR_GRNDS_DSTNC` · `DSPT_REQ_HR` 로 거리와 시간을 직접 든다 — 구간에 못 붙여도 회귀로 볼 수 있다. ★ 좌표는 **신고자 위치(휴대폰)** 이지 반드시 현장이 아니고 결측이 `0.0` 으로 채워져 있어 `lat > 0` 필터가 필수다. ★ 2024 · 2025 는 컬럼명이 달라 파생본에 2022 · 2023 만 들어갔다 — 매핑하면 표본이 몇 배로 는다 |
| 40 | React Native 포팅 | 📄 | `domain`·`infra`·`app` 은 그대로 돈다. `components`(maplibre-gl → @rnmapbox/maps) · `ui`(CSSProperties → StyleSheet)만 재작성. ★ Web Speech 끊김이 OS 엔진으로 사라진다. ★ 번들 1.26MB(gzip 351KB)의 대부분이 `maplibre-gl` 이라 `@rnmapbox/maps` 로 바뀌면서 사라진다 — **지금 코드 분할할 이유가 없다** |
| 41 | 실시간 교통 계층 | 📄 | ITS 소통정보가 통행가능 총연장의 58%(간선 7m+)를 덮는다. `tools/its_linkmap.py` 가 링크 ↔ `seg_uid` 대조표를 만든다. 나머지 42%(골목)는 ITS 도 상용도 못 덮는다 — 거기가 CV·β 자리다. ★ 5분 cron 배치면 되고 상시 서버가 아니다 |
| 47 | `_find` ×5 동치성이 확인되지 않았다 | 🟡 | `dupcheck --min 40` 이 남긴 마지막 사본군. `publish_navi` · `seg/graph.py` · `segments.py` · `bridge_audit` · `kpi` 다섯이 같은 구조를 갖는다. ★ 둘이 **판정 파일**이라 합치면 노드 묶음이 달라질 수 있고 그러면 `seg_uid` 가 움직인다(`DECISIONS §144`). **동치성 증명이 먼저**고, 그 뒤 전량 1회 → `golden.py lock` 이다. 재잠금은 한 번만 쓴다 |
| 51 | 판정 뼈대를 NGII 1:1,000 측량 중심선으로 다시 세운다 | 📄 | 구간이 도로명주소 `road_link` 위에 선다. 주소 관리용 선이라 측량 위치가 아니다 — 필문대로289번길은 건물 위를 10.6m 지나 사람이 승인 보정했다(DECISIONS §170-2). `중앙로` DM-192839-284313 도 건물 11.6m · NGII 도로면 0.1% 로 **같은 형태**다. 엄격 매칭에서 판정이 뒤집힌 사례는 0 이지만 매칭 안 된 240구간은 못 본다. ★ CV 가 활약할 `needs_cv` · `unknown` 이 틀린 선 위에 서면 엉뚱한 골목을 본다 — **CV 무대의 전제다.** seg_uid 가 전부 바뀐다. ★ 2026-09-17 두 번 쟀다(DECISIONS §173-7) — 피복 D=5 · 틈 이음 2m 확정, 차수2 병합 필수(엣지 5,027 → 2,008 · 중앙 25.6m). **근거를 고쳤다** — NGII 가 더 정확해서가 아니라 폭 원천과 뼈대를 같은 측량으로 맞추려는 것이다(현행 뼈대 11% 가 NGII 도로면 밖). **검증 기준을 고친다** — `building` 과 `road_link` 가 둘 다 도로명주소 계열이라 건물 겹침은 road_link 에 유리하다. NGII 1:1,000 건물 레이어(#7)로 교차 대조한다. 남은 측정 — 건물 레이어 교차 대조 · NGII 폭 6m 이상 2.2km 분리대 쌍선 여부(`분리대유무`) · 원천 간 어긋남 중앙값. 전건 — 「대장 · SSD 디렉토리 구조와 해석기 하나」(건물 레이어가 레이크에 들어와야 한다) ★ 2026-09-17 R1 — 뼈대 후보와 대조 도구가 섰다(DECISIONS §184). 건물 교차 대조 결과 NGII 건물 관통이 현행 17 · NGII 중심선 0 이다. 건물 레이어는 raw V-WORLD 묶음에 이미 있다(반입 불필요). 남은 것 — ① `tools/skeleton_compare.py` 의심표를 사람이 판정 ② R2 seg_uid 전이표 · 봉인 ③ R3 뼈대 교체 · 보정 모듈(DECISIONS §127) 걷기 · golden 재잠금 ★ 2026-09-17 R1 실측(DECISIONS §184-4) — 1,281 구간을 NGII 선까지 거리로 가르면 ≤1.5m 1,041 · 1.5~5m 130 · **5~15m 66** · >15m(간선 · NGII 선 없음) 44. 5~15m 중 needs_cv · unknown **32**(판단 기준 20 이상) · 같은 도로명 NGII 가 옆에 있는 것 38/53 → **R2 · R3 로 간다.** ★ 2026-09-18 R1 판 2 · R2 · R3a 가 들어갔다(DECISIONS §184-5 · DECISIONS §187 · DECISIONS §188). 판 2 는 「멀리」를 NGII 선 기준 · 반폭으로 고치고 「짝없음」을 교차점 갈림 제외(`cover`)로 바꿨다 — 짝없음 168 → 8. 「짧은 구간 방향 완화」는 실측으로 **기각**(20m 미만이 32뿐 · 45°로 넓혀도 168→163). R2 는 전이표(`firelane.transition`)와 봉인 `20260918-pre-r3` 를 세웠다 — 항등 자기검사 1,281 전부 1:1. R3a 는 시험 스위치 `FIRE_LANE_SKELETON`(기본 꺼짐)으로 **판정을 안 바꾸고** 이동을 쟀다. 1회차 관측 — 구간 1,281 → 1,573 · blocked 191 → 340 · 소방서 절대편차 8.31 → **16.79m** · 공간 미커버 2.6% → 0.3% · 최대 구간 993m → 193m · 컴포넌트 16 → 456. 뼈대 품질은 올라갔으나 **외부 대조가 2배 나빠졌다.** 세그 수가 같이 무너져(제봉로184번길 19 → 0) 원인은 폭이 아니라 **이름**이다 — `as_road` 가 NGII 도로명을 정본으로 뒀다. ★ R3b 1회차에서 쌍선 한 줄 제거(`drop_twin`)를 같이 시험했다가 **기각**했다(DECISIONS §189-3) — 도달 가능 957 → **90(7%)**. 평행 쌍선은 교차로에서 서로 이어져 있어 한 줄을 지우면 진입선이 갈 곳을 잃는다. **한 배치에 둘을 넣어 원인을 못 갈랐다.** ★ 2026-09-18 R3b 2회차 — 이름만 `road_link` 우선으로 되돌려 다시 쟀다. 결과가 1회차와 **한 값도 다르지 않다**(이름 5,926/6,812 · 소방서 16.79m · 세그 14 · 2 · 42 · 52 · 43 · 7 · 도달 957). **이름 가설도 기각**이다(DECISIONS §189-1) — 두 원천의 도로명이 사실상 같다. 세그 수 붕괴는 **기하 문제**이고 원인을 아직 모른다. ★ **R3c 를 하지 않는다**(DECISIONS §189-5). 막는 것 셋 — 외부 대조(소방서 절대편차 8.31 → 16.79m · 지정 구간 세그 붕괴) · **연결성(컴포넌트 456 · 최대성분 채택으로 엣지 641(9.4%) · 노드 1,089 폐기)** · 되돌리기 비용(seg_uid 유지율 13.0% · 옛 키 87% 소멸 · 실측 26+76행 미아). `nfa_compare` 는 우리 폭에 대한 유일한 외부 대조 수단인데(MASTER §4) 그것이 2배 나빠지면서 뼈대의 9.4%를 버린다. 시험 스위치 `FIRE_LANE_SKELETON` 은 기본 꺼짐이고 판정은 한 번도 안 움직였다. 남은 것 — ① **컴포넌트 456**. NGII 중심선이 교차로에서 안 이어진다는 뜻이고 세그 붕괴와 같은 원인일 가능성이 높다. 틈 이음 2m(DECISIONS §173-7)가 모자란 것인지 원천이 애초에 끊겨 들어오는 것인지부터 가른다 — 다음 R 은 여기서 시작한다 ② 병렬 엣지 길이비 1.5 초과(현행 0 → 시험 7)는 미결. `build_graph` 가 같은 노드쌍에서 긴 쪽을 버리는 규칙 쪽에서 푼다 ③ ①이 풀려 외부 대조가 회복되어야 R3c 가 열린다 |
| 52 | 벽 사이 폭(`width_max_m`) 채택 가드 | 🟡 | 건물 담~담을 판정 범위로 넓히면 결손 675 → 55 지만 측량 3m 미만 30구간이 통행 불가에서 풀린다(MASTER §3-4). 넓힌 판이 **막은** 8구간은 전부 측량과 맞았다. 벽 사이 값이 노면 · 측량폭과 크게 벌어지면 버리는 가드를 세우고 같은 A/B(엄격 매칭)로 다시 잰다 |
| 53 | 폭 산출 이상 — 노면 최솟폭이 측량의 3~10배 | 🟡 | `wmin` > 30m 27구간 중 필문대로 19 는 정상(측량 31~42m). 나머지 8 은 교차부 · 광장 면을 가로질러 잰 것이다 — `지호로` DM-194362-283514 clear · wmin 35.89 · 측량 **3.1m** 는 경계 한 끗이다. `밤실로4번길` 53~58m · `필문대로289번길` 52.2m. 판정은 아직 안 뒤집혔다 |
| 54 | 측량폭을 판정 입력으로 쓸 것인가 | 📄 | `needs_cv` · `unknown` 중 측량 3m 미만 156 · 6m 이상 117 — CV 자원이 측량이 이미 답한 구간에 간다. 반대로 넣으면 **유일한 독립 검증축이 사라진다**(MASTER §2). 교환이다. 결정만 한다 |
| 55 | 대장 · SSD 디렉토리 구조와 해석기 하나 | 📄 | 해석기 · 관문 · 폐기 이름화 · 대장 래칫(DECISIONS §174), 레이크 밖 유일본 보존 · 격리 층 폐지 · 사본 삭제(DECISIONS §176)는 끝났다. **남은 것** — ① 해석 사본 배선 — 래칫 `대장 직접 로드` 40 · `주인 블록 직접 해석` 12 를 배치마다 내린다(acquire 는 옮겼다 · DECISIONS §180). 다음은 lakecheck · doctor · intake ② authority 래칫 63 을 내린다 ③ 중복 등재 hwp(소방청 제작규격 = 소방안전 제작규격) 한 주인으로 ④ 봉인 `20260814-ngii-ngi20` 재생성 여부 확인 — 구 원본(NGI 20도엽)과 당시 기본도 옛 판(gj037~048)이 retired 에 있다. 재생성되면 MASTER §13 을 정정하고, 안 되면 둘을 상폐한다(DECISIONS §178) ⑤ 문서 — MASTER §18-1 구조도(대장 layers 에서 생성 · 대조) · 계층 표와 대장 layers · paths 의 `quarantine` 을 `retired` 로 · MASTER §18-11 획득 게이트 · README 레이크 절 · 대장 `backup_policy`(외장 SSD 한 벌 서술이 DECISIONS §173-1 과 어긋난다) · 해석기 `LAYERS` 상수를 대장 layers 에서 읽기 |
| 56 | 검사 위생 — 카나리아 의무 · 사람이 쓴 칸 대조 | 🟡 | skip 분류(DECISIONS §175)와 오판 넷(DECISIONS §180 — acquire 폐기 판정 · landing 처분 메시지 · dms 산문 파서 · plan_renumber 남의 표 번호)은 닫혔다. **남은 것** — ① 강제자마다 카나리아 의무. `deadcheck` 가 카나리아 없는 강제자를 래칫으로 센다 — 판별식(무엇이 강제자의 카나리아인가)을 먼저 정해야 셀 수 있다. 정의 없이 세면 재현되지 않는 분모다 ② 사람이 쓴 칸(what)과 도구가 쓴 칸(schema)의 대조 — `hydrant_summary` 의 what 이 다른 파일을 서술했는데 schema 는 맞았다(DECISIONS §178-2). 컬럼 이름이 what 에 나오면 schema 에 있어야 한다는 식의 약한 대조부터 ③ 결함 목록 G-8 묶음(docx_fix 산출단위 · devcontainer · pipeline --from 계보 · manifest write_stable · 검사마다 재스캔 · Node 20 액션 · README 재편 · refcheck _gone · 주석 속 PLAN #N · 도달 가능 수 EXPECT · 태그 도달성 · 적용 원형) ★ 2026-09-22 — `test_seg_uid_retention` 은 기준을 커밋본으로 옮겨 닫혔고(DECISIONS §218-5), `dms` 위치 ID 는 W3-5 로 닫혔다 |
| 57 | 상세주소DB — 용도가 바뀌었다 | 🟡 | ★ **2026-09-18 EDA 로 종전 전제가 기각됐다**(DECISIONS §190). 종전 서술은 「아파트 `101동` 단위 목적지에 쓴다」였는데 **이 DB 는 아파트를 담지 않는다** — 안내 §1 이 대상을 「2가구 이상 거주하는 원룸·다가구주택·단독주택」으로 한정하고, 아파트는 건축물대장에 동·호가 이미 있어 부여 대상이 아니다. 데이터가 그대로 확인한다 — 전남광주 184,783행 중 광주 114,659행에서 `동명칭` 보유가 **3.9%**(4,490)뿐이고 값도 `비`·`에이`·`가`·`나`·`주건물` 로 다가구 라벨이다. 건물당 호 **중앙 6 · 평균 7.1**, 50호 이상 건물 10동. 동구 최대 380호 건물조차 `동명칭`·`층명칭` 이 공란이다. ★ **쓸 곳은 #60 이다** — 이것은 주소가 아니라 **세대밀도**다. 좌표는 없고(16필드 전수 확인) 조인은 `건물관리번호`(25자) 하나이며 위치는 `juso_bldg_geom` 이 준다. 판정 범위(동구+남구) 21,915행 · 건물 3,327동 — 동구 1,416동/12,501호, 남구 1,911동/9,414호. 전건 — `juso_bldg_geom` 과 `건물관리번호` 조인 검증, 그리고 MASTER §4 소방서 20구간 **3,452세대** 재현 |
| 58 | raw_only 주소 계열 4종 활용 여부 | 🟡 | `juso_bldg_geom` · `juso_bldggrp_geom` · `juso_etc_geom` · `spotaddr_geom` 은 raw 에 있고 kind raw_only 라 전처리 · ingest 가 없다. 활용 0. 판정 정확도(폭 · 위치)에는 거의 안 들어간다(DECISIONS §173-8) — 쓰면 출동 종점(건물 출입구 · 접속선) 쪽이다. 쓰려면 kind 를 정하고 prep · ingest 를 배선한다. 안 쓰면 retired 로. 전건 — #55 · #51 |
| 59 | 목적지 검색 — 주소 · 건물명 · 관공서 | 📄 | N1.1 · N1.2 코드가 섰다(DECISIONS §181 · DECISIONS §183) — `dest.geojson` 색인(상가 · `navi_build` · `navi_jibun` 지번 · `civil_office`) · 목적지는 **동명동 경계 안만**(DECISIONS §183-1) · 건물은 지도 이동 범위 · `juso_building_db` retired. 남은 것 — ① **뭉개지 않았다**(2026-09-24 실측). `조대명품타운 104동` · `105동` 이 각각 살고, 같은 도로명주소가 둘 이상인 주소는 4개(8행)로 전부 서로 다른 건물이다. `tests/test_dest_scope.py` 가 든다 ② 동명동 안 목적지 검색 → 스냅을 눈으로 확인 ③ ★ **2026-09-24 정정**(DECISIONS §235) — 「상가 1,587건 누락」은 **결함이 아니었다.** 동명동 경계 폴리곤으로 다시 재니 지도 상가 2,106 중 **동명동 안이 519 · 밖이 1,587** 이고, 색인 상가 521 은 **전부 경계 안**이며 **경계 안인데 색인에 없는 것은 0** 이다. 지도는 표출 스코프(동명동 + 회랑)를 그리고 색인은 DECISIONS §183-1 대로 동명동만 담는다 — 그 차이가 1,587 이다. `tests/test_dest_scope.py` 가 양방향(넘침 · 모자람)으로 든다 ④ **관공서 2건도 범위였다**(2026-09-24 실측) — 원천 `civil_office` 64건 중 **동명동 안이 정확히 2**다(동명동행정복지센터 · 광주서석초등학교). 색인과 같고 흘린 것이 없다. `tests/test_dest_scope.py` 가 든다. **남은 물음은 범위 자체다** — 회랑 안 인접 대형 목적지(조선대 · 전남대병원)를 색인에 넣을 것인가. 출동 목적지는 동 경계를 안 지킨다. 넓히려면 DECISIONS §183-1 을 먼저 고친다 ⑤ 초성 검색은 들어갔다(DECISIONS §224-5 · `ㄷㅁㄷㅎㅈ` → 동명동행정복지센터). 판정 1,281 에 영향 없음 |
| 60 | CCTV 없는 구간의 경로 가중치 — 추정 | 🟡 | CV 는 CCTV 가 있는 구간에서만 실시간 통과폭을 낸다. 없는 구간은 추정해야 하는데 **방식이 정해지지 않았다**(가상 환경 논의 중). 구간 단위로 붙일 수 있는 재료 — 단속이력 두 판(`parking_enforce`, 주소 → `navi_build` 로 지오코딩하면 구간 상습도) · 주차장 표준데이터(`parking_std`, 좌표 · 구획수) · 상가 밀도(`poi_store`) · 건축물대장(`bldg_ledger_dm`, 세대수 대비 주차대수 → 노상주차 수요) · 측량폭(NGII). 동 단위 스케일 · 사전분포 — 통계연보(`donggu_statbook`). ★ 통계연보는 동 단위라 동명동 안에서는 상수다. 전건 — R(구간 뼈대가 바뀌면 붙인 가중치가 전부 다시 붙는다)  ★ **2026-09-18. 재료가 하나 늘었다 — 상세주소DB 세대밀도**(#57). 지금 이 행이 든 세대수 원천은 `bldg_ledger_dm` 하나이고 그것은 **동명동뿐**이다. 상세주소DB 는 동구 1,416동·남구 1,911동을 든다 — 범위는 넓지만 **성기다.** ★ 2026-09-18 동명동에서 두 원천을 직접 댔다(DECISIONS §190-6) — 상세주소DB 129동/1,410호 대 건축물대장 주거 951동/가구+세대 2,431. **건물 13.6% · 세대 58%** 다. 건물의 1/7 인데 세대의 6/10 이라는 것이 이 자료의 성격이다 — **다세대 건물만 골라 든다.** 분자로는 쓰고 **분모로는 못 쓴다.** ★ 단속이력과 성격이 다르다 — **단속이력은 걸린 것만 센다.** 안 걸린 상습 점유가 0 으로 들어가는 편향이 세대수에는 없다. 상가밀도는 낮에 압력을 만들고 세대는 24시간 만든다. 전건 — #57 의 조인 검증. ★ 수용 기준은 **동명동 129동 겹침에서 상세주소DB 호 수 ↔ `bldg_ledger_dm` 가구+세대의 건물별 상관**이다 — 겹치는 구역이 있는데 안 겹치는 기준을 쓸 이유가 없다 |
| 62 | **「접근 회랑」이 회랑이 아니라 최단경로 나무다** | 🔴 | 2026-09-19 실측. 출동 대상지는 동명동(0.429km²) 하나이고 안전센터가 동 밖이라 **접근 회랑만** 불가피하게 더한 것이 원래 의도다. 실물은 그렇지 않다 — `web/data/segments.geojson` **1,281 중 911개(71%)** 가 동명동 밖이고 **연장으로는 73%**(42,068m / 57,522m)다. 소화전도 157 중 116(74%) 이 밖이다. ★ **원인은 `seg/graph.py:118` 한 줄이다** — `tgts = {snapn(...) for g in ent[ent.within(poly)].geometry}` 가 동명동 안 건물 출입구 **1,313개 전수**를 목적지로 잡는다. 안전센터 2곳에서 1,313개 각각으로 최단경로를 뽑아 그 경로의 동 밖 엣지를 전부 회랑에 넣으므로, 나오는 것은 「진입로 한 줄기」가 아니라 **동명동을 둘러싼 도로망을 훑는 최단경로 나무**다(304엣지 · 총연장 11.2km · 8방위 전부). ★ 회랑 ⊕70m 가 **1.091km² — 동명동의 2.5배**이고 판정 면적의 65%를 혼자 차지한다. 잔가지(그 엣지를 쓴 경로가 20개 미만)가 연장의 **44.7%**, 그 면적만 0.601km² 로 **동명동 전체보다 넓다.** 안전센터 직선경로 70m 안에 드는 밖 구간은 **17.3%(158/911)** 뿐이다. ★ 판정을 움직이므로 §13 이 아니라 여기다 — 회랑을 좁히면 구간 수·총연장이 바뀐다(§13-5 규칙 2). 수용 조건은 불변이 아니라 **대조**다. 닫는 법 — 목적지를 출입구 전수가 아니라 **동 경계의 진입점**으로 바꾸거나, 엣지 usage 에 하한을 두어 잔가지를 떨군다. 어느 쪽이든 **먼저 기준을 적고 재라**(가드 6). ★ **가드 0건이다** — `tests/test_station_scope.py` 는 「스코프가 안전센터를 덮는가」만 보고 **넓을수록 통과**한다. 회랑 길이·분기·확산에 상한을 두거나 「동명동 밖 비율」을 세는 검사가 저장소에 하나도 없다 ★ **2026-09-20 — 가드가 섰다.** `tools/scopecheck.py` 가 커밋된 `web/data/segments.geojson` 속성만으로 세 비율에 **상한**을 둔다(양방향 래칫 · 로컬과 CI 양쪽에서 돈다). 발행물 기준 실측 — **밖 구간 67.5%(865/1,281) · 밖 연장 68.9%(40,164m/58,309m)**. 위 서술의 911·71%·73% 는 `seg/graph.py` 내부 그래프 기준이라 **발행물 기준과 다른 것을 잰다** — 둘을 같게 맞추는 것은 결함 수정이 아니다. ★ **새로 나온 것이 더 무겁다.** `route_usage`(안전센터 2곳 → 건물 출입구 최단경로 사용횟수) 가 **0** 인 동 밖 구간이 **646개 31,844m** 로 **총연장의 54.6%** 다. 동명동 안에서 usage 0 은 416 중 55(3,365m)뿐이라 사상 오류로는 설명이 안 된다. 즉 **발행 총연장의 절반 이상이 동명동도 아니고 회랑도 아니다** — 「최단경로 나무」보다 한 겹 더 나쁘다. 줄이는 일은 여전히 판정을 움직이므로 이 행은 열려 있다. 가드는 「늘지 않았다」만 말한다 ★ **2026-09-20 EDA — 닫는 법 ①이 반증됐다.** 진짜 도로망에서 목적지를 출입구 전수(1,313) 대신 동 경계 진입점(65)으로 바꿔 재보니 회랑이 **304엣지 11,200m → 309엣지 11,393m(102%)** 로 **오히려 커진다.** 출입구로 가는 최단경로는 어차피 전부 경계 문을 지나므로 동 밖 부분이 같다. ★ 폴리곤을 줄이는 방향에는 **바닥이 있다** — 회랑을 통째로 지워도(센터⊕100m 유지) 밖이 38.6% 다. `동명동⊕50m 링` · 센터원 · 소방청지정이 그 바닥을 만든다. ★ 그래서 진짜 결함은 **수집 범위(스코프 폴리곤)와 발행 범위가 한 층에 붙어 있다**는 것이다. 듣는 지렛대는 구간 단위 필터뿐이고 실측은 — 「밖은 경로가 쓰는 것만」 635구간(밖 34.5%) · 「+소방청지정」 723구간(밖 42.5%). 어느 안이든 떨구는 쪽에 **폭 실측은 0개**다. 고르는 것은 프로젝트 정의라 사람이 한다. ★ 표출은 먼저 갈랐다(판정 불변) — 지도가 대상지·회랑·참고 셋을 진하기로 가르고 좌측이 `416 · 219 · 646` 을 찍는다 |  <!--stale-ok-->
| 63 | 동적 계층 — `current_status` 한 칸과 폴링 루프 | 📄 | 「동적」은 DB 엔진 성질이 아니라 **폴링 루프 + 얇은 상태 컬럼**에서 나온다. 무거운 공간조인은 정적 계층에만 둔다. 지금 실물은 정적 필드(`width_min_m` · `verdict` · `cctv_dist_m`)뿐이고 `tests/test_contract.py` 가 든다 — **컬럼도 루프도 없다.** 그 칸을 채울 원천이 #3 이다 |
| 64 | `serving` 스키마 — FE · QGIS 가 보는 단일 창구 | 📄 | 소비자가 여럿이어도 로직은 백엔드에 하나다. DB 가 아직 밑그림이라(MASTER §18-9) 스키마 실물이 없고 지금 산출은 정적 파일이다. 「프론트에서 판정을 재계산하지 않는다」는 지금도 지켜지며 `test_config_js_thresholds_match_params` 가 문턱 복사를 잡는다. 전건 — #28 |
| 65 | `width_cov` 0.5 미만 4구간 | 🟡 | 채택 소스가 구간의 **절반도 못 덮은 채로 판정**했다. 숨기지 않고 필드로 드러냈으니 실측 우선순위로 쓴다 — **D-25 1순위**(#4). 규모는 `seg/report.py` 커버율 표가 잰다 |
| 66 | 소방서 대조 잔여 편차 — 필문대로289번길 -3.51 · 제봉로184번길 +2.02 | 🟡 | 소방서 7구간은 게이트로 써버려 **검증 수단이 아니다**. 두 구간 편차가 폭 문제인지 뼈대 문제인지 안 갈렸다 — 필문대로289번길은 #51 이 「건물 위를 10.6m 지난다」로 드는 그 구간이고, 제봉로184번길은 R3a 에서 세그 19 → 0 으로 무너진 구간이다. 전건 — #4 또는 R |
| 67 | `road_side` 의미 | 🟡 | **측도 구분이 아니다**만 확인됐고 무엇인지는 모른다. 본선·측도 분리(#77)에 쓸 축인지 판정되지 않았다 |
| 68 | 폭 계산 교차검증 — 거리변환 방법이 없다 | 🟡 | 두 방법(법선 트랜섹트 / 거리변환)으로 재고 **불일치 구간을 먼저 본다**가 설계인데 거리변환이 없다. 단일 방법만 쓰면 인접 폴리곤 경계에서 교차선이 분할돼 폭이 절반 이하로 나온다 — **210구간에서 실제로 났고 현장 답사에서야 발견됐다.** 지금은 `test_intersection_samples_excluded` 가 그 사고만 막는다. 정사영상 전수 검증이 다음 후보 |
| 69 | 안전여유값 0.5m 를 실운전자에게 검증 | ⏳ | 미탐(막혔는데 통과 판정 — 소방차가 갇힌다)과 오탐(조금 돌아간다)의 비용이 **100배** 차이라 임계를 비대칭으로 뒀는데 그 비대칭의 크기에 근거가 없다. D-30 질의 ④는 1차 인터뷰에서 **미해결**로 남았다. ⚠ 줄자 U자 처짐이 5m 구간에서 수십 cm 라 **여유값보다 오차가 크면 실측이 무의미** — 레이저가 전제다. 전건 — #5 · #32 |
| 70 | 근거 없는 상수 나머지 셋 — 붙일 사람이 없다 | 🔴 | 목록과 현재 값의 정본은 MASTER §16-2. `CCTV_RANGE` 는 #15 에 걸려 있고 **나머지 셋은 담당이 없다.** 「누가 언제 붙이는가」는 담당 배정이라 검사 대상 밖이다. `tests/test_measurements.py` 가 「근거 없는 상수가 측정 대장에 행으로 서고 실측으로 닫히는가」를 보므로 **대장 행부터 세워야 한다**(가드 6 — 재기 전에 적는다) |
| 71 | A* 휴리스틱 허용성 — 직선거리를 최소 배율로 나눈다 | 📄 | cost 에 페널티 **곱셈**이 들어가므로 직선거리를 그대로 휴리스틱으로 쓰면 실제 비용을 넘고 **최적해가 깨져 A* 가 다익스트라와 다른 답**을 낸다. A* 를 쓰는 이유는 속도가 아니라 발표 논거이고 **실행시간 비교 그래프 한 장**도 미착수다. A* 가 맞는 자리는 출발1→도착1 내비이며 현재 산출(안전센터→전 노드)엔 이점이 없다. 전건 — #2 |
| 72 | 체크포인트를 엣지 속성으로 | 📄 | 소화전 · CCTV · 단속밀도를 **노드가 아니라 엣지**에 붙인다 — 그래야 「이 경로상 소화전 3개 경유」가 경로 산출 후 바로 나온다. 지금은 구간 필드로만 있고 엣지에 안 붙었다. 전건 — #1 · #29 |
| 73 | TMAP · 카카오 병렬 비교 화면 | 📄 | 병합이 아니라 **병렬**이다 — 외부 API 는 링크별 혼잡도를 낱개로 안 주고 완성된 경로를 반환해서 우리 그래프의 간선을 채우는 방식이 성립하지 않는다. 같은 출발·도착으로 외부 경로(교통체증만)와 우리 경로(통과 가능성)를 나란히 놓는다. **토큰이 필요해 CI 에 못 건다**(`tools/matchcheck.py` 가 같은 이유로 면제 목록에 있다) |
| 74 | 클리핑 범위 확장 — **불확실** | 🟡 | 옛 §4-5 표가 「미착수」로 적는데 지금 발행물은 회랑을 **이미 포함**하고 있고 #62 는 그것이 **너무 넓다**고 적는다. 이 항목이 #62 로 이미 도래한 것인지 아직 남은 별개 일인지 갈리지 않았다 — 실물을 보고 #62 에 흡수하거나 지운다 |
| 75 | 「소방청 건축위원회 표준 가이드라인 2021.10」 원문 확보 | ⏳ | 회전 기준(회전반경 차량 중심 10m · 곡선부 폭 4~6m · 진입 불가 2m 이하)의 **출처가 전부 블로그·기술사 정리**다. 규격 원문이 아니면 플래그를 안 켠다는 규율이 이미 있다(#20 `turn_radius_verified`). 법정 상한을 성능값으로 쓴 DECISIONS §81 과 같은 형태다. 전건 — #5 · #32 |
| 76 | 회전 가능 판정을 노드 속성 사전계산으로 투입 | ⏳ | 폭 3m 골목이라도 직각 코너면 내륜차 때문에 못 돈다. 원칙 — **간선 = 직진 폭 / 노드 = 회전 가능, 둘 다 통과해야 유효 경로.** 적용 범위는 2026-09-01 에 닫혔다(DECISIONS §86) — 양쪽 접근로 중 하나라도 `clear` 가 아닌 코너로 한정하고 새 상수를 안 만든다. `edge_cost` 인자가 아니라 노드 속성 사전계산으로 들어간다. 막는 것 — 판정 반영이 D-30 이후다 |
| 77 | 간선 본선 + 측도 병합 — 벽~벽이 46~57m 로 샌다 | 🔴 | 필문대로289번길 52.2m. '길'인데 52m 인 것은 본선과 측도가 한 폴리곤이라 벽~벽에서 둘이 합쳐지기 때문이고, 그래서 `width_max_m` 를 믿을 수 없다. 보도 커버율 13% · 30m 초과 24건 중 보도 인접 **2건뿐**이라 보도 차감으로는 못 푼다. 도로대장은 부존재 종결이고(#6) **전건으로 두지 않는다** — `ngii1k` 도로중심선이 본선·측도를 다른 링크로 가지므로 한 폴리곤을 두 중심선으로 가르는 쪽이 먼저다. 규모는 `tools/wmax_audit.py` 가 잰다. 담당 없음 |
| 78 | 내비 앱 — GPS 연동 · 1인칭 카메라 · 신고주소 경고 | 📄 | 미착수 셋 — ① `Geolocation API` 연동 ② 1인칭 카메라(`maxPitch` 85 · 진행 방향 기준) ③ 신고 주소 입력 → 그 건물 출입구까지 **경로 위 위험구간 즉시 경고**. 건물출입구 1,313개와 안전센터→출입구 최단경로는 이미 산출돼 있어 주소 조회로 끝난다. 목적지 검색 자체는 #59 |
| 79 | 관측점당 지면 기준점 1회 실측 — 카메라 재현을 포기한다 | 📄 | CCTV 가 아니라 CCTV **모사**이고 삼각대를 매번 들고 나가므로 카메라는 전혀 고정이 아니다. 위치·높이·각도 재현을 버리고 **관측점당 1회(영구)** 지면 영구 지물 4~6개의 실세계 상대좌표를 재둔다 — 매 세션은 같은 지물의 이미지 좌표만 다시 얻어 H 를 재계산한다. 「매번 동일 위치에서 찍어야 한다」가 통째로 사라진다. 2회차부터 특징점 자동 정합. 이 결정이 #84 를 강제한다 |
| 80 | 렌즈 왜곡 보정계수 — 보드는 있고 계수가 없다 | 🟡 | 체커보드를 **카메라당 1회** 찍어 왜곡을 먼저 보정해야 H 가 선다. ChArUco 보드는 확보했고 **보정계수가 없다**(MASTER §19-6). 같은 전제 위에 촬영 제약이 걸린다 — 줌 금지·1배 고정(초점거리가 바뀌면 캘리브레이션 무효) · 광각 0.5배 금지 |
| 81 | DL 투입 지점 확정 · 프라이버시 마스킹 파이프라인 | 🔴 | 단일 사전학습 모델 하나로는 「가져다 쓴 것」이라 주인공이 못 된다. 순위 — ① 번호판·얼굴 마스킹(**없으면 프로젝트가 법적으로 성립 안 함**) ② 주정차/주행 판별(#82) ③ 야간 강건성 ④ 소방관 선호 경로 학습(인터뷰 선행 · 리스크 큼 · #5). **마스킹은 엣지에서** — 원본이 서버로 넘어가는 순간 「수집」이 되고 엣지 마스킹이면 서버는 애초에 보유하지 않는다. 마스킹 정책은 계약의 **합의 대기** 항목이다. 담당 없음 |
| 82 | 주정차/주행 판별 — mock 서버가 프레임 시퀀스를 반환해야 한다 | 🟡 | **가상 중앙선은 쓰지 않는다** — 골목엔 중앙선 개념이 없다. 정답은 **연속 프레임에서 정지 = 주차**이고 이것이 아키텍처를 강제한다. 관측점에서 삼각대 고정 → 30초~1분 동영상 → 프레임 추출 + 타임스탬프 → 순서대로 반환. 주행 차량은 5초면 화면을 벗어나므로 30초면 충분하다. 「24시간 연속 관측 불가」와 「시퀀스 확보 불가」를 혼동하지 않는다 — 전자만 사실이다. 전건 없음 |
| 83 | 같은 관측점 오전 · 오후 · 야간 3회 촬영 | 🟡 | 답사 1회로 셋을 해결한다 — 「14시 통과 가능 / 19시 불가」가 서비스 필요성 최강 소재이고, **관측 신선도**(#90)와 **야간 성능 데이터**가 같은 답사에서 함께 나온다. 야간 답사는 별도 일정이다. 전건 — #84 · 지점 선정 |
| 84 | 관측점을 GIS 1급 개체로 — 「카메라를 세우는 지점」이라는 실체가 없다 | 🔴 | 스키마 — `obs_id` · `seg_uid` · `geom` · `landmark_desc`(예: 「장동로 서쪽 끝에서 세 번째 맨홀」) · `ref_points`(id · desc · `local_xy`) · `ref_image` · `sessions`(시각 · 영상 · H · 조도). 따라오는 것 — 세션 간 H 재계산 · 시간대별 통행폭 비교 · **관측 커버리지 레이어**(회색 구간 중 어디를 실제로 덮었나). ★ CCTV 좌표를 그대로 쓰지 않는다 — 시나리오 경로 위(#101)에서 고른다. **GIS/비전 경계라 담당 공백이 가장 위험한 칸이다** |
| 85 | 실측 보조 앱 — 기존 지도 HTML 에 얹는다 | 🔴 | ★ **다음 실측을 막고 있는 유일한 항목이다.** 네이티브 금지. 다섯 — ① GPS + 최근접 세그먼트 자동 판별 ② 관측점 등록(좌표·시각·`seg_uid` 자동) ③ 사진 + **기준 지물 서술 입력(필수)** ④ 측정값 입력 ⑤ JSON 내보내기. 전제 — **HTTPS 가 아니면 `navigator.geolocation` 이 안 돈다.** GPS 는 보조이고 기준 지물 서술·사진이 가장 중요한 입력이다. #4 · #101 · #84 가 전부 이 뒤에 선다. 담당 없음 |
| 86 | ArUco 보드로 배포 방법의 절대 오차를 잰다 | 🟡 | 배포될 방법은 마커를 안 쓴다 — 세그멘테이션 도로 경계선을 수치지형도 폴리곤에 맞춰 H 를 푼다(CCTV 104지점에 보드를 깔 수 없다). **문제는 그 방법의 오차를 아무도 모른다는 것** — 지도 기준점으로 풀고 지도 폭과 비교하면 순환논증이고 소방서 7구간에서 이미 겪었다. 3~5곳에서 A(지도)·B(보드)·C(레이저)를 같은 골목에서 재 세 차이를 낸다. **D-25 와 같은 현장 — 두 번 나가지 않는다.** 근경 10m 는 보드, 원경 10~25m 는 지도로 가른다 |
| 87 | 실측 표본 트랙 A · B · C — 섞지 않는다 | 🔴 | A 12구간(층화 무작위 · 편향 추정) / B 8구간(임계 근접 + 상대불일치 · 판정 교정) / C 5구간(단순 무작위 · **봉인** · 외부 검증). **A 와 B 를 합쳐 편향을 추정하면 추정치가 망가진다** — B 는 「소스끼리 값이 다른 곳」만 고른 표본이라 오차 분포가 전체 분포가 아니다. 불일치 기준은 절대값이 아니라 **상대값 35%**. **C 는 열지 않는다** — 소방서 7구간이 게이트가 돼 현재 외부 검증 수단은 **0개**다. 담당 없음 |
| 88 | 오차 모델 검증 — 1 · 2 · 3회차 전부 미착수 | 📄 | 1회차 트랙 A 12구간으로 소스별 편향 b · 산포 σ 를 추정한다. **2회차가 핵심** — 그 모델로 트랙 B 를 **미리 예측하고 나가** 실측과 비교한다. 3회차는 안 맞는 구간의 공통점으로 모델을 고친다. 오차를 줄이는 것이 아니라 **오차 모델이 맞는지 검증하는 것**이다. 예측을 적어둘 자리는 `tests/test_measurements.py` 대장. 전건 — #4 · #87 |
| 89 | 네이버 지도 준-실측 — 표본 46구간 생성 완료 · 측정 미착수 | 🟡 | 현장 실측 25구간 75점으로 1,281구간을 못 덮는다. 네이버 거리 측정을 준-실측으로 쓸 수 있으면 **전수 대조**가 열린다. 기준 — `|중앙편차| < 0.3m` · `표준편차 < 0.5m` 면 전수 대조 / 편차만 크고 산포가 작으면 계통오차라 보정 상수 / 산포가 크면 순서형 참고만. ★ 어느 쪽이든 레이저 실측을 없애지 않는다 — 네이버도 지도이고 우리와 같은 국가 데이터 파생일 수 있다. 규모가 줄 뿐이다 |
| 90 | 관측 신선도 — 판정에 시간 개념이 없다 | 🔴 | 마지막 관측이 40분 전이면 그 판정으로 소방차를 보낼 수 있나에 **지금 답이 없다.** 방향은 `신뢰도 = f(경과시간, 단속밀도)`(30분 전 관측 + 상습주차 = 사실상 영상판정 불가). 관측 시각 필드는 계약에 이미 있다(`observed_at`). ★ 소방본부 종합상황실 자문(#110)이 「관측되지 않는 구간을 관제에서 어떻게 다루나」를 직접 답할 수 있다 — 우리가 정할 문제가 아닐 수 있다. 담당 없음 |
| 91 | 평가지표 산출기 `data/processed/eval.json` · 재현성 게이트 | 🔴 | **지표는 값이 아니라 실행이다 — 터미널에만 있는 숫자는 소실된다.** 형식을 다른 대조 산출물과 맞춰 `tools/baseline.py` 가 실행 간 자동 비교하게 한다. 게이트는 지표가 아니라 **통과 조건**이다 — 산출기 첫 줄에서 `baseline.py diff` 전이행렬 · `segments.fingerprint.json` 지문 · `_manifest.json` 셋을 보고 불일치면 지표를 안 뽑고 죽는다. 낡은 산출물로 뽑은 틀린 숫자는 「지표」라는 이름 때문에 아무도 의심하지 않는다. 담당 없음 |
| 92 | E-3 판정 가능률 재산출 | 🟡 | 값은 이미 있고 **재산출만** 하면 된다 — 영상판정 불가 399/1,281(31%) · 연장 17,168m/58,309m(29%) · CCTV 유효범위 밖 830/1,281(65%). 완화 항(불가 구간이 담당하는 통행량 18%)도 같이 낸다. 전건 없음. 막는 것은 산출기(#91)뿐이다 |
| 93 | E-4 폭 정확도 — MAE · 중앙값 · 소스별 분해 | 🟡 | **전건은 D-25 실측(#4) 하나다.** 봉인 표본 트랙 C · 야장이 입력이다. ★ **절대편차 합을 지표로 쓰지 않는다** — 표본 수에 비례해 커져 실행 간·소스 간 비교가 안 된다. MAE 와 중앙값으로 바꾸고 합은 참고로만 둔다. 소스별 분해는 `ngii1k` / `silpok` / `ngii`. ★ 외부 회신을 대안 경로로 두지 않는다 — 부존재 종결됐고 무산되면 지표가 통째로 뜬다 |
| 94 | 평가 표본 설계 확정 — 층화 · 표본 수 · 시드 · E-1 분모 | 🟡 | 전수는 무겁고 무작위는 「왜 그 쌍이냐」를 못 막는다. 층 A `route_usage` 상위 · B `nfa_designated` 인접 · C 영상판정 불가 밀집 · **D 무작위(편향 대조군 — 없으면 순환논증)**. 미정 둘 — 표본 수·시드(`eval.json` 에 박는다) · **E-1 분모를 건물로 할지 노드로 할지**. ★ 결손은 분모에서 빼지 말고 **별도 항**으로 보고한다. ★ **불확실** — 층 A 근거 「579구간이 0 초과」가 #62 실측과 어긋난다. 먼저 재측정 |
| 95 | 소화전 근접으로 통행 불가 구간을 세분화한다 | 📄 | 「도로엔 못 들어가도 근처 소화전이 있으면 호스로 대응 가능」 로직이 **아직 없다.** 근거는 NFTC 401 수평거리 140m · NFSC 109 옥외소화전 40m. 좌표는 이미 있다 — 스코프 안 153 · 동명동 41. 전건 — 매칭 임계(#29)가 정해져야 「근처」가 정의된다 |
| 96 | `processed` 계층 포맷 전환 — GeoParquet · COG | 📄 | **클라우드 적재(#97) 전에 해야 한다.** 지금은 텍스트·SQLite 라 저장 비용과 스캔이 낭비다. `*.geojson` + `*_5186.gpkg` 두 벌을 **GeoParquet 하나로** 대체하면 좌표계가 파일에 들어가 `_5186` 접미사도 없어지고 메타데이터 헤더(#101)도 이때 같이 된다. ⚠ `web/data` 는 브라우저가 읽으므로 GeoJSON 유지. ⚠ 여러 스크립트가 `gpd.read_file(*.gpkg)` 라 **한 번에 바꿔야 하고 회귀 위험이 있다 — 파이프라인 안정 후** |
| 97 | 클라우드 적재 — raw 를 오브젝트 스토리지로 | 📄 | `paths.py` 가 **원격 스킴을 아직 안 받는다.** 나머지 스크립트는 환경변수로 이미 분리돼 있어 고칠 곳은 한 군데다. **다만 지금은 이르다** — 이 규모로 비용·권한·CI 설정을 감수할 단계가 아니고 D-25 실측(#4)과 A*(#1 · #2)가 먼저다 |
| 98 | 정밀 DEM 보류 판단을 다시 세운다 | 🟡 | 안심구역은 **원본 반출 불가**(분석 결과만 · 심의 2회 · 2~3주)다. 배제 근거였던 등판 한계 11~17°는 **출처가 없었다** — KFS 5종 §4.4.1 원문은 18% 등판 = **10.204°**. 공개DEM 90m 최대 경사 9.0° 라 여유가 **1.2°** 뿐이고, 그 공개DEM 은 대장이 「평활화로 경사가 실제보다 낮게 나온다」고 적는다. 결론을 지금 뒤집지는 않되 이 숫자로 보류 판단을 재수립한다 |
| 99 | `publish_web.py` 발행 블록을 대장 선언으로 | 🟡 | 소스마다 발행 블록이 **손으로 쌓여 있다.** `sources.yaml` `outputs` 에 발행 플래그를 두면 루프로 돌릴 수 있다 — 선언은 대장, 코드는 실행만. 어긋남은 `tests/test_ledger_outputs.py` · `tools/web_manifest.py --check` 가 이미 운다 |
| 100 | `web/data` 추적 범위가 근거를 넘어섰다 — 타일 23.6MB | 🟡 | 커밋 근거는 「UI 담당이 raw 없이 작업」이고 **벡터 GeoJSON 여섯 종에만** 성립한다. `.gitignore` 의 `!web/data/**` 가 벡터·래스터를 안 가른다 — 정사영상 1,423장 + 음영기복 22장 = 23.6MB. ★ **불확실 — 같은 문서가 반대를 말했다.** 한쪽은 「추적을 끊으면 배포 지도의 정사영상이 사라진다(재생성에 raw 1.3GB 필요)」로 기각했다. 먼저 한쪽을 지운다 |
| 101 | 대장 · 매니페스트 격차 다섯 칸 | 🟡 | 목표 — raw 개명을 `norm/` 계층 분리로 · 매니페스트에 `git_sha` · `git_dirty` · `input_shas` · `params` 추가 · 스키마 검사를 필드 설명까지 대장에서 생성 · GeoJSON 메타는 GeoParquet 헤더로(#96) · **복원 훈련 월 1회 미실시**. 현재 칸은 `python -m firelane.ledger` 가 이미 보므로 **목표 칸이 대장 스키마에 들어가는 순간 같은 검사가 받는다.** 작업량은 하루 안쪽이다 |
| 102 | 은퇴 기준에 조인 키 칸을 더하고 `retired` 전수를 다시 훑는다 | 🟡 | 현행 기준 「좌표 없음 + 소비자 0곳」이 **틀렸다.** `juso_building_db` 가 그것으로 은퇴했는데, 상세주소DB(#57)가 같은 둘을 갖고도 **밀도에는 좌표가 필요 없다** — 세대수는 세는 값이고 위치는 `juso_bldg_geom` 이 준다. 좌표 보유는 **레이어의 조건이지 테이블의 조건이 아니다.** 같은 이유로 잘못 나간 것이 더 있을 수 있다 |
| 103 | `jijeok_review` 머리말이 삭제된 `naver_page.py` 를 아직 든다 | 🟡 | 산문이라 임포트가 아니고 검사가 안 잡는다. 조사 스크립트 아홉은 면제 목록에 사유와 함께 등재돼 있어 이동은 불필요하다 — **남은 것은 이 죽은 참조 하나뿐**이다. ★ 판정을 안 움직이므로 §13 으로 갈 후보 |
| 104 | 계약 테스트 확충 — 폭 상한 · 도로명 규칙 위반 상한 · 조용한 결측 | 🟡 | 병렬로 돌릴 수 있고 전건이 없다. ★ 판정 산출물을 안 움직이므로 §13 으로 갈 후보 |
| 105 | D-30 ① 진입불가 · 곤란지역 지정 자료를 받을 수 있나 | ⏳ | 광주동부소방서 소방행정과. **포지셔닝 1차 출처이고 질의 중 최우선**이다. 우리 판정을 「소방서가 이미 지정한 것」과 맞댈 유일한 행정 근거다. 보유 중인 「소방통로확보대상 20구간」과 **다른 자료**다. 전건 — #5 · #32 |
| 106 | D-30 ② 「2025 골든타임 확보 종합대책」 원문 | ⏳ | 인용 근거. 대장에 없다. 회신이 오면 `landing → acquire → raw` 를 태워 대장에 올린다. 전건 — #5 · #32 |
| 107 | D-30 ⑤ 진입 실패 사례 빈도 | ⏳ | 프로젝트 **필요성 근거**. 공개 데이터에 없다. 전건 — #5 · #32 |
| 108 | D-30 ⑦ 기사가 경로를 고를 때 보는 요소 | ⏳ | 선호경로 학습(#81 순위 ④)의 유일한 입력이다. ★ **A* 를 여기 걸지 않는다** — 구조(거리·폭·통행제한·내륜차)는 인터뷰 없이 서고 인터뷰가 정하는 것은 **가중치뿐**이다. 전제로 설계하면 무산될 때 A* 전체가 뜬다. 전건 — #5 · #32 |
| 109 | D-30 ⑧ 동명동 출동 이력 통계 | ⏳ | 시나리오 현실성 근거. ★ **부분 대체 가능** — `nfa_dispatch_119` · `nfa_rescue`(동명동 212행) · `nfa_fire_incident` 파생본이 이미 있고 #39 가 그것을 경로 비용 검증축으로 쓴다. 대장 `pending.fire_incident` 는 여전히 **활용신청 미착수**다. 소방서 기록은 그 공개분이 못 덮는 관할 내부 통계에만 필요하다. 전건 — #5 · #32 |
| 110 | D-30 ⑨ 야간 출동 추가 어려움 | ⏳ | #81 의 야간 강건성(저조도·도메인 적응) 문제 정의가 여기서 온다. 답사 1회로 오전·오후·야간 3회 촬영하기로 한 설계(#83)의 우선순위 근거이기도 하다. 전건 — #5 · #32 |
| 111 | D-30 ⑪ 한전에서 전주 실측 좌표를 받고 있나 | ⏳ | 전주 데이터 **대안 경로** 확인. 전주는 유효폭을 깎는 장애물인데 공개 좌표가 없다. 전건 — #5 · #32 |
| 112 | 소방용수 분모 정합 — `fcltySeCode` 가 지상/지하 어디에 대응하나 | ⏳ | **좌표는 확보됐고 남은 것은 분모다** — 표준데이터 좌표 528(동구 445) · 스코프 안 153 · 동명동 41(MASTER §3-12). 관할 589(지상 418 + 지하 171) 대 표준데이터 동구 445 가 안 맞고, `fcltySeCode` 1:318 · 2:150 · 4:35 · 6:18 · 3:2 · 5:1 의 **코드값 의미가 확인되지 않았다.** 확인 전에는 지상/지하와 대조하지 않는다. 전건 — #5 · #32 |
| 113 | 소방통로확보대상 20구간의 시 · 종점 좌표 | ⏳ | 소방서 자료에 **좌표가 없어** 도로명으로만 매칭되고 도로 전체(대로 구간 포함)를 잡는다. 그래서 중앙값으로 비교하며, 최솟값으로 바꾸면 교차로 극협소 지점을 잡아 −3~−7m 로 벌어진다. **정밀 대조에는 구간 좌표가 필수**다. 전건 — #5 · #32 |
| 114 | 겨울 결빙 시 진입 불가 구간 | ⏳ | 계절 축이 판정에 아예 없다. 경사 + 결빙 조합은 공개 데이터로 못 만든다. 전건 — #5 · #32 |
| 115 | 협조 공문 2건 발송 — 동부소방서 · 종합상황실 | ⏳ | **아직 안 보냈다.** ① 동부소방서 예방안전과·119재난대응단 — 차량 제원(사이드미러 포함·미포함) · 운전요원 자문 · 진입곤란 구간 현황 ② 소방본부 종합상황실 — 출동 관제 절차 자문 · 실무 요구사항(갱신 주기 · 표시 방식 · 신뢰도 표기 · **미관측 구간 처리**). ★ **발송 전 기관명을 확인한다** — 2026-07 광주·전남 통합으로 본부 명칭이 바뀌었을 수 있다. ★ 질문지를 지인 인터뷰와 정식 인터뷰로 나눠 보낸다. ②가 #90 · #60 을 직접 푼다 |
| 116 | 멘토 판단 대기 2건 — 표본 일반화 방어 · 외부 검증 0개 중간평가 | ⏳ | **우리가 못 정하는 것**이다. ① 표본 3~5곳으로 1,281구간을 일반화하는 것이 심사에서 방어되나(통계 설계 판단 — #4 규모가 여기 걸린다) ② 외부 검증 수단 **0개**로 중간평가를 받는 것이 맞나(소방서 7구간은 적합에 썼으므로 검증이 아니다) |
| 117 | 오류 비대칭 결정 — 영상판정 불가 399 를 어느 쪽으로 기울일 것인가 | 🔴 | 미탐(치명적) 대 오탐(경미)의 비용이 100배라 임계를 대칭으로 두면 안 된다. **결손과 불확실을 어느 쪽으로 기울이나**가 뿌리이고 판정과 지표 양쪽에 동시에 나타난다. E-2 는 낙관·비관·현행 셋을 다 내기로 했으나 **판정 쪽 기울기는 여전히 미정**이다. 같은 물음이 멘토 질의로도 올라가 있다(#116). 담당 없음 |
| 118 | GIS 담당 한 사람에게 부하가 몰려 있다 | 🔴 | 데이터 확보 + EDA + 그래프 + 인프라를 혼자 든다. **혼자 다 하면 병목이 되고 검증 없이 확정하는 상황이 반복된다** — 이 저장소의 사고 다수가 그 형태다. 이미 #1 · #2 는 2026-09-02 에 GIS 담당이 놓아 ⏳ 가 됐다(DECISIONS §102) |
| 119 | 담당 재분배 회의 | 🔴 | 위 공백(#70 · #81 · #84 · #85 · #87 · #90 · #91 · #101 · #117)을 **회의에서 재분배하기로 했으나 그 회의가 아직 없다.** 재분배 없이는 위 행들이 전부 ⏳ 로 굳는다 |
| 120 | 시나리오 자동 역산 — 「우리가 없었으면 갇혔을 쌍」을 그래프에서 뽑는다 | 🔴 | `d2/d1 > 1.5 and d2 < inf` 역산기가 **없다.** 두 경로(제약 있음/없음)는 이미 돌아 입력은 갖춰져 있다. 출발 대인·지산 2곳, 선정 조건은 코너 통과 가능 / CCTV 커버 / 우회로 존재. **E-1 산출기와 같은 코드다 — 두 작업이 아니다.** ★ 답사보다 먼저다. 순서가 꼬이면 엉뚱한 골목을 찍고 답사가 통째로 날아간다(의존 사슬 `경계→노딩→폭→그래프→시나리오→관측점→촬영`). 담당 없음 |
| 121 | `seg/params.py` 가 환경 스위치 다섯을 import 시점에 읽는다 | 🟡 | `NO_MERGE` · `DEBUG_SEG` · `DEBUG_XY` · `OLD_SNAP` · `MIX_SRC` 를 `paths.flag()` · `paths.env()` 로 읽어 **순수 도메인이 경로 계층을 안다.** `paths` import 는 모듈 수준에서 `.env` 를 파싱하므로 `import firelane.seg.geom` 한 줄이 디스크를 친다 — 「순수 함수. 폐포 없음」이라 적힌 파일이다. 닫는 법 — 다섯을 `WidthEngine.__init__` 인자로 올린다(그 자리는 이미 「실행 내내 불변」 묶음이다). **폐포 안이라 재잠금 1회.** 면제는 `tests/test_layering.py::EXEMPT` 가 사유와 함께 든다 |
| 122 | 계층 강제 목록이 판정 모듈 셋을 안 본다 | 🟡 | `test_layering.DOMAIN` 에 `seg/vehicle.py` · `seg/scope.py` · `seg/centerline_correction.py` 가 없다(`report.py` 제외는 사유가 적혀 있지만 이 셋은 **조용히 빠졌다**). 그중 `seg/vehicle.py` 가 실제로 위반이다 — `paths.ROOT` 를 우회한 제 루트 계산(`parents[3]`)으로 `sources.yaml` 을 직접 읽고 전역에 캐시한다. **TS 쪽이 이미 옳은 모양이다**(`domain/vehicle.ts` 는 `spec` 을 인자로 받는다). 재잠금 1회 |
| 123 | 「순수」 도메인에 모듈 수준 가변 상태 | 🟡 | `seg/params.py::_DBG = {"on": False}` 를 `segments.py` 가 켜고 끄고 `seg/width.py` 가 읽는다 — **두 모듈이 세 번째 모듈의 전역 딕셔너리로 통신한다.** `WidthEngine` 은 `test_engine_state_is_explicit` 까지 두고 상태를 명시적으로 드는데 디버그 스위치 하나만 전역이다. 닫는 법 — `widths(geom, debug=False)` 인자. #121 과 같은 배치 |
| 124 | 늦은 import 한 줄이 1,137줄을 판정 지문에 넣는다 | 🟡 | `seg/report.py::_fire_access_csv` 안의 `from firelane import ledger` 하나 때문에 `ledger`(509) · `naming`(440) · `scope`(188)가 폐포에 든다. **셋 다 판정에 한 톨도 기여하지 않는다** — 파일명 문법 파서와 행정범위 어휘다. `naming.py` 의 정규식 하나만 고쳐도 `golden check` 가 운다. 게다가 그것을 끌어들이는 `nfa_compare` 자체가 판정이 아니다(제 머리말이 「판정이 끝난 것을 받는다. 계산은 안 한다」고 적는다). 닫는 법 — 파이프라인 별도 단계로. **선례가 있다** — `display_scope` 를 같은 이유로 폐포 밖으로 옮겼다. 끝나면 폐포가 21 → 18 파일로 줄어 **후속 수술이 싸진다** |
| 125 | R3(정본 하나)가 판정 핵심 안에서 깨져 있다 | 🟡 | ① 폭 소스 우선순위가 `seg/width.py` 한 파일 안에서 **아홉 번** 재기술된다(`("ngii1k","ngii","silpok")` 꼴). 「결정 63」은 가장 많이 인용되는 규칙인데 순서를 바꾸려면 아홉 곳을 고쳐야 한다 ② 내륜차 무시 임계 `0.05` 가 **이름 없이 세 곳 두 언어**에 있고 `test_sources_of_truth` 의 값 목록에 없다 ③ union-find 15줄이 `segments.py` 와 `seg/graph.py` 에 **공백까지 같게** 있다(#47 이 이미 안다). 셋 다 폐포 안 — **한 PR 로 묶어 재잠금을 한 번만 쓴다** |
| 126 | 파트 경계(`src/contracts/`)에 생산 코드 사용자가 0명 | 🔴 | `import contracts` 를 하는 것은 시험 둘뿐이고 `src/firelane` · `tools` 는 **한 번도 계약을 통과시키지 않는다.** `contracts/__init__.py` 가 그리는 세 갈래 화살표 중 `firelane → contracts` 변이 실물에 없다. 더 아픈 것은 **경계를 넘는 실제 판정 규칙에 계약이 없다는 것**이다 — 차량 통과 규칙(내륜차 · 필요폭 · 회전가부)이 py·ts 두 구현으로 산다. 지금은 동치지만 **알고리즘 동치는 아무도 안 본다**(강제자는 상수 재선언만 본다). 그 규칙이 내비의 「이 골목으로 갈 수 있나」를 정한다. 파트 간 합의 |
| 127 | `_write_route` 에 이음매가 없어 시험이 소스 문자열을 읽는다 | 🟡 | `tests/test_guards.py` 가 `segments.py` 를 **문자열로 읽어** `"P.remove_edges_from" in body` 를 단언한다. 이것은 시험이 아니라 **구현 문자열 잠금**이다 — 변수명만 바꿔도 깨지고, 반대로 그 문자열을 남긴 채 로직을 뒤집어도 통과한다. 원인은 `_write_route(g)` 가 `OUT` 전역에 쓰기 때문이다. 닫는 법 — `_write_route(g, out_dir)` 로 인자화하고 합성 입력으로 실제 호출한다. `golden.py` 머리말이 **이 작업을 위해** 절차를 이미 적어 뒀다 |
| 128 | `ingest.build()` 손 분기 열한 갈래 490줄 | 🟡 | `kinds.py` 가 이미 레지스트리인데 **분류만** 하고 디스패치는 손 분기로 남았다 — 제 머리말이 「새 kind 를 추가하려면 `ingest.build()` 에 분기를 만든다」고 스스로 적는다. 전략/레지스트리가 절반만 적용됐다. 닫는 법 — `KINDS` 항목에 reader 를 달고 `build()` 는 조회만. **판정 재잠금은 없다.** 대신 전 샤드가 찢어져 `ngii_road` 재빌드 = 8GB 기계 OOM 위험이라 **다른 작업과 안 겹치는 날에 단독으로** |
| 129 | `useNavigation` 569줄이 최소 넷을 들고 있다 | 🟡 | `useState`+`useRef` 37개 · 반환 키 40개. 번들 적재 · 위치원 3종 선택 · 5Hz 스냅 스케줄러 · 인접리스트/A*/대안경로 · 진행 추정 · 60fps 추측항법 · 6상태 단계 머신 · 대체 진입. **구조가 잘못됐다기보다 분해를 하다 만 것이다** — 같은 폴더에 `useHudData` · `useScreens` · `useVoice` · `useFleet` · `useShare` · `useOpsUplink` 가 이미 있다. 재잠금 없음 |
| 130 | 프런트 세 파일이 길이 상한 위에 있다 | 🟡 | `OpsApp.tsx` 744 · `App.tsx` 640 · `domain/graph.ts` 627(상한 600). 2026-09-24 에 `sizecheck` 범위를 `web/navi/src` 까지 넓히면서 **처음 보였다** — 그전에는 래칫 밖이라 한 번도 안 잡혔다. 지금은 오늘 수 그대로 박아 「더 나빠지지 않는다」만 지킨다 |

강제자  `tools/verify.sh`

### 1-23. 영상판정 유효거리 실측 설계

★ `MASTER §16-1` 이 광학 계산을 적는다. **여기는 그것을 재는 방법이다.**

**실험**

    한 골목에서 d = 5 · 8 · 12 · 16 · 20 · 25m 지점에 삼각대를 세우고 촬영
    같은 단면을 레이저 거리계로 1회 실측
    → 거리별 오차 곡선 → 0.3m(임계 뒤집힘)를 넘는 d 를 높이별로 확정

높이는 최소 둘(삼각대 1.5m · 연장 3~4m)을 재야 `h` 의 영향이 분리된다.
한 높이만 재면 거리 효과와 높이 효과가 섞인다.

**결과가 나오기 전에는 `CCTV_RANGE = 25.0` 을 유지한다.**

### 1-24. 촬영 계획이 높이에서 갈린다

```
구간 길이 중앙값 36.3m
h = 1.5m (유효 11.0m)  →  구간당 3~4회
h = 4.0m (유효 17.5m)  →  구간당 2~3회
```

1,281구간이면 전자는 감당이 안 된다. **높이를 올리는 것이 곧 촬영 횟수를
줄이는 것**이고, 그것이 장비 판단의 근거다.

### 1-25. ⚠ A* 를 인터뷰에 걸지 않는다

선호도 기반 피처는 인터뷰가 있어야 **가중치**를 정할 수 있다. 그러나 **구조**는
인터뷰 없이 정해진다 — 거리 · 폭 · 통행제한 · 내륜차는 전부 보유 데이터로 된다.
인터뷰를 전제로 설계하면 무산될 때 A* 전체가 뜬다. 인터뷰는 "있으면 좋은 것"이지
"없으면 못 하는 것"이 되면 안 된다.


### 1-26. 도로대장 정보공개가 부존재로 돌아왔다

두 건 모두 2026-08-24 자 **「정보 부존재 등 통지서」** 다.

    17337943   도로대장 (도로명 · 총폭 · 차도폭 · 보도폭 · 차로수 · 연장 …)
    17337939   보도(인도) 및 도로부속물 현황

사유는 둘 다 같다 — *"요청하신 자료는 정보를 취합·가공해야 하는 정보이므로
부존재 처리함"*. 근거는 정보공개법 시행령 제6조 제4항이다.

**절차 자체는 근거가 있다.** 2011년 시행령 개정으로 부존재는 비공개결정이
아니라 민원사무로 처리하게 됐고, 그래서 정보공개법상 이의신청 대상이 아니다
(행정심판·행정소송은 가능).

★ 다만 **두 건의 무게가 다르다.** 도로대장은 「도로법」 제56조 제1항이
작성·보관을 의무화하고 시행규칙 제24조 제5항이 **영구 보관**을 정한 법정
장부다. 청구 항목은 시행규칙이 정한 포함사항 중 「기하구조조서」에 들어간다.
즉 "보유하지 않는다" 가 아니라 "뽑아주기 번거롭다" 에 가깝다.

**그럼에도 이 항목에 일정을 걸지 않는다.** 이유가 셋이다 —

    ① 검증축이 이미 있다 — `ngii1k_center` 의 측량 성과 도로폭.
      우리 기하 계산과 독립이고 n=909 · 절대편차 중앙 0.32m 다
    ② 정본은 D-25 실측이다(#4). 도로대장은 세 번째 축이지 유일한 축이 아니다
    ③ 회신은 빨라야 10일이고, 부존재가 또 올 수 있다

재청구는 **가공 요청을 전부 빼고** 넣었다. 항목을 나열하면 "이 조합을 만들어
달라" 로 읽힌다. "도로법 제56조에 따라 보관 중인 도로대장 사본" 으로 좁히면
같은 사유를 쓸 여지가 없다.

★ 부존재가 또 오면 그것을 발표 논거로 쓴다. 소화전 "654 중 31 공개" 와 같은
자리다 — **소방차 진입 판정을 공공데이터로 하려 할 때 어디서 막히는가**가
이 프로젝트의 문제의식이고, 통지서 두 장이 그 증거다.

### 1-27. 측정 대장 — 재기 전에 적는다 (가드 6)

측정은 강제할 수 없다. 대신 미결 측정마다 셋을 **재기 전에** 적는다 — 무엇을
묻는가 · 무엇이 나오면 어느 쪽으로 판정하는가 · 실측 기록이 봉인될 자리.
판정 기준을 재고 나서 적으면 그것은 판정이 아니라 사후 합리화다(§13-4 가드 6 ·
MASTER §4-1 이 그 사고다).

★ **대장이 §1 에 사는 이유.** 측정은 판정을 움직이므로 6족만 §1 에 산다(§13-2).
§13 은 「판정을 **안** 움직이는 일」의 정본이라 측정 행을 거기 앉히면 그 절이
자기 선언을 어긴다. 그리고 아래 「§1 행」 칸이 가리키는 곳이 바로 위 표다.

| 측정 | §1 행 | 물음 | 판정 기준(재기 전에 적는다) | 봉인 자리 |
|---|---|---|---|---|
| `d25-field` | 4 | 우리 폭(`width_min_m`)과 판정이 실측과 맞는가 — 위험 오판(우리 초록 · 실측 3.0m 미만)이 있는가 | 트랙 A·B 에서 위험 오판 0 이면 판정 유지. 1 이상이면 `tools/field_compare.py` 의 차감 제안을 측정 배치로 적용하고 golden 을 재잠근다. 트랙 C(봉인 표본)는 보정 근거로 안 쓰고 외부 검증에만 쓴다 | data/field/obs_points.csv |
| `r4-ngii-pieces` | 51 | NGII 1:1,000 중심선이 동명동에서 왜 456 조각인가(끊김 · 분리대 쌍선 · 원천 간 어긋남) | 조각 수와 제봉로184번길 세그 수로 1차 판정한다. 병합 뒤 조각이 도로명 구간 수의 1.2배 이하이고 제봉로184번길이 한 줄이면 뼈대 교체 배치로 간다 | data/baseline/(봉인)/r4_pieces.json |
| `parallel-edges` | 51 | 길이비 1.5 를 넘는 병렬 엣지 7건이 실제 두 길인가, 노딩 결함인가 | 정사영상 · NGII 도로면으로 7건을 한 건씩 판정한다. 결함이 4건 이상이면 노딩 규칙을 고치는 측정 배치 | data/baseline/(봉인)/parallel_edges.json |
| `n2-detail-address` | 57 | 상세주소 DB 가 원룸·다가구 목적지를 건물 단위보다 더 정확히 잇는가 | 무작위 50건에서 동·호 단위 좌표가 건물 대표점보다 출입구에 가까운 비율이 60% 이상이면 목적지 색인에 넣는다 | data/field/n2_sample.csv |
| `oneway-disclosure` | — | 동명동 일대 일방통행 지정 구간과 방향(공공데이터포털 표준데이터에 광주 동구분 없음) | 광주경찰청 · 동구청 정보공개 청구(2026-09-22) 회신을 대장에 올리고, `navi_graph` 의 방향 모름(`ow=2`) 구간이 회신 자료로 확정되는 비율을 센다. 50% 미만이면 현장 표지 확인을 D-25 동선에 얹는다 | data/raw 회신 파일(대장 등재 시 경로 확정) |

★ **오염 칸 — `nfa_compare_abs_dev`.** 소방서 대조 절대편차 합(8.31m · 7구간)은
뼈대 · 폭 규칙 적합에 **이미 두 번 썼다**(MASTER §4-1). 적합에 한 번 쓴 지표는
그 순간부터 검증 수단이 아니다(MASTER §17) — R4 성공 조건으로 되쓰지 않는다.
판정 기준 칸에 이 이름이 나오면 `tests/test_measurements.py` 가 운다.

★ **위 표에 행을 늘리지 않는다.** §1 에 이미 있는 측정 행은 「§1 행」으로 잇고,
행이 없는 외부 의존(정보공개 청구 등)은 `—` 로 둔다. `oneway-disclosure` 가
그것이다(DECISIONS §218-6 — PLAN 에 행을 달지 않기로 했다).

강제자  `tests/test_measurements.py` — 칸 다섯이 다 찼는가 · 「§1 행」이 실재하는가 · 오염 지표를 판정 기준에 되쓰지 않는가 · 대장이 비지 않았는가(카나리아)

---

## 2. 아키텍처 · 모듈 경계

→ **§1 로 접었다**(#63 · #64). 2026-09-24 · DECISIONS §244

강제자 없음 — 사유: 묘비 절이다(§0-3)

---

## 3. 폭 산출 · 판정

→ **§1 로 접었다**(#65 · #66 · #67 · #68 · #69 · #70 · #77). 인도 포함 논증은 **MASTER §7-1** 로 갔다.

강제자 없음 — 사유: 묘비 절이다(§0-3)

---

## 4. 그래프 · 경로탐색

→ **§1 로 접었다**(#1 · #2 · #71 · #72 · #73 · #74 · #75 · #76 · #77 · #78). `ROAD_LT` 금지는 **MASTER §3-3** 으로 갔다.

강제자 없음 — 사유: 묘비 절이다(§0-3). 하위 셋이 이 칸을 물려받는다

### 4-4. TMAP · 카카오 — 병합이 아니라 병렬 비교 → §1 #73

### 4-5. 미결정 → §1 #75 · #76

### 4-7. 간선 본선 + 측도 병합 → §1 #77

---

## 5. 비전 파트

→ **§1 로 접었다**(#79 · #80 · #81 · #82 · #83). 캘리브레이션 ≠ 호모그래피 · 오차 공식 · 촬영 체크리스트는 **교과서라 지웠다**.

강제자 없음 — 사유: 묘비 절이다(§0-3). 하위 셋이 이 칸을 물려받는다

### 5-5. 유효폭 계산 → §1 #3 (집계 규칙 정본은 MASTER §19-5)

### 5-6. DL 투입 지점 → §1 #81 · #82 · #83

### 5-7. 상습주차 추정 모델 → §1 #60

---

## 6. 실측

→ **§1 로 접었다**(#84 · #85 · #86 · #87 · #88 · #89). GPS 한계표 · 레이저 실무 팁 · 보드 조달 사양은 **교과서라 지웠다**.

강제자 없음 — 사유: 묘비 절이다(§0-3)

---

## 7. 시나리오 · 평가

→ **§1 로 접었다**(#90 · #91 · #92 · #93 · #94 · #95 · #117 · #120).

강제자 없음 — 사유: 묘비 절이다(§0-3). 하위 셋이 이 칸을 물려받는다

### 7-3. 관측 신선도 → §1 #90

### 7-4. CCTV 사각 구간 대책 → §1 #60

### 7-5. 소화전 → §1 #95

---

## 8. 데이터 관리 · 인프라

→ **§1 로 접었다**(#96 · #97 · #98 · #99 · #100 · #101 · #102 · #103).

강제자 없음 — 사유: 묘비 절이다(§0-3). 하위 넷이 이 칸을 물려받는다

### 8-1. processed 계층 포맷 전환 → §1 #96

### 8-2. 클라우드 적재 → §1 #97

### 8-6. 대장 · 매니페스트 격차 → §1 #101 · #102

### 8-7. 저장소 위생 → §1 #100 · #103

---

## 9. 외부 의존

→ **§1 로 접었다**(#105 ~ #116). D-30 질의 열하나 중 **차량 전폭·전장은 답이 와서 닫혔다**(MASTER §3-13) — 축거·최소회전반경만 #20 에 남았다.

강제자 없음 — 사유: 묘비 절이다(§0-3)

---

## 10. 우선순위

→ **§1 로 접었다.** 기한 두 개는 **#4** 가 든다 — 2026-10-08 MVP 통합(최소 완료선 3골목 12횡단면) · 2026-11-13 결과물. **10-08 에 갈리는 것은 코드가 아니라 현장이다.**

강제자 없음 — 사유: 묘비 절이다(§0-3)

---

## 11. 담당 공백

→ **§1 로 접었다**(#118 · #119). 공백 자체는 각 행의 「담당 없음」이 든다.

강제자 없음 — 사유: 묘비 절이다(§0-3)

---

## 12. 기획서 갱신 대상

`docs/proposal.docx` 는 시제 규칙 밖이지만 **문서 넷 중 유일하게 외부가 읽는
것**이라 어긋나면 비용이 가장 크다. 산출물과 어긋난 부분을 여기 적고, 갱신 후
지운다.

**남은 행 없음**(2026-09-24). 새로 나면 아래 꼴로 적는다 —
`| # | 기획서의 서술 | 바꿀 것 | 상태 |`

★ **숫자가 맞아도 모델이 다르면 더 위험하다.** `docx_check` 는 판정 4종과 구간
수를 대조해 초록불을 내지만, 폭 소스 우선순위나 배경지도·배포 방식이 어긋난
것은 못 본다 — 숫자가 아닌 것은 대조 대상이 아니다(DECISIONS §243-7).

★ **기획서에는 하한과 미검증을 반드시 함께 적는다.** 말하지 않을 것의 목록은
MASTER §15-2 가 정본이다.

강제자  `tools/docx_fix.py` · `tools/docx_check.py` · `tests/test_docx_targets.py`

---

## 13. 결함 대장 — 판정을 안 움직이는 일

2026-09-18 감사가 낸 결함의 정본이다. §1 이 **판정을 움직이는** 일을 담고 이
절이 **안 움직이는** 일을 담는다. 여기 모든 행의 수용 조건은 하나다 —
**판정 산출물이 바이트 동일하게 유지된다.**

강제자 없음 — 사유: 이 절 자체가 대장이다. 「결함이 빠짐없이 등재됐는가」는 강제할 수 없고 §13-4 의 클래스 가드 여섯이 대신한다. 여섯이 전부 섰다(2026-09-23)

### 13-1. 이 절이 생긴 이유

감사가 47건을 냈고 그중 **42건이 이 문서 어디에도 없었다.** §1 은 머리말에
"남은 일의 정본" 이라고 적어놓고 남은 일의 대부분을 모르고 있었다 — **선언이
거짓이었다.** 실측 — `pre-commit`·`hooksPath`·`훅`·`WATCH`·`deptry`·`폰트`·
`gitleaks`·`needs:`·`DISPLAY_` 를 이 문서에서 세면 **전부 0건**이었다.

더 나쁜 것은 그 42건의 유일한 목록이 **저장소 밖**에 있었다는 것이다.
`git clone` 만으로 계획을 가질 수 없으면 그것은 5족(생성물인데 생성기가
없다)이고, **5족을 닫겠다는 계획 자신이 5족이었다.**

★ **이 절은 빚 목록이다**(§0-2). 닫힌 결함은 행이 아니라 부재로 기록된다.
닫힌 배치의 증표는 `tests/test_defect_evidence.py` 의 등록부가 들고, 왜 닫혔는지는
DECISIONS 가 든다. 남은 것이 아래 **9행**이다 — 2026-09-24 전수 감사(DECISIONS §243)가
낸 것 중 **레이크·잠금·네트워크 없이는 못 닫는 것**들이다.

★ **닫힌 건수를 여기 적지 않는다.** 이 절은 행이 닫히기도 하고 **새로 늘기도
한다.** 그래서 닫힌 수는 보존량이 아니다 — 보존되는 것은 지금 남은 행 수뿐이고,
숫자를 하나만 두면 갈릴 데가 없다. 그 하나는 아래 강제자가 센다
(왜 그렇게 정했는지는 DECISIONS §205).

강제자  `tests/test_declaration_sync.py::test_defect_ledger_counts_agree_everywhere`  ★ 2026-09-20. 「수를 세는 강제자가 필요하다」고 적어놓고 안 세웠더니 **하루 만에 또 갈렸다**(30 / 28 / 28). 이제 §13-1 문단 · §13-3 제목 · 실제 표 행 수 셋이 다르면 운다. ★ 2026-09-22 증표 물음도 섰다 — `tests/test_defect_evidence.py` 가 닫힌 ID 마다 그 닫힘을 지키는 시험이 트리에 있는지, DECISIONS 의 `닫힘` 줄과 등록부가 같은지 본다(DECISIONS §217-5)

### 13-2. 족 — 여섯 갈래

| 족 | 묻는 것 | 왜 족으로 묶나 |
|---|---|---|
| 1. 무음 통과 | 가드가 있는데 실행 없이 초록불을 낸다 | 강제자 49개가 전부 **인스턴스 가드**라 같은 형태가 계속 난다 |
| 2. 정본이 둘 | 같은 사실의 주인이 두 곳에 있다 | 한쪽만 고치면 조용히 갈린다 |
| 3. 관문이 갈림 | 로컬과 CI 사이에 검사가 샌다 | 로컬 전용 검사는 사람이 안 돌리면 아무도 모른다 |
| 4. 직접 구현 | 표준 의존성이 할 수 있는 일을 손으로 짠다 | 손 도구는 버그를 혼자 갖는다 |
| 5. 파이프 단절 | 생성물인데 생성기가 없다 | 사람이 다시 못 만든다 |
| 6. 측정 미결 | 세상에 대해 모르는 사실이 남았다 | **판정을 움직인다 — §1 소관이다** |

★ **6족만 §1 에 산다.** 측정은 판정을 움직이므로 수용 조건이 「불변」이
아니라 「예보와 실제의 대조」다. 현재 §1 #4 · #51 · #57 이 그것이다.
  ★ 2026-09-24. `#61` 이 여기 있었다 — **결번이다**(§1 머리의 결번 줄).
  번호는 영구 식별자이고 결번은 다시 안 쓰므로, 이 인용은 영영 빈 곳을
  가리키고 있었다. 대체 행을 **짐작으로 넣지 않는다.**

강제자 없음 — 사유: 분류 어휘다. 족마다의 강제자는 §13-4 표가 따로 든다

### 13-3. 남은 결함 9건

| ID | 결함 | 족 | 상태 | 닫는 법 |
|---|---|---|---|---|
| W13-1 | 미사용 의존성 여덟이 잠금에 남아 있다 — `ruamel-yaml`(core) · `fastapi` · `uvicorn` · `python-multipart` · `opencv-python` · `ultralytics` · `torch` · `torchvision` | 4 | 열림 | 빼고 `uv lock`. 지금은 **잠금을 흔들지 않으려고** `DEP002` 래칫으로 묶어만 뒀다(`pyproject.toml`). 잠금이 움직이면 판정 코드 지문도 같이 움직인다 |
| W13-2 | 대장 `schema` 둘이 실물과 다르다 — `ngii1k.schema.layers: []` · `jijeok.schema` 의 `A0`~`A7` 과 `features: 1000000` | 5 | 열림 | `ledger_schema.py` 를 레이크에서 다시 돌린다. 이 값들은 raw 에서만 나온다 |
| W13-3 | 내비 번들이 **단일 청크 1,386kB**(gzip 392kB) | 4 | 열림 | 동적 `import()` 로 쪼개거나 maplibre 를 별 청크로. **4G 첫 진입 시간이 곧 출동 시각이다** |
| W13-4 | 내비가 오프라인에서 못 뜬다 — service worker · manifest · `404.html` 이 없고 글리프가 외부 CDN(`demotiles.maplibre.org`)이다 | 5 | 열림 | 글리프를 자체 발행으로 옮기고 SW · manifest 를 붙인다. 지금은 지하·산간에서 **지도 글자가 사라진다** |
| W13-5 | React · infra 층에 시험이 없다 — `useNavigation`(552줄) · `gps.ts` · `useVoice` | 1 | 열림 | `deriveStatus` 여덟처럼 **순수 조각부터** 문다. 552줄 훅을 통째로 물려고 하면 안 물게 된다 |
| W13-6 | 액션이 떠 있는 태그(`@v7`)로 고정돼 있고, 작업 넷 중 셋에 `timeout-minutes` 가 없다 | 1 | 열림 | SHA 고정 + 타임아웃. 태그가 옮겨지면 **조용히 다른 코드가 돈다** |
| W13-7 | `data/field` 에 무결성 지문이 없다 | 5 | 열림 | 재취득 불가인데 「그날의 그 파일인가」를 아무도 안 묻는다. `processed` 는 `_manifest.json` 이, 판정은 `segments.fingerprint.json` 이 든다 |
| W13-8 | 소비자 0 인 산출물 넷 — `jijeok` · `jijeok_5186` · `streetlight_point` · `field_sample` | 5 | 열림 | **배선인가 철거인가**를 정한다. 지금은 발행만 되고 아무도 안 읽는다(DECISIONS §243-2) |
| W13-9 | 전량 재실행 시간에 실측 정본이 없다 — 285초 · 2분45초 · 2분40초 · 5분18초 넷이 돌아다녔다 | 2 | 열림 | 한 기계에서 재고 정본을 하나 정한다. 문서는 2분45초로 모아 뒀다(DECISIONS §243-1) | <!--stale-ok-->

★ **비었다고 이 절을 지우지 않는다.** 결함은 다시 자란다 — 표가 비어 있는 것이
「지금 빚이 없다」는 선언이고, 새 결함은 여기 행으로 돌아온다. 표 머리를 남겨
두는 이유가 그것이다. 머리가 없으면
`test_defect_ledger_counts_agree_everywhere` 가 「표를 못 찾았다」와
「행이 0이다」를 구별하지 못한다.

강제자 없음 — 사유: 빚 목록이다. 행이 지워졌는지는 배치의 수용 조건이 보고, 새 결함의 등재 누락은 §13-4 의 가드가 본다

### 13-4. 클래스 가드 — 이 절이 닫히는 조건

**배치가 인스턴스를 지우고 가드가 족을 닫는다.** 가드 없이 인스턴스만 지우면
반년 뒤 같은 47건이 다시 자란다 — 강제자 49개가 그 증거다.

| 가드 | 족 | 묻는 것 | 강제자 | 상태 |
|---|---|---|---|---|
| 1. 증표 계수 | 1 | 실행된 단계 수 = 선언된 단계 수인가 · 닫혔다고 적힌 배치의 증표가 트리에 있는가 | `verify.sh` 끝의 `evidence_check` · `tests/test_verify_evidence.py` · `tests/test_defect_evidence.py` | **섰다**(2026-09-22) |
| 2. 정본 단일화 | 2 | 이 사실이 정본 밖에서 반복되는가 | `MASTER §17-1`(사람이 읽는 표) · `tests/test_sources_of_truth.py`(기계가 읽는 `SPEC` · 양방향 대조 · **열 사실 전부 `scan`**) · `tests/test_verify_citations.py` | **섰다**(2026-09-23) — 목록 밖 반복을 열 사실 **전부**에서 본다. 주석·docstring 은 빼고(`code_only`), 맨숫자는 문맥 낱말과 같은 줄일 때만 센다(`near`) |
| 3. 관문 동등 | 3 | `verify.sh` ↔ CI **미선언** 차집합이 있는가 | `tools/gate_parity.py` · `tests/test_gate_parity_args.py` | **섰다** — 미선언 0(2026-09-22 · 11 → 0), 인자까지 대조 |
| 4. 의존성 회수 | 4 | 선언해놓고 안 쓰는 의존성이 있는가 | `deptry`(잠금 밖 `--with`) · `tests/test_deptry_config.py` | **섰다** — 알려진 예외 여덟은 사유와 함께 래칫 |
| 5. 생성물 등재 | 5 | 모든 생성물이 생성기·`--check`·소비자를 갖는가 | `firelane.generated.FAMILIES` · `tests/test_generated_families.py` | **섰다** — `data/baseline` 만 재현 검사가 성립하지 않는다(봉인이라 다시 못 만든다) |
| 6. 측정 대장 | 6 | 물음·판정 기준·봉인 로그를 재기 전에 갖는가 | `§1-27` 측정 대장 · `tests/test_measurements.py` | **섰다**(대장이다 · 오염 칸 포함) |

★ **여섯이 전부 섰다**(2026-09-23). 가드 2 가 2026-09-22 부터 「일부만 섰다」였다 — 이름
(「정본 밖에서 반복되는가」)이 약속하는 범위가 실제 검사보다 넓었고, 그것을 **적어 두는 것**이
이 절의 규율이었다(W3-8 · W4-8 · W3-16 · W4-9 가 전부 같은 병 — 범위가 이름보다 좁고 그것이
선언돼 있지 않은 것). 닫으면서 막았던 것 둘을 풀었다 —

    주석 오탐   `code_only` — `web/navi/src/domain/vehicle.ts` 는 「3.0 을 여기서 재선언하지
                않는다」고 **주석으로** 적는다. 옳은 코드인데 스캔이 사본으로 읽었다
    맨숫자 오탐 `near` — `3.0` 은 버퍼 거리 · 허용오차로 저장소 곳곳에 있다. 문맥 낱말
                (`TRUCK` · `필요 폭` · `requiredWidth` …)과 같은 줄일 때만 이 사실의 사본으로 센다

잘못된 경보는 진짜 경보를 죽인다(MASTER §18-13) — 그래서 「전부 켜기」가 아니라 **오탐을 먼저
없애고 켰다.** 켜자마자 진짜 사본 넷이 나왔다(DECISIONS §222-5).

강제자  위 표의 강제자 칸

### 13-5. 규약

1. **새 결함은 먼저 「어느 족인가」를 묻는다.** 족이 있으면 그 족의 가드를
   고친다. 강제자를 하나 더 만들어 덮지 않는다 — 49개가 50개 되는 것으로는
   아무것도 안 닫힌다.
2. **배선 배치에 측정을 섞지 않는다.** 판정 숫자를 바꾸는 변경이 하나라도
   들어가면 그 배치는 측정 배치이고, 수용 조건이 불변에서 대조로 바뀐다.
   섞으면 교란을 사후에 못 가른다(DECISIONS §189-3 이 그 사고다).
3. **배선 배치는 합친다.** 수용 조건이 하나(불변)라 교란되지 않는다. 다섯을
   같이 넣고 하나라도 지표가 움직이면 배치 전체가 실패하고, 그 뒤 이분
   탐색이 범인을 찾는다 — 다섯 개면 세 번이다. `golden.py lock` 재잠금도
   N 번에서 1 번으로 준다.
4. **닫히면 행을 지운다.** 표식을 달지 않는다(§0-2). 배치 완료의 증거는
   `git diff docs/PLAN.md` 이고, 그것이 없으면 그 배치는 안 끝난 것이다.
5. **`RED.txt` 로 넘기지 않는다.** 미결 측정은 §1 에, 배선 결함은 이 절에
   적는다. 남은 줄이 다음에 진짜 빨강을 덮는다.
6. **배선 배치의 수용 조건은 「로컬 verify 초록 + CI 초록」 둘이다.**
   ★ 2026-09-18 에 CI 가 네 번 빨개졌고 **넷 다 로컬 45/45 초록에서 안 보였다** —
   `refcheck`(레이크 필요) · `doc_fsck ⑥`(git 히스토리 필요) · 래칫 숫자 두 곳
   (W3-11) · `pr_body_check`(사람이 쓴 PR 전제, W3-12). 근본 원인은 하나다 —
   배치를 `batch-w1-deps` 에서 작업했고 **그 이름이 `contract.yml` 의 트리거
   (`main` · `dev` · `part/**` · `feat/**`) 밖이었다.** 하루 종일 `secret-scan`
   만 돌았고 그것을 「CI 가 돈다」로 읽었다. **배치 브랜치는 반드시 `feat/**` 다.**
   개인 브랜치에서 로컬만 초록인 것은 수용이 아니다.

강제자  §13-4 표의 강제자 칸 — 가드 여섯이 전부 섰다(2026-09-23)
