# Fire-Lane — 실행 계획

> **앞으로 할 일.** 완료된 것은 여기서 빼고 `docs/MASTER.md` 로 옮긴다.
> 그래야 이 문서가 항상 "남은 일" 만 담는다.
>
> ### 문서 셋은 병렬 축이 아니라 한 항목의 생애주기다
>
> ```
> PLAN(미래)  →  도래  →  MASTER(현재)  →  회고  →  DECISIONS(과거)
> ```
>
> | 문서 | 시제 | 담는 것 |
> |---|---|---|
> | `docs/PLAN.md` | 미래 | 남은 일 · 미결정 · 담당 공백 |
> | `docs/MASTER.md` | 현재 | 지금 무엇이 어떤 값인지 · 데이터 · 운영 |
> | `docs/DECISIONS.md` | 과거 | 왜 그렇게 됐나 (append-only) |
> | `docs/기획서_Fire-Lane.docx` | — | 대외 제출용 기획서 |
>
> **한 항목은 한 문서에만 산다.** 두 곳에 있으면 한쪽만 고치는 날이 온다.
> 남은 일의 정본은 §1 하나다.
>
> 2026-08-12 재편. 이전 `PROJECT.md` 의 검증 완료분(✅ 15건)은 MASTER.md 로 옮기고
> 미구현분만 남겼다.
>
> **2026-08-13 갱신.** §4-2 폭 산출은 구현 완료로 MASTER §3-9~3-11 · §16 으로
> 옮겼다. 여기 남은 것은 그 위에서 새로 생긴 미결 항목이다.

## 상태 표기

| 표기 | 뜻 |
|---|---|
| 📄 | 결정은 됐으나 코드/실측으로 확인 안 됨 |
| 🟡 | 미결정. 논의 필요 |
| 🔴 | 담당자 없음. 공백 |
| ⏳ | 대기 중 (외부 의존) |

**완료되면 이 문서에서 지우고 MASTER.md 에 결과를 쓴다.**

---

# 1. 남은 일

MASTER §7 에 있던 목록을 2026-08-18 에 여기로 옮겼다. 남은 일의 정본은
이 절 하나다 — MASTER 는 "현재 무엇이 어떤 값인가" 만 적는다.

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| 1 | `node_link` + `turn_restriction` 투입 | 🟡 | A* 필수. 데이터는 이미 보유(엣지 3427 · 노드 2837 · 회전제한 87행) |
| 2 | A* 비용함수 설계 | 🟡 | 거리 + 폭 + 통행제한 ∞ + 경사. §5-3 · §5-7 선행 |
| 3 | 영상판정(호모그래피) 모듈 연결 | ⏳ | `needs_cv` 190구간. 영상 담당 |
| 4 | D-25 레이저 실측 | 🔴 | `width_verified: false → true`. 폭 검증 전건이 걸려 있다 |
| 5 | D-30 소방서 인터뷰 | ⏳ | 소화전 좌표 · 차량 제원 · 소방통로 구간. **확답 없음** |
| 6 | 5m DEM 신청 | 🟡 | 경사 보정. `length_m` 은 지금 수평거리다 |
| 7 | 정보공개청구 도로대장 17337943 | ⏳ | 8/25경 회신. 필문대로289번길 −3.51 검증용 |
| 8 | 건물 형상 고도화 | 🟡 | 수치지형도 1:1,000 건물 레이어(`B00*`) 보유 여부 확인부터 |
| 9 | D-29 평가지표 | 🟡 | 설계 §8-2 완료(08-18). E-1·E-2 산출 미착수 |
| 11 | ortho rasterio UnicodeDecodeError 스팸 | 🟢 | 무해. 원본 TIF 메타의 cp949 를 GDAL 로그 핸들러가 utf-8 로 읽는다. 타일 정상 |
| 12 | 정사영상 미터 단위 정합 미검증 | 🟡 | .xml·TIF 에 경계좌표 없음. 도엽 bbox(5179→5186)+사방 균등 pad 가정. 조대 정합만 확인(중심선 94.5 vs 옆 110.0) |
| 13 | ingest 가 gpkg 를 덮어쓰지 않는다 | 🔴 | 옛 레이어가 남아 하류가 옛 데이터를 읽는다. 08-18 미커버 13.4% 원인 |
| 14 | web/data/ortho z19 1,423장 커밋 | 🟡 | 29MB. CI 상한 60MB. 생성물인데 저장소에 있다 |
| 10 | `width_max_m` 결손 496건 | 🟡 | 96건은 08-18 ROAD_BT 확대로 해소. 남은 64건은 실측·건물형상(#8) 대기 |

> ### ⚠ 5번에 1·2번을 걸지 마라
>
> 선호도 기반 피처는 인터뷰가 있어야 **가중치**를 정할 수 있다. 그러나
> **구조**는 인터뷰 없이 정해진다 — 거리 · 폭 · 통행제한 · 내륜차는 전부
> 보유 데이터로 된다. 인터뷰를 전제로 설계하면 무산될 때 A* 전체가 뜬다.
> 인터뷰는 "있으면 좋은 것"이지 "없으면 못 하는 것"이 되면 안 된다.

---

# 2. 아키텍처 · 모듈 경계
## 2-1. GIS ↔ 비전 — 접점은 정확히 두 개 📄

```
GIS ──[관측점 선정]──▶ 비전      어느 골목의 어느 지점을 찍을지 GIS가 정한다
GIS ◀──[판정 결과]──── 비전      seg_id + 통행폭 + 시각
```

계약 스키마:

```json
{
  "seg_id": "seg_2157",
  "passable_width_m": 2.8,
  "observed_at": "2026-08-09T14:03:00+09:00",
  "confidence": 0.82
}
```

**반대 방향이 없다는 것이 핵심이다.** GIS 도로폭은 호모그래피 입력으로 들어가지 않는다.
이 오해가 지적도 캘리브레이션 3주를 태웠다(§6-1).
## 2-2. 정적 계층 vs 동적 계층 📄

| 계층 | 갱신 주기 | 내용 |
|---|---|---|
| 정적 | 월 1회 | 도형, `width_min_m`, `verdict`, `cctv_dist_m` |
| 동적 | 수 초~분 | `current_status` 컬럼 **하나만** |

"동적"은 DB 엔진 성질이 아니라 **클라이언트 폴링 루프 + 얇은 상태 컬럼**에서 나온다.
무거운 공간조인은 정적 계층에서만 돌린다.
## 2-3. 서빙 원칙 📄

FE든 QGIS든 **`serving` 스키마 하나만 보고 GeoJSON을 받는다.**
판정 로직을 프론트에서 재계산하게 하면 안 된다. 로직은 백엔드에 하나, 소비자는 여럿.

---
## 4-2. 폭 산출 방식 — **구현 완료 (2026-08-13)**

상세는 MASTER §3-9 ~ §3-11 · §16. 여기에는 결론과 남은 것만 둔다.

```
소스 우선순위   1:1,000 도로경계 → 1:5,000 → 실폭도로   (결정 63)
채택 단위       구간. 표본마다 다른 소스를 섞지 않는다   (결정 69)
교차로 제외     노드 반경 5m 표본 폐기
교차로 파편     동일선상 30° 이웃과 산출단위로 병합      (결정 66)
분위수          쓰지 않는다. 표본 최솟값
폭 미산출       0
```

**하위 5% 분위수는 폐기했다.** 측정 결함을 분포로 가리는 방향이라
측정 자체를 고치는 쪽으로 갔다.

**교차로 blob 은 해소됐다.** 다만 원인 진단이 문서와 달랐다 — 최솟값이냐
분위수냐의 문제가 아니라, 교차로 안의 점에서는 그 도로의 폭이 **기하학적으로
정의되지 않는다**는 것이었다(법선이 교차 도로를 따라 달린다).

### 남은 것 🟡

```
width_cov 0.5 미만 69구간   채택 소스가 구간의 절반도 못 덮었다. D-25 실측 1순위
wmax 교차로 누출            담~담이 46~57m 로 샌다. width_max_m 필드 신뢰 불가
소방서 대조 잔여 편차        중앙로272번길 -1.50 · 동계로9번길 +2.97
road_side 의미               RDS_DPN_SE 0/1/2. 측도 구분이 아님만 확인됨
```

### ⚠ 계산 방식 검증 규칙 — 유효

폭 계산은 **반드시 두 가지 방법(법선 트랜섹트 / 거리변환)으로 하고 불일치 구간을 먼저 본다.**
단일 방법 사용 시 인접 폴리곤 경계에서 교차선이 분할돼 **210개 구간의 폭이 절반 이하로 계산된 사고**가 있었고, 현장 답사에서야 발견됐다.

> 2026-08-13 현재 거리변환 교차검증은 **미구현**이다. 대신 소방서 지정 7구간
> 대조를 외부 게이트로 쓰고 있다(MASTER §4). 정사영상 25cm 388타일을
> 활용한 전수 검증이 다음 후보다.
## 4-4. 판정 오류의 비대칭 ★ 📄

| 오류 | 결과 | 비용 |
|---|---|---|
| **미탐** (막혔는데 통과 가능 판정) | 소방차가 골목에 갇힌다 | **치명적** |
| **오탐** (통과 가능한데 막혔다고 판정) | 조금 돌아간다 | 경미 |

**두 오류의 비용이 100배 차이다. 임계값을 대칭으로 두면 안 된다.**

```
안전 마진 = 차폭 2.5m + 여유 0.5m = 3.0m   (TRUCK)
clear 기준 = 3.0 + 주차 2.0 × 양쪽 = 7.0m
```

> 문서 초안의 여유 0.3m 는 0.5m 로 확정됐다(2026-08-06 답사).
> `clear` 도 한쪽 주차가 아니라 **양쪽 주차**를 가정한다 — 양방 주차가
> 상시인 골목에서 한쪽만 보는 것은 낙관 방향이다.

**발표 방어 논리로 최강이다** — "우리는 틀릴 수 있다. 그래서 틀리는 방향을 설계했다."

⏳ 여유값 0.3m은 실운전자 인터뷰로 검증 필요(§9-2).
## 4-6. 인도 포함 문제 📄

수치지도 도로경계면은 포장면 기준이라 담장 안쪽이 안 들어간다.
동명동 도로 대부분이 좁아 인도가 물리적으로 존재할 수 없고, 인도가 있을 수 있는 5~8m 구간은 인도 2m를 잘못 포함해도 잔여 폭이 통과선을 넘어 **판정이 뒤집히지 않는다.**

---
# 5. 그래프 · 경로탐색
## 5-3. 가중치 설계 📄

**곱셈형으로 확정.**

```
cost = length_m × (1 + α·판정페널티 + β·점유위험도)
```

덧셈이면 짧은 위험구간과 긴 안전구간의 비교가 왜곡된다.

| 판정 | 페널티 |
|---|---|
| RED | ∞ |
| ORANGE / YELLOW | 실측 기반 |
| GRAY | 0.5 (불확실) |
| BLUE / GREEN | 0 |

점유위험도 = 단속밀도(건/100m) 정규화값.

### ⚠ 길이는 지오메트리에서 재계산할 것

`ROAD_LT`(도로 연장)는 **클리핑 전 원본 도로 전체 길이**를 갖고 있다.
필문대로 25m 조각에 4,200m가 기재되는 식이다. **엣지 가중치에 절대 쓰지 마라.**
## 5-4. A* vs 다익스트라 📄

**둘 다 한다. 비용이 0이다.**

```python
nx.dijkstra_path(G, s, t, weight="cost")
nx.astar_path(G, s, t, heuristic=haversine, weight="cost")
```

이 규모에서 성능 차이는 무의미하다. **A*를 쓰는 이유는 속도가 아니라 발표 논거다** — "광주 전역 확장 시 탐색 공간이 커지므로 휴리스틱이 필요하다". 실행시간 비교 그래프 한 장이면 된다.

⚠ **휴리스틱이 실제 비용을 넘으면 최적해가 깨진다.** cost에 페널티 곱셈이 들어가므로 직선거리를 그대로 쓰면 안 되고 **최소 배율로 나눠야** 한다. 여기 틀리면 A*가 다익스트라와 다른 답을 낸다.
## 5-5. 체크포인트를 엣지 속성으로 📄

소화전·CCTV·단속밀도를 **엣지 속성**으로 붙인다. 노드가 아니다.

```
edge: seg_2157
  length_m, width_min_m, width_src, status, confidence,
  cctv_ids[], hydrant_dist_m, enforce_per_100m, turn_ok
```

경로 산출 후 "이 경로상 소화전 3개 경유"를 바로 뽑을 수 있다. 시연에서 강하다.
## 5-6. TMAP/카카오 — 병합이 아니라 병렬 비교 📄

```
1. 같은 출발-도착지로 TMAP API 호출  → "TMAP 추천 경로" (교통체증만 고려)
2. 우리 다익스트라 호출              → "우리 추천 경로" (통과 가능성 반영)
3. 두 결과를 나란히 제시
```

TMAP API는 링크별 혼잡도를 낱개 조회하는 구조가 아니라 완성된 경로를 반환하는 구조라, 우리 그래프의 모든 간선에 혼잡도를 채우는 방식이 API 구조상 성립하지 않는다.

포지셔닝과도 맞는다 — 교통체증 최적화는 TMAP이 이미 최고 수준이고, **우리 차별점은 "TMAP이 모르는 걸 우리는 안다" 하나에 집중한다.**
## 5-7. 미결정 🟡

| 항목 | 선택지 | 비고 |
|---|---|---|
| **그래프 방향성** | 무향 + "긴급차량 예외" 명시 ← 추천 / 유향 + 역주행 페널티 | **결정 안 하면 코드가 두 번 갈아엎힌다** |
| **회전 판정 적용 범위** | 시연 경로 코너만 검증 ← 추천 / 전 구간 | 전 구간 적용 시 골목 대부분 탈락 → 그래프 붕괴 |
| 클리핑 범위 확장 | 소방서~동명동 진입 경로 포함 | 미착수 |

### 회전 반경 기준 📄

**폭 3m 골목이라도 직각 코너면 소방차는 못 돈다.** 내륜차(off-tracking) 때문이다.

| 상황 | 기준 |
|---|---|
| 직진 | 유효 폭 3m 이상 |
| **회전(코너)** | 회전반경 차량 중심 10m + 곡선부 폭 4~6m |
| 진입 불가 | 폭 2m 이하 |

**설계 원칙**: `간선 = 직진 폭 판정 / 노드 = 회전 가능 판정. 둘 다 통과해야 유효 경로.`

⚠ 출처가 전부 블로그·기술사 정리다. 「소방청 건축위원회 표준 가이드라인 2021.10」 원문 확보 필요(§9-2).

발표 문구: "회전 제약을 모델에 포함했고 시연 경로는 검증했다. 전 구간 적용은 코너 실측 데이터 확보 후."

---
# 6. 비전 파트
## 6-2. 캘리브레이션 ≠ 호모그래피 📄

| | 캘리브레이션 | 호모그래피 |
|---|---|---|
| 대상 | 카메라 내부(렌즈) | 카메라와 지면의 관계 |
| 구하는 것 | 초점거리, 주점, 왜곡계수 | 3×3 행렬 H |
| 방식 | 체커보드 20여 장 | 대응점 4쌍 이상 |
| 주기 | 카메라·렌즈당 1회 | **카메라를 옮길 때마다** |

**순서: 렌즈 왜곡 보정 → 대응점 → 호모그래피.** 왜곡을 먼저 펴지 않으면 H를 잘 구해도 화면 가장자리에서 틀어진다.

```python
undistorted = cv2.undistort(img, mtx, dist)
H, mask = cv2.findHomography(src_px, dst_meter, cv2.RANSAC, 5.0)
pts_m = cv2.perspectiveTransform(pts_px.reshape(-1,1,2), H)
```

**대응점은 4쌍이 최소조건이지 권장값이 아니다.** 실무는 6~8쌍 + RANSAC.
## 6-3. 대응점을 지면에 고정한다 ★ 📄

> **폐기된 1차안**: "CCTV는 고정이므로 카메라당 평생 1회 클릭."
> **폐기 사유**: 우리는 CCTV가 아니라 **CCTV 모사**다. 삼각대를 매번 들고 나가므로 카메라는 전혀 고정이 아니다.

**카메라 위치·높이·각도를 재현하려는 시도를 포기한다.**

```
관측점당 1회 (영구)   지면 영구 지물 4~6개의 실세계 상대좌표 실측
매 세션              같은 지물의 이미지 좌표만 다시 얻음 → H 재계산
```

기준 지물 후보: 맨홀 뚜껑 모서리, 보도블록 줄눈 교차점, 전주 밑동, 연석 이음매, 배수구 격자.

**카메라가 어디 서 있든 같은 지면 점들이 프레임에 보이기만 하면 H가 매번 정확히 나온다.**
"매번 동일 위치에서 찍어야 한다"는 제약이 통째로 사라진다.

| 세션 | 방법 |
|---|---|
| 1회차 | 수동 클릭 + 지물 실측 |
| 2회차~ | 1회차 기준 이미지와 특징점 매칭(ORB/SIFT + RANSAC) → 자동 정합 |

**실제 CCTV 배포에서도 쓸모가 있다** — 카메라 흔들림·재설치 시 자동 복구. 발표 논거로 강하다.

이 결정이 §7-2 관측점 개체화를 강제한다.
## 6-4. 오차 구조 ★ 📄

초점거리 f(px), 카메라 높이 h, 대상 거리 d일 때 1픽셀 오차의 실제 크기:

```
깊이 방향   d² / (f · h)      ← 거리의 제곱. 급격히 나빠짐
가로 방향   d / f             ← 거리에 비례. 완만함
```

f=1000px, h=1.5m 기준:

| 거리 | 깊이 1px | 가로 1px |
|---|---|---|
| 10 m | 0.067 m | 0.010 m |
| 20 m | 0.27 m | 0.020 m |
| 30 m | 0.60 m | 0.030 m |

> **골목을 따라 정면으로 촬영할 것. 옆에서 가로지르며 찍지 말 것.**

정면이면 도로 폭이 **가로 방향**이 되어 15m 앞 차량을 5px 틀려도 오차가 0.08m다. 옆에서 찍으면 같은 5px이 1m 이상 벌어진다. 분모에 h가 있으므로 **카메라를 높일수록 깊이 오차가 줄어든다.**

### 오차 요인 순위

| 요인 | 영향 | 대응 |
|---|---|---|
| 대응점 실측 오차 | 최대 | 레이저 거리계 |
| 대응점 배치 | 큼 | 화면 넓게 분산. 한 줄 배열 금지 |
| 지면 평탄 가정 위반 | 큼 | 경사 골목 회피. 관측점 선정 시 고려 |
| 렌즈 왜곡 미보정 | 중 | 체커보드 선행 |
| 카메라 높이 오차 | 중 | 삼각대 고정이면 설정값이라 작음 |
## 6-5. 촬영 체크리스트 📄

| # | 항목 | 이유 |
|---|---|---|
| 1 | 줌 금지, 1배 고정 | 초점거리가 바뀌면 캘리브레이션 무효 |
| 2 | 광각(0.5배) 금지 | 왜곡이 커 가장자리 크게 틀어짐 |
| 3 | 렌즈 왜곡 보정 먼저 | 체커보드 20여 장, 카메라당 1회 |
| 4 | 대응점을 넓게 분산 | 몰리거나 일직선이면 H 불안정 |
| 5 | 4점보다 많이 + RANSAC | 이상치 자동 제거 |
| 6 | 근경~중경에서 측정 | 원경은 d² 때문에 오차 폭발 |
| 7 | 삼각대 필수 | 조금만 움직여도 H 무효 |
| 8 | 평평한 구간 선택 | H는 평면 가정 |
| 9 | 줄자를 지면에 펴놓고 함께 촬영 | 사후 검증용 정답지 |
| 10 | **사진 메신저 전송 금지** | EXIF 소실·리사이즈 → 초점거리 증발 |
## 6-6. 유효폭 계산 📄

**뺄셈 방식("GIS 도로폭 − 감지 차폭") 미채택.** 오차가 두 군데(GIS 서류값 + 차폭 감지)에서 이중 누적된다.
대신 **호모그래피 변환 좌표계 위에서 세그멘테이션 결과를 직접 측정**한다.

- YOLO11-seg (`yolo11n-seg.pt`). COCO `{2:car, 3:motorcycle, 5:bus, 7:truck}`
- **세그멘테이션 필수.** bbox는 비스듬한 차량에서 폭을 크게 과대추정한다
- **바닥 접지선 기준** — 마스크 최하단 행의 좌/우 끝. 타이어 접지점에 가장 가깝다
- 한계: 사이드미러 등 뜬 부분은 못 잡는다. §4-4 안전마진이 흡수하도록 설계
## 6-8. DL 투입 지점 — AI 사관학교 방어선 ★

YOLO11-seg 하나로는 "가져다 쓴 것"이라 주인공이 못 된다.

| 우선 | 항목 | 억지 없음 정도 |
|---|---|---|
| 1 | **프라이버시 파이프라인** (번호판·얼굴 마스킹) | 없으면 프로젝트가 법적으로 성립 안 함 |
| 2 | **주정차 vs 주행 판별** (시간축) | 아키텍처가 이걸 요구함 |
| 3 | **야간 강건성** (저조도 향상/도메인 적응) | 실제 문제에서 출발 |
| 4 | 소방관 선호 경로 학습 | 인터뷰 성공이 선행 조건. 리스크 큼 |

### 프라이버시는 엣지에서 ★

**원본이 서버로 넘어가는 순간 개인정보를 "수집"한 것이 된다.**
엣지에서 마스킹 후 전송하면 서버는 애초에 개인정보를 보유하지 않는다.
**발표에서 이 논리 하나로 개인정보 질문을 다 막을 수 있다.**

### 주정차 판별은 시간축으로

**가상 중앙선은 버려라.** 골목엔 중앙선 개념이 없다. 나쁜 휴리스틱이다.
정답은 **연속 프레임에서 정지 = 주차**다.

이것이 아키텍처를 강제한다 — **mock 서버가 단일 이미지가 아니라 프레임 시퀀스를 반환해야 한다.**

```
관측점에서 삼각대 고정 → 30초~1분 동영상 1개
→ 프레임 추출 + 타임스탬프 → mock 서버가 순서대로 반환
```

주행 차량은 5초면 화면을 벗어난다. 30초면 충분하다.
**"24시간 연속 관측 불가"와 "시퀀스 확보 불가"를 혼동하지 마라.** 전자만 사실이다.

보조 신호: 도로 축과의 각도(주차는 대개 평행), 연석과의 거리, 시동/제동등.

### 답사 1회로 셋을 해결한다 ★

**같은 관측점을 오전/오후/야간 3회 촬영한다.**

- "14시 통과 가능 / 19시 불가" → 서비스 필요성 최강 소재
- 관측 신선도(§8-3) 근거 데이터 동시 확보
- 야간 성능 데이터 동시 확보

---
# 7. 실측
## 7-1. 실측 프로토콜 📄

**8/6 실측은 GPS를 기록하지 않아 대조에 쓸 수 없다.** 세그먼트 중심선과 최대 23m 어긋났고, 그날 산출된 소스별 오차 수치는 전부 무효다. 같은 실수를 반복하지 않기 위한 필수 항목:

```
1. seg_id + 관측점 ID
2. ★ 기준 지물 서술 — "서쪽 끝에서 세 번째 맨홀 중심"
3. GPS 좌표 (보조. 지물이 우선)
4. 기준 사진 (기준 지물이 프레임에 보이게)
5. 측정 종류: 담~담 / 연석~연석 / 통행폭 / 코너폭
6. 시각 · 날씨 · 조도
```

### ⚠ GPS로는 지점 특정이 안 된다

도심 골목 폰 GPS 오차 **5~15m**, 골목 폭 **3m**.

| GPS로 되는 것 | 안 되는 것 |
|---|---|
| 어느 세그먼트 위인가 | 세그먼트 내 어느 지점인가 |

**지점 특정의 해법은 지물 기준이다.** 기준 지물을 적고 그것이 프레임에 보이게 찍으면 재방문 시 재현된다.

### 장비 📄

- **레이저 거리계** (20~30m급, 5만 원 내외) — ⏳ 미구매. **없으면 §7-1이 막힌다**
- 30m 줄자 (백업)
- 반사 대상 없을 때: A4 클립보드/흰 폼보드를 팀원이 들고 선다
- **차량 간 거리는 차체가 아니라 지면 마킹 간 거리를 잰다.** 곡면이라 레이저가 튄다

> 줄자 U자 처짐 오차는 5m 구간에서 수십 cm다. **안전마진(0.5m)보다 오차가 크면 실측이 무의미하다.**
## 7-2. 관측점을 GIS 1급 개체로 📄

§6-3 재설계가 이걸 강제한다. 지금 설계에 **"우리가 카메라를 세우는 지점"이라는 실체가 없다.**

```json
{
  "obs_id": "OBS-001",
  "seg_id": "seg_2157",
  "geom": [x, y],
  "landmark_desc": "장동로 서쪽 끝에서 세 번째 맨홀",
  "ref_points": [
    {"id": "R1", "desc": "맨홀 북동 모서리", "local_xy": [0.0, 0.0]},
    {"id": "R2", "desc": "보도블록 줄눈 교차", "local_xy": [2.4, 0.0]}
  ],
  "ref_image": "obs001_ref_20260810.jpg",
  "sessions": [
    {"at": "2026-08-10T09:30", "video": "...", "H": [[]], "illum": "day"},
    {"at": "2026-08-10T19:40", "video": "...", "H": [[]], "illum": "night"}
  ]
}
```

따라오는 것: 세션 간 H 재계산 / 시간대별 통행폭 비교 / **관측 커버리지 레이어**(GRAY 구간 중 어디를 실제로 덮었는지) / 발표에서 정량 제시.

**관측점으로 CCTV 좌표를 그대로 쓰지 마라.** 시나리오 경로 위에서 골라야 한다.

### 실측 지점 선정 — 데이터로 뽑는다 (2026-08-13)

손으로 고르지 않는다. 세 기준의 상위를 교집합으로 잡는다.

```
width_cov          채택 소스가 구간을 덮은 비율. 0.5 미만 69구간
width_disagree_m   소스 간 최대−최소. 도면이 서로 크게 엇갈리는 곳
소방서 대조 편차    중앙로272번길 -1.50 · 동계로9번길 +2.97 · 제봉로184번길 +1.18
```

`width_cov` 가 낮다는 것은 **그 소스가 못 잰 구간이 있는 채로 판정했다**는
뜻이다. 표본 축 소스 혼합을 없앤 대가이며(MASTER §3-11), 혼합할 때는 다른
소스 값에 섞여 보이지 않던 것이다. 숨기지 않고 필드로 드러냈으니 실측
우선순위로 쓴다.

**선정은 코드로 한다.** 손으로 고르면 근거가 안 남는다.
## 7-3. 실측 보조 앱 📄

**네이티브 앱 금지.** 기존 지도 HTML에 얹는다.

| # | 기능 |
|---|---|
| 1 | 현재 GPS + 최근접 세그먼트 자동 판별 |
| 2 | 관측점 등록 → 좌표·시각·seg_id 자동 기록 |
| 3 | 사진 첨부 + **기준 지물 서술 입력(필수 필드)** |
| 4 | 측정값 입력 |
| 5 | JSON 내보내기 |

**전제**: HTTPS가 아니면 `navigator.geolocation`이 동작하지 않는다. Netlify 배포 선행.

**설계 원칙**: GPS는 보조다. 가장 중요한 입력은 **기준 지물 서술과 기준 사진**이다.
## 7-5. 호모그래피 기준점 실측 — ArUco 보드 🟡

**배포될 방법은 마커를 쓰지 않는다.** 세그멘테이션이 검출한 도로 경계선을
수치지형도 도로경계 폴리곤에 맞춰 H 를 푼다. CCTV 104지점 312대에 보드를
깔 수는 없으므로 처음부터 이 방법이어야 한다.

문제는 **그 방법의 오차를 아무도 모른다**는 것이다. 지도 기준점으로 H 를
풀고 그 H 로 잰 폭을 지도 폭과 비교하면 순환논증이다. MASTER §4 가 소방서
7구간에서 이미 겪은 병이다 — 게이트로 쓴 자료는 검증 수단이 아니다.

**ArUco 보드는 지도 밖에서 온 자다.** 3~5곳에서 한 번 재고 끝낸다.
"모든 골목에 마커를 깔아야 하지 않느냐"는 이 구조에서 성립하지 않는다.

### 세 방법을 같은 골목에서 비교한다

```
방법 A   지도 기준점으로 H        ← 배포될 방법
방법 B   ArUco 보드로 H           ← 독립 기준
방법 C   레이저·줄자 실측          ← 절대 기준

|A − C|  배포 방법의 절대 오차     ← 발표에서 말할 숫자
|A − B|  지도 기준점 자체의 오차
|B − C|  보드 방식의 오차. B 가 A 보다 크면 대조 자체가 무의미하다
```

D-25 와 같은 현장·같은 동작이다. **두 번 나가지 않는다.**

### ⚠ 눕힌 마커는 10m 를 못 넘는다

노면에 눕힌 마커는 얕은 시선각에서 깊이 방향이 짓눌린다.

```
h=1.5m, d=15m → 시선-지면각 5.7° → 깊이 축소율 0.10
150mm 마커 = 가로 32px · 깊이 3px      ArUco 검출 불가
```

인쇄 크기를 키워도 축소율이 그대로 곱해진다. **크기로 못 이긴다.**
실용 한계는 대략 `d < 5~8h` 이며 삼각대 1.5m 면 10m 내외다.

```
h=1.5m → 축소율 0.10
h=3.0m → 0.20
h=6.0m → 0.37      실제 CCTV 높이
```

★ 따라서 **근경 10m 는 보드, 원경 10~25m 는 지도**로 나눈다. 원경에서는
골목 양쪽 도로경계선이 평행하다는 것과 그 실간격을 `ngii1k` 에서 읽을 수
있다는 것이 소실점까지 구속한다. 마커보다 조건이 좋다.

### 보드 사양

| | |
|---|---|
| 재질 | **하드보드지.** 종이가 휘면 평면성이 깨지고 코너가 어긋난다 |
| 크기 | A2 이상. 마커 4개 이상이 한 장에 인쇄된 단일 보드 |
| 인쇄 | **무광.** 코팅·비닐 금지 — 노면 반사광에 마커가 날아간다 |
| 사전 | `cv2.aruco` DICT_4X4_50, 마커 150mm, 간격 인쇄값 기록 |

★ 마커 4장을 흩어 놓고 6거리를 줄자로 재지 않는다. 그러면 ArUco 의
  가장 큰 값(기하가 인쇄로 확정됨)을 버리고 측량 공수만 남는다.

### 현장 절차 — 관측점당 40분

```
1. 삼각대 고정. 높이 기록. 이후 절대 안 건드린다
   AE/AF 락 · 손떨림보정 OFF · 표준 1x · 4K
   ★ 손떨림보정을 끄지 않으면 프레임마다 화각이 흔들려 H 가 무효다

2. 보드를 3m · 5m · 8m · 10m · 12m · 15m 로 옮기며 촬영
   ── 어느 거리에서 검출이 깨지는지 기록한다. 그 값이 이 카메라의
      유효거리이며 §11-1 `CCTV_RANGE 25m 미확인` 을 닫는 근거다

3. 검증점 — 5m · 12m · 20m 노면에 분필 십자. 좌우 폭을 레이저로 실측
   ★ 이 점들은 H 산출에 쓰지 않는다

4. 지도 대응점 — 도로경계 좌우, 교차점, 건물 모서리를 프레임에서 확인
   방법 A 가 쓸 지물이 실제로 보이는지 여기서 판단한다

5. 기준 사진 · 방위 · 시각 · 날씨 · 조도 (§7-1 필수 항목)
```

### ★ 검증점을 H 산출에서 반드시 뺀다

H 를 푼 점으로 오차를 재면 잔차가 0 이다. 자기 자신에 적합시킨 값이다.
이 조건이 없으면 "±6cm" 는 MASTER §4 의 7.24m 와 같은 성질의 숫자가 된다.

### 보고 형식

점추정으로 말하지 않는다. 3~5곳은 표본으로 적다.

```
5개 골목 · 검증점 24개 · 중앙 오차 6cm · 최대 14cm · 유효거리 11m
```

표본 수와 최대값을 같이 밝히는 형태만 방어된다.

### 촬영 골목은 손으로 고르지 않는다

§7-2 선정 기준을 그대로 쓴다. `needs_cv` 190 중 —

```
cctv_dist_m ≤ 25          영상판정이 성립하는 곳
route_usage 상위          실제 출동 동선
width_disagree_m 큰 곳    소스 간 이견 → 실측 가치 높음
nfa_designated 인접       소방청 지정 기준 근처
```

"왜 그 골목이냐"에 GIS 가 답해야 한다. §8-1 이 시나리오에 대해 말한 것과
같은 이유다.

### 이 절이 성립하지 않아도 시스템은 돈다

보드가 없으면 "오차가 얼마인지 모른다"가 될 뿐이다. 발표에서
`±8cm` 를 말할 것인가 "정확도는 D-25 이후"라고 말할 것인가의 차이이며,
하드보드지 한 장과 반나절의 값이 거기 있다.

### 미결

| # | 항목 |
|---|---|
| 1 | 세그멘테이션 모델 선정 — 사전학습 전용인가 골목 이미지 파인튜닝인가 |
| 2 | 실제 CCTV 내부 파라미터 미상. 왜곡을 지도 대응점 20+ 로 흡수할지 |
| 3 | 차량 마스크는 **하단 접지선**만 쓴다. 지붕을 투영하면 차폭이 과대해진다 |
| 4 | 지면 평면 가정이 깨지는 곳 — 최대 경사 9.0°, 계단·턱 |

## 7-4. 안전 — 타협 대상 아님

- **오전 9~11시 고정.** 8월 광주 오후 아스팔트 답사는 실제로 위험하다
- 야간 답사는 별도 일정
- 도로 한복판 실측 시 **2인 1조, 1명은 차량 감시**

---
# 8. 시나리오 · 평가
## 8-1. 시나리오를 손으로 고르지 마라 — 그래프에서 역산 📄

```python
for (출발, 도착) in 조합:
    d1 = 최단경로(제약 없음)
    d2 = 최단경로(RED 제외)
    if d2 / d1 > 1.5 and d2 < inf:
        후보에 추가
```

**"우리 시스템이 없었으면 소방차가 갇혔을 출발-도착 쌍"을 자동으로 찾는 것이다.**
시나리오 선정 자체가 GIS 산출물이 되고, "임의로 고른 것 아니냐"는 질문을 막는다.

출발점은 대인/지산 119안전센터(§1-4). 여기에 실제 화재 출동 통계를 얹으면 "실제로 일어날 법한 시나리오"까지 방어된다.

**선정 조건**: 코너 통과 가능 / CCTV 커버 구간 포함 / 우회로 존재.
## 8-2. ★ 평가지표 — 설계 완료, 산출 미착수 🟡

> **우리 시스템이 잘 작동한다는 걸 무엇으로 증명하나.**

세 회의 연속 미결이었던 이유는 담당자가 없어서가 아니라 **"실측 없이는
정확도를 못 낸다"에 나머지 지표가 전부 묶여 있었기 때문**이다. 하나가
막히자 넷이 같이 섰다. 갈라 적으니 셋은 오늘 낼 수 있는 것이었다.

| ID | 지표 | 지금 | 전건 |
|---|---|---|---|
| E-1 | 진입 실패 회피율 | **가능** | 없음 |
| E-2 | 우회 비용 | **가능** | 없음 |
| E-3 | 판정 가능률 | **값 있음** | 없음 |
| E-4 | 폭 정확도 (MAE) | 불가 | D-25 실측 |
| E-5 | 재현성 게이트 | **가능** | 없음 |

E-4 는 정의만 해둔다. **빈칸으로 두지 말고 "D-25 이후"라고 적는다.**
발표에서 표의 빈칸과 "전건이 이것이다"는 완전히 다르게 읽힌다.

★ §8-1(시나리오 자동 탐색)의 의사코드가 곧 E-1·E-2 의 산출기다.
  두 작업을 별개로 본 것이 미결을 길게 만든 원인 중 하나다.

★ 2026-08-18 에 이 설계를 `docs/EVAL.md` 로 따로 냈다가 되돌렸다.
  `test_no_fifth_doc` 이 잡았다. 평가지표는 **아직 안 한 일**이므로
  PLAN 의 절이다. 산출값이 나오면 그때 MASTER 로 간다. 설계서를 별도
  문서로 빼는 습관이 R14(한 항목은 한 문서에만 산다)를 그대로 어긴다.

### 0. 원칙

#### 0-1. 적합(fit)과 검증(validation)을 섞지 않는다

MASTER §4 가 이미 정한 입장이다. 소방서 지정 7구간 대조는 절대편차를
12.6 → 7.24m 로 줄이는 **게이트로 썼다.** 게이트로 쓴 자료는 그 순간부터
외부 검증 수단이 아니다.

    현재 외부 검증 수단      0개
    그 자리를 대신할 것      D-25 실측 (미착수)

**따라서 이 절의 어떤 지표도 "우리 폭이 정확하다"를 주장하지 않는다.**
주장하는 것은 하나다 — *현행 방식 대비 무엇이 나아지는가*.

#### 0-2. 결손은 관대한 쪽으로 해석하지 않는다

2026-08-18 에 `width_max_m` 결손 496건이 판정을 한쪽으로 기울이고 있음이
확인됐다(`tools/wmax_audit.py`). 지표도 같은 병에 걸릴 수 있다. 산출
불가는 **분모에서 빼지 말고 별도 항으로 보고한다.** 못 잰 것을 조용히
지우면 커버리지가 저절로 올라간다.

#### 0-3. 지표는 값이 아니라 실행이다

`data/processed/eval.json` 으로 떨어뜨린다. 형식은 `nfa_compare.json` 과
같게 맞춘다. `tools/baseline.py` 가 실행 간 자동 비교할 수 있어야 한다.
터미널에만 있는 숫자는 두 번 소실됐다(MASTER §4 · DECISIONS 08-18).

---

### 1. 지표 목록

| ID | 지표 | 지금 | 전건 |
|---|---|---|---|
| E-1 | 진입 실패 회피율 | **가능** | 없음 |
| E-2 | 우회 비용 | **가능** | 없음 |
| E-3 | 판정 가능률 | **값 있음** | 없음 |
| E-4 | 폭 정확도 (MAE) | 불가 | D-25 실측 |
| E-5 | 재현성 게이트 | **가능** | 없음 |

E-4 는 오늘 정의만 한다. **빈칸으로 두지 말고 "D-25 이후"라고 적는다.**
발표에서 표의 빈칸과 "전건이 이것이다"는 완전히 다르게 읽힌다.

---

### 2. E-1 · 진입 실패 회피율

> 우리 시스템이 없었으면 소방차가 못 가는 길로 안내받았을 비율

**정의**

    baseline   거리만으로 최단경로 (현행 내비게이션이 하는 일)
    ours       blocked 구간을 제외한 최단경로

    E-1 = (baseline 경로가 blocked 구간을 1개 이상 지나는 쌍) / (전체 쌍)

### 입력

- 출발: 대인 · 지산 119안전센터 2곳 (MASTER §3-6 — 출동은 소방서가 아니라 안전센터에서 나간다)
- 도착: 동명동 건물 중 층화 표본 (§4 참조)
- 그래프: `node_link.geojson` (엣지 3,427) + `turn_restriction.csv` (87행)

### 산출

`src/etl/` 에 `eval.py` 를 새로 만든다. PLAN §8-1 의 시나리오 자동 탐색
의사코드가 그대로 이 지표의 분자·분모다. **두 작업이 아니라 한 작업이다.**

```python
d1 = shortest(G_all,  s, t)      # baseline
d2 = shortest(G_pass, s, t)      # blocked 제외
```

### 주의

- `blocked` 159 는 여전히 **하한이다.** 08-18 ROAD_BT 확대로 96건을 건졌으나
  대장폭이 3.0m 이상이거나 없는 결손 64건이 남는다(§0-2). E-1 은 과소평가
  쪽으로 치우친다. 그 방향을 명시해 보고한다.
- 표본 1개짜리 clear 는 §10 판정에서 이미 needs_cv 로 내렸다. E-1 에서
  clear 를 통과 가능으로 쓰는 것은 그 처리를 전제한다.

---

### 3. E-2 · 우회 비용

> 막힌 길을 피하면 얼마나 돌아가나

**정의**

    E-2a  d2/d1 중앙값 · 90퍼센타일       거리 증가율
    E-2b  d2 = inf 인 쌍의 수             우회로가 아예 없는 목적지

**E-2b 가 본체다.** 증가율은 "조금 돌아간다"로 끝나지만, 도달 불가는
"소방차가 못 간다"다. 발표에서 지도에 찍을 것도 이쪽이다.

### 주의

- `unknown` 352 를 어느 쪽으로 셀지 정해야 한다. 세 가지 다 낸다.

      낙관   unknown 통과 가능      E-2 하한
      비관   unknown 통과 불가      E-2 상한
      현행   unknown 통과 가능      지금 지도가 하는 일

  **하나만 내면 고른 이유를 방어해야 한다. 셋을 내면 폭이 논거가 된다.**
- 시간 환산은 하지 않는다. 골목 주행속도 근거가 없다. 거리만 쓴다.

---

### 4. 표본 설계

전수(2 × 건물 전체)는 계산이 무겁고, 무작위는 "왜 그 쌍이냐"를 못 막는다.
**층화한다.**

| 층 | 기준 | 왜 |
|---|---|---|
| A | `route_usage` 상위 | 실제 동선. 579/1101 이 usage > 0 |
| B | `nfa_designated` 인접 | 소방청 지정 기준 충족 153구간 |
| C | `unknown` 밀집 | 영상판정 불가 구간의 실제 영향 |
| D | 무작위 | 층화가 만든 편향의 대조군 |

D 를 반드시 넣는다. 위험한 곳만 골라 놓고 "위험하다"는 결론을 내면
그것은 지표가 아니라 순환논증이다.

표본 수·시드는 `eval.json` 에 박는다. 재실행에서 달라지면 E-5 가 잡는다.

---

### 5. E-3 · 판정 가능률

이미 값이 있다. 재산출만 하면 된다.

    영상판정 불가(unknown)     352 / 1,101  (32%)
    연장 기준                  14,484m / 48,580m  (30%)
    CCTV 유효범위(25m) 밖      721 / 1,101  (65%)

**이 숫자를 우리가 먼저 꺼낸다.** 발표에서 가장 먼저 찔릴 지점이고,
질문을 받고 답하는 것과 먼저 말하는 것은 다르게 들린다. MASTER §3-5 가
이미 그 태도로 쓰여 있다 — *알고리즘의 한계가 아니라 인프라의 한계다.*

완화 항도 같이 낸다: 영상판정 불가 구간이 담당하는 소방 통행량 비율.

---

### 6. E-4 · 폭 정확도 — 전건 D-25

**정의** (오늘은 여기까지)

    E-4a  MAE          평균 절대오차 (m)
    E-4b  중앙값 절대오차
    E-4c  소스별 분해   ngii1k / silpok / ngii

**★ 절대편차 **합**을 쓰지 않는다

MASTER §4 는 지금 `절대편차 합`으로 적혀 있다. 표본 수에 비례해 커져서
실행 간·소스 간 비교가 안 된다. `7.24 → 9.23 → 8.99` 를 두고 "나빠진 게
아니라 적합이 풀린 것"이라는 해명을 문서에 써야 했던 이유가 이것이다.
지표로 승격할 때 **MAE 와 중앙값으로 바꾼다.** 합은 참고로만 남긴다.

### 전건

    D-25 레이저 거리계 실측     미착수. 거리계 미구매
    표본                        `data/field/sample_segments.csv` 봉인 5구간
    프로토콜                    `data/field/fieldsheet.md`

정보공개청구 17337943(도로대장, 8/25경 회신)이 필문대로289번길 −3.51 의
검증에 걸려 있다. 회신이 오면 E-4 의 부분 산출이 가능해진다.

---

### 7. E-5 · 재현성 게이트

지표가 아니라 **통과 조건**이다. `eval.py` 첫 줄에서 검사하고, 불일치면
지표를 뽑지 않고 죽는다.

    tools/baseline.py diff 20260814-ngii-ngi20     전이 행렬 0 이 아니면 사유 확인
    data/golden/segments.fingerprint.json          지문 일치
    data/processed/_manifest.json                  전 단계 OK (lineage_check)

**왜 게이트인가**

`1093`(08-17) 과 `1091`(08-18) 은 둘 다 낡은 산출물을 이번 실행 것으로 <!--stale-ok-->
착각해 나온 값이다. 그 상태에서 지표를 뽑았다면 **틀린 숫자가 발표
자료에 들어갔을 것이고, 지표라는 이름이 붙어 있어 아무도 의심하지
않았을 것이다.** 방어는 `src/etl/guards.py` 에 있고 이 게이트는 그것을
지표 산출 앞단에 한 번 더 세우는 것이다.

---

### 8. 발표에서 말할 것과 말하지 않을 것

**말한다**

- 베이스라인 대비 개선 (E-1 · E-2)
- 판정 가능률과 그 원인이 인프라라는 것 (E-3)
- 정확도 지표의 전건이 D-25 라는 것, 그리고 그 일정

**말하지 않는다**

- "우리 폭이 정확하다" — 외부 검증 수단이 0개다
- 소방서 7구간 대조를 정확도 근거로 — 적합값이다 (§0-1)
- 시간 단축 (분·초) — 골목 주행속도 근거가 없다
- `blocked` 159 를 확정 개수로 — 잔여 결손 64건 때문에 하한이다 (§0-2)

---

### 9. 남은 결정

| # | 항목 | 누가 |
|---|---|---|
| 1 | `unknown` 을 통과 가능으로 볼 것인가 (§3) | 미정 — §4-4 오류 비대칭과 같은 결정 |
| 2 | 표본 수와 시드 | 미정 |
| 3 | E-1 분모를 건물로 할지 노드로 할지 | 미정 |
| 4 | `wmax` 결손 처리 (E-1 하한 문제) | `tools/wmax_audit.py` 결과 확인 후 |

1번과 4번은 같은 뿌리다. **결손과 불확실을 어느 쪽으로 기울일 것인가.**
PLAN §4-4(오류 비대칭 · 안전마진)가 담당자 없음으로 비어 있는데,
그 공백이 지금 판정과 지표 양쪽에 동시에 나타나고 있다.

## 8-3. 관측 신선도 🔴

마지막 관측이 40분 전이면 그 판정을 믿고 소방차를 보낼 수 있나. **지금 설계에 시간 개념이 없다.**

```
신뢰도 = f(경과시간, 해당 도로 단속밀도)
30분 전 관측 + 상습주차 구간 = 사실상 GRAY
```

**이게 없으면 "실시간이라면서 얼마나 실시간인가요?"에 답을 못 한다.**
## 8-4. GRAY 구간 대책 📄

CCTV가 없는 구간은 관측이 불가능하다. 대신 **사전확률**을 제시한다.

| 신호 | 출처 | 논리 |
|---|---|---|
| 단속밀도 | `enforcement` (건/100m) | "관측 불가하나 연 N건 단속되는 상습 점유 구간" |
| 상가 밀도 | `poi_store` | 상가 앞 = 주차 압력 |
| 음식점 비율 | `poi_store` (동명동 39%) | 야간 집중, 배달 이륜차 |
| 도로폭 | `width_min_m` | 좁을수록 1대만 세워도 막힘 |

### ⚠ 단속 이력을 시간대 가중치로 쓰지 마라

```
14시 → 33,412건 (전체의 39%)
```

주차 패턴이 아니라 **입력 기본값**이다. 발표에서 방어할 수 없다.
**시간대 근거는 답사 촬영으로만 확보한다(§6-8).**

### ⚠ 단속 이력에 좌표가 없다

`위반장소명`이 도로명주소다. **도로명(RN)으로 도로구간에 조인한다.**
단 절반이 랜드마크 명칭("문화전당 부설주차장" 3,137건 등)이라 필터가 필요하다.
동명동권 도로명 1차 매칭 2,379건.
## 8-6. 소화전 📄

동명동 법정경계 내 공개 소화전 **1건**.

**이 숫자를 그냥 쓰지 말고 이렇게 써라:**

```
동부소방서 관할 실제 소방용수    588개 (지상식 431 + 지하식 157)
공개 표준데이터의 광주 동구       31개
                                 → 공개율 5%
```

**"1개밖에 없다"보다 "588개 중 31개만 공개돼 있다"가 훨씬 강하다.**
발표에서는 *"공개 데이터만으로는 소방 인프라 레이어를 구축할 수 없다. 이것이 실무 연계가 필요한 이유다"* 로 쓰고, 인터뷰 협조요청 근거로도 쓴다.

**소화전 레이어는 좌표 확보 전까지 지도에서 뺀다.** 1개짜리 지도는 안 만드느니만 못하다.

활용 근거(좌표 확보 시): NFTC 401 수평거리 140m, NFSC 109 옥외소화전 40m —
"도로엔 못 들어가도 근처 소화전이 있으면 호스로 대응 가능"이라는 RED 구간 세분화 로직.

---
# 9. 외부 의존
## 9-2. 소방서 정식 인터뷰 ⏳

광주동부소방서 소방행정과 **062-613-8415**

| # | 질문 | 왜 |
|---|---|---|
| 1 | **진입불가/곤란지역 지정 자료를 받을 수 있나** | 포지셔닝 1차 출처. **최우선** |
| 2 | 「2025 골든타임 확보 종합대책」 원문 | 인용 근거 |
| 3 | **중형펌프차 제원(폭·전장·최소회전반경)** | §5-7 |
| 4 | **현장 최소 여유공간 몇 m** | §4-4 안전마진 검증. **1차 미해결** |
| 5 | 진입 실패 사례 빈도 | 필요성 근거 |
| 6 | 경로는 누가 정하나 (상황실/기사) | 서비스 대상 확정 |
| 7 | 기사가 경로 고를 때 보는 요소 | 선호경로 학습 입력 |
| 8 | 동명동 출동 이력 통계 | §8-1 시나리오 현실성 |
| 9 | 야간 출동 추가 어려움 | §6-8 |
| 10 | 「소방청 건축위원회 표준 가이드라인 2021.10」 원문 | §5-7 회전 기준 1차 출처 |
| 11 | 한전에서 전주 실측 TM 좌표를 받고 있나 | §8-5 |

**협조 요청**: 진입곤란지역 지정 목록 / **소방용수 정확 위치(588개 중 31개만 공개)** / 출동 통계 / 차량 실측 허가
## 9-3. 주간 멘토링 — 축을 바꿔가며 물어라

매번 "진행 상황"만 보고하면 낭비다.

```
1주  아키텍처·데이터 계약 / 평가지표(§8-2)
2주  DL 투입 지점 타당성(§6-8)
3주  실험 설계·베이스라인
4주  발표 스토리라인
```

---
# 11. 우선순위
## 11-1. 지금 막혀 있는 것

```
⏳ 레이저 거리계 미구매       ← 실측 프로토콜(§7-1)이 막힘
🟡 평가지표                   ← 설계 §8-2 완료. E-1·E-2 산출 미착수
🔴 오류 비대칭 결정(§4-4)     ← wmax 결손 496건 처리와 unknown 취급이 여기 걸려 있다
⏳ 소방차 실제 제원           ← 소방서 인터뷰(F-03). TRUCK 3.0 의 근거 보강
⏳ 소화전 588개 좌표          ← 소방서 협조(D-30)
🟡 CCTV_RANGE 25m 미확인      ← 352구간(회색 전부)의 판정을 가른다
```

> `juso 전자지도 심사`는 2026-08-11 에 해소됐다. 세그먼트 수는 1,101 이다(2026-08-18).
> 노딩 규칙이 바뀌면 바뀐다 — 확정값이 아니다. unknown 429.
## 11-2. 순서

```
STEP 1  행정구역경계 확보                          ✅ 2026-08-11
STEP 2  폭 산출                                    ✅ 2026-08-13 (미산출 0)
        → 판정 4색 분포 확정. 6색이 아니라 4색이다

STEP 3  그래프 구축 + 다익스트라/A*                 다익스트라 완료 · A* 미착수
        → node_link 1,366 · turn_restriction 87 미투입
        → 시나리오 자동 탐색(§8-1)

STEP 4  관측점 선정 → 기준점 실측 → 오전/오후/야간 3회 촬영
        → 선정 기준은 §7-2 에 데이터로 정의됨

STEP 5  mock 서버 / 프라이버시 파이프라인 / 야간 강건성

STEP 6  재현성 정리                                 ★ 신규
        → ngii1k 를 pipeline STEPS 에 편입          ✅ 2026-08-18
        → streetlight ingest kind 처리              ✅ 2026-08-18
        → segments.py main() 분해 (폭 확정 후)
```

**의존 관계**: `경계 → 노딩 → 폭 → 그래프 → 시나리오 → 관측점 → 촬영`

> STEP 6 을 폭 확정 뒤에 둔 이유. 리팩터링의 성공 판정 기준은 "결과가 안
> 바뀌는 것"인데, 폭이 움직이는 동안 쪼개면 숫자 변화의 원인을 못 가린다.
> **기준선이 있어야 리팩터링을 검증한다.**

**시나리오 자동 탐색이 답사보다 먼저다.** 순서가 꼬이면 엉뚱한 골목을 찍고 답사가 통째로 날아간다.
## 11-3. 병렬로 돌릴 수 있는 것

STEP 1 대기 중에 막히지 않는 것들:

- 평가지표 산출 E-1·E-2 (§8-2) ← 정의는 08-18 에 끝났다
- 소방서 인터뷰 전화(§9-2)
- 그래프 방향성 결정(§5-7) ← 안 정하면 코드 두 번 갈아엎힌다
- CCTV 커버리지 버퍼 반경 확정(§4-5)
- mock 서버 프레임 시퀀스 구조 설계

---
# 12. 담당 공백

| 항목 | 담당 |
|---|---|
| 평가지표·베이스라인 (§8-2) | 설계 완료 · 산출 담당 없음 |
| 오류 비대칭·안전마진 (§4-4) | 없음 |
| 프라이버시 파이프라인 (§6-8) | 없음 |
| 시나리오 설계 (§8-1) | 없음 |
| **관측점 관리 (§7-2)** | **없음** ★ GIS/비전 경계 |
| 실측 보조 앱 (§7-3) | 없음 |

**오창준에게 부하가 몰려 있다** (데이터 확보 + EDA + 그래프 + 인프라).
혼자 다 하면 병목이 되고, **검증 없이 확정하는 상황이 반복된다.** §0-3이 그 결과다.

**회의에서 재분배할 것.**

---

---

# 13. 2026-08-12 추가 (오늘 나온 것)

## 13-1. ★ CCTV 25m 검증 — 최우선 🔴

**이 값 하나가 1,266구간의 판정을 가른다.**

```
현재 가정   25m 초과 시 호모그래피 오차 급증 → 영상판정 불가
결과        영상판정 대상 267 → 139구간
            영상판정 불가 223구간 (전체 연장의 28%)
            CCTV 유효범위 안 36%
```

**호모그래피 담당(백진욱)의 확인 없이 전면 재분류한 상태다.**
30m 면 대상이 늘고 20m 면 준다. `src/etl/segments.py` 의 `CCTV_RANGE` 한 줄이다.

- [ ] 백진욱에게 25m 근거 확인 요청
- [ ] 실측 시 CCTV 거리별 오차 곡선 측정
- [ ] 확정값으로 재산출

## 13-2. A* — node_link · turn_restriction 투입 📄

데이터는 이미 파이프라인에 있다(`node_link` 1,366 · `turn_restriction` 87).
**그래프에 투입만 하면 된다.** 새 데이터 불필요.

- [ ] 일방통행 방향 반영
- [ ] 회전제한 87건 반영
- [ ] 비용함수: 거리 + 폭 페널티 + 통행제한 ∞
- [ ] 대시보드용(3인칭 부감) / 네비용(1인칭 GPS) 분기

**이것 없이 A* 를 만들면 역주행 경로가 나온다.**

## 13-3. 상습주차 추정 모델 📄

`enforcement` 85,380건(2022-01 ~ 2023-10, 위반장소명 보유)이 레이블이다.

```
학습   도면 특징(도로 폭 · 벽 사이 폭 · 상가 밀도 · 건물 층수 ·
       주차장까지 거리 · 도로 위계 · 출입구 밀도) → 단속 발생 빈도
적용   CCTV 없는 223구간의 상습도 예측
검증   답사 촬영 · 소방서 인터뷰
```

**출동 시점의 상태가 아니라 사전 위험도다.** 그 구분을 흐리면 안 된다.
시점 정보는 CCTV 유효범위 안에서만 얻을 수 있고, 그 밖은 확률로 대비하는 수밖에 없다.

⚠ **시간대는 쓸 수 없다.** 14시가 39.1% 로 입력 기본값이 오염돼 있다. 구간별 빈도만 쓴다.

## 13-4. 실측 🔴

- [ ] 레이저 거리계 주문 (아직 미주문. **배송이 병목**)
- [ ] 지점별 GPS 기록 (8/6 답사는 GPS 미기록으로 무효)
- [ ] 10~20지점 실측 → `width_verified: true`
- [ ] 실폭도로 1.76m vs 수치지도 3.86m 중 어느 쪽이 맞는지 판정

## 13-5. 소방서 인터뷰 요청 항목 ⏳

한 번에 받아야 하는 것.

- [ ] 소방용수 588개 좌표 (공개분은 31개, 5%)
- [ ] 소방통로확보대상 20구간의 시·종점 좌표 (현재 도로명만 있어 정밀 대조 불가)
- [ ] 겨울 결빙 시 진입 불가 구간 (경사는 배제했으나 결빙은 별개)
- [ ] 중형펌프차 실제 제원 (전폭·축거·최소회전반경)

**우리 산출값이 소방서 기록과 ±1.5m 로 일치한다는 결과를 들고 가면
빈손으로 부탁하는 게 아니다.**

## 13-6. 네비게이션 앱 📄

- [ ] GPS 연동 (`Geolocation API`)
- [ ] 1인칭 카메라 (`maxPitch` 85, 진행 방향 기준)
- [ ] 신고 주소 입력 → 그 건물 출입구까지 경로 위 위험구간 즉시 경고
- [ ] 대시보드에만 표시, 네비에는 미표시

건물출입구 1,313개와 안전센터→출입구 최단경로는 이미 산출돼 있다. 주소 조회로 끝난다.

## 13-7. 정밀 DEM — 보류 ⬛

안심구역은 **원본 반출이 안 된다.** 분석 결과만 반출 가능하며 심의 2회에 2~3주 걸린다.

**경사는 이미 배제 결론이 났으므로 보류한다.** 공개DEM 90m 로도
최대 경사 9.0° 가 나와 소방차 등판 한계(11~17°)를 넘지 않는다.

지형은 표현용으로만 쓴다(Terrain-RGB 타일, `exaggeration` 1.0).

## 13-9. processed 계층 포맷 전환 (GeoParquet · COG) 📄

**AWS 적재 전에 해야 한다.** 지금은 텍스트·SQLite 라 S3 비용과 Athena 스캔이 낭비다.

| 현재 | 문제 | 전환 |
|---|---|---|
| `*.geojson` | 텍스트. 좌표를 문자열로 저장 | **GeoParquet** |
| `*_5186.gpkg` | SQLite. 컬럼 프루닝 불가 | **GeoParquet** (같은 파일로 통합) |
| `enforcement.csv` 85,380행 | 텍스트 | **Parquet** |
| `dem_scope.tif` | 일반 GeoTIFF | **COG** (범위 요청 가능) |

**GeoParquet 하나로 geojson + gpkg 두 벌을 대체한다.**
좌표계가 파일에 들어가므로 `_5186` 접미사도 필요 없어진다.

```
geojson → parquet   5~10배 압축
csv → parquet       3~5배
Athena              Parquet 네이티브. CSV 스캔은 과금 폭탄
```

- [ ] `publish_web.py` 이전 단계를 전부 parquet 으로
- [ ] `dem_scope.tif` → COG
- [ ] 계약 테스트 갱신
- [ ] 실측: 36종이 몇 MB 에서 몇 MB 로 줄었는지

⚠ **`web/data` 는 GeoJSON 을 유지한다.** 브라우저가 읽어야 한다.
전환 대상은 `data/processed` 뿐이다.

⚠ 5개 스크립트가 `gpd.read_file(*.gpkg)` 로 읽고 있다. 한 번에 바꿔야 하고
회귀 위험이 있다. **파이프라인이 안정된 뒤에 한다.**

## 13-8. 문서 3개 체계 — 지킬 것

```
docs/기획서_Fire-Lane.docx   대외 제출용
docs/MASTER.md               현재 상태 · 데이터 · 운영 · 패치 적용
docs/PLAN.md                 남은 일 (이 문서)
```

**문서를 더 만들지 않는다.** 2026-08-11 에 문서가 6개로 흩어져서
판정 기준이 3.0m 로 바뀐 것을 UI 담당이 모른 채 2.5m 로 화면을 만든 사고가 났다.

완료된 항목은 **이 문서에서 지우고 MASTER.md 에 결과를 쓴다.**
그래야 PLAN 이 항상 "남은 일" 만 담는다.

---

# 13. 2026-08-13 신규 미결

## 13-1. 결정 승인 대기 🟡

MASTER §16 결정 63 개정 + 65~71 신규 7건. 코드에는 들어가 있고 문언 승인만
남았다. 승인 없이 다음 회의를 넘기면 문서와 코드가 또 벌어진다.

**2026-08-14 추가: 결정 72~82 신규 11건.** 71(`seg_id` 불변 키 폐기)은
72(`seg_uid` 신설)로 대체 완료. 나머지는 코드 적용 상태이며 문언 승인 대기.

## 13-2. 재현성 구멍 ✅ (2026-08-14 해소)

```
ngii1k       pipeline STEPS 에 없다. 기기를 옮기면 폭 주 소스 없이 돌아간다
streetlight  ingest 에 csv_point kind 핸들러가 없다
```

**2026-08-13 에 실제로 발목을 잡았다.** 다른 기기에서 파이프라인을 돌렸더니
`ngii1k_5186.gpkg` 가 없는 채로 실행돼 `clear` 392 → 346 이 됐고, 원인을 <!--stale-ok-->
찾는 데 시간을 썼다. `pipeline.py` 한 명령으로 재현되지 않는 단계가 있으면
그 파이프라인은 재현 가능하다고 할 수 없다.

**해소 (2026-08-14)**

```
ngii1k       pipeline STEPS 편입 + sha_of() 로 디렉터리 해시
streetlight  핸들러 부재가 아니라 x_col/y_col 누락이 원인이었다. csv_points 로 정정
```

부수 발견 — 연도 정규식이 `_20201231_` 형태를 하나도 못 잡아 **도엽 연도가
전부 미검출**이었다. "같은 도엽은 최신 연도 채택" 로직이 무력화돼 있었다.

## 13-3. 가로등 표현 ✅ (2026-08-14 결정 승격 → MASTER §6)

3,805등 / 264지점. 좌표가 지번 대표점이라 **마커로 못 쓴다.**
`light_count`(반경 50m 등 수)로 야간 시인성을 표현하는 것이 현재 결론이다.

264지점을 굳이 지도에 올린다면 점이 아니라 **반경 50m 원**이 정직한 표현이다.
데이터의 실제 정밀도를 그대로 그리면서 분포는 보인다.

**결정 (2026-08-14)** — 지점 단위 마커로 쓴다. distinct 는 여전히 금지.
group-by + count 로 등 수를 보존하고 지주 굵기 √n · 반경 50m 점선 원으로
불확실성을 드러낸다. 상세는 MASTER §6.
실제 폴 위치는 `C0220000`(수치지형도 가로등·보안등 점) 편입으로 대체 예정.

## 13-4. UI 라벨 확정 ✅

```
통행 가능 / 판정 보류 / 통행 불가 / 영상판정 불가
```

"도면상" 제거. 회색 하위 구분(`unknown_reason`)은 범례에 별도 줄로 올리지
않는다. 1차 분류라는 단서는 화면 상단 `#warn` 이 유지한다.

> `영상판정 불가` 명칭은 `width` 가 0 이라는 전제 위에 성립한다.
> `width` 가 다시 생기면 그 이름이 틀린 것이므로 재논의한다.

## 13-5. wmax 교차로 누출 🟡

담~담(`width_max_m`)이 교차로에서 46~57m 로 샌다. `min(B)` 라 판정 영향은
작지만 필드 자체는 신뢰할 수 없다. 폭(A)에 넣은 조각 병합·cap 처리를
건물 트랜섹트에도 적용해야 한다.

**2026-08-14 — `wmax` 를 `wmin` 과 비교해 모순을 잡으려던 안은 폐기했다.**
`wmax/wmin > 5` 면 자기모순으로 보고 판정을 보류하는 규칙을 넣었다가
걸린 11건을 열어보니 전부 정상이었다. `wmax` 는 담~담이고 도로 양옆이
공터·주차장이면 당연히 크다. **`wmax` 와 `wmin` 은 애초에 비교 대상이 아니다.**
안전장치가 아니라 새 벌레였다.

---

# 14. 2026-08-14 신규

## 14-1. 완료 ✅

```
seg_uid          실행 간 유지 키. 흔들기 유지율 99.4% · 현재 100%   결정 72
관측점 방위각      구간이 아니라 지점 단위                          결정 73
raw 레이아웃      폴더명 = 제공기관 코드                           결정 74
원본 인벤토리      inventory.py → sources.yaml AUTO. 미사용 속성 267개
데이터 대장       outputs · consumers · stable_key · known_issues   결정 75
계보·영향분석      datalog.py impact K
외장 백업 + 검증   3,179개(raw 포함) · 누락 0 · 손상 0
실측 표본 설계     25구간 · 관측점 75 · 시드 20260814              결정 76·77
도로폭 신뢰 필터   미탐 77건 제거                                  결정 78·79·80
교차부 실제 형상   A0080000 1154건                                 결정 81
A0020000 속성     도로폭 6490 · 일방통행 387 · 차로수 · 보도 772
```

## 14-2. 실측 표본 트랙 — 섞지 마라 🔴

```
A 12구간   width_src × 폭대역 층화 무작위    편향 추정용
B  8구간   임계 근접 + 상대불일치            판정 교정용
C  5구간   단순 무작위 · 봉인                외부 검증용
```

**A 와 B 를 합쳐서 편향을 추정하면 추정치가 망가진다.** B 는 "소스끼리 값이
다른 곳"만 고른 표본이라 그 오차 분포는 전체 분포가 아니다.

★ 초안은 불일치를 **절대 1.0m** 로 잡았다. 20m 도로의 10% 오차(2.0m)는 통과하고
  3m 골목의 10% 오차(0.3m)는 탈락한다. 그 결과 B 가 간선도로로 채워졌고
  간선은 이미 `clear` 확정이라 "판정이 뒤집힐 구간"이라는 목적과 정반대였다.
  **상대 기준(35%)으로 교정 → 폭중앙 7.06m → 3.13m**

**C 는 열지 않는다.** 소방서 7구간이 이미 학습셋이 되었으므로(MASTER §4)
현재 외부 검증 수단은 0개다. C 가 그 자리를 대신한다.
회차마다 2~3구간씩 봉인 몫을 추가하되 분석에는 쓰지 않는다.

## 14-3. 오차를 줄이는 순서 📄

```
1회차   트랙 A 12구간 → 소스별 편향 b, 산포 σ 추정
2회차   그 모델로 트랙 B 를 미리 예측하고 나간다 → 실측과 비교
3회차   안 맞는 구간의 공통점을 찾아 모델 개선
```

**2회차가 핵심이다.** 예측을 적어놓고 재면 맞았는지가 현장에서 갈린다.
오차를 줄이는 것이 아니라 **오차 모델이 맞는지 검증하는 것**이다.

## 14-4. 근거 없는 상수 🔴

```
XSEC_EXCL    5.0    폴리곤 폴백용으로 축소됐으나 값 자체는 여전히 눈대중
WMAX_CAP    60.0    ★ 주석에는 40m 로 적혀 있다. 문서-코드 불일치부터 정리
SNAP_MAX     6.0
MIN_SEG_LEN  3.0
CCTV_RANGE  25.0    352구간의 판정을 가른다. 호모그래피 담당 확인 요청
```

상수만 보면 눈대중처럼 보이지만 **근거가 붙어 있으면 함부로 못 건드린다.**
`SNAP_TRUST`·`COV_MIN` 에는 2026-08-14 에 근거를 붙였다. 나머지도 같이 붙인다.

## 14-5. 간선 본선+측도 병합 🔴

필문대로289번길 52.2m. '길'인데 52m 다. 본선과 측도가 한 폴리곤이라 담~담을
재면 둘이 합쳐진다. 보도(`A0033320`) 커버율 13%, 30m초과 24건 중 보도 인접은
2건뿐이라 **보도 차감으로는 못 푼다.**

→ 정보공개청구 17337943(도로대장 차도폭·보도폭) 결과 대기. 2026-08-14 접수.

## 14-6. web/data 생성물 커밋 🟡

`.gitignore` 의 `!web/data/**` 로 산출물이 추적된다. 매 실행 2,000줄 diff.
"UI 담당이 raw 없이 작업 가능해야 함"이 근거이므로 지금은 유지하되,
GeoParquet 전환(§13-9) 시 함께 정리한다.

## 14-7. 다음 순서

```
1  실측 DB + 입력 화면 (HTTPS)   ★ 다음 주 실측을 막고 있는 유일한 항목
2  가로등 3D 마커 + C0220000 실제 폴 위치
3  계약 테스트 확충              폭 상한 · 도로명 규칙 위반 상한 · 조용한 결측
4  norm/ 계층 분리               raw 원본 격리
5  근거 없는 상수 5개에 근거 붙이기
```

## 14-8. 마커 아키텍처 리팩 ✅ (2026-08-14)

가로등 마커 하나를 추가하는 데 **6곳**을 고쳐야 했다.
`config.js` 주석은 "config.js만 고치면 재발행 없이 반영된다"고 적고 있었으나
**선언은 config 로 뺐는데 결선이 코드에 남아** 그 약속이 깨져 있었다.

### 바뀐 것

```
config.js   스펙이 data · popup · scale · cover 를 전부 든다
app.js      CONFIG.markers 를 돌며 선언대로 실행만 한다
              fetch 손나열      → spec.data 에서 생성
              MK_SRC 손딕셔너리 → DATA[spec.data]
              POPUP 손딕셔너리  → spec.popup
              굵기 id 특수분기  → spec.scale
              cctv-cov 하드코딩 → spec.cover (radius | by | themed)
index.html  마커 토글 행 손나열 → app.js 가 생성
```

**마커 추가 = `config.js` 한 곳 + 데이터 발행 한 곳.**

### 계약 테스트가 지킨다

`test_marker_spec_self_contained` 가 손딕셔너리·id 특수분기·손토글의 부활을
잡는다. 실제로 지혜님 PR #18 과 충돌을 푸는 과정에서 `spec.id === "m-cctv"`
분기를 되살렸다가 이 테스트에 걸렸다. 테마 연동은 `cover.themed` 로 뺐다.

> **문서가 프롬프트로 쓰인다.** UI 담당이 MASTER §11 을 그대로 작업 지시로
> 쓰기로 했으므로, §11 은 현재 코드와 어긋나면 안 된다. 구조를 바꿀 때
> §11 을 같이 고치는 것을 기본 절차로 한다.

## 14-9. publish 도 같은 문제 🟡

`publish_web.py` 에 소스마다 발행 블록이 손으로 쌓여 있다.
`sources.yaml` 의 `outputs` 에 `web_publish: true` 를 두면 루프로 돌릴 수 있다.
2026-08-14 에 만든 대장이 여기서 쓰인다.

→ 마커 쪽과 같은 방식. 선언을 대장에 두고 코드는 실행만 한다.
