# Fire-Lane — 실행 계획

> **앞으로 할 일만 담는 문서다.** 끝난 것은 결과를 `MASTER.md` 로, 왜
> 그렇게 됐는지는 `DECISIONS.md` 로 옮기고 **여기서는 행을 지운다**(§0-2).
>
> | 문서 | 시제 | 담는 것 |
> |---|---|---|
> | **`docs/PLAN.md`** | **미래** | **남은 일 · 미결정 · 담당 공백 (이 문서)** |
> | `docs/MASTER.md` | 현재 | 판정 · 데이터 · 용어 · UI 계약 · 운영 |
> | `docs/DECISIONS.md` | 과거 | 왜 그렇게 됐나 (append-only) |
> | `docs/proposal.docx` | — | 대외 제출용. 시제 규칙 밖 |
>
> 문서 넷은 병렬 축이 아니라 한 항목의 **생애주기**다.
>
> ```
> PLAN(미래)  →  도래  →  MASTER(현재)  →  회고  →  DECISIONS(과거)
> ```
>
> **한 항목은 한 문서에만 산다.** 남은 일의 정본은 §1 하나다.

## 0. 서술 규약 · 상태 표기

### 0-1. 서술 규약

`MASTER §0` 을 따른다. 이 문서에만 적용되는 것은 셋이다.

| 항목 | 규칙 |
|---|---|
| 시제 | **미래·미완**. 끝난 일은 즉시 뺀다. 완료 목록을 여기 쌓지 않는다 |
| 절 구성 | 절은 **주제**로 나누고 날짜는 본문에 적는다 |
| 항목 형식 | 남은 일 한 줄에 `상태` · `전건` · `막고 있는 것` 이 함께 있어야 한다 |

★ 날짜를 절 제목으로 쓰지 않는다. 그날 나온 것을 그날 절로 만들면 번호가
날짜만큼 늘어나고, 같은 번호가 두 곳을 가리키면 인용이 성립하지 않는다.
강제자 — `tests/test_reproducibility.py::test_plan_headings_are_numbered_and_unique`

### 0-2. 상태 표기

| 표기 | 뜻 |
|---|---|
| 📄 | 결정은 됐으나 코드·실측으로 확인 안 됨 |
| 🟡 | 미결정. 논의 필요 |
| 🔴 | 담당자 없음. 공백 |
| ⏳ | 대기 중 — 외부 응답·타 파트 산출을 기다린다. **여기서 못 푼다** |
| ⚠ | 전제가 틀리기 쉬운 항목 |

★ **닫힘 표식은 없다.** 위 다섯이 전부이고 `✅` 도 `⬛` 도 어휘 밖이다.
표식을 만들면 사람은 행을 지우는 대신 표식을 단다 — 2026-09-06 에
`✅` 셋이 그렇게 생겨 아흐레를 남았고, 검사는 폐지된 `⬛` 를 찾고
있어서 울지 않았다(DECISIONS §159).
강제자 — `tests/test_doc_fsck.py::test_plan_status_vocabulary_is_closed`
        카나리아 — `::test_plan_status_probe_is_alive`

**완료되면 결과를 `MASTER.md` 에 쓰고 여기서는 행을 지운다.**
`uv run python tools/plan_renumber.py --apply` 가 뒤 번호를 당긴다.

★ PLAN 은 **빚 목록**이다. 갚은 빚은 목록에 없다. 슬롯으로 남기면
그것이 완료 목록이 되고, 이 문서는 완료 목록을 쌓지 않는다(§0-1).

★ **`⏳` 는 담당이 없다는 뜻이지 필요 없다는 뜻이 아니다.**
설계와 막힌 이유가 행 안에 그대로 있으므로 이어받는 사람이 처음부터
판단하지 않아도 된다. **지우지 않는 이유가 그것이다**(DECISIONS §102).

★ **행 번호는 영구 식별자가 아니다.** 현재 목록에서의 자리일 뿐이다.
닫힌 행을 지우고 뒤 번호를 당긴다 — 결번이 있으면 지운 것인지 아직 안 쓴
것인지 갈리지 않는다.
강제자 — `tests/test_declaration_sync.py::test_plan_row_numbers_are_contiguous_and_sorted`
재배번 — `uv run python tools/plan_renumber.py --apply`

★ 2026-09-14 정정. 여기에 "행 번호는 자산이다 · `⬛` 슬롯으로 남긴다" 가
남아 있었다. **2026-09-13 에 강제자는 이미 반대로 바뀌었는데 문서만
안 따라왔다.** 그래서 그날 이 문단을 읽은 사람이 "지우면 검사가 운다" 고
단정했고 틀렸다. 규약이 두 판으로 갈려 있으면 **먼저 읽은 쪽이 이긴다** —
그것이 이 저장소가 232번 당한 형태다.

---

★ **닫힘 표기는 없다.** 완료·기각한 항목은 결과를 `MASTER` 로, 왜 그렇게
됐는지를 `DECISIONS` 로 옮기고 **이 문서에서 행을 지운다.**

PLAN 은 빚 목록이다 — **갚은 빚은 목록에 안 남는다.** 슬롯을 남기면
영원히 안 비고, 그러면 이 문서는 "앞으로 할 일" 이 아니라 "했던 일 목록" 이
된다. 그것은 `DECISIONS` 가 하는 일이고, 한 항목이 두 문서에 사는 것이다.

★ **개발이 끝나면 이 문서는 비어야 한다.** 남은 일이 없다는 뜻이다.
남은 일이 없는데 문서가 두꺼우면 무언가 잘못된 것이다.

강제자 — `tests/test_doc_fsck.py::test_plan_has_no_closed_items`

## 1. 남은 일

남은 일의 정본은 이 절 하나다. `MASTER.md` 는 "현재 무엇이 어떤 값인가"만 적는다.

| # | 항목 | 상태 | 전건 · 막고 있는 것 |
|---|---|---|---|
| 1 | `node_link` + `turn_restriction` 그래프 투입 | ⏳ | A* 필수. 데이터는 이미 보유(엣지 3,427 · 노드 2,837 · 회전제한 87행). ★ 막힌 것은 데이터가 아니라 `D-07`(일방통행 역주행 허용 여부)이고 그것은 `D-30` 소방서 인터뷰다. ★ **2026-09-02. GIS 담당이 이 항목을 놓는다**(DECISIONS §102). 안 하기로 한 것이 아니라 **하는 사람이 없어진 것**이다. 설계와 막힌 이유를 그대로 남긴다 |
| 2 | A* 비용함수 설계 | ⏳ | 거리 + 폭 + 통행제한 ∞ + 경사. §4-1 · §4-5 선행. 곱셈형으로 확정돼 있다(§4-1). ★ **2026-09-02. GIS 담당이 이 항목을 놓는다**(DECISIONS §102). 안 하기로 한 것이 아니라 **하는 사람이 없어진 것**이다. 설계와 막힌 이유를 그대로 남긴다 |
| 3 | 영상판정(호모그래피) 모듈 연결 | ⏳ | `needs_cv` 191구간. 영상 담당. ★ 2026-09-15. ROI(관심영역 점유율)로 갈아타자는 안이 나왔으나 **판정 축을 버리게 된다** — 출력이 `width_max_m` 이고 임계가 3.0/7.0(MASTER §2-2)인데 점유율은 미터가 아니다. 환산하려면 정적 도로폭이 필요하고 그것은 지적도에서 오는데, 지적도 오차를 메우려고 CCTV 를 쓰는 것이므로 순환이다. 면적 기준은 **세로로 긴 장애물과 가로로 긴 것을 구분 못 한다** — 통행 가능 여부는 면적이 아니라 최소 폭이 정한다. ★ 다만 ROI 는 **변화 감지 트리거**로는 싸고 정확하다. 앵글이 고정이므로 "무언가 들어왔나" 를 GPU 없이 판단한다. 둘을 대립으로 두지 않는다 — ① ROI 로 변화를 잡고 ② 그 위치에서 호모그래피로 통과 폭을 잰다 |
| 4 | D-25 레이저 실측 | 🔴 | `width_verified` 전건이 걸려 있다 |
| 5 | D-30 소방서 인터뷰 | ⏳ | 소화전 좌표 · 차량 제원 · 소방통로 구간. **확답 없음** |
| 6 | 도로대장 정보공개 재청구 | 📄 | 1차(17337943)·보도현황(17337939) 모두 **부존재 종결**(2026-08-24). 사유는 「취합·가공해야 하는 정보」다. 가공 요청을 빼고 재청구했다. ★ **결과에 일정을 걸지 않는다** — `ngii1k_center`(n=909 · 절대편차 중앙 0.32m)가 이미 독립 검증축이고 정본은 D-25 실측(#4)이다. 부존재가 또 오면 발표 논거로 전환한다(§1-26) |
| 7 | 건물 형상 고도화 | 🟡 | 수치지형도 1:1,000 건물 레이어 보유 여부 확인부터. ★ 2026-09-17 레이크 밖 스냅숏에서 나왔다 — 1:1,000 2022 SHP zip 16도엽 안 `N1A_B0010000`(DECISIONS §173-5). 레이크에 편입되기 전이라 **보유가 아니다.** 「대장 · SSD 디렉토리 구조와 해석기 하나」 가 retired 로 보존한 뒤 `ngii1k` 판과 도엽 · 판차를 대조한다. R 교차 대조 재료이기도 하다(#51) |
| 8 | D-29 평가지표 산출 | 🟡 | 설계 §7-2 완료. E-1 · E-2 산출 미착수 |
| 9 | `width_max_m` 결손 — 막을 근거가 하나뿐인 92건 | 🟡 | 노면 3m 미만인데 대장폭이 3m 이상이거나 없다. 건물 담~담이 없어 통행 불가로 못 간다. 실측 또는 건물형상(#7) 대기. ★ 결손 675 전체가 아니다 — 대부분은 동 경계 80m 밖이라 벽 사이를 **재지 않는** 것이다(MASTER §3-4) |
| 10 | 정사영상 미터 단위 정합 미검증 | 🟡 | TIF 에 경계좌표 없음. 도엽 bbox + 사방 균등 pad **가정** |
| 11 | 도로경계 폴리곤 밖 37구간 | 🟡 | `uncovered_units.json`. 게이트는 통과하나 개별 형상 확인 필요 |
| 12 | `desk_check` · `wmax_audit` 테스트 없음 | 🟡 | 좌표 변환이 틀려도 아무도 모른다 |
| 13 | `turn_restriction` 건수 게이트 | 🔴 | 좌표 없는 DBF(전국 44,125행)라 `node_point` 로만 걸러진다. 그 소스가 실패하면 필터가 사라지는데 상태는 정상으로 뜬다 |
| 14 | clearance 기각 재판정 | 🔴 | 기각 근거가 `n=7` 이고 게이트가 오염돼 있었다. D-25 에 3~7m 대역 20~30곳을 재서 정한다 |
| 15 | 영상판정 유효거리 실측 | 🔴 | `CCTV_RANGE` 가 단일 상수로 못 잡힌다(MASTER §16-1). §1-23 |
| 16 | 촬영 높이 결정 | 🟡 | 유효거리가 높이에 비례한다. #17 결과가 나오면 장비를 정한다. §1-24 |
| 17 | `min` → 통과폭 재판정 | 🟡 | `min` 은 표본 하나에 뒤집힌다. `width_fn.py` 로 대조 중. **판정 반영은 실측 후** |
| 18 | `node_link` 속성 투입 | 🟡 | `LANES` · `REST_W` 는 **계보가 다른 폭 하한**이다 |
| 19 | 경로 비용을 차량 기준으로 | 🟡 | `access_corridor()` 가 거리만 쓴다. 판정 반영은 golden 재잠금이 필요하다 |
| 20 | 차량 제원 확정 | 🔴 | 축거·최소회전반경이 추정값이다. ★ **2026-09-03 정정. 최소회전반경은 D-30 대기 항목이 아니다** — #30 가 이미 값의 소재를 적는다(`profiles.json` 7,300~11,889mm · 강원 인제소방서 자료 · 나라장터 공고). **재취득 가능하고 대장 밖일 뿐이다.** #30 를 태우면 닫힌다. 관내 보유 차종은 `gjfire_fleet_dongbu`(2026-05-14) 가 이미 든다. 종전 서술이 닫을 수 있는 항목을 인터뷰 병목으로 걸어두었다. 축거(`wheelbase_m`)만 D-30 대상이다. ★ 2026-08-24 팀 일지가 `profiles.json` 근거로 중형펌프차 7.3m · 대형물탱크차 11.2m 를 적었으나 **출처가 확인되지 않았다.** KFS-1-0073 · KFS-1-0030 · MAS 제작규격 셋에는 회전반경이 없다(MASTER §3-13). 출처가 규격 원문이 아니면 `turn_radius_verified` 를 켜지 않는다 — 법정 상한을 성능값으로 쓴 DECISIONS §81 과 같은 형태다. ★ 2026-09-17 관리카드 4개 반입(대인5호 이름분은 지산2호 사본)(`gjfire_vehicle_card` · DECISIONS §172) — 카드 1쪽에 축거·회전반경이 **없다.** 이 행은 카드로 안 닫힌다. 남은 31대는 받을 때마다 대장 `parts` 에 적고 `pull_data` 를 돌린다 ★ 2026-09-17 섀시 제원표(축거)는 공개 경로로 못 구했다 — D-30 인터뷰로 받는다(DECISIONS §177-4). |
| 21 | `LANES` 매칭률 24% | 🟡 | 266/1,101 만 매칭(2026-09 · 1,101구간 기준 측정 <!--stale-ok-->). 소스 문제인지 매칭 로직 문제인지 아직 안 갈렸다 |
| 22 | 미불용지 82구간 | 🟡 | 우리 노선이 지목 `대`(사유지)를 밟는 구간. 폭이 충분해도 권리 문제다 |
| 23 | 문서의 죽은 경로 참조 | 🟡 | **2026-09-02 `refcheck` 를 `verify.sh` 에 걸어 실측했다.** 남은 것 둘 — `PLAN §7` 이 `data/processed/eval.json` 을(평가지표 산출이 미착수라 아직 없다. #8 가 나오면 생긴다), `README` 파트 표가 `src/cv/README.md` 를 가리킨다(CV 파트가 아직 안 썼다). **둘 다 '없는 것을 가리킨다' 가 아니라 '아직 안 만든 것을 가리킨다' 이므로 경고로 둔다.** ★ 2026-09-16 실측 — **이 행이 두 경고를 스스로 끄고 있다.** `refcheck ⑦` 은 같은 문서에서 `삭제`·`폐기` 가 든 줄에 경로가 함께 있으면 넘기는데, 이 행이 `삭제된 스크립트` 와 두 경로를 한 줄에 든다. 행을 지우면 경고 둘이 되살아난다(DECISIONS §162). 닫으려면 두 경로를 만들거나 `_gone` 범위를 경로가 적힌 그 줄의 **폐기 대상**으로 좁힌다 — 후자는 코드다 `DECISIONS §1~§9` 의 삭제된 스크립트 아홉은 부검 기록이라 검사에서 뺐다(`DECISIONS §97`) |
| 24 | `contract-strict` 가 저장소 전체를 잠갔다 | 🔴 | 2026-08-27 신설된 잡인데 기존 코드가 **1,291건** 걸린다(`dev` 에서도 동일). Required 가 아니라 머지는 되지만 매번 빨간불이면 아무도 안 읽는다. ① `--ignore` 확장 — 이 저장소 문체와 충돌하는 것(`TRY003`·`EM101`·`EM102` 는 예외 메시지에 "왜" 와 "고치는 법" 을 길게 적는 규약, `PLR2004` 는 `1990 <= y <= 2100` 같은 것이 상수화하면 덜 읽힌다, `C901`·`PLR0912` 는 파서·검증기의 성격, `PLC0415` 는 순환 임포트 회피, `CPY001` 은 이 저장소가 저작권 헤더를 안 쓴다) ② 또는 `CODEOWNERS` 범위를 신규 파일로 축소 — 워크플로 머리말의 취지가 그것이다("범위를 좁혀서 강도를 유지한다"). ★ **2026-09-02 실측 — `.ruff-strict.toml` 이 들어온 뒤 `All checks passed` 다.** 1,291건은 그 파일이 생기기 전 수치이고 지금은 `verify.sh` 의 `엄격 린트` 스텝이 매번 초록불이다. **남은 것은 범위 판단뿐** — 지금 `--ignore` 가 이 저장소 문체와 충돌하는 규칙을 빼고 있고, 그 목록이 맞는지는 사람이 정한다 |
| 25 | 소화전 좌표가 도로망에서 멀다 | 🟡 | **2026-09-01 재측정으로 원인이 갈렸다.** 발행 소화전 153개 기준 도로까지 중앙 10.1m · p75 15.7 · p90 30.9 이고 15m 안이 110/153(72%)이다. 원인 후보 셋 중 **③ 매칭 반경 8m 가 좁다** 가 맞다 — 소화전은 건물 앞·담장 옆에 서고 우리 도로는 중심선이라, 폭 6m 도로면 중심선→가장자리 3m + 보도 2~3m 로 5~10m 가 정상이다. 종전에 적힌 "동명동 41개 중 8m 내 11개" 는 소스 교체 전 값이다. 남은 일은 커버리지 반경 산정에 쓸 매칭 임계를 정하는 것 하나다 |
| 26 | 1차 사용자 정의 재검토 | 🟡 | 회의록 축 `P-57`. 관제 주체가 소방서가 아니라 도청·시청으로 추정되고 경로 지시 권한이 없다는 진술이 나왔다(MASTER §9-1). 정의가 바뀌면 화면 범위와 발표 포지셔닝이 함께 움직인다. **D-30 정식 인터뷰 전에는 닫지 않는다** |
| 27 | 통과선 3.0m 와 현장 감각의 간극 | 📄 | 회의록 축 `P-58`(폭 사전 판정 전제의 유효성) · `P-59`(임계값과 현장 감각의 간극). 현장은 통행폭을 미리 따지지 않고 내려서 판단하며 여유 8cm 에도 통과를 시도한다고 답했다. **임계는 낮추지 않는다** — 미탐 비용이 오탐보다 크다(§3-3). 남은 일은 값 변경이 아니라 이 간극을 발표에서 먼저 밝히는 것이다. MASTER §9-1 에 기록됨 |
| 28 | 산출물 형태 — 독립 앱 대 출동 패드 부가기능 | 🟡 | 회의록 축 `P-60`. 현장은 독립 앱보다 기존 출동 패드의 부가기능을 선호한다고 답했다. 지금 산출은 정적 파일이므로(MASTER §20-5) 어느 쪽이든 인터페이스는 같다. **결정 시점은 화면 설계서 착수 전이다** |
| 29 | 소화전 매칭 임계 확정 | 🟡 | #25 에서 갈린 원인 ③ 의 후속. 8m 를 몇 m 로 올릴지는 커버리지 반경(NFTC 401 수평거리 140m · NFSC 109 옥외소화전 40m)과 함께 정한다. §7-5 |
| 30 | 회전반경 출처를 `raw` 로 | 🟡 | `profiles.json` 의 7,300~11,889mm 는 강원 인제소방서 자료·나라장터 공고에서 왔다. **재취득 가능한데 대장 밖이다.** `landing → acquire → raw` 를 태우고 `datasets` 에 등재한다. 그래야 `absent` 선언(DECISIONS §87-2)이 출처를 가리킬 수 있다 ★ 2026-09-17 정정 — 원문을 반입해도 `turn_radius_verified` 는 **안 켜진다.** 값은 공고의 현행 예시이지 규격이 아니다(MASTER §3-13). 켜는 근거는 D-30 실측이다. 이 행은 출처를 대장 안으로 들이는 일일 뿐이다(DECISIONS §178-3). |
| 31 | 교통량 축을 무엇으로 채우나 | 🟡 | 차량 탑재 내비는 API 없이 TPEG 를 방송 수신한다. **내비의 기본 소양이라 자리를 비워두지 않는다**(DECISIONS §88-6). 우리 소스는 단속이력 상습도다 — `gjcity_parking_enforce` 2024·2025 두 판. 시간대별 주정차 빈도를 간선 가중치로. ITS 소통정보는 간선만 덮어 판정 구간을 하나도 안 덮으므로 안 쓴다(2026-09 · 1,101구간 기준 · 1,281 재대조는 ITS 대조표 배치) ★ 2026-09-17 두 판을 retired 에서 raw 로 복귀했다 — 대장 `parking_enforce`(csv_table_multi — 2024-01-08 판에만 있는 `비고` 를 optional_cols 로 선언하고 이어붙인다 · DECISIONS §179-4). | <!--stale-ok-->
| 32 | D-30 광주 인터뷰 일정 | 🔴 | **대기가 아니라 안 한 것이다.** 절차는 2026-08-05 에 확인됐다(일정팀 전화). 일정을 우리가 못 정한다 — `sources.yaml` `pending`(vehicle_spec_measured)로 옮겼고 fallback 을 적었다. `D-07` · `D-25` · #21 · `P-57`~`P-60` 이 전부 여기 매달려 있다. §9-1 에 날짜를 박는다 — 안 박히면 이탈 후 아무도 안 한다 |
| 33 | 운전요원 여정지도 복원 | ⏳ | 원본이 소실됐고 페르소나(문재현)는 남아 있어 복원 가능하다. ★ **지금 그리지 않는다** — `P-60`(독립 앱 대 패드 부가기능)이 열려 있어 접점이 흔들린 상태다. `D-30` 이후 그려야 근거가 선다. 관제사 여정지도는 최저점이 「이동 중 지원」(-4)이며 `MASTER §9-2` 가 든다. ★ 2026-09-16 `📄` → `⏳`. 행 스스로 `D-30` 이후라고 적으므로 결정이 아니라 대기다(DECISIONS §162) |
| 34 | README 가 루트인데 GIS 전용이다 | 🟡 | clone 한 사람이 처음 보는 파일인데 파이프라인만 설명한다. `src/firelane/README.md` 가 이미 파트 문서의 선례다. 루트는 **무엇을 만드나 · 5분 시작 · 파트별 갈림길 · 문서 어디에** 넷만 두고 나머지를 내린다. `src/cv/README.md` · `web/README.md` 는 해당 파트가 쓴다. ★ 2026-09-16 **코드 배치로 옮긴다** — `part/**` 는 유지로 정해졌으므로 「파트별 갈림길」 은 4계층 그대로 적는다(DECISIONS §163). 남은 것은 README 에 걸린 강제자 다섯의 검사 대상 경로를 함께 옮기는 일이다. 문서만의 일이 아니다(DECISIONS §162). ★ 2026-09-17 **README 전수 대조를 같이 한다** — 루트 · `src/firelane/README.md` · `web/README.md` 셋이 실물과 어긋났을 수 있다(예: `web/README.md` 는 MapLibre 5 라고 적는데 내비는 6 이다 · `src/cv/README.md` 는 없다 · `web/navi` 는 README 가 없다). 배포 주소는 N1.1 에서 `cleveraifox.github.io` 로 먼저 고쳤다(DECISIONS §181-6). `data/baseline/*/README.md` 셋은 당시 기록이라 고치지 않는다 |
| 35 | CI 에 LLM 에이전트를 붙일지 | 🟡 | **게이트 자리에는 붙이지 않는다.** 근거 넷 — ① 이 저장소가 낡음을 잡아온 방식이 결정론적 대조라 같은 입력에 같은 판정이 나온다. 에이전트는 그 성질이 없고 `DECISIONS §160`(재실행하면 통과하는 실패)이 검사 전체로 번진다 ② 빨간불 이유가 매번 다르면 끄는 것 말고 대응이 없다(`#24`) ③ 판정 근거를 되짚을 수 없다. 대조 검사는 근거가 곧 diff 다 ④ **2026-09-04 부터 남은 넷이 프롬프트 드리프트를 디버깅할 수 없다.** ★ 붙일 자리가 있다면 **막지 않고 알리는 쪽**이다 — PR 본문 초안, 기획서 산문 대조 후보 제시. 틀려도 손해가 0인 것만. 하네스·의존 그래프는 이미 있다(`verify.sh` · `pipeline.Step` · `lineage`). ★ **2026-09-02 범위 확정.** 붙일 자리가 릴리즈 브리핑으로 좁혀졌다 — `tools/release_brief.py` 가 판정·계보·대장·계약 넷의 차이를 **결정론적으로** 내고, 그 위에 "이 변경이 무엇을 뜻하나" 한 문단만 얹는 형태다. **재료 없이 요약만 붙이면 지어낸다** — 차이표가 먼저이고 그것은 이제 있다. 게이트 자리에는 여전히 붙이지 않는다 |
| 36 | 검사마다 저장소를 다시 스캔한다 | 🟡 | `refcheck` · `treecheck` · `doc_fsck` · `docnum_check` 가 **각자 트리를 스캔하고 각자 경로를 조립한다.** 같은 파일을 네 번 읽고 해석이 네 벌이라 오늘 그중 하나만 `stem` 대조를 안 해서 오탐이 났다(`DECISIONS §97`). `treecheck` 가 이미 전수 스캔 결과와 지문을 내므로 `--json` 을 나머지 셋이 읽으면 스캔이 한 번이고 해석도 한 곳이다. ★ **지금 하지 않는다** — 하루짜리이고, 이탈 전에 하면 남은 넷이 이해할 코드가 하나 더 는다. 브로커나 API 가 아니라 **인덱스 한 벌**이면 되는 문제라는 것을 적어 둔다 |
| 37 | 그림 배치가 코드에 박혀 있다 | 🟡 | `render_figures` 의 구조 그림 둘은 박스 좌표를 코드가 정한다(`x = 60 + i*200`). **노드가 늘면 겹치거나 넘친다** — 파트가 넷이 되면 `fig_branch` 가 720 을 넘는다. 넘침은 `_fits()` 가 잡지만(`DECISIONS §111`) **겹침은 못 잡는다.** ★ 갈 곳은 레이아웃 엔진이다 — Graphviz `dot` 으로 노드·엣지만 선언하고 배치를 엔진에 맡긴다. ★ **지금은 안 간다** — 의존이 하나 늘고 CI 에 `graphviz` 설치가 필요한데 노드가 고정이다(브랜치 넷 · 배포 다섯). ★ **가는 조건 셋** — ① 구조 그림이 넷을 넘는다 ② 한 그림의 노드가 여덟을 넘는다 ③ `_fits()` 가 배치 손보기로 안 풀린다. 둘 이상이면 옮긴다. `dot` 은 출력이 결정론적이라 `--check` 지문 대조가 그대로 산다 — 그것이 Mermaid 대신 `dot` 인 이유다 |
| 38 | `vehicle_profiles` 에 섀시 세대 축이 없다 | 🔴 | 같은 중형펌프차가 섀시 세대로 갈린다 — 메가트럭(~2019) / 파비스(2019~). 그런데 `vehicle_profiles` 는 차종당 축거 하나라 **축거를 확보해도 넣을 자리가 없다. 값보다 스키마가 먼저다.** 판정 단위는 **센터 × 차종**이고 서로 독립이다(`DECISIONS §113`) — 그 조합의 보유 대수만큼 차종을 전부 알 때만 최대가 확정된다. ★ 갈 곳은 `CONFIG.fleet` 각 항목에 `chassis` · `wheelbase_m` · `wheelbase_verified` 를 달고 `offtracking()` 이 선택된 항목의 플래그를 보는 것이다. `can_turn()` 이 `turn_radius_verified` 를 보는 구조가 이미 있으므로 **이식이지 발명이 아니다.** ★ 지금 확정된 조합은 둘뿐이다 — 대인 펌프차(중형) 2대 파비스 · 대인 굴절 27m 1대 구쎈(사양 미확정). 기본 판정 차량인 지산 펌프차 2대는 0대 확인이다 |
| 39 | 119 · 구조 데이터로 경로 비용 외부 검증축 | 🟡 | `route_vehicle.csv` 의 `cost` 는 **한 번도 실물과 대조된 적이 없다.** 폭에는 `ngii1k_center` 라는 독립 검증축이 있는데(n=909 · 절대편차 중앙 0.32m) 경로 비용에는 그런 축이 없었다. `nfa_dispatch_119` 의 `DSPT_DRTV_DT` → `GRNDS_ARVL_DT` 가 실제 도달 소요이고 `DCLR_PSTN_LOT/LAT` 이 그 지점이다. `nfa_rescue` 도 좌표를 갖는다(동명동 212행). `nfa_fire_incident` 는 좌표가 없는 대신 `FRSTN_GRNDS_DSTNC` · `CNTR_GRNDS_DSTNC` · `DSPT_REQ_HR` 로 거리와 시간을 직접 든다 — 구간에 못 붙여도 회귀로 볼 수 있다. ★ 좌표는 **신고자 위치(휴대폰)** 이지 반드시 현장이 아니고 결측이 `0.0` 으로 채워져 있어 `lat > 0` 필터가 필수다. ★ 2024 · 2025 는 컬럼명이 달라 파생본에 2022 · 2023 만 들어갔다 — 매핑하면 표본이 몇 배로 는다 |
| 40 | React Native 포팅 | 📄 | `domain`·`infra`·`app` 은 그대로 돈다. `components`(maplibre-gl → @rnmapbox/maps) · `ui`(CSSProperties → StyleSheet)만 재작성. ★ Web Speech 끊김이 OS 엔진으로 사라진다. ★ 번들 1.26MB(gzip 351KB)의 대부분이 `maplibre-gl` 이라 `@rnmapbox/maps` 로 바뀌면서 사라진다 — **지금 코드 분할할 이유가 없다** |
| 41 | 실시간 교통 계층 | 📄 | ITS 소통정보가 통행가능 총연장의 58%(간선 7m+)를 덮는다. `tools/its_linkmap.py` 가 링크 ↔ `seg_uid` 대조표를 만든다. 나머지 42%(골목)는 ITS 도 상용도 못 덮는다 — 거기가 CV·β 자리다. ★ 5분 cron 배치면 되고 상시 서버가 아니다 |
| 42 | `--fast` 가 부분 실행 안전장치를 안 탄다 | 📄 | `--only` 일 때는 `부분 실행` 단계가 일부러 실패해 `seal` 이 반쪽 실행을 봉인하지 못하게 막는다. **`--fast` 는 그 장치를 안 탄다** — `파이프라인 전량` 을 통째로 생략하는데도 전수처럼 통과한다. 조건 한 줄이다 |
| 43 | `seal` 이 `생략` 을 닫힘으로 읽는다 | 📄 | `_closed_declarations` 가 *지금 빨갛지 않은 선언* 을 닫힌 것으로 본다. 그런데 `--fast` 로 돌리면 `파이프라인 전량` 이 `생략` 이라 빨갛지 않고, **안 닫혔는데 닫혔다고 판정한다.** 2026-09-14 에 실제로 선언 둘을 지우게 만들었다. `생략` 은 통과가 아니다 |
| 44 | `seal` 이 생성물을 작업나무 더러움으로 센다 | 📄 | 파이프라인이 돌 때마다 `web/data/_manifest.json` 이 갱신되고, 그러면 `seal` 이 *로그보다 나중에 바뀐 파일* 로 거부한다. 커밋하면 HEAD 가 바뀌어 또 거부다 — **20분짜리를 두 번 쓰게 만든다.** 2026-09-14 에 세 번 겪었다. `web/data`·`data/processed` 는 커밋 시점이 따로이므로 신선도 판정에서 빼는 것이 맞다 |
| 45 | `dupcheck` 가 타입 주석이 다르면 사본을 놓친다 | 🟡 | AST 지문이 인자 **이름**은 지우는데 **타입 주석은 안 지운다.** `human(n: int)` 과 `human(n: float)` 이 다른 지문을 내서 세 벌짜리 사본을 **문턱 12 에서도 못 잡았다**(2026-09-14 실측). 고치면 **분모가 는다** — 지금 안 보이는 사본이 드러난다. 그것이 맞는 방향이다 |
| 46 | `dupcheck` 가 인라인 표현식을 안 센다 | 🟡 | 함수 단위로만 지문을 낸다. 곡선 중점 `interpolate(0.5, normalized=True)` 가 `segkey.py` 와 `segments.py` 두 곳에 인라인으로 있었는데 축 밖이었다 — 사람이 `grep` 으로 찾았다(`DECISIONS §144` 가 "등거리 근사 3벌" 로 센 것이 이 부류다). 축을 만들지 말지는 필요해질 때 정한다 |
| 47 | `_find` ×5 동치성이 확인되지 않았다 | 🟡 | `dupcheck --min 40` 이 남긴 마지막 사본군. `publish_navi` · `seg/graph.py` · `segments.py` · `bridge_audit` · `kpi` 다섯이 같은 구조를 갖는다. ★ 둘이 **판정 파일**이라 합치면 노드 묶음이 달라질 수 있고 그러면 `seg_uid` 가 움직인다(`DECISIONS §144`). **동치성 증명이 먼저**고, 그 뒤 전량 1회 → `golden.py lock` 이다. 재잠금은 한 번만 쓴다 |
| 48 | 커버리지를 재기 시작했다 — 13% | 🟡 | 2026-09-14 에 처음 쟀다. `14,021 stmts · 12,161 miss · 13%`. ★ `tools/` 가 거의 전부 0% 다 — `treecheck` 216 · `triage` 347 · `sweep` 159 · `vintage_check` 119. **`verify.sh` 가 매번 부르는데 `pytest` 는 한 줄도 안 밟는다.** 그날 나온 결함(별칭 미탐 · 부분문자열 오판 · 상태파일만 보는 검사)이 전부 그 구역에서 나왔다. ★ `--cov-fail-under=13` 래칫으로 시작해 내린다. 80% 를 목표로 잡지 않는다 — `dupcheck --max` 와 같은 구조다. `pytest-cov` 를 `pyproject` 에 넣어야 한다 |
| 49 | 내비가 실시간이 되면 휘발 구역이 필요하다 | 📄 | `route_vehicle.json` 이 지금은 산출물로 커밋된다. 실시간 경로로 가면 요청마다 달라지고, 그러면 `web/data 계보` 와 `커밋된 web/data` 가 **매 실행 빨간불**이 된다 — 빨간 게이트는 아무도 안 본다(원칙 ③). `layers` 에 *휘발*(커밋 안 함 · 계보 안 셈) 구역을 정의해야 한다. ★ DB 직결로 가도 같다 — **스키마는 감사 대상이고 행은 아니다.** ★ 지금 만들지 않는다. 쓰이지 않는 검사는 `EXEMPT` 에 죽은 항목이 쌓이던 것과 같은 형태가 된다 |
| 50 | 내비 출발·도착이 가장 가까운 **노드**에 붙는다 | 🟡 | 2026-09-16 웅토피아 코드 대조에서 나왔다. `web/navi/src/domain/graph.ts` 의 `findRoute` 가 `nearestNode()` 로 출발·도착을 잡는다 — 구간 **안쪽**에 선 사람을 가까운 교차점으로 옮겨 경로를 짠다. 웅토피아 저장소 `DECISIONS §134` 가 같은 결함으로 제봉로 20m 를 1,172m 로 안내했고 **구간 위 투영 + 부분 구간 비용**으로 고쳤다. 끊긴 도로에서 옆 도로로 옮겨 성공 처리하던 둘째 결함은 여기 없다 — 목표에 못 닿으면 `null` 이다(DECISIONS §166-4) |
| 51 | 판정 뼈대를 NGII 1:1,000 측량 중심선으로 다시 세운다 | 📄 | 구간이 도로명주소 `road_link` 위에 선다. 주소 관리용 선이라 측량 위치가 아니다 — 필문대로289번길은 건물 위를 10.6m 지나 사람이 승인 보정했다(DECISIONS §170-2). `중앙로` DM-192839-284313 도 건물 11.6m · NGII 도로면 0.1% 로 **같은 형태**다. 엄격 매칭에서 판정이 뒤집힌 사례는 0 이지만 매칭 안 된 240구간은 못 본다. ★ CV 가 활약할 `needs_cv` · `unknown` 이 틀린 선 위에 서면 엉뚱한 골목을 본다 — **CV 무대의 전제다.** seg_uid 가 전부 바뀐다. ★ 2026-09-17 두 번 쟀다(DECISIONS §173-7) — 피복 D=5 · 틈 이음 2m 확정, 차수2 병합 필수(엣지 5,027 → 2,008 · 중앙 25.6m). **근거를 고쳤다** — NGII 가 더 정확해서가 아니라 폭 원천과 뼈대를 같은 측량으로 맞추려는 것이다(현행 뼈대 11% 가 NGII 도로면 밖). **검증 기준을 고친다** — `building` 과 `road_link` 가 둘 다 도로명주소 계열이라 건물 겹침은 road_link 에 유리하다. NGII 1:1,000 건물 레이어(#7)로 교차 대조한다. 남은 측정 — 건물 레이어 교차 대조 · NGII 폭 6m 이상 2.2km 분리대 쌍선 여부(`분리대유무`) · 원천 간 어긋남 중앙값. 전건 — 「대장 · SSD 디렉토리 구조와 해석기 하나」(건물 레이어가 레이크에 들어와야 한다) ★ 2026-09-17 R1 — 뼈대 후보와 대조 도구가 섰다(DECISIONS §184). 건물 교차 대조 결과 NGII 건물 관통이 현행 17 · NGII 중심선 0 이다. 건물 레이어는 raw V-WORLD 묶음에 이미 있다(반입 불필요). 남은 것 — ① `tools/skeleton_compare.py` 의심표를 사람이 판정 ② R2 seg_uid 전이표 · 봉인 ③ R3 뼈대 교체 · 보정 모듈(DECISIONS §127) 걷기 · golden 재잠금 ★ 2026-09-17 R1 실측(DECISIONS §184-4) — 1,281 구간을 NGII 선까지 거리로 가르면 ≤1.5m 1,041 · 1.5~5m 130 · **5~15m 66** · >15m(간선 · NGII 선 없음) 44. 5~15m 중 needs_cv · unknown **32**(판단 기준 20 이상) · 같은 도로명 NGII 가 옆에 있는 것 38/53 → **R2 · R3 로 간다.** ★ 2026-09-18 R1 판 2 · R2 · R3a 가 들어갔다(DECISIONS §184-5 · DECISIONS §187 · DECISIONS §188). 판 2 는 「멀리」를 NGII 선 기준 · 반폭으로 고치고 「짝없음」을 교차점 갈림 제외(`cover`)로 바꿨다 — 짝없음 168 → 8. 「짧은 구간 방향 완화」는 실측으로 **기각**(20m 미만이 32뿐 · 45°로 넓혀도 168→163). R2 는 전이표(`firelane.transition`)와 봉인 `20260918-pre-r3` 를 세웠다 — 항등 자기검사 1,281 전부 1:1. R3a 는 시험 스위치 `FIRE_LANE_SKELETON`(기본 꺼짐)으로 **판정을 안 바꾸고** 이동을 쟀다. 1회차 관측 — 구간 1,281 → 1,573 · blocked 191 → 340 · 소방서 절대편차 8.31 → **16.79m** · 공간 미커버 2.6% → 0.3% · 최대 구간 993m → 193m · 컴포넌트 16 → 456. 뼈대 품질은 올라갔으나 **외부 대조가 2배 나빠졌다.** 세그 수가 같이 무너져(제봉로184번길 19 → 0) 원인은 폭이 아니라 **이름**이다 — `as_road` 가 NGII 도로명을 정본으로 뒀다. ★ R3b 1회차에서 쌍선 한 줄 제거(`drop_twin`)를 같이 시험했다가 **기각**했다(DECISIONS §189-3) — 도달 가능 957 → **90(7%)**. 평행 쌍선은 교차로에서 서로 이어져 있어 한 줄을 지우면 진입선이 갈 곳을 잃는다. **한 배치에 둘을 넣어 원인을 못 갈랐다.** ★ 2026-09-18 R3b 2회차 — 이름만 `road_link` 우선으로 되돌려 다시 쟀다. 결과가 1회차와 **한 값도 다르지 않다**(이름 5,926/6,812 · 소방서 16.79m · 세그 14 · 2 · 42 · 52 · 43 · 7 · 도달 957). **이름 가설도 기각**이다(DECISIONS §189-1) — 두 원천의 도로명이 사실상 같다. 세그 수 붕괴는 **기하 문제**이고 원인을 아직 모른다. ★ **R3c 를 하지 않는다**(DECISIONS §189-5). 막는 것 셋 — 외부 대조(소방서 절대편차 8.31 → 16.79m · 지정 구간 세그 붕괴) · **연결성(컴포넌트 456 · 최대성분 채택으로 엣지 641(9.4%) · 노드 1,089 폐기)** · 되돌리기 비용(seg_uid 유지율 13.0% · 옛 키 87% 소멸 · 실측 26+76행 미아). `nfa_compare` 는 우리 폭에 대한 유일한 외부 대조 수단인데(MASTER §4) 그것이 2배 나빠지면서 뼈대의 9.4%를 버린다. 시험 스위치 `FIRE_LANE_SKELETON` 은 기본 꺼짐이고 판정은 한 번도 안 움직였다. 남은 것 — ① **컴포넌트 456**. NGII 중심선이 교차로에서 안 이어진다는 뜻이고 세그 붕괴와 같은 원인일 가능성이 높다. 틈 이음 2m(DECISIONS §173-7)가 모자란 것인지 원천이 애초에 끊겨 들어오는 것인지부터 가른다 — 다음 R 은 여기서 시작한다 ② 병렬 엣지 길이비 1.5 초과(현행 0 → 시험 7)는 미결. `build_graph` 가 같은 노드쌍에서 긴 쪽을 버리는 규칙 쪽에서 푼다 ③ ①이 풀려 외부 대조가 회복되어야 R3c 가 열린다 |
| 52 | 벽 사이 폭(`width_max_m`) 채택 가드 | 🟡 | 건물 담~담을 판정 범위로 넓히면 결손 675 → 55 지만 측량 3m 미만 30구간이 통행 불가에서 풀린다(MASTER §3-4). 넓힌 판이 **막은** 8구간은 전부 측량과 맞았다. 벽 사이 값이 노면 · 측량폭과 크게 벌어지면 버리는 가드를 세우고 같은 A/B(엄격 매칭)로 다시 잰다 |
| 53 | 폭 산출 이상 — 노면 최솟폭이 측량의 3~10배 | 🟡 | `wmin` > 30m 27구간 중 필문대로 19 는 정상(측량 31~42m). 나머지 8 은 교차부 · 광장 면을 가로질러 잰 것이다 — `지호로` DM-194362-283514 clear · wmin 35.89 · 측량 **3.1m** 는 경계 한 끗이다. `밤실로4번길` 53~58m · `필문대로289번길` 52.2m. 판정은 아직 안 뒤집혔다 |
| 54 | 측량폭을 판정 입력으로 쓸 것인가 | 📄 | `needs_cv` · `unknown` 중 측량 3m 미만 156 · 6m 이상 117 — CV 자원이 측량이 이미 답한 구간에 간다. 반대로 넣으면 **유일한 독립 검증축이 사라진다**(MASTER §2). 교환이다. 결정만 한다 |
| 55 | 대장 · SSD 디렉토리 구조와 해석기 하나 | 📄 | 해석기 · 관문 · 폐기 이름화 · 대장 래칫(DECISIONS §174), 레이크 밖 유일본 보존 · 격리 층 폐지 · 사본 삭제(DECISIONS §176)는 끝났다. **남은 것** — ① 해석 사본 배선 — 래칫 `대장 직접 로드` 40 · `주인 블록 직접 해석` 12 를 배치마다 내린다(acquire 는 옮겼다 · DECISIONS §180). 다음은 lakecheck · doctor · intake ② authority 래칫 63 을 내린다 ③ 중복 등재 hwp(소방청 제작규격 = 소방안전 제작규격) 한 주인으로 ④ 봉인 `20260814-ngii-ngi20` 재생성 여부 확인 — 구 원본(NGI 20도엽)과 당시 기본도 옛 판(gj037~048)이 retired 에 있다. 재생성되면 MASTER §13 을 정정하고, 안 되면 둘을 상폐한다(DECISIONS §178) ⑤ 문서 — MASTER §18-1 구조도(대장 layers 에서 생성 · 대조) · 계층 표와 대장 layers · paths 의 `quarantine` 을 `retired` 로 · MASTER §18-11 획득 게이트 · README 레이크 절 · 대장 `backup_policy`(외장 SSD 한 벌 서술이 DECISIONS §173-1 과 어긋난다) · 해석기 `LAYERS` 상수를 대장 layers 에서 읽기 |
| 56 | 검사 위생 — 카나리아 의무 · 사람이 쓴 칸 대조 | 🟡 | skip 분류(DECISIONS §175)와 오판 넷(DECISIONS §180 — acquire 폐기 판정 · landing 처분 메시지 · dms 산문 파서 · plan_renumber 남의 표 번호)은 닫혔다. **남은 것** — ① 강제자마다 카나리아 의무. `deadcheck` 가 카나리아 없는 강제자를 래칫으로 센다 — 판별식(무엇이 강제자의 카나리아인가)을 먼저 정해야 셀 수 있다. 정의 없이 세면 재현되지 않는 분모다 ② 사람이 쓴 칸(what)과 도구가 쓴 칸(schema)의 대조 — `hydrant_summary` 의 what 이 다른 파일을 서술했는데 schema 는 맞았다(DECISIONS §178-2). 컬럼 이름이 what 에 나오면 schema 에 있어야 한다는 식의 약한 대조부터 ③ 결함 목록 G-8 묶음(docx_fix 산출단위 · devcontainer · pipeline --from 계보 · manifest write_stable · 검사마다 재스캔 · Node 20 액션 · README 재편 · refcheck _gone · 주석 속 PLAN #N · 도달 가능 수 EXPECT · 태그 도달성 · 적용 원형) ★ 2026-09-18 하나 더 — **`test_seg_uid_retention` 이 전량 실행에서 아무것도 지키지 않는다**(DECISIONS §189-6). `seg_uid_map.csv` 와 `web/data/segments.geojson` 을 같은 실행이 새로 쓰므로 유지율은 항상 100% 다. 진짜 비교는 `segments.py:871` 이 덮어쓰기 전에 하는 것인데 그건 경고만 찍고 안 죽는다 — 뼈대 시험의 13.0% 가 그냥 지나갔다. 부분 실행(`--only segments`)에서는 반대로 **항상 거짓 실패**한다. 비교 기준을 커밋된 봉인 쪽으로 옮겨야 한다 ★ 그리고 **`dms` 절 ID 가 위치 기반이다**(DECISIONS §189-7) — MASTER 가운데에 절을 끼우면 뒤 75절이 「변경」으로 찍힌다. 내용 기반 ID 로 바꾸는 일은 한 번의 전수 재검사를 동반하므로 별도 배치다 |
| 57 | landing 보류 — 상세주소DB | 🟡 | 7건 중 여섯은 처분했다(DECISIONS §179 — 내비게이션용DB · 민원행정기관 · 통계연보 반입, 주소DB · 상세주소 표시 · 사물주소 전국분 상폐). 남은 것은 상세주소DB(28MB) 하나다 — 아파트 「101동」 단위 목적지에 쓴다. raw_only `juso_bldggrp_geom`(동 도형 8,221 · 동 출입구 939)과 짝이다. 전건 — 「목적지 검색 — 주소 · 건물명 · 관공서」 |
| 58 | raw_only 주소 계열 4종 활용 여부 | 🟡 | `juso_bldg_geom` · `juso_bldggrp_geom` · `juso_etc_geom` · `spotaddr_geom` 은 raw 에 있고 kind raw_only 라 전처리 · ingest 가 없다. 활용 0. 판정 정확도(폭 · 위치)에는 거의 안 들어간다(DECISIONS §173-8) — 쓰면 출동 종점(건물 출입구 · 접속선) 쪽이다. 쓰려면 kind 를 정하고 prep · ingest 를 배선한다. 안 쓰면 retired 로. 전건 — #55 · #51 |
| 59 | 목적지 검색 — 주소 · 건물명 · 관공서 | 📄 | N1.1 · N1.2 코드가 섰다(DECISIONS §181 · DECISIONS §183) — `dest.geojson` 색인(상가 · `navi_build` · `navi_jibun` 지번 · `civil_office`) · 목적지는 **동명동 경계 안만**(DECISIONS §183-1) · 건물은 지도 이동 범위 · `juso_building_db` retired. 남은 것 — ① 같은 도로명주소로 뺀 757행이 아파트 동을 뭉개지 않았는지 브라우저로 확인(뭉개면 기준을 건물관리번호로) ② 동명동 안 목적지 검색 → 스냅을 눈으로 확인. 판정 1,281 에 영향 없음 |
| 60 | CCTV 없는 구간의 경로 가중치 — 추정 | 🟡 | CV 는 CCTV 가 있는 구간에서만 실시간 통과폭을 낸다. 없는 구간은 추정해야 하는데 **방식이 정해지지 않았다**(가상 환경 논의 중). 구간 단위로 붙일 수 있는 재료 — 단속이력 두 판(`parking_enforce`, 주소 → `navi_build` 로 지오코딩하면 구간 상습도) · 주차장 표준데이터(`parking_std`, 좌표 · 구획수) · 상가 밀도(`poi_store`) · 건축물대장(`bldg_ledger_dm`, 세대수 대비 주차대수 → 노상주차 수요) · 측량폭(NGII). 동 단위 스케일 · 사전분포 — 통계연보(`donggu_statbook`). ★ 통계연보는 동 단위라 동명동 안에서는 상수다. 전건 — R(구간 뼈대가 바뀌면 붙인 가중치가 전부 다시 붙는다) |

강제자  `tools/verify.sh`

### 1-23. 영상판정 유효거리 실측 설계

★ `MASTER §16-1` 이 광학 계산을 적는다. **여기는 그것을 재는 방법이다.**

**실험**

    한 골목에서 d = 5 · 8 · 12 · 16 · 20 · 25m 지점에 삼각대를 세우고 촬영
    같은 단면을 레이저 거리계로 1회 실측
    → 거리별 오차 곡선 → 0.3m(임계 뒤집힘)를 넘는 d 를 높이별로 확정

높이는 최소 둘(삼각대 1.5m · 연장 3~4m)을 재야 `h` 의 영향이 분리된다.
한 높이만 재면 거리 효과와 높이 효과가 섞인다.

**결과가 나오기 전에는 `CCTV_RANGE = 25.0` 을 유지한다.**

### 1-24. 촬영 계획이 높이에서 갈린다

```
구간 길이 중앙값 36.3m
h = 1.5m (유효 11.0m)  →  구간당 3~4회
h = 4.0m (유효 17.5m)  →  구간당 2~3회
```

1,281구간이면 전자는 감당이 안 된다. **높이를 올리는 것이 곧 촬영 횟수를
줄이는 것**이고, 그것이 장비 판단의 근거다.

### 1-25. ⚠ A* 를 인터뷰에 걸지 않는다

선호도 기반 피처는 인터뷰가 있어야 **가중치**를 정할 수 있다. 그러나 **구조**는
인터뷰 없이 정해진다 — 거리 · 폭 · 통행제한 · 내륜차는 전부 보유 데이터로 된다.
인터뷰를 전제로 설계하면 무산될 때 A* 전체가 뜬다. 인터뷰는 "있으면 좋은 것"이지
"없으면 못 하는 것"이 되면 안 된다.


### 1-26. 도로대장 정보공개가 부존재로 돌아왔다

두 건 모두 2026-08-24 자 **「정보 부존재 등 통지서」** 다.

    17337943   도로대장 (도로명 · 총폭 · 차도폭 · 보도폭 · 차로수 · 연장 …)
    17337939   보도(인도) 및 도로부속물 현황

사유는 둘 다 같다 — *"요청하신 자료는 정보를 취합·가공해야 하는 정보이므로
부존재 처리함"*. 근거는 정보공개법 시행령 제6조 제4항이다.

**절차 자체는 근거가 있다.** 2011년 시행령 개정으로 부존재는 비공개결정이
아니라 민원사무로 처리하게 됐고, 그래서 정보공개법상 이의신청 대상이 아니다
(행정심판·행정소송은 가능).

★ 다만 **두 건의 무게가 다르다.** 도로대장은 「도로법」 제56조 제1항이
작성·보관을 의무화하고 시행규칙 제24조 제5항이 **영구 보관**을 정한 법정
장부다. 청구 항목은 시행규칙이 정한 포함사항 중 「기하구조조서」에 들어간다.
즉 "보유하지 않는다" 가 아니라 "뽑아주기 번거롭다" 에 가깝다.

**그럼에도 이 항목에 일정을 걸지 않는다.** 이유가 셋이다 —

    ① 검증축이 이미 있다 — `ngii1k_center` 의 측량 성과 도로폭.
      우리 기하 계산과 독립이고 n=909 · 절대편차 중앙 0.32m 다
    ② 정본은 D-25 실측이다(#4). 도로대장은 세 번째 축이지 유일한 축이 아니다
    ③ 회신은 빨라야 10일이고, 부존재가 또 올 수 있다

재청구는 **가공 요청을 전부 빼고** 넣었다. 항목을 나열하면 "이 조합을 만들어
달라" 로 읽힌다. "도로법 제56조에 따라 보관 중인 도로대장 사본" 으로 좁히면
같은 사유를 쓸 여지가 없다.

★ 부존재가 또 오면 그것을 발표 논거로 쓴다. 소화전 "654 중 31 공개" 와 같은
자리다 — **소방차 진입 판정을 공공데이터로 하려 할 때 어디서 막히는가**가
이 프로젝트의 문제의식이고, 통지서 두 장이 그 증거다.

---

## 2. 아키텍처 · 모듈 경계

### 2-1. GIS ↔ 비전 — 접점은 정확히 두 개 📄

```
GIS ──[관측점 선정]──▶ 비전      어느 골목의 어느 지점을 찍을지 GIS 가 정한다
GIS ◀──[판정 결과]──── 비전      seg_uid + 통행폭 + 시각
```

**반대 방향이 없다는 것이 핵심이다.** GIS 도로폭은 호모그래피 입력으로 들어가지
않는다. 반환 스키마의 정본은 `MASTER §19` 다.

### 2-2. 정적 계층 대 동적 계층 📄

| 계층 | 갱신 주기 | 내용 |
|---|---|---|
| 정적 | 월 1회 | 도형 · `width_min_m` · `verdict` · `cctv_dist_m` |
| 동적 | 수 초~분 | `current_status` 컬럼 **하나만** |

"동적"은 DB 엔진 성질이 아니라 **클라이언트 폴링 루프 + 얇은 상태 컬럼**에서
나온다. 무거운 공간조인은 정적 계층에서만 돌린다.

### 2-3. 서빙 원칙 📄

FE 든 QGIS 든 **`serving` 스키마 하나만 보고 GeoJSON 을 받는다.** 판정 로직을
프론트에서 재계산하게 하면 안 된다. 로직은 백엔드에 하나, 소비자는 여럿.

---

## 3. 폭 산출 · 판정

산출 방식 자체는 구현이 끝났고 정본은 `MASTER §3` 이다. 여기에는 남은 것만 둔다.

### 3-1. 남은 것 🟡

```
width_cov 0.5 미만 4구간    채택 소스가 구간의 절반도 못 덮었다. D-25 실측 1순위
wmax 교차로 누출            벽~벽이 46~57m 로 샌다. width_max_m 필드 신뢰 불가
소방서 대조 잔여 편차        필문대로289번길 -3.51 · 제봉로184번길 +2.02
road_side 의미               측도 구분이 아님만 확인됨
```

### 3-2. 계산 방식 교차검증 — 미구현 🟡

폭 계산은 **두 가지 방법(법선 트랜섹트 / 거리변환)으로 하고 불일치 구간을 먼저
본다.** 단일 방법만 쓰면 인접 폴리곤 경계에서 교차선이 분할돼 폭이 절반 이하로
계산되는 사고가 난다 — 실제로 210구간에서 났고 현장 답사에서야 발견됐다.

거리변환 교차검증은 아직 없다. 정사영상 388타일을 활용한 전수 검증이 다음 후보다.

### 3-3. 판정 오류의 비대칭 📄

| 오류 | 결과 | 비용 |
|---|---|---|
| **미탐** (막혔는데 통과 가능 판정) | 소방차가 골목에 갇힌다 | **치명적** |
| **오탐** (통과 가능한데 막혔다고 판정) | 조금 돌아간다 | 경미 |

**두 오류의 비용이 100배 차이다. 임계값을 대칭으로 두면 안 된다.**
이것이 발표 방어 논리로 최강이다 — "틀릴 수 있다. 그래서 틀리는 방향을 설계했다."

⏳ 여유값 0.5m 는 실운전자 인터뷰로 검증한다(§9-1 #4).

### 3-4. 인도 포함 문제 📄

수치지형도 도로경계면은 포장면 기준이라 담장 안쪽이 안 들어간다. 동명동 도로
대부분이 좁아 인도가 물리적으로 존재할 수 없고, 인도가 있을 수 있는 5~8m
구간은 인도 2m 를 잘못 포함해도 잔여 폭이 통과선을 넘어 **판정이 뒤집히지 않는다.**

### 3-5. 근거 없는 상수에 근거 붙이기 🔴

목록과 현재 값은 `MASTER §16-2` 가 든다. 여기서는 **누가 언제 붙이는가**만 다룬다.
`CCTV_RANGE` 는 §1-23 실측에 걸려 있다. ★ 종전에 `WMAX_CAP` 이 "주석과
코드가 어긋난다" 고 적혀 있었으나 **2026-09-03 실측 결과 40m 는 저장소
어디에도 없다.** 없는 문제를 찾아가게 만드는 서술이라 지웠다. 나머지
셋은 담당이 없다.

---

## 4. 그래프 · 경로탐색

### 4-1. 가중치 설계 📄

**곱셈형으로 확정.**

```
cost = length_m × (1 + α·판정페널티 + β·점유위험도)
```

덧셈이면 짧은 위험구간과 긴 안전구간의 비교가 왜곡된다.

| 판정 | 페널티 |
|---|---|
| 통행 불가 | ∞ |
| 판정 보류 | 실측 기반 |
| 영상판정 불가 | 0.5 (불확실) |
| 통행 가능 | 0 |

점유위험도 = 단속밀도(건/100m) 정규화값.

⚠ **길이는 지오메트리에서 재계산한다.** `ROAD_LT`(도로 연장)는 **클리핑 전
원본 도로 전체 길이**를 갖고 있다. 필문대로 25m 조각에 4,200m 가 기재되는
식이다. **엣지 가중치에 절대 쓰지 않는다.**

### 4-2. A* 대 다익스트라 📄

**둘 다 한다. 비용이 0이다.** 이 규모에서 성능 차이는 무의미하다. A* 를 쓰는
이유는 속도가 아니라 발표 논거다 — "광주 전역 확장 시 탐색 공간이 커지므로
휴리스틱이 필요하다". 실행시간 비교 그래프 한 장이면 된다.

⚠ **휴리스틱이 실제 비용을 넘으면 최적해가 깨진다.** cost 에 페널티 곱셈이
들어가므로 직선거리를 그대로 쓰면 안 되고 **최소 배율로 나눠야** 한다.
여기 틀리면 A* 가 다익스트라와 다른 답을 낸다.

★ 목적지가 다수인 현재 산출(안전센터 → 전 노드)에는 A* 의 이점이 없다.
A* 가 맞는 자리는 출발 1 → 도착 1 인 네비게이션 쪽이다(MASTER §3-10).

### 4-3. 체크포인트를 엣지 속성으로 📄

소화전 · CCTV · 단속밀도를 **엣지 속성**으로 붙인다. 노드가 아니다.
경로 산출 후 "이 경로상 소화전 3개 경유"를 바로 뽑을 수 있다.

### 4-4. TMAP · 카카오 — 병합이 아니라 병렬 비교 📄

```
1. 같은 출발-도착지로 외부 API 호출  → 추천 경로 (교통체증만 고려)
2. 우리 다익스트라 호출              → 우리 경로 (통과 가능성 반영)
3. 두 결과를 나란히 제시
```

외부 API 는 링크별 혼잡도를 낱개 조회하는 구조가 아니라 완성된 경로를 반환하는
구조라, 우리 그래프의 모든 간선에 혼잡도를 채우는 방식이 성립하지 않는다.
포지셔닝과도 맞는다 — **차별점은 "그쪽이 모르는 걸 우리는 안다" 하나에 집중한다.**

### 4-5. 미결정 🟡

| 항목 | 선택지 | 비고 |
|---|---|---|
| **그래프 방향성** | 무향 + "긴급차량 예외" 명시 ← 추천 / 유향 + 역주행 페널티 | **결정 안 하면 코드가 두 번 갈아엎힌다**. `D-07` |
| 클리핑 범위 확장 | 소방서~동명동 진입 경로 포함 | 미착수 |

#### 회전 반경 기준 📄

**폭 3m 골목이라도 직각 코너면 소방차는 못 돈다.** 내륜차 때문이다.
계산식과 값은 `MASTER §3-13` 이 든다.

| 상황 | 기준 |
|---|---|
| 직진 | 유효 폭 3m 이상 |
| **회전(코너)** | 회전반경 차량 중심 10m + 곡선부 폭 4~6m |
| 진입 불가 | 폭 2m 이하 |

**설계 원칙** — `간선 = 직진 폭 판정 / 노드 = 회전 가능 판정. 둘 다 통과해야
유효 경로.`

⚠ 출처가 전부 블로그·기술사 정리다. 「소방청 건축위원회 표준 가이드라인
2021.10」 원문 확보가 필요하다(§9-1 #9).

★ **적용 범위는 2026-09-01 에 닫혔다**(DECISIONS §86 · `D-08`). 양쪽 접근로
중 하나라도 `clear` 가 아닌 코너로 한정하며 새 상수를 만들지 않는다. 판정
반영은 `D-30` 이후이고, 그때 `edge_cost` 인자가 아니라 **노드 속성 사전
계산**으로 들어간다(DECISIONS §86-6).

발표 문구 — "회전 제약을 모델에 포함했고 시연 경로는 검증했다. 전 구간 적용은
코너 실측 데이터 확보 후."

강제자 없음 — 사유: 미결정 목록이다. 행 번호 참조는 `tools/plan_renumber.py` 가 따라 고친다

### 4-6. 일방통행 · 회전제한 투입 📄

데이터는 이미 파이프라인에 있다. **그래프에 투입만 하면 된다.**

- [ ] 일방통행 방향 반영
- [ ] 회전제한 87건 반영
- [ ] 비용함수 — 거리 + 폭 페널티 + 통행제한 ∞
- [ ] 대시보드용(3인칭 부감) / 네비용(1인칭 GPS) 분기

**이것 없이 A* 를 만들면 역주행 경로가 나온다.**

### 4-7. 간선 본선 + 측도 병합 🔴

필문대로289번길 52.2m. '길'인데 52m 다. 본선과 측도가 한 폴리곤이라 벽~벽을
재면 둘이 합쳐진다. 보도 레이어 커버율 13%, 30m 초과 24건 중 보도 인접은
2건뿐이라 **보도 차감으로는 못 푼다.**

→ 종전에 정보공개청구 17337943 결과를 기다렸으나 **부존재로 종결됐다**(2026-08-24 · §1-26). 재청구 중이되 이 항목의 전건으로 두지 않는다.
  본선·측도 분리는 도로대장 없이도 된다 — `ngii1k` 도로중심선(`N3L_A0020000`)이
  본선과 측도를 **다른 링크로** 갖는다. 한 폴리곤을 두 중심선으로 가르는
  방향이 도로대장보다 먼저다.

### 4-8. 네비게이션 앱 📄

- [ ] GPS 연동 (`Geolocation API`)
- [ ] 1인칭 카메라 (`maxPitch` 85, 진행 방향 기준)
- [ ] 신고 주소 입력 → 그 건물 출입구까지 경로 위 위험구간 즉시 경고

건물출입구 1,313개와 안전센터 → 출입구 최단경로는 이미 산출돼 있다.
주소 조회로 끝난다.

---

## 5. 비전 파트

### 5-1. 캘리브레이션 ≠ 호모그래피 📄

| | 캘리브레이션 | 호모그래피 |
|---|---|---|
| 대상 | 카메라 내부(렌즈) | 카메라와 지면의 관계 |
| 구하는 것 | 초점거리 · 주점 · 왜곡계수 | 3×3 행렬 H |
| 방식 | 체커보드 20여 장 | 대응점 4쌍 이상 |
| 주기 | 카메라·렌즈당 1회 | **카메라를 옮길 때마다** |

**순서 — 렌즈 왜곡 보정 → 대응점 → 호모그래피.** 왜곡을 먼저 펴지 않으면 H 를
잘 구해도 화면 가장자리에서 틀어진다. **대응점은 4쌍이 최소조건이지 권장값이
아니다.** 실무는 6~8쌍 + RANSAC.

### 5-2. 대응점을 지면에 고정한다 📄

**카메라 위치·높이·각도를 재현하려는 시도를 포기한다.** CCTV 가 아니라 CCTV
모사이고, 삼각대를 매번 들고 나가므로 카메라는 전혀 고정이 아니다.

```
관측점당 1회 (영구)   지면 영구 지물 4~6개의 실세계 상대좌표 실측
매 세션              같은 지물의 이미지 좌표만 다시 얻음 → H 재계산
```

기준 지물 후보 — 맨홀 뚜껑 모서리, 보도블록 줄눈 교차점, 전주 밑동, 연석 이음매,
배수구 격자.

**카메라가 어디 서 있든 같은 지면 점들이 프레임에 보이기만 하면 H 가 매번
정확히 나온다.** "매번 동일 위치에서 찍어야 한다"는 제약이 통째로 사라진다.
2회차부터는 1회차 기준 이미지와 특징점 매칭으로 자동 정합한다.

이 결정이 §6-2 관측점 개체화를 강제한다.

### 5-3. 오차 구조 📄

초점거리 f(px), 카메라 높이 h, 대상 거리 d 일 때 1픽셀 오차의 실제 크기다.

```
깊이 방향   d² / (f · h)      ← 거리의 제곱. 급격히 나빠진다
가로 방향   d / f             ← 거리에 비례. 완만하다
```

| 거리 | 깊이 1px | 가로 1px |
|---|---|---|
| 10 m | 0.067 m | 0.010 m |
| 20 m | 0.27 m | 0.020 m |
| 30 m | 0.60 m | 0.030 m |

> **골목을 따라 정면으로 촬영한다. 옆에서 가로지르며 찍지 않는다.**

정면이면 도로 폭이 **가로 방향**이 되어 15m 앞 차량을 5px 틀려도 오차가
0.08m 다. 옆에서 찍으면 같은 5px 이 1m 이상 벌어진다. 분모에 h 가 있으므로
**카메라를 높일수록 깊이 오차가 줄어든다.**

| 오차 요인 | 영향 | 대응 |
|---|---|---|
| 대응점 실측 오차 | 최대 | 레이저 거리계 |
| 대응점 배치 | 큼 | 화면 넓게 분산. 한 줄 배열 금지 |
| 지면 평탄 가정 위반 | 큼 | 경사 골목 회피. 관측점 선정 시 고려 |
| 렌즈 왜곡 미보정 | 중 | 체커보드 선행 |
| 카메라 높이 오차 | 중 | 삼각대 고정이면 설정값이라 작다 |

### 5-4. 촬영 체크리스트 📄

| # | 항목 | 이유 |
|---|---|---|
| 1 | 줌 금지, 1배 고정 | 초점거리가 바뀌면 캘리브레이션 무효 |
| 2 | 광각(0.5배) 금지 | 왜곡이 커 가장자리 크게 틀어진다 |
| 3 | 렌즈 왜곡 보정 먼저 | 체커보드 20여 장, 카메라당 1회 |
| 4 | 대응점을 넓게 분산 | 몰리거나 일직선이면 H 불안정 |
| 5 | 4점보다 많이 + RANSAC | 이상치 자동 제거 |
| 6 | 근경~중경에서 측정 | 원경은 거리 제곱 때문에 오차 폭발 |
| 7 | 삼각대 필수 · 손떨림보정 OFF | 조금만 움직여도 H 무효 |
| 8 | 평평한 구간 선택 | H 는 평면 가정 |
| 9 | 줄자를 지면에 펴놓고 함께 촬영 | 사후 검증용 정답지 |
| 10 | **사진 메신저 전송 금지** | EXIF 소실·리사이즈 → 초점거리 증발 |

### 5-5. 유효폭 계산 📄

**뺄셈 방식("GIS 도로폭 − 감지 차폭") 미채택.** 오차가 두 군데에서 이중
누적된다. 대신 **호모그래피 변환 좌표계 위에서 세그멘테이션 결과를 직접
측정**한다.

- YOLO11-seg. COCO `{2:car, 3:motorcycle, 5:bus, 7:truck}`
- **세그멘테이션 필수.** bbox 는 비스듬한 차량에서 폭을 크게 과대추정한다
- **바닥 접지선 기준** — 마스크 최하단 행의 좌·우 끝
- 한계 — 사이드미러 등 뜬 부분은 못 잡는다. 안전마진이 흡수하도록 설계한다

집계 규칙과 함정은 `MASTER §19-5` 가 정본이다.

### 5-6. DL 투입 지점 🟡

단일 사전학습 모델 하나로는 "가져다 쓴 것"이라 주인공이 못 된다.

| 우선 | 항목 | 억지 없음 정도 |
|---|---|---|
| 1 | **프라이버시 파이프라인** (번호판·얼굴 마스킹) | 없으면 프로젝트가 법적으로 성립 안 함 |
| 2 | **주정차 대 주행 판별** (시간축) | 아키텍처가 이걸 요구한다 |
| 3 | **야간 강건성** (저조도 향상·도메인 적응) | 실제 문제에서 출발 |
| 4 | 소방관 선호 경로 학습 | 인터뷰 성공이 선행 조건. 리스크 큼 |

#### 프라이버시는 엣지에서

**원본이 서버로 넘어가는 순간 개인정보를 "수집"한 것이 된다.** 엣지에서 마스킹
후 전송하면 서버는 애초에 개인정보를 보유하지 않는다. 발표에서 이 논리 하나로
개인정보 질문을 막을 수 있다.

#### 주정차 판별은 시간축으로

**가상 중앙선은 쓰지 않는다.** 골목엔 중앙선 개념이 없다. 정답은 **연속
프레임에서 정지 = 주차**다. 이것이 아키텍처를 강제한다 — **mock 서버가 단일
이미지가 아니라 프레임 시퀀스를 반환해야 한다.**

```
관측점에서 삼각대 고정 → 30초~1분 동영상 1개
→ 프레임 추출 + 타임스탬프 → mock 서버가 순서대로 반환
```

주행 차량은 5초면 화면을 벗어난다. 30초면 충분하다. **"24시간 연속 관측
불가"와 "시퀀스 확보 불가"를 혼동하지 않는다.** 전자만 사실이다.

#### 답사 1회로 셋을 해결한다

**같은 관측점을 오전·오후·야간 3회 촬영한다.** "14시 통과 가능 / 19시 불가"는
서비스 필요성 최강 소재이고, 관측 신선도(§7-3)와 야간 성능 데이터가 함께 나온다.

### 5-7. 상습주차 추정 모델 📄

단속 이력이 레이블이다.

```
학습   도면 특징(도로 폭 · 벽 사이 폭 · 상가 밀도 · 건물 층수 ·
       주차장까지 거리 · 도로 위계 · 출입구 밀도) → 단속 발생 빈도
적용   CCTV 없는 구간의 상습도 예측
검증   답사 촬영 · 소방서 인터뷰
```

⚠ **시간대는 쓸 수 없다.** 14시가 39.1% 로 입력 기본값이 오염돼 있다.
구간별 빈도만 쓴다. **출동 시점의 상태가 아니라 사전 위험도다.**

⚠ **단속 이력에 좌표가 없다.** `위반장소명` 이 도로명주소이므로 도로명으로
조인한다. 단 절반이 랜드마크 명칭이라 필터가 필요하다.

---

## 6. 실측

### 6-1. 실측 프로토콜 📄

**2026-08-06 답사는 GPS 를 기록하지 않아 대조에 쓸 수 없다.** 세그먼트
중심선과 최대 23m 어긋났고 그날 산출된 소스별 오차 수치는 전부 무효다.
같은 실수를 반복하지 않기 위한 필수 항목이다.

```
1. seg_uid + 관측점 ID
2. ★ 기준 지물 서술 — "서쪽 끝에서 세 번째 맨홀 중심"
3. GPS 좌표 (보조. 지물이 우선)
4. 기준 사진 (기준 지물이 프레임에 보이게)
5. 측정 종류 — 벽~벽 / 연석~연석 / 통행폭 / 코너폭
6. 시각 · 날씨 · 조도
7. 삼각대 높이 · 방위 · 촬영 설정
```

#### ⚠ GPS 로는 지점 특정이 안 된다

도심 골목 폰 GPS 오차 **5~15m**, 골목 폭 **3m**.

| GPS 로 되는 것 | 안 되는 것 |
|---|---|
| 어느 세그먼트 위인가 | 세그먼트 내 어느 지점인가 |

**지점 특정의 해법은 지물 기준이다.** 기준 지물을 적고 그것이 프레임에 보이게
찍으면 재방문 시 재현된다.

#### 장비 📄

- **레이저 거리계** (20~30m급) — 확보 완료
- 30m 줄자 (백업). ⚠ 줄자 U자 처짐 오차는 5m 구간에서 수십 cm 다.
  **안전마진(0.5m)보다 오차가 크면 실측이 무의미하다**
- 반사 대상이 없을 때는 A4 클립보드·흰 폼보드를 팀원이 들고 선다
- **차량 간 거리는 차체가 아니라 지면 마킹 간 거리를 잰다.** 곡면이라 레이저가 튄다

### 6-2. 관측점을 GIS 1급 개체로 📄

지금 설계에 **"카메라를 세우는 지점"이라는 실체가 없다.**

```json
{
  "obs_id": "OBS-001",
  "seg_uid": "DM-192942-283921-YM7N",
  "geom": [x, y],
  "landmark_desc": "장동로 서쪽 끝에서 세 번째 맨홀",
  "ref_points": [
    {"id": "R1", "desc": "맨홀 북동 모서리",   "local_xy": [0.0, 0.0]},
    {"id": "R2", "desc": "보도블록 줄눈 교차", "local_xy": [2.4, 0.0]}
  ],
  "ref_image": "obs001_ref_20260810.jpg",
  "sessions": [
    {"at": "2026-08-10T09:30", "video": "...", "H": [[]], "illum": "day"},
    {"at": "2026-08-10T19:40", "video": "...", "H": [[]], "illum": "night"}
  ]
}
```

따라오는 것 — 세션 간 H 재계산 / 시간대별 통행폭 비교 / **관측 커버리지
레이어**(회색 구간 중 어디를 실제로 덮었는지) / 발표에서 정량 제시.

**관측점으로 CCTV 좌표를 그대로 쓰지 않는다.** 시나리오 경로 위에서 골라야 한다.

강제자 없음 — 사유: 수치는 흡수-2 실측값이다(DECISIONS §170). 판정 수 · CCTV · 소방청 지정은 docnum_check 가 대조하고 나머지는 대조 도구가 없다

#### 실측 지점 선정 — 데이터로 뽑는다

손으로 고르지 않는다. 손으로 고르면 근거가 안 남는다.

| 묶음 | 기준 | 산출 |
|---|---|---|
| 촬영 | `needs_cv` + `cctv_dist_m` ≤ 25 + `route_usage` 상위 | `tools/desk_check.py --track shoot` |
| 폭 검증 | `width_disagree_m` 상위 | `--track disagree` |
| 결손 확인 | `width_max_m` 결손 — 막을 근거가 하나뿐인 92건 | `--track wmax` |
| 형상 확인 | 도로경계 폴리곤 밖 37구간 | `uncovered_units.json` |

`width_cov` 가 낮다는 것은 **그 소스가 못 잰 구간이 있는 채로 판정했다**는
뜻이다. 숨기지 않고 필드로 드러냈으니 실측 우선순위로 쓴다.

### 6-3. 실측 보조 앱 📄

**네이티브 앱 금지.** 기존 지도 HTML 에 얹는다.

| # | 기능 |
|---|---|
| 1 | 현재 GPS + 최근접 세그먼트 자동 판별 |
| 2 | 관측점 등록 → 좌표·시각·`seg_uid` 자동 기록 |
| 3 | 사진 첨부 + **기준 지물 서술 입력(필수 필드)** |
| 4 | 측정값 입력 |
| 5 | JSON 내보내기 |

**전제** — HTTPS 가 아니면 `navigator.geolocation` 이 동작하지 않는다.

**설계 원칙** — GPS 는 보조다. 가장 중요한 입력은 **기준 지물 서술과 기준
사진**이다.

### 6-4. 호모그래피 기준점 실측 — ArUco 보드 🟡

**배포될 방법은 마커를 쓰지 않는다.** 세그멘테이션이 검출한 도로 경계선을
수치지형도 도로경계 폴리곤에 맞춰 H 를 푼다. CCTV 104지점에 보드를 깔 수는
없으므로 처음부터 이 방법이어야 한다.

문제는 **그 방법의 오차를 아무도 모른다**는 것이다. 지도 기준점으로 H 를 풀고
그 H 로 잰 폭을 지도 폭과 비교하면 순환논증이다. 소방서 7구간에서 이미 겪은
병이다 — **게이트로 쓴 자료는 검증 수단이 아니다.**

**ArUco 보드는 지도 밖에서 온 자다.** 3~5곳에서 한 번 재고 끝낸다.

#### 세 방법을 같은 골목에서 비교한다

```
방법 A   지도 기준점으로 H        ← 배포될 방법
방법 B   ArUco 보드로 H           ← 독립 기준
방법 C   레이저·줄자 실측          ← 절대 기준

|A − C|  배포 방법의 절대 오차     ← 발표에서 말할 숫자
|A − B|  지도 기준점 자체의 오차
|B − C|  보드 방식의 오차. B 가 A 보다 크면 대조 자체가 무의미하다
```

D-25 와 같은 현장·같은 동작이다. **두 번 나가지 않는다.**

#### ⚠ 눕힌 마커는 10m 를 못 넘는다

노면에 눕힌 마커는 얕은 시선각에서 깊이 방향이 짓눌린다.

```
h=1.5m, d=15m → 시선-지면각 5.7° → 깊이 축소율 0.10
150mm 마커 = 가로 32px · 깊이 3px      ArUco 검출 불가
```

인쇄 크기를 키워도 축소율이 그대로 곱해진다. **크기로 못 이긴다.** 실용 한계는
대략 `d < 5~8h` 이며 삼각대 1.5m 면 10m 내외다.

★ 따라서 **근경 10m 는 보드, 원경 10~25m 는 지도**로 나눈다. 원경에서는 골목
양쪽 도로경계선이 평행하다는 것과 그 실간격을 수치지형도에서 읽을 수 있다는
것이 소실점까지 구속한다. 마커보다 조건이 좋다.

#### 보드 사양

| | |
|---|---|
| 재질 | **하드보드지.** 종이가 휘면 평면성이 깨지고 코너가 어긋난다 |
| 크기 | A2 이상. 마커 4개 이상이 한 장에 인쇄된 단일 보드 |
| 인쇄 | **무광.** 코팅·비닐 금지 — 노면 반사광에 마커가 날아간다 |
| 사전 | `cv2.aruco` DICT_4X4_50, 마커 150mm, 간격 인쇄값 기록 |

★ 마커 4장을 흩어 놓고 6거리를 줄자로 재지 않는다. 그러면 ArUco 의 가장 큰
값(기하가 인쇄로 확정됨)을 버리고 측량 공수만 남는다.

#### 현장 절차 — 관측점당 40분

```
1. 삼각대 고정. 높이 기록. 이후 건드리지 않는다
   AE/AF 락 · 손떨림보정 OFF · 표준 1x · 4K
2. 보드를 3 · 5 · 8 · 10 · 12 · 15m 로 옮기며 촬영
   ── 어느 거리에서 검출이 깨지는지 기록한다. 그 값이 이 카메라의 유효거리다
3. 검증점 — 5 · 12 · 20m 노면에 분필 십자. 좌우 폭을 레이저로 실측
   ★ 이 점들은 H 산출에 쓰지 않는다
4. 지도 대응점 — 도로경계 좌우, 교차점, 건물 모서리가 프레임에 보이는지 확인
5. 기준 사진 · 방위 · 시각 · 날씨 · 조도 (§6-1 필수 항목)
```

#### ★ 검증점을 H 산출에서 반드시 뺀다

H 를 푼 점으로 오차를 재면 잔차가 0 이다. 자기 자신에 적합시킨 값이다.
이 조건이 없으면 그 숫자는 소방서 대조와 같은 성질이 된다.

#### 보고 형식

점추정으로 말하지 않는다. 3~5곳은 표본으로 적다.

```
5개 골목 · 검증점 24개 · 중앙 오차 6cm · 최대 14cm · 유효거리 11m
```

표본 수와 최대값을 같이 밝히는 형태만 방어된다.

#### 이 절이 성립하지 않아도 시스템은 돈다

보드가 없으면 "오차가 얼마인지 모른다"가 될 뿐이다. 발표에서 숫자를 말할
것인가 "정확도는 D-25 이후"라고 말할 것인가의 차이이며, 하드보드지 한 장과
반나절의 값이 거기 있다.

### 6-5. 안전 — 타협 대상 아님

- **오전 9~11시 고정.** 8월 광주 오후 아스팔트 답사는 실제로 위험하다
- 야간 답사는 별도 일정
- 도로 한복판 실측 시 **2인 1조, 1명은 차량 감시**

### 6-6. 실측 표본 트랙 — 섞지 않는다 🔴

```
A 12구간   width_src × 폭대역 층화 무작위    편향 추정용
B  8구간   임계 근접 + 상대불일치            판정 교정용
C  5구간   단순 무작위 · 봉인                외부 검증용
```

**A 와 B 를 합쳐서 편향을 추정하면 추정치가 망가진다.** B 는 "소스끼리 값이
다른 곳"만 고른 표본이라 그 오차 분포는 전체 분포가 아니다.

★ 불일치 기준은 **절대값이 아니라 상대값(35%)** 이다. 절대 1.0m 로 잡으면
20m 도로의 10% 오차는 통과하고 3m 골목의 10% 오차는 탈락한다.

**C 는 열지 않는다.** 소방서 7구간이 이미 게이트가 되었으므로 현재 외부 검증
수단은 0개이고 C 가 그 자리를 대신한다. 회차마다 2~3구간씩 봉인 몫을 추가하되
분석에는 쓰지 않는다.

### 6-7. 오차를 줄이는 순서 📄

```
1회차   트랙 A 12구간 → 소스별 편향 b, 산포 σ 추정
2회차   그 모델로 트랙 B 를 미리 예측하고 나간다 → 실측과 비교
3회차   안 맞는 구간의 공통점을 찾아 모델 개선
```

**2회차가 핵심이다.** 예측을 적어놓고 재면 맞았는지가 현장에서 갈린다.
오차를 줄이는 것이 아니라 **오차 모델이 맞는지 검증하는 것**이다.

### 6-8. 네이버 지도 준-실측 가능성 🟡

현장 실측 25구간 75점으로 1,281구간을 덮지 못한다. 네이버 지도의 거리 측정
도구를 준-실측으로 쓸 수 있으면 전수 대조가 가능해진다.

표본 46구간을 폭 대역별로 층화해 뽑았다. 판정 임계(3.0 / 7.0m) 근처를 두껍게
하고, 12m 이상은 재봐야 판정이 안 바뀌므로 얇게 한다.

**판단 기준**

```
|중앙 편차| < 0.3m · 표준편차 < 0.5m   전수 대조 가능
중앙 편차는 크나 산포가 작다            계통오차. 보정 상수로 잡힌다
산포가 크다                            못 쓴다. 순서형 참고로만
```

★ 어느 쪽이든 레이저 실측을 없애지 않는다. 네이버도 지도이고 우리 소스와 같은
국가 데이터에서 파생됐을 수 있다. **실측 규모가 줄어드는 것이지 없어지는 것이
아니다.**

상태 — 표본 생성 완료. 측정 미착수.

강제자 없음 — 사유: 수치는 흡수-2 실측값이다(DECISIONS §170). 판정 수 · CCTV · 소방청 지정은 docnum_check 가 대조하고 나머지는 대조 도구가 없다

---

## 7. 시나리오 · 평가

### 7-1. 시나리오는 손으로 고르지 않는다 — 그래프에서 역산 📄

```python
for (출발, 도착) in 조합:
    d1 = 최단경로(제약 없음)
    d2 = 최단경로(통행 불가 제외)
    if d2 / d1 > 1.5 and d2 < inf:
        후보에 추가
```

**"우리 시스템이 없었으면 소방차가 갇혔을 출발-도착 쌍"을 자동으로 찾는
것이다.** 시나리오 선정 자체가 GIS 산출물이 되고, "임의로 고른 것 아니냐"는
질문을 막는다.

출발점은 대인·지산 119안전센터다. **선정 조건** — 코너 통과 가능 / CCTV 커버
구간 포함 / 우회로 존재.

### 7-2. 평가지표 — 설계 완료, 산출 미착수 🟡

> **우리 시스템이 잘 작동한다는 걸 무엇으로 증명하나.**

세 회의 연속 미결이었던 이유는 담당자가 없어서가 아니라 **"실측 없이는 정확도를
못 낸다"에 나머지 지표가 전부 묶여 있었기 때문**이다. 갈라 적으니 셋은 지금 낼
수 있는 것이었다.

| ID | 지표 | 지금 | 전건 |
|---|---|---|---|
| E-1 | 진입 실패 회피율 | **가능** | 없음 |
| E-2 | 우회 비용 | **가능** | 없음 |
| E-3 | 판정 가능률 | **값 있음** | 없음 |
| E-4 | 폭 정확도 (MAE) | 불가 | D-25 실측 |
| E-5 | 재현성 게이트 | **가능** | 없음 |

E-4 는 정의만 해둔다. **빈칸으로 두지 말고 "D-25 이후"라고 적는다.** 발표에서
표의 빈칸과 "전건이 이것이다"는 완전히 다르게 읽힌다.

강제자 없음 — 사유: 수치는 흡수-2 실측값이다(DECISIONS §170). 판정 수 · CCTV · 소방청 지정은 docnum_check 가 대조하고 나머지는 대조 도구가 없다

#### 7-2-1. 원칙 셋

**적합과 검증을 섞지 않는다.** 소방서 7구간 대조는 게이트로 썼으므로 외부 검증
수단이 아니다. **이 절의 어떤 지표도 "우리 폭이 정확하다"를 주장하지 않는다.**
주장하는 것은 하나다 — *현행 방식 대비 무엇이 나아지는가*.

**결손은 관대한 쪽으로 해석하지 않는다.** 산출 불가는 **분모에서 빼지 말고
별도 항으로 보고한다.** 못 잰 것을 조용히 지우면 커버리지가 저절로 올라간다.

**지표는 값이 아니라 실행이다.** `data/processed/eval.json` 으로 떨어뜨리고
형식을 다른 대조 산출물과 맞춘다. `tools/baseline.py` 가 실행 간 자동 비교할 수
있어야 한다. **터미널에만 있는 숫자는 소실된다.**

#### 7-2-2. E-1 · 진입 실패 회피율

> 우리 시스템이 없었으면 소방차가 못 가는 길로 안내받았을 비율

```
baseline   거리만으로 최단경로 (현행 내비게이션이 하는 일)
ours       통행 불가 구간을 제외한 최단경로

E-1 = (baseline 경로가 통행 불가 구간을 1개 이상 지나는 쌍) / (전체 쌍)
```

입력 — 출발은 대인·지산 119안전센터 2곳, 도착은 동명동 건물 층화 표본,
그래프는 표준노드링크 + 회전제한이다. 산출기는 §7-1 의 의사코드가 그대로 된다.
**두 작업이 아니라 한 작업이다.**

⚠ 통행 불가 191 은 **하한이다.** 막을 근거가 하나뿐인 결손 92건 때문에 E-1 은 과소평가 쪽으로
치우친다. 그 방향을 명시해 보고한다.

#### 7-2-3. E-2 · 우회 비용

```
E-2a  d2/d1 중앙값 · 90퍼센타일       거리 증가율
E-2b  d2 = inf 인 쌍의 수             우회로가 아예 없는 목적지
```

**E-2b 가 본체다.** 증가율은 "조금 돌아간다"로 끝나지만, 도달 불가는 "소방차가
못 간다"다. 발표에서 지도에 찍을 것도 이쪽이다.

⚠ 영상판정 불가 399 를 어느 쪽으로 셀지 정해야 한다. **세 가지 다 낸다** —
낙관(통과 가능 · 하한) · 비관(통과 불가 · 상한) · 현행(지금 지도가 하는 일).
**하나만 내면 고른 이유를 방어해야 하고, 셋을 내면 폭이 논거가 된다.**

⚠ 시간 환산은 하지 않는다. 골목 주행속도 근거가 없다. 거리만 쓴다.

#### 7-2-4. 표본 설계

전수는 계산이 무겁고, 무작위는 "왜 그 쌍이냐"를 못 막는다. **층화한다.**

| 층 | 기준 | 왜 |
|---|---|---|
| A | `route_usage` 상위 | 실제 동선. 579구간이 0 초과 |
| B | `nfa_designated` 인접 | 소방청 지정 기준 충족 구간 |
| C | 영상판정 불가 밀집 | CCTV 사각의 실제 영향 |
| D | 무작위 | 층화가 만든 편향의 대조군 |

D 를 반드시 넣는다. 위험한 곳만 골라 놓고 "위험하다"는 결론을 내면 그것은
지표가 아니라 순환논증이다. 표본 수·시드는 `eval.json` 에 박는다.

#### 7-2-5. E-3 · 판정 가능률

이미 값이 있다. 재산출만 하면 된다.

```
영상판정 불가        399 / 1,281      (31%)
연장 기준            17,168m / 58,309m  (29%)
CCTV 유효범위 밖      830 / 1,281      (65%)
```

**이 숫자를 우리가 먼저 꺼낸다.** 발표에서 가장 먼저 찔릴 지점이고, 질문을 받고
답하는 것과 먼저 말하는 것은 다르게 들린다 — *알고리즘의 한계가 아니라
인프라의 한계다.* 완화 항(영상판정 불가 구간이 담당하는 통행량 18%)도 같이 낸다.

#### 7-2-6. E-4 · 폭 정확도 — 전건 D-25

```
E-4a  MAE          평균 절대오차 (m)
E-4b  중앙값 절대오차
E-4c  소스별 분해   ngii1k / silpok / ngii
```

★ **절대편차 합을 쓰지 않는다.** 표본 수에 비례해 커져서 실행 간·소스 간 비교가
안 된다. 지표로 승격할 때 **MAE 와 중앙값으로 바꾼다.** 합은 참고로만 남긴다.

전건 — D-25 실측(미착수) · 봉인 표본 트랙 C · 야장 `data/field/fieldsheet.md`.
★ 종전에 "정보공개청구 17337943 회신이 오면 부분 산출이 가능해진다" 고
적었으나 그 청구는 부존재로 종결됐다(§1-26). **전건은 D-25 실측 하나다.**
외부 회신을 지표 산출의 대안 경로로 두면, 무산될 때 지표가 통째로 뜬다
— §1-25 가 인터뷰에 대해 적은 것과 같은 형태다.

#### 7-2-7. E-5 · 재현성 게이트

지표가 아니라 **통과 조건**이다. 산출기 첫 줄에서 검사하고, 불일치면 지표를
뽑지 않고 죽는다.

```
tools/baseline.py diff <태그>                  전이 행렬이 0 이 아니면 사유 확인
data/golden/segments.fingerprint.json          지문 일치
data/processed/_manifest.json                  전 단계 정상
```

**왜 게이트인가** — 낡은 산출물을 이번 실행 것으로 착각한 값으로 지표를 뽑았다면
**틀린 숫자가 발표 자료에 들어갔을 것이고, 지표라는 이름이 붙어 있어 아무도
의심하지 않았을 것이다.**

#### 7-2-8. 남은 결정 🟡

| # | 항목 | 상태 |
|---|---|---|
| 1 | 영상판정 불가를 통과 가능으로 볼 것인가 | 미정 — §3-3 오류 비대칭과 같은 결정 |
| 2 | 표본 수와 시드 | 미정 |
| 3 | E-1 분모를 건물로 할지 노드로 할지 | 미정 |

1번의 뿌리는 **결손과 불확실을 어느 쪽으로 기울일 것인가**이며, §3-3 담당
공백이 판정과 지표 양쪽에 동시에 나타나고 있다.

### 7-3. 관측 신선도 🔴

마지막 관측이 40분 전이면 그 판정을 믿고 소방차를 보낼 수 있나.
**지금 설계에 시간 개념이 없다.**

```
신뢰도 = f(경과시간, 해당 도로 단속밀도)
30분 전 관측 + 상습주차 구간 = 사실상 영상판정 불가
```

**이게 없으면 "실시간이라면서 얼마나 실시간인가"에 답을 못 한다.**

### 7-4. CCTV 사각 구간 대책 📄

관측이 불가능한 구간에는 **사전확률**을 제시한다.

| 신호 | 출처 | 논리 |
|---|---|---|
| 단속밀도 | 단속이력 (건/100m) | "관측 불가하나 연 N건 단속되는 상습 점유 구간" |
| 상가 밀도 | 상가정보 | 상가 앞 = 주차 압력 |
| 음식점 비율 | 상가정보 (동명동 39%) | 야간 집중, 배달 이륜차 |
| 도로폭 | `width_min_m` | 좁을수록 1대만 세워도 막힌다 |

### 7-5. 소화전 📄

**"1개밖에 없다"를 단독으로 인용하지 않는다.** 분모와 함께 말한다(MASTER §3-12).
**소화전 레이어는 좌표 확보 전까지 지도에서 뺀다.** 1개짜리 지도는 안 만드느니만
못하다.

좌표 확보 시 활용 근거 — NFTC 401 수평거리 140m, NFSC 109 옥외소화전 40m.
"도로엔 못 들어가도 근처 소화전이 있으면 호스로 대응 가능"이라는 통행 불가 구간
세분화 로직이 된다.

---

## 8. 데이터 관리 · 인프라

### 8-1. processed 계층 포맷 전환 📄

**클라우드 적재 전에 해야 한다.** 지금은 텍스트·SQLite 라 저장 비용과 스캔이
낭비다.

| 현재 | 문제 | 전환 |
|---|---|---|
| `*.geojson` | 텍스트. 좌표를 문자열로 저장 | **GeoParquet** |
| `*_5186.gpkg` | SQLite. 컬럼 프루닝 불가 | **GeoParquet** (같은 파일로 통합) |
| 대용량 CSV | 텍스트 | **Parquet** |
| `dem_scope.tif` | 일반 GeoTIFF | **COG** (범위 요청 가능) |

**GeoParquet 하나로 geojson + gpkg 두 벌을 대체한다.** 좌표계가 파일에 들어가므로
`_5186` 접미사도 필요 없어진다. 메타데이터를 파일 헤더에 박는 것도 이때 같이 한다.

⚠ **`web/data` 는 GeoJSON 을 유지한다.** 브라우저가 읽어야 한다.
⚠ 여러 스크립트가 `gpd.read_file(*.gpkg)` 로 읽고 있다. 한 번에 바꿔야 하고
회귀 위험이 있다. **파이프라인이 안정된 뒤에 한다.**

### 8-2. 클라우드 적재 📄

raw 를 오브젝트 스토리지로 옮기고 CI 와 엮으면 데이터 파이프라인이 완성된다.
`paths.py` 만 원격 스킴을 받도록 고치면 되고 나머지 스크립트는 그대로다.
그래서 지금 환경변수로 분리해둔 것이다.

**다만 지금은 이르다.** 이 규모를 위해 비용과 권한·CI 설정을 감수할 단계가
아니고, D-25 실측과 A* 가 먼저다.

### 8-3. 정밀 DEM — 보류 🟡

안심구역은 **원본 반출이 안 된다.** 분석 결과만 반출 가능하며 심의 2회에
2~3주 걸린다.

★ 2026-08-25 재검토. 종전 근거는 "등판 한계(11~17°)를 넘지 않는다" 였는데
**그 11~17° 는 출처가 없었다.** KFS 5종 §4.4.1 원문이 정하는 값은 18 %
등판, 즉 **10.204°** 이고 가정했던 하한 11° 보다 낮다.

    공개DEM 90m 최대 경사   9.0°
    KFS §4.4.1 등판 한계   10.2°
    여유                    1.2°

`sources.yaml` 의 dem_public 이 "평활화로 경사가 실제보다 낮게 나온다" 고
적고 있다. 낮게 나오는 값이 한계선 1.2° 아래에 있으므로 배제 결론의 근거는
얇다. 결론을 지금 뒤집지는 않되, **보류 판단은 이 숫자를 근거로 다시 세운다.**

### 8-4. publish 선언화 🟡

`publish_web.py` 에 소스마다 발행 블록이 손으로 쌓여 있다. `sources.yaml` 의
`outputs` 에 발행 플래그를 두면 루프로 돌릴 수 있다. 마커 쪽과 같은 방식이다 —
**선언을 대장에 두고 코드는 실행만 한다.**

### 8-5. `web/data` 생성물 커밋 🟡

산출물이 추적되므로 매 실행 diff 가 크다. "UI 담당이 raw 없이 작업 가능해야
함"이 근거이며, 그 근거는 벡터 GeoJSON 여섯 종에 대해서만 성립한다.

★ **범위가 근거를 넘어섰다.** `.gitignore` 40행 `!web/data/**` 는 벡터와
래스터를 구분하지 않는다.

    정사영상 타일   z15~z19   1,423장
    음영기복 타일   z12~z15      22장
    타일 소계                 23.6MB / 저장소 36.0MB

타일은 `ortho.py` · `terrain.py` 가 재생성하며 UI 작업에 원본을 요구하지
않는다. 발행은 `pages.yml` 이 맡으므로 빌드 산출물로 옮긴다. 벡터 GeoJSON
은 지금 근거 그대로 유지한다.

**포맷 전환(§8-1)을 기다리지 않는다.** 전환 대상은 `processed` 계층이고
타일은 그 대상이 아니다. `DECISIONS §124`

### 8-6. 대장 · 매니페스트 격차 🟡

| 항목 | 현재 | 목표 |
|---|---|---|
| raw 명명 | `normalize_raw.py` 로 raw 안에서 개명 | `norm/` 계층 분리 (`DECISIONS §123`) |
| 매니페스트 | 원본 sha · 건수 · 상태 | `git_sha` · `git_dirty` · `input_shas` · `params` |
| 스키마 검사 | 컬럼 집합 대조 도입됨 | 필드 설명까지 대장에서 생성 |
| GeoJSON 메타 | 불가 | GeoParquet 전환 시 헤더 (§8-1) |
| 복원 훈련 | 미실시 | 월 1회 |

**작업량은 하루 안쪽이다.** 대부분 이미 있는 것을 확장하는 것이다.

---

### 8-7. 저장소 위생 🟡

**★ 2026-08-26 재판정.** 세 항목을 "하류 없음"으로 묶었으나 실제로 대조하니
둘이 틀렸다. 근거 없이 묶은 것이 원인이다.

| 순 | 대상 | 판정 | 근거 |
|---|---|---|---|
| 1 | `web/data/{ortho,terrain}` 타일 | **기각** | `web/js/map.js` 가 `./data/ortho/{z}/{x}/{y}.jpg` 를 상대경로로 읽는다. `pages.yml` 은 `checkout` 한 트리를 그대로 발행하므로, 추적을 끊으면 **배포된 지도의 정사영상·지형이 사라진다.** 재생성에는 raw 정사영상 1.3GB 가 필요해 CI 가 만들 수 없다 |
| 2 | 조사 스크립트 아홉 | **둔다** | ★ 2026-09-16. `tests/test_tools_are_wired.py` 의 `EXEMPT` 가 아홉을 **조사 도구** 칸에 사유와 함께 이미 등재한다 — 상주가 아니라 분류된 거주다. 옮겨서 얻는 것이 없다(DECISIONS §162). 종전 서술 — 하류가 있다. `test_guards` 가 `route_probe` 를, `test_layers` 가 `jijeok_probe` 를, `width_fn` · `jijeok_probe` · `jijeok_review` 가 서로를 참조한다(넷). 옮기려면 임포트를 함께 고친다. ★ `jijeok_review` 머리말이 삭제된 `naver_page.py` 를 아직 든다 — 산문이라 임포트는 아니다 |
| 3 | `data/baseline/*` 전량 복사 | **둔다** | ★ 2026-09-17 판정(DECISIONS §171-3). 태그와 golden 지문은 *달라졌다* 만 말하고 *어떻게* 는 못 말한다 — `baseline.py diff` 는 피처가 있어야 돈다. 봉인 뒤 raw 가 교체되면(2026-08-15 선례 · 뼈대 재구축) 태그로 재생성할 수 없다. 한 벌 약 1.1MB 다. 소방서 대조 사본은 이제 봉인 시점 산출물을 복사한다 |

1 번을 정말 걷어내려면 타일을 저장소 밖(오브젝트 스토리지)에 두고 `map.js` 가
절대 URL 을 보게 해야 한다. **그것은 배포 인프라 결정이라 여기서 다루지 않는다.**
23.6MB 는 지금 아무것도 막고 있지 않다.

강제자 없음 — 사유: | 위생 규약은 도구 하나가 아니라 verify.sh 의 `로컬 찌꺼기`·`루트 잔재·유령 면제`·`저장소 내 일회성` 셋이 나눠 강제한다

## 9. 외부 의존

### 9-1. 소방서 정식 인터뷰 ⏳

광주동부소방서 소방행정과 **062-613-8415**

| # | 질문 | 왜 |
|---|---|---|
| 1 | **진입불가·곤란지역 지정 자료를 받을 수 있나** | 포지셔닝 1차 출처. **최우선** |
| 2 | 「2025 골든타임 확보 종합대책」 원문 | 인용 근거 |
| 3 | **중형펌프차 제원 — 전폭·전장·축거·최소회전반경** | §1 #20 |
| 4 | **현장 최소 여유공간 몇 m** | §3-3 안전마진 검증. **1차 미해결** |
| 5 | 진입 실패 사례 빈도 | 필요성 근거 |
| 6 | 경로는 누가 정하나 (상황실 / 기사) | 서비스 대상 확정 |
| 7 | 기사가 경로 고를 때 보는 요소 | 선호경로 학습 입력 |
| 8 | 동명동 출동 이력 통계 | §7-1 시나리오 현실성 |
| 9 | 야간 출동 추가 어려움 | §5-6 |
| 10 | 「소방청 건축위원회 표준 가이드라인 2021.10」 원문 | §4-5 회전 기준 1차 출처 |
| 11 | 한전에서 전주 실측 좌표를 받고 있나 | 전주 데이터 대안 |

**협조 요청** — 소방용수 589개 좌표(공개분 31개) / 소방통로확보대상 20구간의
시·종점 좌표 / 겨울 결빙 시 진입 불가 구간 / 출동 통계 / 차량 실측 허가.

**우리 산출값이 소방서 기록과 대체로 일치한다는 결과를 들고 가면 빈손으로
부탁하는 게 아니다.**

강제자 없음 — 사유: | 외부 일정이다. 인터뷰가 잡히는지는 코드가 못 본다

### 9-2. 협조 공문 ⏳

| 수신 | 부서 | 요청 |
|---|---|---|
| 광주 동부소방서 | 예방안전과 · 119재난대응단 | 차량 제원(사이드미러 포함·미포함) · 운전요원 자문 · 진입곤란 구간 현황 |
| 소방본부 종합상황실 | — | 출동 관제 절차 자문 · 산출 정보의 실무 요구사항(갱신 주기 · 표시 방식 · 신뢰도 표기 · 미관측 구간 처리) |

★ **종합상황실은 발송 전 기관명을 확인한다.** 2026-07 광주·전남 통합으로 본부
명칭이 바뀌었을 수 있다.

★ 상황실 자문은 §7-3(관측 신선도)과 §7-4(CCTV 사각 대책)의 미결을 직접 풀 수
있다. "관측되지 않는 구간을 관제에서 어떻게 다루나"는 우리가 정할 문제가 아니라
실무에서 받아올 답이다.

**인터뷰 질문지는 지인 인터뷰(현장 감각)와 정식 인터뷰(§9-1 자료)로 나눈다.**

### 9-3. 주간 멘토링 — 축을 바꿔가며 묻는다

매번 "진행 상황"만 보고하면 낭비다.

```
1주  아키텍처·데이터 계약 / 평가지표(§7-2)
2주  DL 투입 지점 타당성(§5-6)
3주  실험 설계·베이스라인
4주  발표 스토리라인
```

답을 반드시 받아야 하는 셋이다. **셋 다 기술이 아니라 판단이다.**

| # | 질문 | 왜 우리가 못 정하나 |
|---|---|---|
| 1 | 표본 3~5곳으로 1,281구간을 일반화하는 것이 심사에서 방어되나 | 통계 설계 판단 |
| 2 | 영상판정 불가 399 를 통행 가능으로 볼 것인가 | §3-3 오류 비대칭. 안전 정책 판단 |
| 3 | 외부 검증 수단 0개로 중간평가를 받는 것이 맞나 | 심사 감각 |

강제자 없음 — 사유: 수치는 흡수-2 실측값이다(DECISIONS §170). 판정 수 · CCTV · 소방청 지정은 docnum_check 가 대조하고 나머지는 대조 도구가 없다

---

## 10. 우선순위

### 10-1. 지금 막혀 있는 것

```
🔴 오류 비대칭 결정(§3-3)      ← 결손 처리와 불확실 취급이 여기 걸려 있다
🟡 평가지표 산출(§7-2)         ← 설계 완료. E-1 · E-2 미착수
🟡 CCTV_RANGE 미확인           ← 399구간(회색 전부)의 판정을 가른다
⏳ 소방차 실제 제원             ← D-30. TRUCK 3.0 의 근거 보강
⏳ 소화전 589개 좌표            ← D-30
🔴 실측 DB · 입력 화면(HTTPS)   ← 다음 실측을 막고 있는 유일한 항목
```

강제자 없음 — 사유: 수치는 흡수-2 실측값이다(DECISIONS §170). 판정 수 · CCTV · 소방청 지정은 docnum_check 가 대조하고 나머지는 대조 도구가 없다

### 10-2. 순서

**기한** — 팀 일정이다. ★ 2026-09-16 옮겼다 — 개요서 v1.6 의 MVP 09-30 을 팀이 23회 진행일지(09-16)에서 **10-08 통합 · 11-13 결과물**로 바꿨다(DECISIONS §166-5). 먼저 적은 날짜를 사유 없이 고치지 않는다.

```
2026-10-08   MVP 통합 · 최소 완료선 3골목 12횡단면
2026-11-13   결과물
```

★ **12횡단면은 `D-25` 레이저 실측에 매달려 있고 그 행은 담당이 없다(🔴).**
폭 산출·판정·지도·내비는 이미 돈다. 10-08 에 갈리는 것은 코드가 아니라 현장이다.
실측이 안 붙으면 MVP 는 **"미검증 폭으로 전수 판정"** 까지이고, 그 사실을 발표에서
먼저 밝힌다(MASTER §15-2). 날짜를 넘기면 이 절에 사유를 먼저 적고 날짜를 옮긴다.

```
STEP 1  경계 확보 · 폭 산출                       완료
STEP 2  그래프 구축 + 다익스트라 / A*              다익스트라 완료 · A* 미착수
        → 일방통행·회전제한 투입 · 시나리오 자동 탐색
STEP 3  관측점 선정 → 기준점 실측 → 3회 촬영
        → 선정 기준은 §6-2 에 데이터로 정의됨
STEP 4  mock 서버 / 프라이버시 파이프라인 / 야간 강건성
STEP 5  평가지표 산출 → 발표 스토리라인
```

**의존 관계** — `경계 → 노딩 → 폭 → 그래프 → 시나리오 → 관측점 → 촬영`.

**시나리오 자동 탐색이 답사보다 먼저다.** 순서가 꼬이면 엉뚱한 골목을 찍고
답사가 통째로 날아간다.

강제자 없음 — 사유: 외부 일정 서술이다. 기한 도래는 사람이 본다

### 10-3. 병렬로 돌릴 수 있는 것

- 평가지표 산출 E-1 · E-2 (§7-2) ← 정의는 끝났다
- 소방서 인터뷰 전화(§9-1)
- 그래프 방향성 결정(§4-5) ← 안 정하면 코드를 두 번 갈아엎는다
- mock 서버 프레임 시퀀스 구조 설계
- 계약 테스트 확충 — 폭 상한 · 도로명 규칙 위반 상한 · 조용한 결측

---

## 11. 담당 공백

| 항목 | 담당 |
|---|---|
| 평가지표·베이스라인 (§7-2) | 설계 완료 · 산출 담당 없음 |
| 오류 비대칭·안전마진 (§3-3) | 없음 |
| 프라이버시 파이프라인 (§5-6) | 없음 |
| 시나리오 설계 (§7-1) | 없음 |
| **관측점 관리 (§6-2)** | **없음** ★ GIS/비전 경계 |
| 실측 보조 앱 (§6-3) | 없음 |
| 근거 없는 상수 (§3-5) | 없음 |

**GIS 담당에게 부하가 몰려 있다**(데이터 확보 + EDA + 그래프 + 인프라).
혼자 다 하면 병목이 되고, **검증 없이 확정하는 상황이 반복된다.**

**회의에서 재분배할 것.**

---

## 12. 기획서 갱신 대상

`docs/proposal.docx` 는 시제 규칙 밖이지만 **문서 넷 중 유일하게
외부가 읽는 것**이라 어긋나면 비용이 가장 크다. 아래는 현재 산출물과 어긋나
있는 부분이다. 갱신 후 이 절에서 지운다.

★ 2026-09-01 4차. **닫힌 표에서 셋이 새로 나왔다**(§12 #13 · §12 #14 · §12 #15). 전부
숫자가 아니라 **모델**이 어긋난 것이라 `docx_check` 도 `test_docx_targets` 도
구조적으로 못 잡는다 — 전자는 문서에 있는 숫자를 대조하고 후자는 **이 표가
지목한 것**만 본다. 아무도 안 적은 것은 양쪽 다 못 센다.

★ 4차에서 둘은 **지목이 틀렸다.** 기획서의 `YELLOW = needs_cv + CCTV 있음` 은
MASTER §2-1 과 같은 모델이라 고칠 것이 없었고, "교차로 반경 5m" 는 폐기값이
아니라 폴백으로 살아 있어 서술 보강으로 끝났다. **선언을 보고 대책을 정하지
않는다**(DECISIONS §78-2).

★ 2026-08-31 3차. **열 건 전부 닫혔다.** 2차에서 넷 — 하나는 실제 수정(§12 표 4 · 4a번),
**셋은 "고치라고 적힌 것이 문서에 없었다"**(§12 표 6 · 11 · 12번 — `#` 를 뗐다. §1 참조로 읽혀 재배번에 끌려갔다).

★ **이 표에 강제자가 없어서 생긴 일이다.** `docx_check` 는 문서에 있는 숫자를
산출물과 대조하지만 **없는 것은 못 센다.** 방향을 뒤집는 검사를 붙였다 —
`tests/test_docx_targets.py` 가 이 표의 지목이 문서에 실재하는지 본다.

★ 종전 1차. 열 건 중 다섯을 처리했다. `tools/docx_fix.py --write` 가 숫자·용어를
고치고 `tools/docx_check.py` 가 산출물과 대조한다. **남은 것은 도구가 못 고친다** —
숫자가 아니라 서술과 모델이 어긋나 있어 사람이 문장을 다시 써야 한다.

| # | 기획서의 서술 | 바꿀 것 | 상태 |
|---|---|---|---|
| 15 | **[그림 13] 이미지가 옛 방식을 그린다** | 🟡 | 캡션은 #13 에서 고쳤으나 **그림 자체가 여전히 반경 5m 원을 그린다.** 이미지 재생성이 필요하며 도구가 못 고친다. 발표 전까지 처리한다 |
| 20 | **`원천 11종` 이 여섯 자리에 있다** | 🟡 | **2026-09-02 정체가 밝혀졌다** — `§ 원천 데이터 목록` 표(`T16`)가 헤더 1 + 데이터 11행이고, `11` 은 **그 표의 행 수**다. 오타도 소실도 아니고 표 자체가 낡았다(대장 43종). 파급이 여섯이다 — `[그림 9]` 캡션 · 심사 논거(`T6R1` "원천 데이터 11종이 대장으로 관리되고") · 일정(`T8R1`) · `REQ-DAT-001`("11종 원천의 수집·압축해제·정규화") · 산출물(`T54R2`) · 이력(`T55R1`, 2026-08-09 기록이라 그대로 둔다). ★ **표를 43행으로 늘리는 것은 발표 문서로 못 쓴다** — `T16` 을 "주요 원천" 발췌로 성격을 바꾸고 나머지 넷의 수를 문맥에 맞게 고친다. 사람이 문장을 봐야 하므로 도구가 못 고친다 |
| 21 | `산출단위 1,102` 두 곳 | 🟡 | 정본은 1,101 이다. `docx_check` 가 `구간` 뒤에 붙은 수만 봐서 **`산출단위` 로 적힌 것을 비껴갔다** — 이 도구의 머리말이 바로 이 숫자를 만들어진 이유로 든다(DECISIONS §92). 어휘를 넓혔으므로 이제 잡힌다. `docx_fix --write` 로 고친다 |  <!--stale-ok-->
| 22 | 수치 표기 원칙에 날짜가 박혀 있다 | 🟡 | *"본 문서의 모든 수치는 2026-08-14 기준으로"* 라고 적는데 표지 최종 수정일은 2026-09-02 다. 그 사이에 소화전(`DECISIONS §49`) · 회전 판정(`DECISIONS §86`) · 차량 제원 대장이 바뀌었다. `MASTER §15-3` 에는 **날짜가 없다** — 정본이 산출물이라는 것이 그 절의 요지이고 날짜를 박으면 그 날짜가 낡는다. 날짜를 뺀다 |

★ **숫자가 맞아도 모델이 다르면 더 위험하다.** `docx_check` 는 판정 4종과 구간
수를 대조해 초록불을 냈지만, `width_min_m` 의 정의와 tier 임계가 어긋난 것은
보지 못했다. 외부 독자는 필드 표를 보고 구현을 상상한다.

★ **기획서에는 하한과 미검증을 반드시 함께 적는다.** 말하지 않을 것의 목록은
MASTER §15-2 가 정본이다.

강제자  `tools/docx_fix.py`
