# ETL 파이프라인 최소 의존성 — 검증 완료 버전 고정 (2026-08-09)
#
#   pip install -r requirements-etl.txt
#
# 전체 스택(FastAPI/DB/YOLO 포함)은 pyproject.toml 참조.
# 이 파일은 "GIS 데이터만 만지는 사람"이 최소한으로 설치할 목록이다.

geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.13.0
pandas==3.0.2
numpy==2.4.4
networkx==3.6.1
PyYAML==6.0.3
