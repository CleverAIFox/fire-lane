# ★ 2026-09-24 (PLAN §13 W13-8). `engines` 위반을 **경고가 아니라 실패**로 만든다.
#   종전에는 `tools/navi_env.py` 가 semver 범위를 손으로 평가해 그 일을 했는데,
#   하이픈 범위(`18 - 22`)를 오판하고 모르는 문법을 통과로 셌다. 표준 의존성이
#   할 수 있는 일을 손으로 짜는 것이 이 저장소가 4족이라 부르는 결함이다.
#   npm 이 정본이고 `navi_env` 는 **잠금 ↔ 설치물** 축만 남는다.
engine-strict=true
