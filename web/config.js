/* Fire-Lane · 설정 계층
   ─────────────────────────────────────────────────────────────
   ★ GIS 담당과 UI 담당이 함께 보는 유일한 파일이다.
     여기 값을 바꾸면 지도 전체에 반영된다. 다른 파일에 같은 값을 복사하지 말 것.

   ★ 임계값(3.0 / 7.0 / 25.0)은 산출 파이프라인의 값과 반드시 같아야 한다.
     정본은 src/firelane/seg/params.py 다. 여기 값은 화면 설명용 사본이며,
     바꿔도 판정 결과는 안 바뀐다. 파이프라인을 먼저 고치고 여기를 맞출 것.
   ───────────────────────────────────────────────────────────── */

const CONFIG = {

  /* V-World 배경.
     &domain= 이 등록 URL 과 문자열까지 같아야 타일이 나온다.
     WMTS 축 순서는 {z}/{y}/{x} 다. {z}/{x}/{y} 로 쓰면 타일이 어긋난다. */
  vworld: {
    key    : "95E47552-4B32-36AB-885B-E9DC3FCB5352",
    enabled: true,           // 2026-09-01 woongtopia.github.io 등록 완료
  },

  /* 판정 색상. style.css 의 --blocked 등과 같은 값을 유지할 것.
     ★ color = 다크 모드용, lightColor = 라이트 모드용.
       다크용 색은 검은 배경에 올리려고 밝고 채도 높게 고른 것이다. 흰 배경에
       그대로 쓰면 '밝은 것 위의 밝은 것'이 되어 대비는 떨어지고 채도만 남아
       눈이 아프다.
     ★ 그렇다고 style.css 의 패널용 색(#0f8a55 등)을 그대로 가져오면 안 된다.
       그건 작은 글씨용이라, 화면 절반을 덮는 굵은 도로선에 쓰면 너무 무겁고
       주황·회색은 지면에 묻혀 4색이 사실상 1색이 된다(2025-08 실패 사례).
       아래 값은 배경을 #dfe3ea 로 낮춘 상태에서 네 색이 모두 대비 2.4~3.5,
       색상(hue)도 서로 충분히 떨어지도록 다시 고른 것이다. */
  verdict: {
    blocked : { color:[255, 77, 61], lightColor:[224, 53, 38], label:"통행 불가",
                desc:"벽 사이 폭 3.0m 미만. 주차 차량이 없어도 소방차가 못 지나간다" },
    needs_cv: { color:[255,171, 46], lightColor:[212,124, 10], label:"판정 보류",
                desc:"도면만으로 결론이 안 난다. 영상판정(호모그래피) 대상" },
    clear   : { color:[ 74,209,143], lightColor:[ 26,150, 96], label:"통행 가능",
                desc:"도로 폭 7.0m 이상. 양쪽에 주차가 있어도 통과. 영상판정 불필요" },
    unknown : { color:[ 90, 98,114], lightColor:[110,120,138], label:"영상판정 불가",
                desc:"CCTV 유효범위 25m 밖. 영상판정 자체가 성립하지 않는다" },
  },

  /* CCTV 커버리지 원. 색은 마커와 맞추고, 농도는 배경 밝기에 따라 다르게 간다. */
  /* CCTV 노랑. 마커·커버리지 원·간판이 이 한 값을 함께 본다.
     ★ app.js 의 makeCctvImage() 도 여기서 원판색을 가져간다. 세 군데에
       같은 색을 적어두면 반드시 한 곳만 고치고 잊는다.
     ★ 감시카메라 표지판의 관습색이다. 가로등은 하늘색으로 비켰다.
     ★ 주의 — 판정 주황(#ffab2e)과 대비가 1.3 밖에 안 난다. 커버리지 원이
       주황 구간(216개) 위에 겹치면 경계가 흐려질 수 있어 농도를 낮춰 뒀다.
       겹쳐 보이면 opacityDark 를 0.42 에서 더 낮출 것.
     ★ 라이트는 호박색으로 눌렀다. 밝은 지면에 노랑을 그대로 올리면 안 보인다. */
  cctvCov: { colorDark:"#ffd24a", colorLight:"#d69600", opacityDark:0.42, opacityLight:0.30 },

  /* 라이트 모드 지면 색조. CARTO 타일 위에 얇게 덮는 층이다.
     ★ 배경(bg) 색을 바꿔봐야 화면에는 거의 안 나온다. 그 위를 CARTO 타일이
       95% 로 덮고 있어서다. 지면 색을 실제로 바꾸려면 타일 '위'를 덮어야 한다.
     opacity 를 올릴수록 색조가 진해지지만 도로명·지명 라벨도 함께 물든다.
     ★ opacity 0 = 색조를 안 입힌다. CARTO 라이트 타일 본래의 노란기 도는
       베이지가 그대로 나온다(2025-08 판단: 그쪽이 낫다).
       하늘색을 다시 보고 싶으면 0.4 정도로 올리면 된다. */
  lightTint: { color:"#cfe0ee", opacity:0 },

  /* noCctvColor(갈색)는 제거했다. 화면 색은 verdict 4종이 전부다.
     CCTV 사각 구간은 unknown(회색)에 그대로 포함된다 — 회색의 정의가 곧
     "CCTV 없음 / 25m 밖"이기 때문이다. 되살리지 말 것. */

  /* 회색(unknown)의 사유. 2026-08-22 에 넷으로 쪼갰다.
     종전에는 352구간이 전부 no_cctv 하나여서, 화면이 "왜 회색인가" 를
     설명하지 못했다. 판정과 색은 그대로다 — 문구만 정확해진다.
     ★ 정본은 segments.py 다. 여기 없는 키가 오면 툴팁이 빈칸이 된다. */
  reason: {
    no_cctv_narrow: "노면 3m 미만 · 도로대장도 3m 미만 · 담 사이는 여유 있음",
    no_cctv_thin  : "노면 3m 미만 · 대장폭은 3m 이상 — 근거 하나뿐",
    no_cctv_band  : "3~7m 대역 · 주정차 여부로 갈림 · CCTV 25m 밖",
    no_cctv_single: "7m 이상이나 표본 부족으로 통과 확정 보류",
    /* 옛 키. 재발행 전 산출물을 열었을 때를 위해 남긴다. */
    no_cctv: "CCTV 유효범위 25m 밖 · 영상판정 불가",
    width  : "실폭도로·건물 폴리곤 부재 · 폭 산출 불가",
  },

  /* 마커. 원기둥 몇 개를 쌓아 실제 시설의 실루엣을 근사한다.
     비율은 실물을 따르되 크기는 과장한다.
     실물 치수(소화전 0.9m)로 그리면 1km 시야에서 점 하나가 되어 안 보인다.
     parts = [{r: 반지름m, z: 바닥높이m, h: 높이m, c: 색}] 아래에서 위로.

   ★ 2026-09-01 전 마커 축소. 종전 배율(약 5배)에서 2~3배로 내렸다.
     근거는 조기성 소방관 인터뷰(2026-08-24) — 마커는 "과장 없이 심플하게".
     5배는 골목 폭보다 마커가 굵어서, 판정색을 보러 온 화면에서 시설
     마커가 먼저 눈에 들어왔다. 판정이 주인공이고 시설은 조연이다.
     배율: CCTV·소방서·119안전센터·가로등 x0.4, 소화전만 x0.6.
   ★ 소화전만 덜 줄인 이유 — 11개뿐이라 CCTV 104·가로등 폴 1,143 사이에
     묻힌다. 인터뷰에서 직접 요청받은 유일한 시설이기도 하다.
   ★ 축소할 때 parts 의 r·z·h·hw·hl 과 sign.top 을 같은 배율로 함께
     곱해야 한다. sign.top 만 잊으면 간판이 기둥에서 떨어져 공중에 뜬다.
     cover.radius(25m)는 실제 유효범위라 절대 같이 줄이지 말 것. */
  markers: [
    /* CCTV — 총 5.2m (13.0m x0.4, 2026-09-01).
       실물 CCTV 지주가 4~5m 이므로 이제 거의 실척에 가깝다.
       간판이 있어 멀리서도 위치는 잡히므로 기둥을 과장할 이유가 없다.
       ★ 이 마커의 간판은 평시에 꺼 둔다 — js/ui/toggles.js 의
         syncCctvSign() 을 볼 것. 출동 모드에서만 켠다. */
    { id:"m-cctv", label:"CCTV", data:"cctv",
      popup: p=>`<b>CCTV</b> ${p.카메라대수}대<br>${p.카메라화소||"—"} · ${p.촬영방면||""}
                <br>설치 ${p.최초설치}${p.최근설치!==p.최초설치?`~${p.최근설치}`:""} (${p.설치회차}회 증설)
                <br><span class="a">${p.소재지도로명주소||""}</span>`,
      /* themed:"cctvCov" — 색·농도를 CONFIG.cctvCov 에서 가져온다.
         라이트/다크 전환 시 syncCctv() 가 이 레이어를 다시 칠한다. */
      cover:{ radius:25, style:"solid", themed:"cctvCov" },
      /* 간판. draw = app.js SIGN_DRAW 에 등록된 그림 이름, top = 기둥 총높이(m). */
      sign:{ draw:"cctv", top:5.2 },
      /* c = 다크, cl = 라이트. cl 을 안 적으면 c 를 그대로 쓴다.
         ★ 간판·커버리지 원과 같은 노랑이라야 셋이 한 시설로 읽힌다.
         ★ 노랑은 밝은 지면에서 그대로 묻힌다. 라이트는 호박색으로 눌러야
           보인다 — 여기만 '연하게'의 반대로 간다. */
      /* ★ 지주는 회색이다. 한 번 노랑으로 칠해 봤다가 되돌렸다(2026-08-17).
         색은 '무엇인가'를 말하는 자리이고, 그 일은 헤드와 간판이 한다.
         기둥까지 같은 색이면 마커가 색덩어리 하나로 뭉쳐 형태가 안 읽힌다.
         실물 지주도 회색 아연도금이라 근거도 맞는다. */
      parts:[ {r:0.24, z:0,    h:4, c:[170,175,190], cl:[140,146,162]},   // 지주
              {r:0.6, z:4, h:0.64,  c:[255,210, 74], cl:[214,150, 10]},   // 카메라 하우징
              {r:0.32, z:4.64, h:0.36,  c:[255,240,180], cl:[240,190, 60]},   // 렌즈
              {r:0.16, z:5, h:0.2,  c:[255,210, 74], cl:[214,150, 10]} ]},
    /* 소화전 — 총 6.54m. 실물 소화전 실루엣을 근사했다.
       아래에서 위로: 베이스 플랜지 → 하부 배럴 → 볼트 플랜지 → 노즐부 몸통
       → 어깨 링 → 보닛(2단 테이퍼) → 목 → 오각 캡.
       ★ 파트는 전부 지점 중심에 놓이므로 옆으로 튀어나온 방수구(노즐)와 체인은
         표현할 수 없다. 노즐 높이의 굵은 단(4·5번)이 그 자리를 대신한다.
       ★ 물결 애니메이션 색(#4fc3f7)은 app.js 에 따로 있다. 여기 색과 무관하다. */
    /* ★ 축소 이력. 16.8 → 10.9(x0.65, 2026-08) → 6.54(x0.6, 2026-09-01).
         형상 비율은 두 번 다 그대로 두고 전체 배율만 곱했다.
         간판 높이는 아래 sign.top 이 정본이다(6.54). 둘이 어긋나면 간판이 뜬다.
       ★ 다른 마커가 x0.4 인데 여기만 x0.6 이다. 11개뿐이라 같이 줄이면
         CCTV 104개·가로등 폴 1,143개 사이에서 사라진다. */
    { id:"m-hyd", label:"소화전", data:"hyd", sign:{ draw:"hydrant", top:6.54 }, pulse:true, popup: p=>`<b>소화전</b> ${p.시설번호||""}<br>${p.상세위치||""}
                <br>설치 ${p.설치연도||"—"} · 보호틀 ${p.보호틀유무||"—"}
                <br><span class="a">${p.소재지도로명주소||""}</span>`,
      parts:[ {r:1.62, z:0,    h:0.48, c:[150, 22, 18]},   // 베이스 플랜지(어두운 적)
              {r:0.96, z:0.48,  h:2.46, c:[198, 30, 24]},   // 하부 배럴
              {r:1.2, z:2.94,  h:0.3, c:[150, 22, 18]},   // 볼트 플랜지
              {r:1.38, z:3.24,  h:1.32, c:[226, 40, 32]},   // 노즐부 몸통(가장 굵은 단)
              {r:1.5, z:4.56,  h:0.36, c:[150, 22, 18]},   // 어깨 링
              {r:1.2, z:4.92,  h:0.6, c:[226, 40, 32]},   // 보닛 하단
              {r:0.84, z:5.52,  h:0.48, c:[240, 60, 50]},   // 보닛 상단(테이퍼)
              {r:0.48, z:6, h:0.36, c:[198, 30, 24]},   // 목
              {r:0.24, z:6.36, h:0.18, c:[255,120,105]} ]},// 오각 캡
    { id:"m-fs", label:"소방서", data:"sta", popup: p=>`<b>${p["소방서 및 안전센터명"]}</b><br>관할 본서
                <br>${p.전화번호||""}<br><span class="a">${p.주소||""}</span>`, kind:"station",
      sign:{ draw:"119", top:6, dx:52 },
      parts:[ {hw:2.8, hl:2.8, z:0, h:6, c:[226, 34, 28]} ]},   // 붉은 119 박스(정육면체)
    { id:"m-sta", label:"119안전센터", data:"sta", popup: p=>`<b>${p["소방서 및 안전센터명"]}</b><br>출동 시작점
                <br>${p.전화번호||""}<br><span class="a">${p.주소||""}</span>`, kind:"center",
      sign:{ draw:"truck", top:16.8, dx:-52 },
      /* 원기둥 3단 → 단일 직사각 기둥으로 교체(2025-08).
         119 표지판을 정면에 붙이려면 면이 평평해야 읽힌다.
         h 를 키우면 그만큼 표지판도 같이 올라간다(sign.top 을 함께 맞출 것).
         ★ 42 → 16.8m (x0.4, 2026-09-01). 종전에는 6층 건물 높이라 실제
           안전센터 건물보다 마커가 높았다. */
      parts:[ {hw:2.4, hl:2.4, z:0, h:16.8, c:[226, 34, 28]} ]},
    /* 가로등 — 총 14.4m. 지주 + 암 + 등기구.
       ★ 높이가 등 수를 뜻하지 않는다. 등 수는 지주 굵기 √n 로 표현한다.
         높이로 하면 "15층짜리 가로등"처럼 보인다.
       ★ 좌표는 지번 대표점이다. 실제 폴 위치가 아니므로 반경 50m 원
         (app.js light-cov)과 반드시 같이 켜고 끈다. */
    { id:"m-light", label:"가로등", data:"streetlights",
      /* 굵기 = √(등 수). 면적이 등 수에 비례해 보이려면 반지름은 √n 이다.
         ★ 높이로 표현하지 않는다. "15층짜리 가로등"처럼 보인다. */
      scale:{ by:"n_lights", mode:"sqrt", k:0.28 },
      /* ★ 이 원은 "빛이 닿는 범위"가 아니라 "폴이 이 안 어딘가에 있다"는 뜻이다.
         CCTV 커버리지와 의미가 정반대이므로 점선을 쓴다.
         ★ 색은 등기구와 같은 청록이다(2026-08-17).
         ★ 기본 농도(0.10)로는 거의 안 보였다. 면을 올리고 점선 테두리는 더
           진하게·굵게 잡는다 — 이 원의 뜻은 '면적'이 아니라 '경계'다.
           "이 선 안 어딘가에 폴이 있다"가 읽혀야 한다. */
      cover:{ by:"pos_accuracy_m", style:"dashed", color:"#3fb8d8",
              opacity:0.16, lineOpacity:0.75, lineWidth:1.6,
              /* ★ 라이트용. 밝은 지면(#e8e6e0)에서 밝은 청록은 그대로 묻힌다.
                 색을 눌러 진하게 하고 점선을 조금 더 굵게 잡는다.
                 면은 거의 안 올린다 — 밝은 배경에선 옅은 면도 금방 탁해진다. */
              colorLight:"#17708f", opacityLight:0.14,
              lineOpacityLight:0.9, lineWidthLight:1.9 },
      popup: p=>`<b>가로등</b> ${p.n_lights}등`
        +`<br><span class="a">지번 대표점 · 실제 폴 위치 아님 (±${p.pos_accuracy_m||50}m)</span>`
        +(p.mgmt_no_sample?`<br>관리번호 ${p.mgmt_no_sample}`:"")
        +(p.addr?`<br><span class="a">${p.addr}</span>`:""),
      /* ★ 실물 실루엣 근사(총높이 5.76m — 14.4m x0.4, 2026-09-01). 아래에서 위로:
         굵은 베이스 플린스 → 플린스 캡 → 지주 하부 → 지주 상부(테이퍼)
         → 상부 칼라 → 암 → 목 → 둥근 전등(구).
         실물 가로등은 밑동이 굵고 위로 갈수록 가늘어진다. 같은 굵기 막대 하나면
         전봇대와 구분이 안 된다.
       ★ 색은 청록(#3fb8d8)이다(2026-08-17 확정). 노랑은 CCTV 표지판 관습색이라
         그쪽에 양보했고, 요즘 LED 가로등은 백색광이라 찬 계열도 아주 틀리지 않다.
         옅은 하늘색을 한 단계 눌러 잡았다 — 상가 "소매"(#8ecbfa, 540개)와
         대비가 1.1 밖에 안 나 줌 17 이상에서 상가 점들에 섞여 보였다.
       ★ 전등은 원기둥 9단으로 구(球)를 근사한다. 반지름 R 구를 R/4.5 씩
         잘라 각 층 반지름을 sqrt(R^2 - d^2) 로 잡았다. 축소 후 R=0.342m,
         층 두께 0.076m 다(원값 0.855 / 0.19 에 x0.4).
         fill-extrusion 은 옆면이 곧은 기둥만 만들 수 있어 완전한 구는 불가능하다.
         층을 늘릴수록 매끈해지지만 파트 수가 곧 폴리곤 수다(46지점 x 층수).
         5단은 계단이 눈에 띄었고 9단이 타협점이다.
       ★ 옆으로 뻗은 암은 표현할 수 없다(파트가 전부 지점 중심에 놓인다).
         암 높이의 납작한 원반이 그 자리를 대신한다. */
      /* ★ 지주는 회색이다. 한 번 청록으로 칠해 봤다가 되돌렸다(2026-08-17).
         색이 곧 시설 구분인데 기둥까지 칠하면 마커가 색덩어리로 뭉친다.
         청록은 전등에서만 쓴다 — 빛을 내는 부분이 그 시설의 정체다.
         아래에서 위로 아주 조금씩 밝게 두어 원기둥이 원기둥으로 보이게 한다. */
      parts:[ {r:0.22, z:0,     h:0.88, c:[100,104,118]},                   // 베이스 플린스
              {r:0.26, z:0.88,  h:0.1, c:[120,124,138]},                   // 플린스 캡
              {r:0.12, z:0.98,  h:2.2, c:[120,124,138]},                   // 지주 하부
              {r:0.104, z:3.18,  h:1.48, c:[132,136,150]},                   // 지주 상부(테이퍼)
              {r:0.136, z:4.66, h:0.12, c:[120,124,138]},                   // 상부 칼라
              {r:0.28, z:4.78, h:0.12, c:[132,136,150]},                   // 암(원반 근사)
              {r:0.08, z:4.9, h:0.176, c:[120,124,138]},                   // 목
              /* ── 둥근 전등: 구 근사 9단 (R 0.855, 중심 z 13.545) ── */
              {r:0.157, z:5.076, h:0.076, c:[ 42,150,178], cl:[ 18, 96,120]},
              {r:0.255, z:5.152, h:0.076, c:[ 52,170,198], cl:[ 22,110,136]},
              {r:0.306, z:5.228, h:0.076, c:[ 63,184,216], cl:[ 26,124,152]},
              {r:0.334, z:5.304, h:0.076, c:[ 80,196,226], cl:[ 30,136,166]},
              {r:0.342, z:5.38, h:0.076, c:[104,210,236], cl:[ 34,148,180]},
              {r:0.334, z:5.456, h:0.076, c:[ 80,196,226], cl:[ 30,136,166]},
              {r:0.306, z:5.532, h:0.076, c:[ 63,184,216], cl:[ 26,124,152]},
              {r:0.255, z:5.608, h:0.076, c:[ 52,170,198], cl:[ 22,110,136]},
              {r:0.157, z:5.684, h:0.076, c:[ 42,150,178], cl:[ 18, 96,120]} ]},
  ],


  /* ── 기준 차량 — 동명동 관할 보유 차량 ────────────────────────
     좌측 패널의 차량 선택이 이 배열을 읽는다(js/ui/vehicle.js).

   ★ 여기는 "무엇을 띄우는가"만 적는다. 제원(전폭·회전반경)의 정본은
     web/assets/vehicles/profiles.json 이고 vehicle.js 가 그 파일을
     fetch 해서 profile 키로 붙인다. 숫자를 여기 복사하지 말 것 —
     실측으로 교체될 때 두 곳이 갈라진다.

   ★ 왜 profiles.json 10종을 다 띄우지 않는가.
     그 파일은 조달 규격 분류라 전국 대표 제원이다. 동명동에 실제로
     출동하는 차와 다르다. 예를 들어 대형 펌프차(회전 10.7m)는 동부소방서에
     한 대도 없다 — 그것을 목록에 두면 있지도 않은 차의 회전반경을 근거로
     골목을 판단하게 된다.

   ★ 목록의 근거. 동부소방서 소방차량현황(2026-05-14 기준, 소방안전본부)의
     안전센터별 보유 대수 표다. 여기 담은 것은 web/data/stations.geojson 에
     들어 있는 세 곳 — 대인119안전센터(1.05km) · 지산119안전센터(1.13km) ·
     동부소방서 본서(1.05km) 다. 용산119안전센터는 GRAPH_BUFFER 1.5km 밖이라
     데이터에 없고 여기에도 넣지 않는다.
     https://www.gwangju.go.kr/fire/contentsView.do?pageId=fire224
     공공누리 4유형(출처표시·상업적이용금지·변경금지) — 화면에 출처를 밝힌다.

   ★ 뺀 것과 이유. 생활안전차(대인1·지산1)는 화재 출동 차량이 아니고,
     구조버스(2)는 인원 수송이라 골목 진입 대상이 아니다. 행정·순찰·진단·
     화물·화재조사·트레일러·장비운반·교육차는 출동 차량이 아니다.
     되살릴 일이 생기면 근거를 함께 적을 것.

   ★ match — profiles.json 항목과의 대응 신뢰도. 이것을 화면에 밝힌다.
       확정  보유 대장의 차종명이 profiles.json 항목과 그대로 맞는다
       추정  가장 가까운 항목을 골랐다. 다른 축으로 분류돼 있다
       없음  profiles.json 에 대응 항목이 없다
     ★ 추정을 확정처럼 보이게 하지 않는다. 회전반경은 7.3 과 11.9 사이가
       세 배 차이 나는 자리라, 모르면서 아는 척하면 그게 곧 사고다.

   ★ grade 를 문자열로 박아 둔 이유. 화면이 반경 수치를 보고 등급을
     계산하면 그것도 일종의 재판정이다. 등급은 선언이지 계산이 아니다.

   ★ 차량을 바꿔도 지도 색은 바뀌지 않는다(설계 원칙 6). 색은 데이터의
     verdict 문자열이 정한다. 선택이 바꾸는 것은 통과선과 여유 표기뿐이다.
     폭으로는 차종이 거의 안 갈린다 — 구급차(1.995m)만 빼고 전부 2.5m 다.

       id       내부 키
       label    화면에 뜨는 이름. 보유 대장의 어휘를 그대로 쓴다
       profile  profiles.json 의 id. 없으면 null
       count    동명동 관할 보유 대수
       at       배치된 곳
       match    "확정" · "추정" · "없음"
       grade    "여유" · "주의" · "미판정"
       group    드롭다운 묶음. fleetGroups 의 값 중 하나여야 한다
       turnUnknown  true 면 profiles.json 의 회전반경을 안 가져온다.
                    제원표 값이 실물과 다르다는 근거가 있을 때만 쓴다
       outrigger    아우트리거 최대 전개너비(m). 붐을 세우는 데 드는 폭이다.
                    ★ 전폭과 다른 축이다 — 2.5m 로 골목에 들어가도 이 폭이
                      안 나오면 세울 수 없다. 지금 판정은 이것을 안 본다.
       note     추정·없음일 때 그 사유. 화면에 그대로 뜬다

   ★ group 을 쓰임새로 가른 이유. 배치(대인·지산·본서)로 가르면 펌프차가
     두 묶음에 걸친다 — 대인 2대와 지산 2대는 같은 차종이다. 배치는 선택
     요약에서 "보유" 줄로 이미 보여준다.
   ──────────────────────────────────────────────────────────── */
  fleet: [
    { id:"pump",    label:"펌프차 (중형)",         profile:"pump_medium",     group:"진압",
      count:4, at:"대인 2 · 지산 2", match:"확정", grade:"여유" },
    { id:"tanker",  label:"물탱크차 (대형)",        profile:"tanker_large",    group:"진압",
      count:1, at:"대인", match:"확정", grade:"주의" },
    { id:"chem",    label:"화학차",              profile:"chemical_large",  group:"진압",
      count:1, at:"대인", match:"추정", grade:"주의",
      note:"보유 대장에 대형·고성능 구분이 없어 대형으로 잡았다" },
    { id:"ladder-s",label:"직진형 사다리차 53m",    profile:"ladder_33m_plus", group:"사다리",
      count:1, at:"대인", match:"추정", grade:"미판정",
      turnUnknown:true, outrigger:6.0,
      note:"광주 규격서(2025-06)에 회전반경 규정이 없다. 차대가 6x4 3축·전장 13m 이하라 제원표의 2축 대표값(11.89m)은 실제보다 작다 — 그래서 안 띄운다" },
    { id:"ladder-a",label:"굴절형 사다리차 27m",    profile:"ladder_under_33m",group:"사다리",
      count:1, at:"대인", match:"추정", grade:"미판정",
      turnUnknown:true,
      note:"제원표는 길이로 갈리는데 굴절형은 관절 유무로 갈린다. 다른 축이라 회전반경을 가져오지 않는다" },
    { id:"rescue",  label:"구조차",              profile:"rescue_5t",       group:"구조·구급",
      count:2, at:"본서 119구조대", match:"확정", grade:"미판정",
      note:"제원표에 회전반경이 비어 있다" },
    { id:"amb",     label:"구급차",              profile:"ambulance_current_example", group:"구조·구급",
      count:4, at:"대인 3 · 지산 1", match:"확정", grade:"미판정",
      note:"제원표에 회전반경이 비어 있다" },
    { id:"light",   label:"조연차",              profile:null,              group:"진압",
      count:1, at:"대인", match:"없음", grade:"미판정",
      note:"제원표에 항목이 없다. 전폭 미상" },
  ],
  /* 드롭다운 묶음의 표시 순서. 여기 없는 group 은 맨 뒤에 "그 외"로 묶인다.
     골목 진입 가능성이 높은 순서다 — 관제사가 처음 보는 것이 펌프차여야 한다. */
  fleetGroups: ["진압", "사다리", "구조·구급"],
  /* 기본 선택. 동명동 관할에서 가장 많고(4대) 골목 진입을 실제로 시도하는 차다. */
  fleetDefault: "pump",
  /* 제원 정본 위치. vehicle.js 가 이 경로를 fetch 한다. */
  vehicleProfiles: "./assets/vehicles/profiles.json",
  /* 통과선 = 전폭 + 안전여유. 2.5 + 0.5 = 3.0 이 params.py 의 TRUCK 과 맞는다.
     ★ 정본은 src/firelane/seg/params.py 의 TRUCK(3.0)이다. 여기 값을 바꿔도
       판정은 안 바뀐다 — 화면에 적히는 통과선만 바뀐다. 어긋나게 두지 말 것. */
  vehicleClearance: 0.5,
  /* 회전·높이 미판정 고지. 화면설계서 FRLN_DISP_02 3번과 같은 문장이다. */
  turnNote: "경로 판정에는 전폭만 반영됩니다. 회전반경·높이는 판정하지 않습니다.",
  /* 보유 대수 출처. 공공누리 4유형이라 화면 표기가 의무다.
     제원 출처는 profiles.json 이고, 사다리차 전폭·전고·차대·아우트리거는
     「소방고가차-사다리차(53m) 규격서」(2025-06, 광주 소방안전본부 119대응과,
     나라장터 R25BK00911861)에서 확인했다. 근거는 data/field/ 를 볼 것. */
  fleetSource: "동부소방서 소방차량현황 (2026-05-14) · 광주광역시 소방안전본부",

  /* 카메라 초기값. 범위(maxBounds 등)는 web/data/view.json 에서 온다. */
  camera: { zoom:15.4, pitch:52, bearing:-18, maxPitch:80 },

  /* 출동 모드. 갈 수 있는 길만 남기고 나머지를 죽인다.
     UI 담당(@marscoolcat) 이 자유롭게 조정하는 값이다. */
  dispatch: {
    clearWidthScale: 1.6,   // clear 구간 선 굵기 배수
    dimAlpha       : 102,   // 그 외 구간 투명도 (0~255). 102 = 40%
    pulseMs        : 1200,  // 소화전 물결 주기(ms). 작을수록 빠르다
    /* ★ 소화전 물결 on/off. 2025-08 현재 꺼 둔다 — 화면이 산만하다는 판단.
       코드는 app.js 에 그대로 살아 있으니 true 로만 바꾸면 즉시 되살아난다. */
    hydPulse       : false,
  },

  minimap: { showFromZoom: 16 },

  /* 상가 POI. 소상공인시장진흥공단 상가(상권)정보. 지상 1층만 남겼다.

     ★ 업종 색은 "길찾기용 랜드마크"다. 판정색이 아니다. 그래서 판정 4색
       (빨강·주황·초록·회색)과 겹치지 않는 계열만 쓴다.
     ★ 점과 상호 글자가 같은 색이라야 "이 라벨이 어느 점의 것인가"가 읽힌다.
     ★ 여기 적는 대분류는 데이터의 실제 어휘여야 한다. 예전 코드에 있던
       "생활서비스"는 데이터에 없는 값이라 죽은 분기였고, 그 바람에 3·4위인
       과학·기술(281)·수리·개인(152)이 색을 못 받았다(2026-08-17 정정).
       실제 어휘 10종: 음식 소매 과학·기술 수리·개인 부동산 숙박
                      시설관리·임대 교육 예술·스포츠 보건의료
     ★ 10종을 다 칠하면 구분이 안 된다. 상위 5종 + 보건의료(병원은 랜드마크)만
       칠하고 나머지는 회색으로 둔다.
     ★ 색은 두 테마 공통이다. 검은 테두리를 두르면 밝은 지면에서도 글자가
       배경에서 떨어지므로 테마별로 나눌 필요가 없다. */
  /* ── 가로등 폴 ───────────────────────────────────────────
     ★ 2026-08-23 배선. 그전까지 publish_web.py 가 lightpoles.geojson
       1,143점을 발행하는데 web/js 어디에서도 읽지 않았다. 브라우저가
       요청조차 안 했다(고아 레이어).

     ★ 가로등이 두 개인 이유.
         streetlights   46지점 · 573등  지번 대표점이라 ±50m 오차.
                        등 수와 관리번호는 이쪽이 정본이다
         lightpoles  1,143점         수치지형도 C0220000. 실제 폴 위치.
                        구분(보안등/가로등/조명등)만 있고 등 수는 없다
       둘은 다른 데이터다. 위치를 보려면 이쪽, 등 수를 보려면 저쪽이다.

     ★ 마커(3D extrusion)로 만들지 않는다. 1,143개를 폴리곤으로 세우면
       무겁고, 무엇보다 소화전·CCTV 같은 "시설" 과 위계가 같아져 버린다.
       가로등은 배경 정보라 점으로 깐다. poi 와 같은 취급이다. */
  poles: {
    fromZoom: 16,               // 이 줌 아래에서는 안 그린다. 1,143개라 소음이 된다
    color: { "보안등": "#7fd4ff", "가로등": "#ffd98a", "조명등": "#c9b6ff",
             other: "#9aa4b2" },
    otherLabel: "그 외",
    haloColor: "#141a26",
  },

  poi: {
    labelFromZoom: 17,          // 이 줌 이상에서 상호가 뜬다
    /* ★ 2026-08-17 한 단계 옅게. 상가는 배경 정보라 판정색보다 앞에 나오면 안 된다.
       라벨이 수십 개 깔리는 줌 17 이상에서 채도가 높으면 그 자체가 소음이 된다. */
    color: { "음식":"#ffe08a", "소매":"#8ecbfa", "과학·기술":"#cbbcfd",
             "수리·개인":"#8fe6f4", "숙박":"#f9b6cc", "보건의료":"#eef2f7",
             other:"#b3bbc8" },
    /* 글자 테두리. 순수 검정(#000)은 지도 위에서 구멍처럼 보인다.
       배경(#0a0d13)에 가까운 짙은 남색으로 한 단계 눅였다. */
    haloColor: "#141a26",
    haloWidth: 1.5,
    /* 패널 업종 범례에 쓸 이름. color 의 키와 같아야 한다. */
    otherLabel: "그 외",
  },

  /* 지형. 공개DEM 90m 를 8배 보간한 표현용 값이다.
     ★ 판정에는 쓰지 않는다. 90m 격자는 골목 20개를 한 픽셀로 덮는다.
     raster-dem 타일(terrain.py 산출) + map.setTerrain() 으로 지면 자체를 휘게 한다.
     exaggeration 을 올리면 기복이 과장되어 입체감이 살지만 실제 지형과 멀어진다. */
  terrain: {
    enabled     : true,
    exaggeration: 1.0,   // ★ 1.0 = 실제 비율. GIS 는 현실을 반영해야 하므로 기본은 1.0 이다.
                         //    올리면 보기는 좋아지지만 표고가 왜곡된다.
                         //    발표 영상용으로 잠깐 올릴 수는 있으나 그 사실을 명시할 것.
  },
};
